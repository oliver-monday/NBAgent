# NBAgent — Session Context & Handoff Reference

**Purpose:** Dense technical handoff for new Claude sessions. Load this alongside `CLAUDE.md`
and `@docs/AGENTS.md` at session start. Covers current implementation state, design decisions,
non-obvious gotchas, and live prompt format — things that take time to re-derive from source.

**Last updated:** April 13, 2026 ((80) **Playoff intelligence layer** — three files. `context/nba_season_context.md`: new `### PLAYOFF INJURY LANDSCAPE` section with confirmed OUT players (Doncic, Reaves, Embiid), active concerns (Edwards, McDaniels, Quickley, Cunningham, Wemby, Curry, Gordon), and R1 matchup injury context. `agents/analyst.py`: `build_playoff_adjustments()` updated — Luka/Reaves/Embiid marked ⚠ OUT with DO NOT PICK; Edwards knee note added; 3 new entries (Cade ramp-up, LeBron load risk, Wemby rib caveat). `agents/injury_profiles.py`: **simplified from noisy auto-classifier to clean data provider** — removed `classify_risk_tier()`, `elevated_reasons`, risk threshold constants. Output now uses `current_status` (OUT/DOUBTFUL/QUESTIONABLE/ACTIVE from injury overlay) instead of `risk_tier` (OUT/ELEVATED/MANAGED/CLEAR from auto-classification). `status_summary` replaces `tier_summary`. The curated `PLAYOFF INJURY LANDSCAPE` in season context provides the qualitative intelligence; injury_profiles.py provides the quantitative data foundation only.)
**Last updated (prior):** April 13, 2026 ((79) **New agent: Injury & Availability Profiles** — created `agents/injury_profiles.py`, added to `analyst.yml`.)
**Last updated (prior):** April 12, 2026 ((78) **P1.4: Game-in-series confidence modifier (H31 activation)** — Two files. `agents/playoff_matchup.py`: `game_in_series` (int, 1-indexed) and `series_phase` ("early"/"mid"/"late") added to series_entry dicts; `format_series_block()` header shows `GAME N (PHASE)`. `agents/analyst.py`: `SERIES_PROGRESSION_JSON` path constant. New `build_series_progression_rules(today_str)` function — date-gated to `PLAYOFFS_R1_DATE` AND any active series at `game_in_series >= 5`. Reads H31 `backtest_series_progression.json` for per-player LATE_FADER/LATE_RISER/STABLE flags. Renders `## GAME-IN-SERIES CONFIDENCE MODIFIER` prompt block with: LATE_FADER players get -5% PTS confidence (Harden -33pp, Curry -15pp, Edwards -15pp measured deltas), LATE_RISER players get +3% PTS confidence (SGA +8pp, Kawhi +20pp, Durant +8pp). STABLE players: no adjustment. PTS props only. Cannot push above cap ceilings. Wired into all 3 prompt builders: `build_prompt()` and `build_scout_prompt()` via `series_progression_section` variable, `build_pick_prompt()` via inline call. Inert during Games 1-4 — activates automatically at Game 5. Games 1-4 serve as a validation window before the modifier fires. Not touched: `compute_series_state()`, `load_series_context()`, `build_playoff_context()`, `build_playoff_adjustments()`, `build_playoff_blowout_override()`, all other agents, all workflows.)
**Last updated (prior):** April 12, 2026 ((77) **P1.5: Playoff calibration early warning** — `agents/auditor.py` only. New `_compute_playoff_calibration(regular_season_bands)` function called from `save_audit_summary()` between summary dict construction and file write. Filters picks with `date >= PLAYOFFS_R1_DATE` ("2026-04-18"), non-voided, HIT/MISS result. Requires `PLAYOFF_CAL_MIN_PICKS` (15) graded playoff picks before activating. Groups by confidence band (70-75/76-80/81-85/86+), computes per-band playoff hit rate, compares to regular-season baseline from `conf_summary` (the already-computed `confidence_calibration_totals`). When any band diverges ≥`PLAYOFF_CAL_DIVERGE_PP` (10pp) underperformance with ≥5 picks in that band: prints `[PLAYOFF CALIBRATION WARNING]` to stdout and appends a diagnostic warning to `summary["recent_recommendations"]` — which the Analyst reads via Rolling Performance Summary. Writes `playoff_calibration` key to `audit_summary.json`: per-band `{picks, hits, hit_rate_pct, regular_season_pct, divergence_pp}` + `_overall` sub-key `{picks, hits, hit_rate_pct, date_range}`. Purely diagnostic — no automatic rule changes, no pick modifications. Not touched: `build_audit_prompt()`, `grade_picks()`, `save_audit()`, existing `confidence_calibration_totals` computation, all other agents, all workflows.)
**Last updated (prior):** April 12, 2026 ((76) **P1.2: Playoff blowout rule relaxation** — `agents/analyst.py` only. New `build_playoff_blowout_override(today_str)` function, date-gated to `PLAYOFFS_R1_DATE` (2026-04-18). Returns `## PLAYOFF BLOWOUT ADJUSTMENT — THRESHOLD OVERRIDES` block that explicitly replaces regular-season spread_abs thresholds with +3 raised values and reduces BLOWOUT penalty from −10% to −5%. Iron_floor fully neutralizes playoff BLOWOUT penalty. Injected via f-string `{build_playoff_blowout_override(TODAY_STR)}` in both `build_prompt()` and `build_pick_prompt()` between the SPREAD/BLOWOUT RISK section end ("confirms the player's scoring floor holds regardless of game script") and `KEY RULES — VOLATILITY:`. Returns empty string before R1 — play-in uses regular-season thresholds. `build_scout_prompt()` untouched (no rules). Structural rationale: playoff starters play 38-42 min regardless of margin, Q4 rest patterns eliminated, spreads cluster 3-7. Not touched: existing regular-season blowout rule text (unchanged — override block explicitly replaces threshold values when active), `PLAYOFFS_R1_DATE` constant, `build_playoff_context()`, `build_playoff_adjustments()`, quant-level `blowout_risk` flag (still fires at spread_abs > 8), all other agents, all workflows.)
**Last updated (prior):** April 12, 2026 ((75) **P1.1: Per-player calibration wiring (H29 → odds layer)** — `ingest/odds_today.py` only. `_get_calibrated_prob()` now accepts optional `player_name` and `per_player` dict parameters. Priority: (1) per-player actual hit rate from H29 `data/backtest_confidence_calibration.json` if player has ≥`PER_PLAYER_CAL_MIN_PICKS` (10) graded picks, (2) population band from `audit_summary.json`, (3) stated confidence fallback. Returns `tuple[float, str]` — probability + source tag (`"per_player"` / `"population_band"` / `"fallback"`). New `CONFIDENCE_CAL_JSON` path constant. New `_load_per_player_calibration()` loader reads H29 JSON into `{_norm_name(player): actual_hit_rate}` dict. `compute_edge()` loads per-player data once alongside bands, passes both to `_get_calibrated_prob()` for each pick. New `calibration_source` field in `bet_recommendation` sub-object tracks which method was used. Log line shows source in parentheses. Directly improves Kelly sizing: Banchero 85.1%→95.7%, Mitchell 88.3%→65.2%. Graceful fallback when H29 file missing. `pretip_sweep()` automatically benefits via its `compute_edge()` call. Not touched: `_load_calibration_bands()`, `pretip_sweep()`, `main()`, `save_morning_baseline()`, all agents, all workflows.)
**Last updated (prior):** April 12, 2026 ((74) **Parlay Builder UI + Spec 3 Cannibalization Badges** — `agents/build_site.py` only. Two new Python functions: `build_cannib_lookup()` reads `data/backtest_teammate_cannibalization.json` into a slim `{sorted_names|STAT: {idx, label}}` dict (MODERATE+ pairs only); `build_corr_lookup(today_picks)` reads `teammate_correlations` from `player_stats.json` for today's players. Both injected as `cannib_lookup` and `corr_lookup` in the JS `DATA` object and wired through `build_site()` → `page_data` → `generate_html()`. **Spec 3:** `renderParlays()` leg loop gains `legIdx` param + cannibalization badge insertion before each leg — checks consecutive same-team same-stat pairs against `cannib_lookup`, renders inline ⊖/⊕ badges with amber/teal coloring and hover tooltip. **Custom Parlay Builder:** new `<div id="parlay-builder-container">` on Parlays tab. Full CSS suite (`.builder-*` classes, responsive grid, sticky card). JS: `renderBuilder()` groups today's non-voided ungraded picks by game, renders click-to-toggle pick rows (left column) and reactive "Your Parlay" card (right column) with live-computed combined probability, American odds, $10 payout, net calibrated edge, H33 cannibalization warnings (all O(n²) same-team pairs checked), Pearson correlation badges, Clear + Copy-to-clipboard actions. `toggleBuilderLeg()`, `clearBuilder()`, `copyBuilder()` manage state via `window._builderState` Set. Builder re-renders on every toggle. Not touched: `renderParlays()` structure beyond the forEach change, all Python data prep functions, all other agents, all workflows.)
**Last updated (prior):** April 12, 2026 ((73) **Parlay Track 1: Variety, Bold Card, Expanded Candidates** — `agents/parlay.py` only. `TARGET_MAX_ODDS` raised 600→900 (opens 4–5 leg ambitious plays). `TOP_N_TO_CLAUDE` raised 15→25 (broader candidate pool). Leg-count diversity enforcement added to `build_candidates()` as a second diversity pass after the existing dominant-player fix: `_leg_count_backfill()` local helper ensures ≥3 candidates with 4+ legs and ≥2 with exactly 2 legs by backfilling from the remaining candidate pool. Prompt rewritten: YOUR TASK gains MANDATORY VARIETY RULE (1× 2-leg + 1× 4+ leg non-negotiable) and BOLD CARD concept (exactly one per day, `card_type: "bold"`, +400–800 target, 4–5 legs, may include one speculative 70–74% leg with STRONG edge). SELECTION CRITERIA item 1 split into core/bold odds targets (+100–350 / +400–800), item 2 split core/bold floor confidence (≥75% / may allow 70–74%), item 5 mandates ≥2 different leg counts across the slate, item 6 emphasizes edge compounding over raw odds. AVOID extended with two new bullets: anti-monotony (all same leg count = violation) and anti-weak-bold (bold card must be structurally different, not a slightly worse core). OUTPUT FORMAT gains `card_type` field ("core" | "bold"). Not touched: `score_combination()`, `CORR_BONUS`, `enforce_concentration_cap()`, `call_parlay_agent()`, `save_parlays()`, `MIN_LEGS`, `MAX_LEGS`, `MIN_CONFIDENCE`, cannibalization integration (Spec 68), FADE pre-filter, all other agents, all workflows.)
**Last updated (prior):** April 11, 2026 ((72) **Spec 5: Skip Re-evaluation Path C — Cannibalization Release** — `agents/lineup_update.py` only. `build_skip_reconsiderations()` gains Path C between Path B and the final `if not reconsider: continue` gate. When a skipped player has H33 `cannib_idx < -15.0` with the absent star (STRONG cannibalization), the skip is reconsidered even if Paths A and B both fail — the cannibalization release mechanism itself predicts meaningful ceiling lift. Stricter threshold (−15pp) than Specs 2/4 (−8pp) because skip reconsideration is a higher-stakes claim. Fires only as fallback: `if not reconsider:` guard ensures Path C never overrides A or B. Reuses `_load_cannib_data()` from Spec 4 (already cached). Reasoning string: `"H33 cannibalization release: {prop} was suppressed {idx}pp by {star} → ceiling freed"`. PERSONAL_DRAG_WARNING guard (from H26 Part 2) still fires first at the top of the per-skip loop, preventing reconsideration for drag players regardless of Path C. `build_skip_reconsiderations()` now has three paths: A (per-player without-star rate ≥70%), B (population delta positive), C (H33 cannib_idx < −15pp release). Not touched: Path A logic, Path B logic, `build_opportunity_suggestions()`, all other agents, all workflows.)
**Last updated (prior):** April 11, 2026 ((71) **Spec 4: Opportunity Flags Cannibalization Priority** — `agents/lineup_update.py` only. `build_opportunity_suggestions()` enriched with H33 cannibalization data: teammates cannibalized by absent player (cannib_idx < −8.0) flagged as `priority: "high"` with `cannib_freed=True`, `cannib_idx` (float), `cannib_detail` (string "PTS was cannibalized -X.Xpp by [player] → freed ceiling"). Teammate-side only — opponent-side suggestions unaffected. Reasoning string gets `H33: {detail}` appended. Suggestions sorted by priority (high first, then alphabetical) before save via `sorted(seen.values(), key=lambda s: ...)`. Debug logging shows `★ HIGH` + H33 detail for cannibalized teammates. New `CANNIBALIZATION_JSON` path constant + `_load_cannib_data()` module-level cached loader (same pattern as quant.py/parlay.py/auditor.py). Graceful no-op when file missing. Not touched: `save_opportunity_flags()`, `compute_lineup_diff()`, `build_skip_reconsiderations()`, dedup logic, all other agents, all workflows.)
**Last updated (prior):** April 11, 2026 ((70) **Spec 2: Auditor Cannibalization-Aware Parlay Grading** — `agents/auditor.py` only. `CANNIBALIZATION_JSON` path constant + `_load_cannib_data()` module-level cached loader (same pattern as quant.py/parlay.py). `grade_parlays()` enriched: after grading a parlay as MISS, scans all pairwise missed legs for same-team same-stat pairs with H33 `cannib_idx < -8.0` (MODERATE+ threshold). Detected pairs written as `cannibalization_flags` list on the graded parlay dict: `[{player_a, player_b, stat, cannib_idx, label, note}]` or `None`. `build_audit_prompt()` item 6 (PARLAY MISS analysis) rewritten: LLM now checks `cannibalization_flags` on each missed parlay and references specific pair names + cannib index in `parlay_lessons` — this teaches the Parlay agent to avoid the construction via the existing `load_parlay_audit_feedback()` → prompt injection loop. Completes the H33 learning cycle: parlay.py blocks STRONG pairs + penalizes MODERATE → auditor detects when MODERATE pairs slip through and fail → parlay_lessons → next morning's parlay agent reads lesson. Not touched: `grade_parlays()` HIT/MISS/PARTIAL determination logic, `parlay_lessons`/`parlay_reinforcements` output schema (LLM-written), `save_audit_report()` markdown rendering, all other agents, all workflows.)
**Last updated (prior):** April 11, 2026 ((69) **Spec 1: H26 × H33 Star-Absence Cannibalization Mechanism** — `agents/quant.py`: new `CANNIBALIZATION_JSON` path constant, `_load_cannib_data()` module-level cached loader reading `data/backtest_teammate_cannibalization.json`. `compute_star_absence_deltas()` return dict gains `"cannib_mechanism"` field — `{PTS: {cannib_idx, label}, AST: {cannib_idx, label}}` or `None`. Looks up this player↔star pair in H33 data for PTS and AST. Negative `cannib_idx` means star's usage suppresses this player → star being OUT frees ceiling. Positive means synergy → star being OUT may hurt. `agents/analyst.py`: `build_quant_context()` renders "H33 mechanism:" line after the existing per-player qualifier in the `star_lift_line` block. Three rendering paths: "was cannibalized X.Xpp by [star] → freed ceiling" (idx < -8), "independent of [star] (X.Xpp) → minimal change" (near zero), "had +X.Xpp synergy with [star] → may decline without" (idx > +8). Prompt rule guidance added to BOTH `build_prompt()` WITHOUT-STAR BASELINE block AND `build_pick_prompt()` TEAMMATE ABSENCE USAGE ABSORPTION block — explains how to weight freed-ceiling/independent/may-decline-without against population lift. Annotation-only — no directive rules. Not touched: existing `compute_star_absence_deltas()` key_deltas/qualifier logic, existing STAR_ABSENT_LIFT population/per-player lines, `build_scout_prompt()`, `build_review_prompt()`, `agents/parlay.py`, `agents/auditor.py`, `agents/backtest.py`, all ingest files, all workflows.)
**Last updated (prior):** April 11, 2026 ((68) **H33 Parlay Agent Integration: Teammate Cannibalization Context** — `agents/parlay.py` only. STRONG PTS/AST cannibalization pairs mechanically filtered from parlay candidates in `build_candidates()` (added `"cannibalization_strong"` to the `scoring_rivals`/`board_rivals` block set). MODERATE pairs get `CORR_BONUS` penalty (−0.06), SYNERGY pairs get boost (+0.08) — 3 new entries in `CORR_BONUS` dict. `get_correlation_tag()` upgraded: after the existing Pearson r lookup, same-stat same-team pairs (PTS or AST only) check H33 data via `load_cannibalization_data()` and override Pearson tag when tier-level signal exists (`CANNIBALIZATION_STRONG` → `cannibalization_strong`, `CANNIBALIZATION_MODERATE` → `cannibalization_moderate`, `SYNERGY_*` → `cannibalization_synergy`). Cross-stat pairs (PTS+AST from same team) still use Pearson — intentional, H33 is same-stat only. `score_combination()` dominant correlation type extended: `cannibalization_synergy` → positive, `cannibalization_strong`/`cannibalization_moderate` → negative. New `build_cannibalization_context(picks)` generates `## SAME-TEAM SCORING CANNIBALIZATION` prompt block for today's relevant pairs where both players are in the pick pool with matching prop types (⊖ NEVER CO-PICK / ⊖ CAUTION / ⊕ SYNERGY icons, cannib_idx per pair). Injected between audit feedback and PRE-SCORED CANDIDATES. `build_parlay_prompt()` signature extended with `cannibalization_context: str = ""`. `main()` wired: `cannib_ctx = build_cannibalization_context(picks)` passed to prompt builder. Module-level `_cannib_cache` loaded once per run from `data/backtest_teammate_cannibalization.json`. Graceful no-op when file missing (prints diagnostic, returns empty). Not touched: `call_parlay_agent()`, `save_parlays()`, `enforce_concentration_cap()`, existing Pearson correlation lookup logic, FADE pre-filter, diversity fix.)
**Last updated (prior):** April 11, 2026 ((67) **Ingest Team Abbreviation Normalization** — ESPN-style abbreviations (SA, NY, GS, UTAH, NO, WSH) normalized to standard NBA (SAS, NYK, GSW, UTA, NOP, WAS) at ingest write time. `ingest/espn_player_ingest.py`: added `_ABBR_NORM` + `_norm_team()`, applied at 3 ESPN extraction points (team_map in `infer_game_meta_from_summary`, `parse_boxscore_players`, `parse_boxscore_teams`). `ingest/espn_daily_ingest.py`: applied existing `to_roto_code()` to `home_team_abbrev` and `away_team_abbrev` in the nba_master.csv row builder (was previously only used for injury lookups and standings, not the CSV output). One-time backfill of 5 CSV files (player_game_log 3,747 values, team_game_log 978, nba_master 489, player_dim 48, playoff_career_log 0 already clean). Post-backfill: zero ESPN abbreviations in any CSV. Eliminates root cause of recurring silent player drops in downstream consumers. `team_abbr_alt` in whitelist is now legacy — no consumer needs it, existing alt-tuple `load_whitelist()` implementations are harmlessly redundant. H33 re-run confirms SAS/NYK now appear with standard abbrevs (16 teams). `ingest/espn_player_ingest.py`, `ingest/espn_daily_ingest.py`, 5 data CSVs (one-time backfill).)
**Last updated (prior):** April 11, 2026 ((66) **Backtest: H33 teammate scoring cannibalization** — new `--mode teammate-cannibalization` in `agents/backtest.py`. Pairwise conditional tier hit rates between teammates (A's hit rate at A's best tier on nights when B hits vs misses B's best tier). Asymmetric cannibalization index. PTS + AST. 16 teams, 83 PTS pairs, 81 AST pairs. Population PTS mean −0.0pp (centered). HOU/LAL/DEN CANNIBALIZATION_CLUSTER, PHI SYNERGY_CLUSTER, MIN INDEPENDENT, MIA strongest synergy (+33.8pp). SAS confirmed CANNIBALIZATION_CLUSTER (Wembanyama↔Castle −15.1/−13.5pp PTS). `agents/backtest.py` only.)
**Last updated (prior):** April 11, 2026 ((65) **Odds Layer 3: Frontend Line Movement Indicators** — new visual indicator on pick cards surfacing whether FanDuel's implied OVER probability has moved toward or away from each pick since the morning odds snapshot. All data fields (`morning_implied_prob`, `market_implied_prob`, `bet_recommendation.calibrated_prob`) were already present on picks via `ingest/odds_today.py` `main()` (morning) + `pretip_sweep()` (hourly re-fetch) — this task makes them visible on the frontend without any Python/agent/workflow/ingest changes. **Scope:** `agents/build_site.py` only — 4 edits inside the `generate_html()` template string. **Edit 1 — CSS block** after existing `.edge-line` classes adds `.movement-line` (font-size 10px, margin-top 6px, nowrap, line-height 1.5), `.movement-line.agrees` (teal `#2dd4bf`, matches POSITIVE edge tier color), `.movement-line.disagrees` (amber `#FF9800`, matches opportunity-card no-edge color), `.movement-line.significant` (font-weight 600), `.movement-line .edge-shift` (font-size 9px, opacity 0.8), `.movement-line .edge-shift s` (text-decoration-color inherit + opacity 0.6 for strikethrough), `.movement-drawer` (font-size 10px, muted, monospace, opacity 0.75). **Edit 2 — new JS function `buildMovementLine(p)`** inserted immediately after `buildEdgeLine()` and before `displayConf()`. Returns empty string on three no-op conditions: `p.voided`, `morning_implied_prob == null`, `market_implied_prob == null`. Computes `delta = current - morning`; card face dead zone is `absDelta < 3.0` (smaller moves go to drawer only). For >3pp moves: `agrees = delta > 0` means market implied probability UP = market agrees with OVER pick; renders `"Market agrees ↑"` teal or `"Market disagrees ↓"` amber with rounded-integer prob shift `62→68%` and edge-shift span `<s>+X.Xpp</s> → +Y.Ypp` when `bet_recommendation.calibrated_prob` exists. **Edit 3 — `buildOddsSizing()` drawer extension** — new block inserted AFTER the existing `kelly_quarter` line and BEFORE the `alt_tiers` rendering. Shows movement detail for ALL moves >0.5pp (including the 0.5–3pp range that doesn't appear on card face). Uses underscore-prefixed locals (`_morning`, `_current`, `_delta`, `_absDelta`, `_dir`, `_color`, `_mEdge`, `_cEdge`) to avoid scope collision. Appends "── Line Movement ──" header + directional line (with inline color style) + morning prob + current prob unconditionally, plus 2 more conditional edge lines when `br.calibrated_prob != null`. **Edit 4 — pick card template wiring** — added `${{buildMovementLine(p)}}` between the existing lineup update IIFE `${{(function() {{ ... }})()}}` and `${{buildOddsSizing(p)}}`. Placement intentional: the card-face indicator sits below the lineup-update amendment badge and above the odds drawer toggle. **Thresholds:** card-face ±3pp (significant repricing, calibrated to skip typical 0.5–2pp line ticks), drawer ±0.5pp (everything above rounding noise). **Semantic design:** "Market agrees" (teal ↑) means market OVER prob went UP, agreeing with OVER pick — but the edge *shrinks* because market is closer to system calibrated prob. "Market disagrees" (amber ↓) means market OVER prob went DOWN, disagreeing — but edge *grows* assuming `calibrated_prob` is stable. Edge-shift strikethrough visualizes the dual nature (`edge +5.2 → +2.1pp` with strikethrough on +5.2 when market agrees and compresses the edge). **Voided picks excluded** — `p.voided` short-circuits to empty string even with populated prob fields. **Graceful degradation** — any missing prob field returns empty string and card renders as before. **Verification (8-point matrix + build test, all pass):** Python `ast.parse` clean; `function buildMovementLine` = 1; `movement-line` = 7 (CSS + JS refs); `buildMovementLine(p)` = 2 (definition + pick card call site); `Line Movement` = 1 (drawer header); `absDelta < 3.0` = 1 (card-face threshold); `_absDelta >= 0.5` = 1 (drawer threshold); `p.voided) return` ≥1 (voided guard). `python3 agents/build_site.py` produced `site/index.html` cleanly (0 today's picks for 4/11 expected — analyst hasn't run yet, no pretip data on 4/11 picks). **Not touched:** `buildEdgeLine()` function, `buildOddsSizing()` existing lines (only ADDED block after kelly_quarter), all other JS functions, Python code in `build_site.py`, `picks.json` schema, `agents/analyst.py`, `agents/auditor.py`, `agents/quant.py`, `agents/parlay.py`, `agents/lineup_watch.py`, `agents/lineup_update.py`, `ingest/odds_today.py`, all `.yml` workflow files. `agents/build_site.py` only.)
**Last updated (prior):** April 11, 2026 ((64) **Prompt text audit — stale player stat references fixed** — 4 targeted edits to example text in `agents/analyst.py` rule sections. No Python logic changes, no schema changes, no new rules, no `skip_reason` enum changes. All edits to worked examples and player-name illustrations inside existing KEY RULES blocks that were cited with stale or structurally-invalid scenarios. Motivated by a read-through of the prompt during the H26 Part 1 wiring — the CO-PRIMARY SCORER GATE (added in entry 61 Skip Rule Refinements) used a SAS Fox/Wembanyama example that no longer applied (Fox was traded from SAC before the 2025-26 season, plays at 17.8 PPG on SA not ~25 PPG; `player_game_log.csv` has 0 Fox rows under SAS abbrev for this season). Ground truth extracted from `data/player_game_log.csv` whitelisted-player PPG averages for the four references that needed updating. **Fix 1 — CO-PRIMARY SCORER GATE example (both prompts via `replace_all=true`):** Replaced "Fox (25 PPG) + Wembanyama (24 PPG) on SAS" with "Mitchell (25 PPG) + Harden (22 PPG) on CLE" — a structurally-valid co-primary pair present in the 2025-26 data (Donovan Mitchell leads CLE in PTS, Harden was traded from LAC to CLE at the Feb 2026 deadline and plays as a secondary creation hub). Both are `raw_avgs PTS >= 22.0` so the CO-PRIMARY GATE correctly exempts both from the `blowout_secondary_scorer` skip. **Fix 2 — PRIMARY/SECONDARY tiebreaker example rewrite (both prompts):** The Fox→Mitchell/Harden swap in Fix 1 created a structural problem in the fallback tiebreaker example below it — Mitchell and Harden are BOTH ≥22.0 PPG so the CO-PRIMARY gate always fires first and the tiebreaker never executes. The tiebreaker example was rewritten to a generic hypothetical: "Player A's best qualifying PTS tier = T25, Player B's best qualifying PTS tier = T20 → Player A is primary, Player B is secondary; if both qualify at the same tier, fall back to season PPG." No named players in the tiebreaker example any more — the generic form survives player movement and roster changes across seasons. **Fix 3 — Wembanyama PPG precision** (WITHOUT-STAR BASELINE rule in `build_prompt()` only, `build_pick_prompt()` didn't reference him): the example line cited "Wembanyama (24.3 PPG)" — ground truth from `player_game_log.csv` through 2026-04-09 is 24.56 PPG. Tightened to "Wembanyama (24.6 PPG)" to match the actual season average. No structural change — just a rounding precision fix. **Fix 4 — ELITE SCORER BLOWOUT EXEMPTION example expansion (both prompts):** The exemption cited only SGA as an example of a player above the 27.0 PPG threshold, which undersold the rule's coverage. Expanded to three examples above the threshold (SGA, Luka Doncic, Anthony Edwards — all ≥27.0 PPG per `player_game_log.csv`) plus two non-exempt players below it (Tyrese Maxey ~24 PPG, Kawhi Leonard ~21 PPG) for contrast. Makes the rule's coverage visually explicit in the prompt without changing any behavior. **Ground truth source:** `pd.read_csv("data/player_game_log.csv")` filtered to `dnp != "1"`, grouped by `player_name`, mean of `pts` column — the same aggregation used by `build_player_leaderboard()` in `analyst.py`. All four player references were cross-validated against this calculation before editing. **Verification (4-point spec grep matrix, all pass):** Python `ast.parse` clean on `agents/analyst.py`; `CO-PRIMARY SCORER GATE` references still count 2 (one per prompt, unchanged); `Mitchell \(25 PPG\)` references appear 2× (one per prompt, Fix 1 applied via replace_all); stale `Fox \(25 PPG\)` = 0, stale `24.3 PPG` = 0, stale `Wembanyama \(24 PPG\)` with single-decimal = 0; Luka Doncic + Anthony Edwards co-mentions inside ELITE SCORER BLOWOUT EXEMPTION block = 2 (one per prompt). No runtime tests needed — these are string fixes inside prompt text that feeds Claude, not Python logic. Untouched: `build_scout_prompt()` (scout is intentionally rules-free), `build_review_prompt()`, all other agents, workflow files, Python post-processors, JSON schema, `skip_reason` enum values, the structural logic of any rule (only the example text citing which players fit the condition). `agents/analyst.py` only — 4 Edit calls, all targeted to illustrative example text inside existing rule blocks.)
**Last updated (prior):** April 11, 2026 ((63) **H26 Skip Re-evaluation on Star Absence — Part 2 of 2** — new `build_skip_reconsiderations()` function in `agents/lineup_update.py` re-evaluates morning `merit_below_floor` PTS/AST skips when a team's leading scorer is confirmed OUT during afternoon injury refresh. Closes the H26 downstream loop — entry 62 (Part 1) surfaced the star-absence lift as an annotation, this task acts on it by recovering picks skipped at sub-70% confidence this morning that would cross the floor with the absence lift. **Scope:** `agents/lineup_update.py` only. **Output target:** `data/opportunity_flags.json` via existing `save_opportunity_flags()` with new `card_type="skip_reconsideration"` — no new JSON files, no frontend work (existing opportunity-card renderer handles the file). **New constant `_SA_POPULATION_DELTAS`** — mirror of `agents/quant.py` `SA_POPULATION_DELTAS` copied to avoid cross-module import (PTS_T15 +11.3, PTS_T20 +12.7, PTS_T25 +10.5, AST_T4 +9.6, AST_T6 +6.7). Inline comment flags both must be updated together if H26 deltas are recomputed. **`build_skip_reconsiderations(changes, player_stats, game_log, now_iso)`** inserted between `save_opportunity_flags()` and `build_pick_quant_summary()`. **Control flow:** filter changes to `new_absence` only → return `[]` if none → lowercased `absent_names` set → `load_morning_skips()` existence check → read `SKIPPED_PICKS_JSON` filtered to `date == TODAY_STR AND skip_reason == "merit_below_floor" AND prop_type in ("PTS", "AST")` (REB/3PM excluded — no H26 signal) → per-skip loop with sequential gates: player in `player_stats` → `star_absence_lift` present (graceful no-op if Part 1 not deployed) → star name in `absent_names` → **PERSONAL_DRAG_WARNING guard fires before any reconsider logic with explicit `continue`** (invariant — drag players structurally cannot benefit from population lift; prevents Jalen Green / Booker class of misses). **Two reconsideration paths:** (Path A) `compute_without_player_rates()` call for per-player tier hit rate when game_log available and `without_n >= 3` — `hit_rate >= 0.70` → reconsider, hit rate below 70% → record reasoning but do NOT reconsider; (Path B) population-delta fallback when no per-player data — only fires when `qualifier in {STRONG_PERSONAL_SIGNAL, POPULATION_ONLY, NEUTRAL_PERSONAL_DATA}`, prefers per-player `key_deltas[pop_key] > 0` when present, falls back to population delta only when qualifier is not NEUTRAL_PERSONAL_DATA. **Card schema:** `{date, generated_at, triggered_by (star_name), triggered_by_team, side="teammate", player_name, team, card_type="skip_reconsideration", prop_type, tier_considered, original_skip_reason="merit_below_floor", without_star_hit_rate_pct, without_star_n, star_absence_qualifier, reasoning}` PLUS required dedup fields `qualifying_tiers={}`, `upgrade_tiers={}`, `spread_delta=None`, `morning_context="skipped this morning (merit_below_floor)"` so the existing `save_opportunity_flags()` dedup on `(date, player_name_lower, triggered_by_lower)` works unchanged. **`main()` wiring** placed immediately after the existing `if has_absences: build_opportunity_suggestions()` block, gated on the same `has_absences` flag. Separate `not debug` and `debug` branches: non-debug calls `save_opportunity_flags(recons)`, debug prints cards to stdout without saving. Both branches reuse the in-scope `game_log` via defensive ternary `game_log_recon = game_log if game_log is not None else load_game_log()`. **Verification — 7-point spec grep matrix + logical invariant, all pass:** Python `ast.parse` clean; `def build_skip_reconsiderations` 1×; `_SA_POPULATION_DELTAS` refs 2×; `skip_reconsideration` refs 6×; `build_skip_reconsiderations` refs 3× (def + 2 main() call sites); `PERSONAL_DRAG_WARNING` refs 2×; `save_opportunity_flags(recons)` 1×; invariant — DRAG_WARNING guard at position 552 precedes `compute_without_player_rates` call (3995) and first `reconsider = True` (4758), and exits via `continue` at position 3786 before the next logic block at 3874. **Runtime test** — synthetic fixture with Tatum/Pritchard/White/Jalen Green `player_stats` entries + 5 fake skip records reproducing the motivating Tatum/Brown case: Tatum PTS T20 correctly reconsidered with reasoning "Jaylen Brown confirmed OUT; player-specific delta +27.3pp at PTS_T20 [STRONG_PERSONAL_SIGNAL]; original skip: merit_below_floor"; Pritchard AST T4 reconsidered with +27.9pp; Derrick White REB filtered out (PTS/AST-only); volatile_weak_combo filtered out (merit_below_floor-only); wrong-date skip filtered out (TODAY_STR guard); **Jalen Green PTS T20 filtered out by the DRAG_WARNING guard** (logged with "DRAG_WARNING for Devin Booker, skipping reconsideration" as expected). All 6 assertions pass. **H26 downstream loop now closed** — Part 1 (entry 62) + Part 2 (this entry) both shipped same day. **Not touched** per spec: `build_opportunity_suggestions()` unchanged, `compute_without_player_rates()` unchanged, `load_morning_skips()` unchanged, `save_opportunity_flags()` unchanged, no new JSON output files, `agents/quant.py`, `agents/analyst.py`, `auditor.py`, `parlay.py`, `backtest.py`, `build_site.py`, `lineup_watch.py`, all `ingest/*.py`, all `.yml` workflow files.)
**Last updated (prior):** April 11, 2026 ((62) **H26 Star-Absence Uplift Annotation — Part 1 of 2** — new `compute_star_absence_deltas()` function in `agents/quant.py`, new `star_absence_lift` field in `player_stats.json`, new `STAR_ABSENT_LIFT` two-line annotation in `build_quant_context()`, qualifier reference added to WITHOUT-STAR BASELINE rule in `build_prompt()` and to TEAMMATE ABSENCE USAGE ABSORPTION rule in `build_pick_prompt()`. **Scope:** `agents/quant.py` + `agents/analyst.py` only. **Annotation-only** — informational context for confidence reasoning, does NOT override any existing hard-skip or tier-qualification rule. **Companion to existing `key_teammate_absent`** — that field uses the top WHITELISTED teammate with absolute without-star rates for the WITHOUT-STAR BASELINE rule; this new field uses the team's OVERALL leading scorer (full player_log, ≥20 games, not whitelist-filtered) with with-vs-without DELTAS at H26 key tiers (PTS T15/T20/T25, AST T4/T6). Both fields coexist unchanged. **Constants added to quant.py:** `SA_KEY_TIERS`, `SA_POPULATION_DELTAS` (PTS_T15 +11.3, PTS_T20 +12.7, PTS_T25 +10.5, AST_T4 +9.6, AST_T6 +6.7 — from H26 backtest), `SA_MIN_STAR_GAMES=20`, `SA_MIN_CONDITION_N=3`. **`compute_star_absence_deltas()`** takes `(player_log, player_name, team, star_name, star_ppg, star_absent_game_ids)` and returns `{star_name, star_ppg, n_without, n_with, key_deltas (dict), qualifier}` or `None`. Qualifier values: STRONG_PERSONAL_SIGNAL (mean delta > +3pp), NEUTRAL_PERSONAL_DATA (±3pp), PERSONAL_DRAG_WARNING (< −3pp), POPULATION_ONLY (< 3 without-star games). Uses `.isin()` for game_id filtering (sort-direction independent); DNP exclusion via `dnp != "1"` coerced numeric. Returns None if player IS the star or has fewer than `MIN_GAMES` total active games. **Team-stars pre-computation** in `build_player_stats()` placed after `_rest_cache` block: iterates `teams_today`, for each team finds the full-log player (not whitelist-filtered) with highest PTS avg and ≥20 non-DNP games, builds `(star_name, star_ppg, absent_game_ids_set)` tuple in `_team_stars[team]`. Per-player loop looks up the team's star tuple and calls `compute_star_absence_deltas()` after the existing `compute_teammate_absence_splits()` + trend override block. Self-star guard prevents a leading scorer from showing star_absence_lift for themselves. **`star_absence_lift` output field** added to `stats_out[player_name]` dict immediately after `key_teammate_absent`. **`build_quant_context()` annotation rendering** placed after the existing `kta_line` block and before `stat_parts = []`. Two-line output when `sal["star_name"]` is in the `_out_set`: (1) population reference `  STAR_ABSENT_LIFT: [<star> OUT] population PTS T15-T25 +11 to +13pp, AST T4 +10pp`; (2) per-player line showing the highest-delta key tier from `key_deltas` with the qualifier tag. PERSONAL_DRAG_WARNING case appends " — population lift does NOT apply to this player". POPULATION_ONLY case shows `<3 without-star games [POPULATION_ONLY]`. `SA_MIN_CONDITION_N=3` hardcoded in analyst.py to avoid cross-module import coupling (documented in inline comment). `star_lift_line` wired into the `lines.append(...)` block between `kta_line` and `opp_absence_str`. **Prompt rule additions (both prompts):** (a) `build_prompt()` WITHOUT-STAR BASELINE — TWO REQUIRED GATES block gains a new paragraph explaining STAR_ABSENT_LIFT is informational context that does NOT override the two gates; per-qualifier guidance (PERSONAL_DRAG_WARNING → do NOT apply population lift, STRONG_PERSONAL_SIGNAL → factor positively, POPULATION_ONLY → directional only, NEUTRAL → no lift). (b) `build_pick_prompt()` KEY RULES — TEAMMATE ABSENCE USAGE ABSORPTION block gains the same semantic paragraph adapted to its existing wording (Pick prompt uses "TEAMMATE ABSENCE USAGE ABSORPTION" framing instead of "WITHOUT-STAR BASELINE TWO GATES"). Both references explicitly annotation-only, no directive rules. **Verification (9-point spec grep matrix + runtime invariant, all pass):** Python `ast.parse` clean on both files; `def compute_star_absence_deltas` 1×; `SA_KEY_TIERS` definition 1×; `"star_absence_lift"` field 1×; `_team_stars` refs 3×; `STAR_ABSENT_LIFT` refs in analyst 5×; `star_lift_line` refs 6×; `population PTS T15-T25` 1×; `PERSONAL_DRAG_WARNING` refs 3×; `key_teammate_absent` count = 7 (existing consumers unchanged, 1 new docstring reference in `compute_star_absence_deltas` docstring only). **Runtime invariant check** via `TODAY_STR` override against 2026-04-09 slate: 49 players got `star_absence_lift` populated, `n_with + n_without == total player non-DNP games on team` passes 49/49 (0 failures), self-star exclusion works (0 errors), qualifier distribution 37 STRONG / 6 NEUTRAL / 5 POPULATION_ONLY / 1 DRAG. **Motivating Tatum/Brown case reproduces exactly**: with Jaylen Brown in `_out_set`, Tatum's entry shows `n_without=3 n_with=11 STRONG_PERSONAL_SIGNAL PTS_T25=+57.6pp PTS_T20=+27.3pp` (spec cited "+27pp PTS T20" — exact match); Pritchard shows PTS_T20 +65.0pp (n=10); Derrick White shows AST_T6 +29.4pp. With empty OUT set, 0 STAR_ABSENT_LIFT annotations render (correctly gated). Generated `data/player_stats.json` from test run was reverted to git HEAD to avoid polluting the working directory — production workflow regenerates fresh each morning. **Part 2 deferred** — skip re-evaluation in `lineup_update.py` that reads `star_absence_lift` from `player_stats.json` is a separate future prompt. **Untouched** per spec: `compute_teammate_absence_splits()` function unchanged, `key_teammate_absent` output field unchanged, no `skip_reason` enum changes, no hard skip rules, no confidence overrides, `build_scout_prompt()`, `build_review_prompt()`, `auditor.py`, `parlay.py`, `backtest.py`, `build_site.py`, `lineup_watch.py`, `lineup_update.py`, all `ingest/*.py`, all `.yml` workflow files, `context/nba_season_context.md`.)
**Last updated (prior):** April 11, 2026 ((61) **Skip Rule Refinements — 4 edits to `agents/analyst.py` prompt text** — no Python logic, no schema changes, no new `skip_reason` enum values. All edits applied to BOTH `build_prompt()` (monolithic) AND `build_pick_prompt()` (Pick stage) — they share parallel copies of the same rule sections. `build_scout_prompt()` untouched (rules-free by design). **Edit A — `blowout_t25_skip` elite scorer exemption** (2 hunks in both prompts via `replace_all=true`, source text was byte-identical): (a) TIER CEILING RULES `PTS T25 BLOWOUT HARD SKIP` block now exempts players with `raw_avgs PTS >= 27.0` — they proceed through normal T25 evaluation where the 80% hit rate bar (8+/10) still applies and the existing `spread_abs >= 15` cap (74% max) still constrains the pick; non-elite scorers remain hard-skipped. (b) ELITE SCORER BLOWOUT EXEMPTION "Exception to the exemption" paragraph cross-references the new T25 carve-out. Rationale: H27 backtest (Apr 11, n=68 primary scorers at spread_abs ≥ 15) found T25 hits at 58.8% MARGINAL vs 50.6% baseline, NOT SUPPRESSED; graded false-skip rate was 100% (2/2: Maxey actual 32, Wembanyama actual 40). **Edit B — `blowout_secondary_scorer` threshold + gates + T10 exception** (applied separately in each prompt because source texts differed — monolithic had stale pre-H19 `spread_abs > 8` version, Pick had H19 `≥ 15` version; both rewritten to identical final text): (a) threshold lowered to `spread_abs >= 13.5` (splits the difference between H19's ≥15 and the original >8); (b) new `T10 FLOOR EXCEPTION` — the skip does NOT apply when pick tier is T10 PTS since T10 has near-certain population hit rates and min_floor rules adequately handle minute compression; (c) new `CO-PRIMARY SCORER GATE` — a player with `raw_avgs PTS >= 22.0` is always PRIMARY regardless of teammate tier comparison, preventing the rule from firing against one of two co-primary scorers (example: Fox ~25 PPG + Wembanyama ~24 PPG both above 22.0 → both primaries, rule fires for neither); (d) at `spread_abs 8–13` apply standard BLOWOUT_RISK penalty (-10 to -15pp) instead of hard skip (H19 backtest showed secondary scorer PTS at that range hits at 80.7%, lift=1.083 n=140); (e) pre-existing PRIMARY/SECONDARY tiebreaker text (based on best qualifying PTS tier) retained as fallback when co-primary gate doesn't apply. **Edit C — 3PM trend=down T1 iron_floor exception** (both prompts via `replace_all=true`, identical source): the unconditional T1 step-down skip now has an exception — if the player has `iron_floor on 3PM OR a 9+/10` hit rate at T1, hold at T1 (do not skip) and apply a -5% confidence penalty to reflect the declining trend. Fox 3PM T1 (actual 2) and Wembanyama 3PM T1 (actual 2) were both false-skipped on 4/10 by the unconditional step-down. The exception applies ONLY to the trend=down step-down from T1 — the trend=down + tough DvP hard skip and trend=down + limited minutes hard skip still override it when their conditions are met. **Edit D — Align monolithic 3PM blowout rule with H19** (`build_prompt()` only — `build_pick_prompt()` was already H19-correct): the stale `3PM hard skip — trend=down AND blowout_risk=True` block (hard-skipping all 3PM at `spread_abs > 8` on favored side) replaced with the H19-relaxed version — at `spread_abs 8–18` apply the existing trend=down step-down rule only (no hard skip); at `spread_abs >= 19` the unconditional extreme-blowout hard skip (pre-existing) still fires. The step-down benefits from Edit C's T1 iron_floor exception. H19 backtest (n=96–137) showed blowout_win 3PM at 78.8–79.2% above 71.8% baseline — hard skip was blocking a high-hit-rate population. **`skip_reason` enum unchanged:** `blowout_t25_skip`, `blowout_secondary_scorer`, `3pm_blowout_trend_down` all retained with exact same names and semantics — the rule text changed, the auditor-facing enum did not. `blowout_secondary_scorer` occurrence count in `analyst.py` = 8 (unchanged). **Verification (10-point spec grep matrix, all pass):** Python `ast.parse` clean; `EXCEPTION — elite scorers` 2× (one per prompt); `spread_abs >= 13.5` 4× (header + "At spread_abs >= 13.5" + T10 exception per prompt × 2); stale `SECONDARY SCORER SKIP.*spread_abs.*15` pattern = 0; `CO-PRIMARY SCORER GATE` 2×; `T10 FLOOR EXCEPTION` 2×; `iron_floor on 3PM OR a 9+/10` 2×; old `3PM hard skip — trend=down AND blowout_risk=True` header = 0; new `3PM trend=down in blowout context (spread_abs 8–18)` header 2× (both prompts now aligned); `SECONDARY SCORER SKIP.*spread_abs > 8` = 0. Untouched: `build_scout_prompt()`, `build_review_prompt()`, all other agents, workflow files, Python post-processors, JSON schema, skip_reason enum values and OUTPUT FORMAT documentation lines. `agents/analyst.py` only — 7 Edit calls total (2 via `replace_all=true`, 5 unique-context).)
**Last updated (prior):** April 11, 2026 ((60) **Playoff Player Adjustments context block** — new `build_playoff_adjustments(today_str)` sibling function added to `agents/analyst.py` immediately after `build_playoff_context()`. Returns a `## PLAYOFF PLAYER ADJUSTMENTS` prompt block with per-player directional guidance for 20 players synthesized from H27–H32 backtests (career playoff 2021–2025, minutes elasticity, confidence calibration, consistency index, series progression, blowout resilience). Players covered with net directions: SGA (NEUTRAL), Jokic (BOOST PTS/3PM), Ant Edwards (BOOST G1-4/caution G5-7), Luka (BOOST PTS), Tatum (pick AST, avoid 3PM), Mitchell (caution PTS, hard avoid 3PM), Brown (BOOST), Brunson (BOOST PTS), Harden (AST ONLY — -33pp late-series PTS fade), Curry (PTS G1-4 / AST G5-7), Murray (BOOST early), Booker (pick REB), Embiid (strong caution PTS/AST), KAT (strong caution), Herro (avoid all), Maxey (caution PTS/3PM/AST), J. Williams (BOOST), Reaves (BOOST), Kawhi (BOOST when healthy), Banchero (BOOST ⚠ limited sample). **Date-gated to `PLAYOFFS_R1_DATE` ("2026-04-18", inclusive)** — returns empty during regular season AND during play-in (4/14–4/17), since play-in teams have thin/no career playoff history. Sits alongside (not inside) `build_playoff_context()` which fires at the broader `PLAYOFF_START_DATE` (2026-04-14). **Annotation-only** — block header explicitly states "annotation-only — use to inform confidence direction alongside quant data, not as hard overrides"; no hard skips, no confidence modifiers, no overrides. **Wired into all 3 prompt builders:** (1) `build_prompt()` v1 fallback — new `_playoff_adj` + `playoff_adjustments_section` section variables constructed adjacent to `playoff_context_section`, f-string injection between `{series_context_section}` and `## PLAYER RECENT PERFORMANCE`; (2) `build_scout_prompt()` v2 scout — identical pattern (single `replace_all` Edit applied both because they share identical block text); (3) `build_pick_prompt()` v2 pick — inline `{build_playoff_adjustments(TODAY_STR)}` call placed directly after the existing `{build_playoff_context(TODAY_STR)}` inline call, matching Pick's lighter section-injection pattern (Pick docstring says it intentionally skips playoff_picture/leaderboard/team_defense). `build_review_prompt()` untouched per spec. **Verification (20 assertions, all pass):** Python `ast.parse` clean; 6-date gate matrix (4/10, 4/14, 4/17 → empty; 4/18, 4/20, 5/15 → block); 8 content markers present; block is 4,376 chars / 53 lines; at 4/20 all 3 prompt builders emit exactly 1× adjustments header + 1× SGA marker + 1× playoff_ctx header; at pre-R1 4/15 all 3 emit 0× adjustments header but still 1× playoff_ctx header (play-in gets general context, adjustments waits for R1). Verified via stubbed `anthropic`/`httpx`/`json_repair` + runtime `TODAY_STR` module-attribute swap per test — no temporary override written into `analyst.py`. Untouched: `build_playoff_context()` (sibling function), `PLAYOFF PROFILE` quant annotations in `build_quant_context()` (separate per-player system), `build_review_prompt()`, `PLAYOFFS_R1_DATE` and `PLAYOFF_START_DATE` constants. `agents/analyst.py` only.)
**Last updated (prior):** April 11, 2026 ((59) **Pretip sweep: closing-line re-fetch architecture** — `ingest/odds_today.py` `pretip_sweep()` only. Replaces "fetch once, dedup forever" with "re-fetch when closer to tip-off" — each hourly sweep re-fetches games where `(prior_mtt − current_mtt) >= CLOSING_LINE_MIN_IMPROVEMENT` (new constant, default 30 min). Motivated by CLV quality: a line captured 3–5 hours before tip is noisy because sharp-money movement, injury news, and lineup confirmations happen in the last 1–2 hours. New model converges toward the true closing line across sweeps, and the tip-off guard (entry 58) ensures the last successful pre-tip fetch is preserved. **Step 4 rewrite:** iterates `in_window_events` and classifies each as first-fetch (no prior → include), commenced (`mtt < 0` → skip, locked), missing prior mtt (legacy snapshot → defensive re-fetch), significant improvement (≥30 min → re-fetch), or marginal (< 30 min → skip). Counters: `n_refetch`, `n_skip_commenced`, `n_skip_marginal`. Status print: `Closing-line filter: 7 re-fetch, 1 commenced (locked), 1 first capture`. **Step 5 log update:** distinguishes first-fetch from re-fetch — re-fetches log `Re-fetched: AWAY @ HOME (was +Nmin, now +Mmin to tip)` showing the improvement. **Snapshot save path untouched:** `pretip_data.setdefault("snapshots", {})[event_id] = {...}` already overwrites by event_id, so re-fetched games naturally replace their prior snapshot — only the closest-to-tip capture is preserved. **Credit budget (paid tier, 20k/month):** ~20–28 credits/game/day (5–7 hourly fetches × 4 alternate markets) vs 4 under old model. For a 10-game slate: ~200–280 credits/day. **Unit tests (9 total):** 8-sweep synthetic walkthrough (Game A tips 4:30 PM → 5 fetches/20 credits/locked at 5 PM; Game B tips 7:00 PM → 8 fetches/32 credits, closing line captured at +0 min right at tip; total 52 credits matches spec); 6 edge cases (5-min marginal skip, 30-min threshold fetch, first-fetch no counter bump, legacy snapshot defensive re-fetch, mtt=0 fetch, mtt=-5 locked). All pass. **Verification:** `grep -c "already_fetched"` = 0 (old set fully removed); 19 refs to new logic across constant/classification/counters/status-print/re-fetch-log. Python `ast.parse` clean. Untouched: `PRETIP_GRACE_MINUTES`, tip-off guard, `morning_implied_prob` set-once guard, step 6 annotation loop, `compute_edge()`, `save_morning_baseline()`, `_write_pretip_file()`, `main()`.)
**Last updated (prior):** April 11, 2026 ((58) **Pretip sweep: tip-off guard on `market_implied_prob`** — `ingest/odds_today.py` `pretip_sweep()` only. Root cause: `PRETIP_GRACE_MINUTES=30` allows post-tip fetches for dedup/snapshot purposes, but step 6 (pick annotation loop) had no tip-off awareness and would happily overwrite `market_implied_prob` with post-tip lines. 4/10 incident: MIN@HOU fetched at `minutes_to_tip=-29` overwrote Amen Thompson AST T4 from 81.13 → 81.82, contaminating CLV. Fix (two parts): (1) after `in_window_events` construction (before dedup/early-exit), build `commenced_teams: set[str]` from any event with `minutes_to_tip < 0` — adds both `home_abbr` and `away_abbr`. Log line `[odds-pretip] Commenced teams (live line guard active): ...` when non-empty. (2) In the step 6 annotation loop, after the `fetched` match lookup and before the morning-implied capture, a tip-off guard checks `pick.team`/`pick.opponent` against `commenced_teams`. If the game has commenced AND the pick already has `market_implied_prob`, `continue` (preserve pre-game price). If commenced AND no prior price, log `[odds-pretip] WARNING: first odds capture for ... is post-tip — CLV may be unreliable` and allow the overwrite (first capture is better than null for `compute_edge()`). Phase 1 (fetch/dedup/snapshot) is completely unchanged — games in the grace window still get fetched and recorded in `odds_pretip.json` so future runs dedup correctly on `event_id`. CLV semantics: for games that commence before a clean pretip capture, CLV degrades to 0 (morning == market, no movement) — which is correct, not harmful. Synthetic unit test (4 picks): HOU picks with prior prices → SKIP (preserved); DET @ CHA pick at +204 min → normal overwrite; Ghost player with null prior on HOU → WARN + overwrite. All 4 assertions pass. `grep -n "commenced_teams\|tip-off guard\|post-tip" ingest/odds_today.py` confirms 5 set refs + guard comment + WARNING log. Python `ast.parse` clean. Untouched: `PRETIP_GRACE_MINUTES`, fetch/dedup logic, `morning_implied_prob` set-once guard, `main()`, `compute_edge()`, `save_morning_baseline()`, `_write_pretip_file()`.)
**Last updated (prior):** April 11, 2026 ((57) **Opportunity flags: FanDuel odds enrichment on tier cards** — `agents/lineup_update.py` + `agents/build_site.py`. Adds `market_implied_pct` and `market_odds` to each tier entry in opportunity flag output, and renders them on opportunity cards with green (edge) / orange (no edge) color coding. Motivated by 4/10's odds landscape where 67% of picks were FADE — users need to see market price next to quant hit rate to evaluate late-scratch opportunities. Purely additive — no behavioral changes for consumers that don't read the new fields. **`agents/lineup_update.py`:** (1) new constant `ODDS_AVAILABLE_JSON = DATA / "odds_available.json"` adjacent to `INJURIES_JSON` / `MASTER_CSV`; (2) new helper `_norm_odds_name(name)` immediately after `_norm()` — uses `re.sub(r"[^a-z0-9 ]", "", name.lower()).strip()` to match the normalization pattern in `ingest/odds_today.py`; deliberately different from `_norm()` which handles team abbreviations; `re` imported inline; (3) odds load block in `build_opportunity_suggestions()` placed after the `excluded_players` construction and before the morning-spread block — reads `odds_available.json`, validates `date == TODAY_STR`, extracts `players` dict into `odds_players`, graceful no-op on any failure (missing/stale file, parse error); (4) tier enrichment inside the per-prop scan loop, placed right after `tier_entry` construction and the `without_player_*` annotation — normalizes `tm_name` via `_norm_odds_name()`, looks up `odds_players.get(odds_key, {}).get(prop, [])`, walks entries for matching `tier`, copies `implied_prob` and `odds` onto `tier_entry` as `market_implied_pct` and `market_odds`. Both fields optional — enrichment covers both `qualifying_tiers` and `upgrade_tiers` paths (they share the same dict reference). **`agents/build_site.py`:** (1) `qtLines` JS template (qualifying-tier rows) extended with a `(mkt N%)` span rendered when `info.market_implied_pct` is present; color green (`#4CAF50`) when `market_implied_pct <= hit_rate_pct` (system has edge) or orange (`#FF9800`) otherwise; `Math.round()` for display; font-size 10px; guard `!== undefined && !== null`. (2) `utLines` JS template (upgrade-tier rows) given the same treatment after the `(upgrade)` label. Both use f-string `{{` / `}}` double-brace escaping because the JS is inside a Python f-string. **Graceful degradation:** missing/stale `odds_available.json` → `odds_players = {}` → no tier gains market fields → frontend unchanged; player not in odds file → enrichment skipped silently; tier not offered by FanDuel → enrichment skipped; JS guard suppresses the span when field is missing. **Unit-tested against real `data/odds_available.json` (137 players, 4/10):** `_norm_odds_name` produces correct output for all 5 previously-broken hyphen/apostrophe/period cases (karlanthony towns, deaaron fox, cj mccollum, jabari smith jr — matching the 4/9 player name normalization fix); 5 of 6 test names match real file keys (SGA missing because OKC isn't playing — expected); synthetic enrichment lookup `Jaylen Brown PTS T20 → mkt=88.89% odds=-800`; no-market path on Ghost Player leaves tier_entry untouched; color logic test confirms 5/5 edge/no-edge scenarios render correct color. `grep` confirms `ODDS_AVAILABLE`, `_norm_odds_name`, `odds_players`, `market_implied_pct`, `market_odds` all present in `lineup_update.py`; 4 `market_implied_pct` references in `build_site.py` (2 templates × 2 uses each). Python `ast.parse` clean on both files. Untouched: `opportunity_flags.json` schema (additive only), `save_opportunity_flags()`, `compute_lineup_diff()`, card_type logic, `classify_absent_player()`, opportunity-card left-column rendering, pick-card rendering outside the two templates, `_norm()`. Two-file change — `agents/lineup_update.py` + `agents/build_site.py`.)
**Last updated (prior):** April 11, 2026 ((56) **Lineup update: filter OUT/DOUBTFUL players from opportunity flags** — `agents/lineup_update.py` `build_opportunity_suggestions()` now accepts an optional `injuries: dict | None = None` kwarg (default preserves backward compat). When provided, the function builds an `excluded_players` set keyed by `name_lower` from any injury row with status OUT/DOUBTFUL/OFS, and the scanning loop skips any candidate player whose name is in that set. `main()` call site at line 1386 now passes the in-scope `injuries` dict. Root-cause incident on 4/10: Jokic's absence triggered an opportunity scan on DEN/OKC, surfacing SGA, Holmgren, Hartenstein, and Jalen Williams as opportunities despite all four being OUT for rest (Quickley/TOR had the same issue from a separate absence). Unit-tested with a synthetic Jokic-OUT scenario exactly matching the 4/10 pattern: pre-fix surfaced 5 suggestions (4 of them OUT players), post-fix surfaced 1 (the legitimate DEN teammate Jamal Murray), zero OUT players in post-fix output. Existing self-exclusion guard (skip the absent player themselves) is unchanged — this is an additional filter on the candidate side. `agents/lineup_update.py` only — signature + docstring + excluded_players build + loop check + call site kwarg. Not touched: `compute_lineup_diff()`, `save_opportunity_flags()`, `opportunity_flags.json` schema, `classify_absent_player()`, `_norm()`.)
**Last updated (prior):** April 11, 2026 ((55) **Analyst: review file staleness check on re-run** — `agents/analyst.py` `apply_review_flags()` guard rewritten. Previously: simple `if review_path.exists(): return` which treated every existing file as manual and blocked same-day re-runs from writing fresh review coverage. 4/10 effect: after the Scout 4096→12288 token fix expanded the shortlist from ~8 to 30+ picks on a re-run, 18 new picks had no review badges because the old auto-generated 15-pick review file was preserved by the existence guard. New guard: (1) load existing file (empty on JSON error), (2) if ANY entry lacks `source: "auto"` → manual file, skip overwrite (preserves manual review priority, handles mixed auto+manual files conservatively), (3) compute set-difference between current pick keys `(name.lower(), prop_type, pick_value)` and existing review keys, (4) if covers-all → skip regeneration (no API burn), (5) if stale → log "STALE — covers N picks but current run has M (K uncovered). Regenerating." and fall through to the existing write logic. Docstring updated with the 4-path guard documentation. Unit-tested against 6 scenarios (no-file, manual, mixed auto+manual, covers-all, stale 4/10 reproduction, empty file, corrupt JSON) — all pass. Manual files are still never overwritten; this is strictly a staleness fix for the auto path. `agents/analyst.py` only (only the guard block + docstring in `apply_review_flags()`).)
**Last updated (prior):** April 10, 2026 ((54) **picks.json cross-workflow merge guard for injuries.yml** — new `ingest/merge_picks.py` + modifications to `.github/workflows/injuries.yml` commit steps 2 and 3. Prevents the race condition where an in-flight `injuries.yml` run clobbers `picks.json` with a stale version after `analyst.yml` pushes new picks between the injuries checkout and commit (the 4/10 incident: re-run #3's 33 picks clobbered back to 15 six seconds later by an in-flight lineup_update commit). Guard logic: after `git pull --rebase`, save origin's picks.json alongside the pre-existing local copy, then call `merge_picks.py --local --origin --output`. Script compares today-pick counts: if `origin > local`, analyst pushed new picks during our run — use origin as base and overlay local's injury mutations (`voided`, `void_reason`, `lineup_risk`, `injury_status_at_check`, `injury_check_time`, `lineup_update`) onto matching picks by key `(player_name.lower(), prop_type, pick_value, date)`. Otherwise local is kept as-is (no-op). On any error, falls back to copying local → output and exits 1 — preserves pre-guard behavior so the guard can never regress. Matches the exact recovery logic proven on commit `a131c1f`. Applied to BOTH commit steps that touch picks.json — "Commit picks status" (after `lineup_watch.py`) and "Commit lineup amendments" (after `lineup_update.py`). First commit step ("Commit injuries") unchanged — doesn't touch picks.json. Unit-tested against 5 scenarios: normal path, 4/10 race reproduction, corrupt-JSON error fallback, analyst-reduction edge case, empty today-slate. All pass. Known limitations (not blocking): analyst re-runs that *reduce* pick count bypass the guard because the "origin > local" heuristic requires origin to have more; same-count-different-picks also not caught. Both edge cases remain manually recoverable via the same merge script. `ingest/merge_picks.py` (NEW), `.github/workflows/injuries.yml`.)
**Last updated (prior):** April 10, 2026 ((53) **Parlay: FADE pre-filter + candidate diversification** — `agents/parlay.py`. Moves the "max 1 FADE leg per parlay" constraint from the Claude prompt into Python pre-filtering inside `build_candidates()` and adds candidate-pool diversity check to prevent a single dominant player from monopolizing the top-N. Root-cause: 4/10 incident where every one of 15 top candidates contained Kawhi Leonard, creating unresolvable constraint tension for Claude (all combos had 2+ FADE legs). Design principle: **constraint satisfaction belongs in Python; curation belongs in the LLM**. Fix: (1) FADE pre-filter — new block in `build_candidates()` counts `recommendation_tier == "FADE"` per combo, skips combos with ≥2 FADE legs, graceful when `bet_recommendation` is absent. Claude now never sees combos with 2+ FADE legs — the paralysis scenario is impossible by construction. (2) Diversity fix — after composite-score sort, if any player appears in >80% of top `TOP_N_TO_CLAUDE` (15) candidates, bottom third is replaced with the best alternatives excluding that player. Uses `Counter` from `collections`. Logs `[parlay] Diversity fix: X appeared in N/15 — replaced bottom K with alternatives`. Graceful when no alternatives exist or candidate pool ≤ TOP_N_TO_CLAUDE. (3) Prompt rule 6 (edge quality) and AVOID item softened from hard constraint to advisory — "Candidates with 2+ FADE legs have already been filtered out; prefer combos where the other legs compensate the single allowed FADE leg." Unit-tested with synthetic data: 3 FADE + 2 POSITIVE → all survivors have ≤1 FADE; 5 all-FADE → 0 candidates (graceful); dominant Star player → diversity fix fires, 10 with Star + 5 without. Untouched: `score_combination()`, odds/correlation/trim filters, `enforce_concentration_cap()`, `call_parlay_agent()`, `save_parlays()`. `agents/parlay.py` only.)
**Last updated (prior):** April 10, 2026 ((52) **Parlay: graceful failure + empty-result handling** — `agents/parlay.py`. Fixes a workflow-killing crash where `call_parlay_agent()` called `sys.exit(1)` on JSON parse failure, blocking the `analyst.yml` commit step and preventing pick deployment. 4/10 incident: 30 picks generated + odds-enriched, but parlay step crashed because Claude returned prose instead of JSON — no picks committed. Fix: (1) `call_parlay_agent()` now returns `[]` on every failure path (API error, empty-array literal, json.loads failure, bracket-extraction failure, `json_repair` failure); only remaining `sys.exit(1)` is the missing-API-key config guard at line 551. Added explicit `"[]"/"[ ]"` literal check at the top for Claude's clean empty-result case. Fallback 1 (bracket extraction) tightened to use `rfind("]")` for proper slice bounds. Fallback 2 added: `json_repair` library (same one used by `analyst.py`). API error catch wraps the `client.messages.create()` call. (2) `build_parlay_prompt()` OUTPUT FORMAT extended with an explicit empty-array escape hatch: "If no valid parlays can be constructed… return an empty JSON array: `[]`" + "NEVER output prose analysis — always output a JSON array, even if it's empty." This fixes the root-cause prompt gap that caused Claude to produce prose reasoning on 4/10. (3) `main()` short-circuits on zero parlays: prints "No parlays to save — skipping save step" and `sys.exit(0)` before `enforce_concentration_cap()` and `save_parlays()`, so empty bundles are never written to `parlays.json`. Downstream effect: parlay failures are now non-fatal, workflow continues to commit step, picks deploy normally even on days when no valid parlays can be constructed. Untouched: `build_candidates()`, `score_combination()`, `enforce_concentration_cap()`, `save_parlays()`, FADE/POSITIVE/STRONG edge quality rules, parlay selection criteria. `agents/parlay.py` only.)
**Last updated (prior):** April 10, 2026 ((51) **Frontend: Pick card headline confidence = calibrated prob** — `agents/build_site.py`. Pick cards (regular, top-picks, best-bets) now display `Math.round(bet_recommendation.calibrated_prob)` instead of raw `confidence_pct` as the headline `% conf` number. Raw stated confidence moved into the Odds + Sizing drawer as the first line `System conf: X%`. Fallback to `confidence_pct` preserved when `bet_recommendation` is null/NO_MARKET. New JS helper `displayConf(p)`. Three headline sites updated: `.conf-line` (regular card), `.tp-conf` (top-picks), `.tp-conf` (best-bets). `buildEdgeLine()` still reads `recommendation_tier` + `calibrated_edge_pct` — unchanged. Motivation: previous display showed "84% conf" next to "NEUTRAL (+2.8pp)" but the edge calc uses 88.3% calibrated — user couldn't reconcile the numbers. Now 85% headline + FADE (-8.5pp) reads consistently (85 − 93.3 ≈ -8.3). Untouched: `odds_today.py` computation, `confidence_pct` field in picks.json, Results tab pick history table, Parlays tab leg display, lineup-update morning/revised reasoning text, `get_top_picks()` and best-bets sort tiebreakers (still use raw `confidence_pct` for ranking). Browser-verified on 2026-04-10 15-pick slate: 15 conf-line + 3 tp-conf cards all display calibrated values; sample drawer shows `System conf: 74%` as first line; 18 edge badges render unchanged. `agents/build_site.py` only.)
**Last updated (prior):** April 10, 2026 ((50) **Analyst: Scout token budget + shortlist validation fix** — `SCOUT_MAX_TOKENS` raised 4096 → 12288 in `agents/analyst.py`. Root cause: 4096 physically capped the Scout at ~8–9 structured shortlist entries at ~450 tokens each, far short of the 20–25 target. On 2026-04-10 a 57-player slate produced only 8 shortlisted players; strong candidates (KAT 85% PTS, Brunson 75% PTS, OG Anunoby 90% PTS, 40+ others) were never seen by the Pick stage. New undersize fallback in `main()`: when `active_count >= 40` and `len(shortlist_names) < 12`, print a warning and fall back to single-call `build_prompt()` mode (identical to the existing `<5 matched` pattern). Small-slate behavior unchanged. `build_scout_prompt()` INCLUSION GUIDANCE rewritten with stronger imperative language — "CRITICAL: You are the only gate between 57 eligible players and the Pick agent" — explicitly naming B2B, tough defense, and blowout risk as NON-reasons to exclude (those are Pick-agent penalty rules, not Scout gates) and forbidding the Scout from applying tier-walk or confidence penalty reasoning. Cost impact: ~$0.03–0.05 per Scout call at Opus pricing, negligible. `agents/analyst.py` only — no changes to `call_scout()` JSON parsing, `call_analyst()`, `call_review()`, `build_pick_prompt()`, `build_review_prompt()`, `MAX_TOKENS`, `MODEL`, `MODEL_LARGE`, or `LARGE_SLATE_THRESHOLD`.)
**Last updated (prior):** April 10, 2026 ((49) **Backtest: H26 star absence teammate impact** — new `--mode star-absence` in `agents/backtest.py`. Motivated by 4/9 Tatum penalty-skip + Jaylen Brown OUT scenario. Identifies each team's leading scorer (≥20 non-DNP games, highest PTS avg), finds star-absent game dates via full-log `team_dates − star_played_dates`, computes per-teammate tier hit rate deltas at every PTS/REB/AST/3PM tier (min 3 games per condition). Population-level weighted average by `min(n_with, n_without)`. Constants: `SA_MIN_STAR_GAMES=20`, `SA_MIN_ABSENT_GAMES=5`, `SA_MIN_CONDITION_N=3`. Writes `data/backtest_star_absence.json`. **Production run (14 teams, 31 obs, 2026 season): strong signal** — PTS_T15 +11.3pp, PTS_T20 +12.7pp, PTS_T25 +10.5pp, AST_T4 +9.6pp, AST_T6 +6.7pp (all cross +3pp SIGNAL threshold). Motivating BOS case validated: Tatum PTS T20 Δ=+27.3pp (4 without-Brown games), Pritchard PTS T20 Δ=+71.2pp (6 without-Brown games). One sort-key bug found and fixed during verification (`"T"` → `"_T"` split). Downstream consumers (skip re-evaluation in `lineup_update.py`, absence-driven analyst confidence rule) deferred pending multi-season verification. `agents/backtest.py` only.)
**Last updated (prior):** April 10, 2026 ((48) **Pre-Game Reporter: team abbreviation fix + ESPN error split** — two bugs in `agents/pre_game_reporter.py`. (a) `load_target_players()` now matches on BOTH `team_abbr` (Rotowire: NYK, SAS, GSW) and `team_abbr_alt` (ESPN: NY, SA, GS) from `player_whitelist.csv` when filtering against `teams_today` (from `nba_master.csv`, which uses ESPN short codes). Previously only `team_abbr` was checked, silently dropping 10 players: Brunson, KAT, OG Anunoby, Mikal Bridges (NYK); Wembanyama, De'Aaron Fox, Castle, Vassell, Harper (SAS); Curry (GSW). Offline test confirmed — `teams_today={'NY','SA','GS'}` returns exactly 10 players. Parallel fix to the 2026-03-25 whitelist-tuple fix in `quant.py`/`analyst.py`. (b) Single `fetch_errors: list[str]` in `main()` replaced with three typed lists: `no_id_errors` (athlete_id missing from `player_dim.csv`), `espn_errors` (ESPN HTTP/network failure), `no_news_players` (fetch succeeded but returned zero items). `fetch_errors = no_id_errors + espn_errors` kept for backward compat. Log line shows all four counts including "with items" derived from total minus the three buckets — every target player accounted for. `write_output()` extended with three optional kwargs written as new top-level keys in `pre_game_news.json`. `write_empty()` unchanged. Downstream compat: analyst reads only `player_notes`/`game_notes`/`suggested_context_updates`/`staleness_flags` — new fields are additive and diagnostic-only. Ships alongside ROADMAP_active W10 upgrade to SYSTEMIC — all per-athlete ESPN fetches currently failing, news arriving via league feed only. `agents/pre_game_reporter.py` only.)
**Last updated (prior):** April 10, 2026 ((47) **Odds: morning baseline capture + pre-tip sweep timing fix** — two bugs blocking CLV computation. (a) **Primary:** `ingest/odds_today.py main()` now writes `morning_implied_prob` directly in its match block after `pick["odds_fetched_at"] = fetched_at`, guarded by `if "morning_implied_prob" not in pick`. Previously the field was written only by `pretip_sweep()` as a side effect of overwriting `market_implied_prob` — and pretip wasn't executing reliably (see b). CLV in `auditor.save_audit()` no longer depends on pretip firing; the morning `analyst.yml` run now populates both `market_implied_prob` AND `morning_implied_prob` to the same value, and pretip (if it runs) later overwrites only `market_implied_prob`. Degrades cleanly to CLV=0 when pretip fails. (b) **Secondary:** `pretip_sweep()` timing relaxed for GitHub Actions cron delays (typically 1–6h). New module constant `PRETIP_GRACE_MINUTES = 30` — games that started ≤30 min ago are still fetchable (FanDuel pre-game lines linger briefly post-tip). `minutes_to_tip < 0` check changed to `minutes_to_tip < -PRETIP_GRACE_MINUTES`. Default `window_minutes` widened 75 → 360 in both the function signature AND argparse default; event_id deduplication keeps credit cost unchanged. Log lines + docstring updated. (c) **Workflow:** `.github/workflows/odds_pretip.yml` schedule rewritten from 10 entries every 30 min 3:00–7:30 PM PT → 8 entries hourly noon–7 PM PT. Starting at noon gives 4+h of buffer before earliest tip-offs. `picks.json` schema expectation: every odds-matched pick has `morning_implied_prob` after morning run. `ingest/odds_today.py`, `.github/workflows/odds_pretip.yml`.)
**Last updated (prior):** April 10, 2026 ((46) **Frontend — Playoff Career Profiles panel + Playoff Game Explorer** — Research tab in `agents/build_site.py` now has three stacked collapsible sections instead of one: (1) **Playoff Career Profiles** — 58 sortable overview cards (sort by Games / PTS Δ / REB Δ / AST Δ / 3PM Δ / FG% Δ / A–Z) with colour-coded same-season deltas, season chips, expandable per-season breakdown, small-sample badge for <10 games; (2) **Playoff Game Explorer** — filterable (player/stat/season/round/opponent/H-A) explorer with tier hit rates + grouped-by-series game log and inferred round labels (R1/R2/CF/Finals); (3) **Player Explorer** — existing current-season explorer, extracted into `renderCurrentSeasonExplorer(container)`, wrapped in a `<details open>` section labelled "2025-26 season game logs", `runExplorer()` and all `ex-*` DOM ids preserved byte-for-byte. New Python `build_playoff_data()` reads `data/playoff_career_log.csv` once at build time, filters zero-minute / All-Star rows, groups by player, computes deltas + per-season detail + per-game list with inferred series numbers; graceful no-op on missing file. New JS: `ROUND_LABELS` + `roundLabel()` + `round1()` helpers, `renderPlayoffProfiles()`, `renderPlayoffExplorer()`, `runPlayoffExplorer()`, `renderCurrentSeasonExplorer()`, plus a rewritten 3-section `renderResearch()`. `DATA.playoff` embedded in JS DATA object. Pure static, no LLM calls. Build result: 58 profiles + 1,879 game rows; site 1.14 MB → 1.53 MB (+391 KB). Browser-verified: sort toggles work, Banchero +4.3 leads PTS sort, Tatum × 2023-24 × R1 filter shows 5 games vs MIA, current-season explorer unchanged. `agents/build_site.py` only.)
**Last updated (prior):** April 10, 2026 ((45) **Daily ingest `season_type` integration + playoff dual-write** — added `season_type` column to `nba_master.csv` (from ESPN scoreboard `season.type`, position 4 after `game_time_utc`) and to `player_game_log.csv` (position 2 after `season_end_year`, joined via `game_id`). `ingest/espn_daily_ingest.py`: extraction via `safe_int((ev.get("season") or {}).get("type"))` in `parse_scoreboard()`. `ingest/espn_player_ingest.py`: new `load_master_season_type_lookup()` helper, `season_type_lookup` param on `ingest_events()`, new `append_playoff_rows()` dual-write called after main CSV write. Dual-write filters to `season_type == 3`, transforms to `playoff_career_log.csv` schema, upserts by `(player_id, game_id)`, returns immediately when no playoff rows present (zero overhead during regular season). `.github/workflows/ingest.yml` commits `playoff_career_log.csv`. Historical rows (pre-change) carry NaN for `season_type` — expected and correct. Verified manually against 2026-04-09 run: nba_master season_type=2 for 21 new rows, player_game_log season_type=2 for 36 new rows, playoff_career_log.csv unchanged at 20,139 rows, quant re-run produced identical `playoff_profile` output (58 of 68 players). `ingest/espn_daily_ingest.py`, `ingest/espn_player_ingest.py`, `.github/workflows/ingest.yml`.)
**Last updated (prior):** April 9, 2026 ((44) **Playoff career splits quant integration** — new `compute_playoff_splits(player_name, playoff_df)` in `agents/quant.py` reads `data/playoff_career_log.csv` (2021–2025) and computes career playoff vs regular-season averages + deltas for PTS/REB/AST/3PM/FG%. Same-season comparison (only regular-season rows from seasons where the player also has playoff data). FG% from aggregate FGM/FGA totals, not per-game mean of percentages. `PLAYOFF_MIN_GAMES = 5` threshold. New `load_playoff_career_data()` loader filters zero-minute rows, graceful on missing file. `playoff_profile` wired into per-player dict in `build_player_stats()` immediately after `shooting_regression`. `agents/analyst.py`: new `PLAYOFFS_R1_DATE = "2026-04-18"` constant (separate from `PLAYOFF_START_DATE = "2026-04-14"` — play-in slate excluded). `build_quant_context()` emits `  PLAYOFF PROFILE (Ng, N seasons): PTS +X | REB +Y | AST +Z | 3PM +A | FG% +B%` annotation line after the H2H block when `TODAY_STR >= PLAYOFFS_R1_DATE` (inclusive) and `playoff_profile` is non-null. `(small sample)` tag appended when `playoff_games < 10`. Annotation-only — no directive rules. Offline verification against 20,137-row CSV: 58/65 players eligible, 11 small-sample, zero invariant violations. `agents/quant.py`, `agents/analyst.py`.)
**Last updated (prior):** April 9, 2026 ((43) **Player name normalization fix in athlete ID lookups** — `data/player_dim.csv` stores `player_name_norm` with hyphens replaced by spaces and apostrophes/periods removed, but `ingest/espn_playoff_backfill.py`, `agents/pre_game_reporter.py`, and `agents/post_game_reporter.py` queried the lookup map with raw `.lower()`. 5 players silently skipped: KAT, SGA, De'Aaron Fox, Nickeil Alexander-Walker, Jabari Smith Jr. Fix: identical `_norm_name()` helper added to all three files (hyphens → space, apostrophes/periods stripped, whitespace collapsed, lowercase). Applied at all lookup call sites — 1 in backfill `main()`, 1 in pre_game_reporter main loop (left `target_names_lower` headline-match set unchanged), 2 in post_game_reporter (both news-fetch and DNP fallback loops). Backfill re-run for the 5 players added +1,617 rows. Total now: 20,139 rows (18,256 regular / 1,883 playoff) for 65 players. Remaining 3 whitelisted players with no rows are 2025-draft rookies with no pre-2026 history (Harper, Knueppel, Edgecombe) — expected. `ingest/espn_playoff_backfill.py`, `agents/pre_game_reporter.py`, `agents/post_game_reporter.py`.)
**Last updated (prior):** April 9, 2026 ((42) **ESPN Playoff Career Backfill** — new standalone local-run script `ingest/espn_playoff_backfill.py` fetches 2021–2025 regular + playoff gamelogs from ESPN public v3 endpoint for all active whitelisted players. Probe-first format discovery (`--probe` writes `data/probe_gamelog_response.json`). Actual ESPN format differs from spec's three proposed patterns: top-level `labels` + top-level `events` dict (metadata) + `seasonTypes[].categories[].events[]` with inline `{eventId, stats:[array]}` parallel to labels. Compound shooting fields `FG="11-21"`, `3PT="3-7"`, `FT="5-7"` split via `_split_compound()`. Season type filtered by `displayName` since API returns all types regardless of query param. Writes `data/playoff_career_log.csv` (21 columns, upsert idempotent on `(player_id, game_id, season_type)`). Full backfill run complete: 18,522 rows (16,768 regular / 1,754 playoff) for 60 players. Zero null PTS/minutes, zero shooting invariant violations. 5 active whitelisted players missing from `player_dim.csv` due to name normalization (KAT, SGA, Fox, NAW, Jabari Smith Jr.) — logged as warnings. Downstream consumer: future `compute_playoff_splits()` in `quant.py` (separate prompt). `ingest/espn_playoff_backfill.py` (NEW), `data/playoff_career_log.csv` (NEW).)
**Last updated (prior):** April 9, 2026 ((41) **H2H Splits — per-opponent tier hit rates annotation** — new `compute_h2h_splits(grp, opponent)` in `agents/quant.py` computes per-stat tier hit rates from the player's full season game log vs today's specific opponent. `H2H_MIN_GAMES = 2` constant added near `MIN_MATCHUP_GAMES`. DNP exclusion applied once to the filtered H2H subset — sample invariant: `games` count is shared across all tiers and all stats. Wired into `build_player_stats()` as new `h2h_splits` field in `player_stats.json` (positioned between `matchup_tier_hit_rates` and `spread_split_hit_rates`). Rendered in `agents/analyst.py` `build_quant_context()` as a compact line appended to `stat_parts` after the per-stat loop: `H2H vs BOS (4g): PTS T20 3/4(75%) | AST T6 3/4(75%)`. Shows the highest qualifying tier (≥70% rate) per stat; no line when no stat qualifies or when null. Annotation-only — no directive rules, no confidence adjustments, no tier step-downs. Replaces formal H9 backtest (CLOSED). Signal accumulates through playoffs. `agents/quant.py`, `agents/analyst.py`.)
**Last updated (prior):** April 9, 2026 ((40) **Playoff Context Block + CLV Tracking + Pre-Tip Odds + Session Fixes** — comprehensive session covering 18 changes across 8 files. Key additions: (a) `build_playoff_context()` — date-gated `## PLAYOFF CONTEXT — POSTSEASON MODE` injected into all 3 prompt builders on/after `PLAYOFF_START_DATE = "2026-04-14"`; annotation-only behavioral framing for postseason play. (b) CLV Tracking (Layer 2) — `pretip_sweep()` persists `morning_implied_prob` on picks before overwriting; `auditor.py` computes per-pick `clv_pp` in `save_audit()`, aggregates `clv_summary` in `save_audit_summary()`. (c) Pre-Tip Odds Sweep (Layer 1) — `pretip_sweep()` in `odds_today.py` fetches FanDuel odds for games tipping within window, updates picks, logs line movement; `_parse_fanduel_outcomes()` extracted from `main()`; new `odds_pretip.yml` workflow. (d) `walked_tier` integer field added to pick JSON schema; `reconcile_pick_values()` rewritten — primary path reads `walked_tier` directly, regex fallback preserved for transition. (e) `enforce_market_gate()` Python post-processor in `save_picks()` chain (third step after filter + reconcile). (f) Best Bets frontend section + Top Pick edge tiebreaker. (g) Alt-tier edge display in Odds+Sizing drawer. (h) Bug fixes: odds calibration key mismatch, post-processor greedy regex, frontend calibrated_prob ×100, parlay JSON extraction fallback, auditor.yml bs4+rotowire deps, injuries.yml last-run trim, pre-game reporter staleness token + ESPN error logging, trim badge removal.)
**Last updated (prior):** April 7, 2026 ((38) **Playoff Matchup Agent** — new pure-Python agent `agents/playoff_matchup.py`. Reads `data/playoff_bracket.json` (manually created by Oliver) + `nba_master.csv` + `player_game_log.csv` + whitelist. Computes per-series game results, series state tags (dominant/favorable/even/trailing/desperate), per-player series averages + tier hits + season H2H averages + tier hits + H2H home/away split. Writes `data/playoff_matchup.json` with pre-formatted `context_block`. `agents/analyst.py`: `PLAYOFF_MATCHUP_JSON` constant, `load_series_context()` function, `series_context` param added to all 3 builders + 4 call sites in `main()`. Injected as `## SERIES CONTEXT — PLAYOFFS` between TEAM DEFENSIVE PROFILES and PLAYER RECENT PERFORMANCE in `build_prompt`/`build_scout_prompt`; between SCOUT SHORTLIST and TODAY'S GAMES in `build_pick_prompt`. `.github/workflows/analyst.yml`: step between quant and pre_game_reporter, `playoff_matchup.json` in commit FILES. Graceful no-op: exits if `playoff_bracket.json` absent; `load_series_context()` returns `""` if `playoff_matchup.json` absent/stale. `agents/playoff_matchup.py` (NEW), `agents/analyst.py`, `.github/workflows/analyst.yml`.)
**Last updated (prior):** April 7, 2026 ((37) **Post-Game Reporter rewrite — ESPN recaps + Rotowire news + Claude classification** — complete pipeline rewrite of `agents/post_game_reporter.py`. Replaced Brave Search web narratives with ESPN game recap pages (fetched per `game_id` from `nba_master.csv`, parsed via BeautifulSoup `div.Story__Body` / `<article> <p>` fallback, truncated to 3000 chars) and Rotowire authenticated news feed (`news.php`, same login as `ingest/rotowire_injuries_only.py`, matched by last name). Replaced all deterministic phrase-matching (`_INJURY_EXIT_TERMS`, `_DNP_TERMS`, `classify_from_news()`, `news_contains_injury_language()`) with single Claude API call (`call_claude_classify_events()`) receiving three source blocks per missed-pick player → classifies `event_type` + `confidence` + `web_narrative`. Removed: `fetch_web_narratives()`, `call_claude_summarise_narratives()`, `BRAVE_SEARCH_URL`, `INJURY_SCAN_TERMS`. Added: `load_yesterdays_game_ids()`, `fetch_espn_recaps()`, `login_rotowire()`, `fetch_rotowire_news()`, `call_claude_classify_events()`, `ESPN_RECAP_URL`, `ROTOWIRE_NEWS_URL`, `CLAUDE_MODEL`, `MASTER_CSV`. Output schema unchanged. Legacy fields `injury_language_detected` (always False) and `injury_scan_term` (always None) preserved for auditor compatibility. Graceful degradation: source failures → Claude classifies with available sources; Claude failure → fallback deterministic DNP from box scores. Root cause: Wembanyama 4/6 rib injury misclassified as `no_data` because "won't return" not in hardcoded term list. `agents/post_game_reporter.py` only.)
**Last updated (prior):** April 7, 2026 ((36) **Frontend Odds Integration Display** — `agents/build_site.py` extended with odds/edge/sizing UI on pick cards. Two new elements per pick: (1) colored recommendation tier label (`buildEdgeLine()`) with calibrated edge shown in the right column below confidence line — STRONG=#22c55e, POSITIVE=#2dd4bf, NEUTRAL=var(--muted), FADE=#ef4444; NO_MARKET returns empty string; (2) expandable "Odds + Sizing" drawer (`buildOddsSizing()`) in left column before tier-walk toggle, showing market line, market prob, calibration band, calibrated prob, edge (pp), quarter-Kelly fraction. `toggleOddsSizing()` uses same `btn.nextElementSibling` pattern as existing tier-walk and lineup-update toggles. Top Picks cards also show `buildEdgeLine(p)` after confidence. All rendering gracefully handles missing `bet_recommendation` — returns empty string when absent or NO_MARKET. New CSS: `.edge-line` (+ `.strong`/`.positive`/`.neutral`/`.fade`), `.odds-sizing-toggle`, `.odds-sizing-body`. `agents/build_site.py` only — no workflow or agent files touched.)
**Last updated (prior):** April 7, 2026 ((35) **Calibration-Corrected Edge Enrichment** — `ingest/odds_today.py` extended with `compute_edge()` function and two helpers: `_load_calibration_bands()` reads `audit_summary.json` confidence_calibration section, `_get_calibrated_prob()` maps stated confidence to calibrated probability via band lookup (70-75% / 76-80% / 81-85% / 86%+). `compute_edge()` iterates today's non-voided picks, computes calibrated edge vs market implied probability, derives quarter-Kelly fraction, assigns recommendation tier (STRONG >+8pp / POSITIVE +3–8pp / NEUTRAL ±3pp / FADE <−3pp / NO_MARKET), and writes `bet_recommendation` sub-object to each pick in-place. New constant: `AUDIT_SUMMARY_JSON = DATA / "audit_summary.json"`. `main()` refactored: steps 6–8 are now compute_edge → single atomic picks.json write → odds cache write (was: picks.json write → odds cache write). Graceful degradation: missing/empty calibration data falls back to stated confidence / 100 as probability. `ingest/odds_today.py` only — no workflow or agent files touched.)
**Last updated (prior):** April 7, 2026 ((34) **Market Availability Gate** — integrated `odds_available.json` into the analyst pipeline as an unconditional hard rule: if no FanDuel alternate market exists for a player+prop at the analyst's selected tier, the pick MUST NOT be generated (`no_market` skip_reason). `agents/analyst.py`: `ODDS_AVAILABLE_JSON` constant, `load_available_markets()` + `format_available_markets()` functions, `available_markets: str = ""` parameter added to all 3 prompt builders (`build_prompt`, `build_scout_prompt`, `build_pick_prompt`), `## FANDUEL MARKET AVAILABILITY` section injected into prompts, `MARKET AVAILABILITY GATE` as priority 0 in KEY FRAMEWORK (above hard skips), `no_market` added to skip_reason enum and HARD SKIPS list, wired in `main()` at all 4 call sites (scout, both fallbacks, pick). `.github/workflows/analyst.yml`: `ODDS_API_KEY` env var, prefetch step before analyst, enrichment step after analyst, `odds_available.json` + `odds_today.json` in commit FILES (12 entries, was 10). `.github/workflows/odds.yml`: converted to manual-only trigger; primary odds flow now in `analyst.yml`. Graceful degradation: missing/stale `odds_available.json` disables gate entirely. `agents/analyst.py`, `.github/workflows/analyst.yml`, `.github/workflows/odds.yml`.)
**Last updated (prior):** April 7, 2026 ((33) **Odds prefetch mode (`--prefetch`)** — `ingest/odds_today.py` extended with `prefetch_all_markets()` function and `argparse` CLI dispatch. `--prefetch` fetches ALL available FanDuel alternate player prop markets for today's NBA games before the analyst runs; writes `data/odds_available.json` (per-player, per-prop, per-tier available lines with implied probabilities). New constants: `ODDS_AVAILABLE_JSON`, `WHITELIST_CSV`, `VALID_TIERS`. New helper: `_load_whitelist_teams()` loads active team abbrevs from whitelist CSV to filter API calls to relevant games only. Output schema: `{as_of, games [{home_team, away_team, players [{name, props [{prop_type, tiers [{tier, line, over_price, implied_prob}]}]}]}, meta {games_fetched, players_found, tiers_found, credits_used}}`. Only writes valid system tiers (PTS [10–30], REB [4–12], AST [2–12], 3PM [1–4]). Bare invocation (`python3 odds_today.py`) still runs existing `main()` unchanged. Graceful failure: exits 0 without writing file on any error. Credit cost: 4 credits/game (same as Phase 1). `ingest/odds_today.py` only — no workflow or agent files touched.)
**Last updated (prior):** April 7, 2026 ((32) **Standings rank sort fix** — `fetch_standings()` in `ingest/espn_daily_ingest.py` now sorts teams by win percentage descending (with total wins tiebreaker) before assigning rank. ESPN API returns standings entries in division-grouped order, NOT by record — ranks have been wrong all season. `teams.sort(key=lambda t: (t["wins"] / max(t["wins"] + t["losses"], 1), t["wins"]), reverse=True)` inserted between data collection and rank assignment. `ingest/espn_daily_ingest.py` only.)
**Last updated (prior):** April 7, 2026 ((31) **analyst.yml injuries preservation** — added `data/injuries_today.json` to the FILES preservation list in `analyst.yml` commit step. Root cause: `git checkout -- .` wiped fresh injury data before `build_site.py` could read it, producing an empty injury drawer on the frontend. FILES list now has 10 entries (was 9). `.github/workflows/analyst.yml` only — 1 token added.)
**Last updated (prior):** April 5, 2026 ((30) **Git autostash → copy-aside pattern across all workflows** — replaced `git rebase --autostash origin/main` with copy-aside/clean-checkout/rebase/restore pattern in all 5 workflow commit steps: `injuries.yml` (3 steps), `analyst.yml`, `auditor.yml`, `ingest.yml`, `odds.yml`. Root cause: `--autostash` stashes local changes, then `stash pop` conflicts when remote also modified the same data file. New pattern: (1) `cp` files to `.local`, (2) `git checkout -- .`, (3) `git pull --rebase origin main`, (4) `mv .local` back. `.github/workflows/injuries.yml`, `.github/workflows/analyst.yml`, `.github/workflows/auditor.yml`, `.github/workflows/ingest.yml`, `.github/workflows/odds.yml`.)
**Last updated (prior):** April 5, 2026 ((29) **Defensive field access in `save_picks()`** — fixed `KeyError: 'reasoning'` crash in `save_picks()` print/logging line. Bare dict indexing (`p['reasoning']`, `p['player_name']`, etc.) replaced with defensive `.get()` calls on all pick fields. Pipeline crashed on 2026-04-05 large-slate day (31 players) when Claude omitted the `reasoning` field. Crash occurred after picks were already written — non-destructive but caused exit code 1, blocking downstream parlay + deploy. `agents/analyst.py` only — single line in `save_picks()`.)
**Last updated (prior):** April 1, 2026 ((28) **pick_value output contract** — added explicit rule to both `build_pick_prompt()` and `build_prompt()`: emitted `pick_value` must ALWAYS equal the final stepped-down tier threshold, not the pre-step original tier. Contract placed in output format section adjacent to `pick_value` field definition; reinforcing reminder in TIER_WALK FORMAT section. Audit evidence: Harden AST T6→T4 step-down 2026-03-31, `pick_value=6` emitted instead of 4. `agents/analyst.py` only — prompt text in both functions.)
**Last updated (prior):** April 1, 2026 ((27) **Minimum-tier REB confidence cap** — new prompt rule in both `build_pick_prompt()` and `build_prompt()`: when REB pick is at T4 (minimum valid tier) AND `b2b_hit_rates` is null for REB (`b2b <5g`), cap final confidence at 72% regardless of overall hit rate, iron_floor, or trend. Tier_walk must flag explicitly. Stacks with existing B2B <5g one-tier-down penalty. Audit evidence: Harden REB T4 2026-03-31, 0 actual in 28 min, 78% confidence. `agents/analyst.py` only — prompt text in both functions.)
**Last updated (prior):** April 1, 2026 ((26) **VOLATILE + iron_floor 3PM incompatibility** — new prompt rule in both `build_pick_prompt()` and `build_prompt()`: when a 3PM prop has BOTH `iron_floor=true` AND VOLATILE tag, iron_floor is treated as OVERRIDDEN for that prop; standard non-iron-floor 3PM rules apply; confidence capped at 80%; iron_floor must not be cited in tier_walk. Rationale: iron_floor is valid for creation-based props (AST, low-threshold REB) where floor is role-structural; for 3PM the floor depends on shot attempts. Audit evidence: Mitchell 3PM T2 2026-03-31, 0 actual in 24 min. `agents/analyst.py` only — prompt text in both functions.)
**Last updated (prior):** April 1, 2026 ((25) **Amendment gates: sub-70% auto-skip + B2B <5g upside block** — `apply_amendments()` in `lineup_update.py` now has two deterministic gates. Gate 1: `direction=="down"` + `revised_confidence_pct < 70` → void pick immediately (sub-object remains for audit). Gate 2: `direction=="up"` + `player_stats[player].b2b_hit_rates[prop]` is null + `on_back_to_back` → override to `direction="unchanged"`, preserve original confidence. Function signature extended with `player_stats: dict | None = None`; call site passes `player_stats=player_stats`. Fixes SGA AST T4 miss 2026-03-30 (Gate 1) and Harden AST/REB misses 2026-03-31 (Gate 2). `agents/lineup_update.py` only — 3 edits.)
**Last updated (prior):** March 31, 2026 ((24) **Network error retry in analyst streaming** — `call_analyst()` and `_call_with_overload_retry()` now catch `httpx.RemoteProtocolError`, `httpx.ReadTimeout`, and `httpx.ConnectError` in addition to `overloaded_error`. Same exponential backoff (10s/20s/40s/80s, 4 retries). Fixes 2026-03-31 complete analyst workflow failure where peer closed connection mid-stream during Pick call (`httpx.RemoteProtocolError: peer closed connection without sending complete message body`). Scout and Review calls were already resilient (bare `except Exception` fallback). `agents/analyst.py` only — `import httpx` added, 2 except blocks added.)
**Last updated (prior):** March 31, 2026 ((23) **Odds Phase 1 alternate markets fix** — first production run matched zero picks because FanDuel main markets only post consensus lines (structurally higher than NBAgent's conservative tier floors). Dropped main markets entirely; `PROP_MARKET_MAP` now maps 4 alternate market keys → prop types (was main keys); `ALTERNATE_MARKET_MAP` and `ALL_MARKET_KEYS` removed as redundant. `VALID_PROP_TYPES` helper set for pick filtering. Credit cost: 4 credits/game, ~12–28 credits/day. Free tier (500 credits/month) covers full regular season comfortably. `ingest/odds_today.py` only.)
**Last updated (prior):** March 31, 2026 ((22) **Odds Integration Phase 1** — new standalone odds agent: `ingest/odds_today.py` fetches FanDuel NBA player prop lines from The Odds API; `.github/workflows/odds.yml` chains off Analyst Agent workflow (also `workflow_dispatch`). Annotates today's picks in `picks.json` with `market_line`, `market_implied_prob`, `market_book`, `edge_pct`, `odds_fetched_at`. Writes diagnostic cache to `data/odds_today.json` (overwritten each run). `analyst.yml` untouched. Required secret: `ODDS_API_KEY`. Two new files only.)
**Last updated (prior):** March 31, 2026 ((21) **`volatile_weak_combo` PTS trigger raised T20→T25** — tier threshold for VOLATILE PTS hard skip raised from T20+ to T25+ in both `build_prompt()` and `build_pick_prompt()`. Full-season skip archive: 0/3 correct skips at T20 (JJ actual 20, Mitchell actual 28, Wembanyama actual 41). T15–T24 VOLATILE 7/10 or 8/10 now evaluated under standard -5% treatment. `agents/analyst.py` only.)
**Last updated (prior):** March 31, 2026 ((20) **Wembanyama `volatile_weak_combo` PTS exemption** — exception (c) added to `volatile_weak_combo` exceptions in both `build_prompt()` and `build_pick_prompt()`. Wembanyama PTS props receive standard VOLATILE treatment (-5%) rather than hard skip at 7/10 + T20+. Consistent with existing blowout exemption philosophy. Fixes 2026-03-30 PTS T20 skip (actual 41). `agents/analyst.py` only.)
**Last updated (prior):** March 31, 2026 ((19) **BLOWOUT_SECONDARY_SCORER PPG-tier tiebreaker** — both `build_prompt()` and `build_pick_prompt()` now contain a "PRIMARY vs. SECONDARY SCORER TIEBREAKER" paragraph in their BLOWOUT_SECONDARY_SCORER blocks. Tiebreaker: if a whitelisted teammate has a higher best qualifying PTS tier, that teammate is primary and this player is secondary — regardless of ball-handler role. Falls back to season PPG. Fixes 2026-03-30 Harden PTS T15 miss (ball-handler rationalization bypassed skip at spread=17.5). `agents/analyst.py` only.)
**Last updated (prior):** March 31, 2026 ((18) **Amendment sub-70% auto-skip** — `apply_amendments()` in `lineup_update.py` now voids picks when `direction="down"` and `revised_confidence_pct < 70`. The `lineup_update` sub-object remains on the voided pick for audit visibility. Fixes 2026-03-30 SGA AST T4 miss (amended to 68%, ran anyway). `agents/lineup_update.py` only.)
**Last updated (prior):** March 31, 2026 ((17) **Overload retry logic** — `_call_with_overload_retry()` helper added; `call_scout()` and `call_analyst()` now retry up to 4 times on `overloaded_error` with exponential backoff (10s/20s/40s/80s). Scout retries before falling back to single-call mode; analyst retries before raising. Fixes 2026-03-31 complete run failure due to unhandled `overloaded_error`. `agents/analyst.py` only.)
**Last updated (prior):** March 31, 2026 ((16) **H15 Run 3 complete** — ≥600 picks threshold crossed. Three suppressors confirmed: HOU (65.2%, n=23), PHX (75.0%, n=24, NEW), PHI (64.7%, n=17, NEW — game-script/tanking caveat). IND confirmed amplifier (100.0%, n=23, NEW). MIN×AST at 63.6% n=11 — 4 more picks needed for ≥15 formal gate. All updated in `nba_season_context.md` and `docs/BACKTESTS.md`. No prompt rule changes — context-file guidance only.)
**Last updated (prior):** March 26, 2026 ((15) **Eight workflow + prompt fixes**: (a) `lineups_today.json` added to `injuries.yml` commit loop — lineup data now persists across hourly runs; (b) `projected_minutes` integer extraction fix — `el.find_parent("li")` replaces `el.parent` so minutes integer is captured from full list item not just name wrapper; (c) `road_underdog_near_threshold` PTS penalty added to both `build_prompt()` and `build_pick_prompt()` — away team + spread_abs 4–7 (underdog) + raw_avgs PTS − pick_value ≤ 5 → -5% confidence; iron_floor exempt; (d) `volatile_weak_combo` narrowed — trigger changed from `(7/10 OR 8/10) + T15+` to `(7/10 only) + T20+`; 8/10 removed as false-skip driver (KD T20, Sengun T15 confirmed false skips); (e) `parlays.json` added to `auditor.yml` commit loop — parlay grading results were silently discarded since launch; (f) `standings_today.json` added to `ingest.yml` commit loop — auditor was reading stale/missing standings; (g) `Without [X]` context line gated on teammate OUT status — `build_quant_context()` only renders the line when `teammate_name in _out_set`; (h) diagnostic files `pre_game_news.json`, `team_defense_narratives.json` (analyst.yml), `post_game_news.json` (auditor.yml) added to commit loops for retroactive debugging. `agents/analyst.py`, `agents/build_site.py`, `.github/workflows/analyst.yml`, `.github/workflows/auditor.yml`, `.github/workflows/injuries.yml`, `.github/workflows/ingest.yml`, `ingest/rotowire_injuries_only.py`.)
**Last updated (prior):** March 25, 2026 ((14) **Scout omitted block removed** — prompt OMITTED BLOCK section, `call_scout()` omitted extraction + sentinel, `save_scout_omitted()`, `SCOUT_OMITTED_JSON`, `analyst.yml` commit glob all removed. Never populated correctly in 6 days. `call_scout()` return type simplified to `list[dict] | None`. `agents/analyst.py`, `.github/workflows/analyst.yml`.)
**Last updated (prior):** March 25, 2026 ((13) **Absence-aware trend activation guard** — trend override now requires key teammate confirmed OUT/OFS in `injuries_today.json` via `load_players_out_today()` + `players_out_today` param on `build_player_stats()`. Previously fired for any player with ≥5 historical absence games even when teammate was healthy. Gate: `_teammate_name in _out_set`. Safe fallback: empty set means override never fires. Full `key_teammate_absent` dict still written to `player_stats.json` regardless. `agents/quant.py` only.)
**Last updated (prior):** March 25, 2026 (Rotowire parser rewrite: (10) **parse_rotowire_lineups** rewritten — anchors on `data-team` in On/Off Court Stats button (same as working `parse_rotowire_injuries`); reads player names from `<a title="">` attribute; was returning 0 teams since launch due to stale DOM-walking selectors. (11) **parse_projected_minutes** rewritten — anchors on team logo `<img src="/100{ABBREV}.png">`; old parser searched for `div.lineups-viz` class which doesn't exist on `projected-minutes.php`. (12) **parse_onoff_usage** rewritten as graceful `return {}` — On/Off Court Stats data is JS-loaded, not in server-rendered HTML; deferred to offseason (Playwright). `ingest/rotowire_injuries_only.py` only — no agent files touched.)
**Last updated (prior):** March 25, 2026 (Nine fixes applied: (1) **Absence-aware trend** — `compute_teammate_absence_splits()` now returns `absence_trend` per-stat dict (L5 vs full absence-window mean, TREND_THRESHOLD=0.10, only when n≥5); `build_player_stats()` overrides top-level `trend` dict with `absence_trend` values when key teammate is absent and `absence_trend` non-empty — eliminates regime-crossing artifact. Confirmed: Randle trend=up→stable. (2) **Team abbreviation mismatch fix** — `load_whitelist()` in both `quant.py` and `analyst.py` now emits tuples for both `team_abbr` AND `team_abbr_alt`; `load_injuries()` in `analyst.py` normalises `teams_today` via `_ABBR_NORM`. Fixes 10 invisible players (SAS/SA, NYK/NY, GSW/GS, UTA/UTAH). (3) **Scout omitted mandatory accounting** — `build_scout_prompt()` OMITTED BLOCK requires one entry per non-shortlisted player; `call_scout()` injects sentinel on empty return. (4) **is_skip boolean field** — `is_skip: true/false` in pick JSON schema; `filter_self_skip_picks()` checks it as primary gate; `save_picks()` defaults to false. (5) **Streak pill removed** — system-wide streak pills on pick cards were misleading; removed from `buildMicroStats()` in `build_site.py`. (6) **merit_below_floor skip_reason** — added to enum in both prompt functions and `SKIP_CONCLUSIONS`; distinguishes confidence-floor failures from `3pm_blowout_trend_down`. (7) **filter_self_skip_picks merit-floor regex** — two patterns added to catch 75%/78% floor failures in tier_walk; fixed LaMelo/Miller 3PM leaks. (8) **Self-skip picks leaking into picks_review** — `save_picks()` now returns the filtered list; all three call sites in `main()` capture return value. (9) **Signed spread in quant context header** — player header now shows `spread=-10.5(abs=10.5)` rather than bare `spread_abs=X.X`; also: lineup snapshot staleness guard fixed in `write_analyst_snapshot()`.)
**Last updated (prior):** March 22, 2026 (H19 Finding 2 applied: `3PM blowout trend-down hard skip` (spread_abs 8–18) retired in `build_pick_prompt()`. trend=down AND BLOWOUT_RISK=True at spread_abs 8–18 now defers to the existing trend=down step-down rule only — no hard skip. The spread_abs ≥ 19 unconditional hard skip is unchanged. `build_prompt()` (fallback) is NOT updated. Backtest evidence: H19 n=96–137, lift=1.097–1.103 — blowout_win 3PM hits at 78.8–79.2%, above 71.8% baseline. `agents/analyst.py` only.)
**Last updated (prior):** March 22, 2026 (H19 Finding 1 applied: `BLOWOUT_RISK SECONDARY SCORER SKIP` threshold raised from `spread_abs > 8` to `spread_abs ≥ 15` in `build_pick_prompt()` only. At spread_abs 8–14, secondary scorers now receive the standard BLOWOUT_RISK confidence penalty only — no hard skip. `build_prompt()` (fallback) is NOT updated. Backtest evidence: H19 n=140, lift=1.083 — secondary scorers at spread_abs 8–14 hit PTS props at 80.7%, above baseline. `agents/analyst.py` only.)
**Last updated (prior):** March 22, 2026 (H20 backtest `--mode losing-side-ast` added to `agents/backtest.py`; tests underdog spread_abs ≥ 10 AST suppression; NO_SIGNAL verdict — underdog_10plus 75.9% vs 74.1% baseline, lift=1.024; rule NOT shipped. `WITHOUT-STAR BASELINE — TWO REQUIRED GATES` block added to `build_pick_prompt()` in `analyst.py` between RETURN FROM INJURY and MINUTES FLOOR sections. Gate 1 (Confirmed-OUT): Without-Star baseline may only be the primary tier qualifier when the star is confirmed OUT in today's injury report — QUES/GTD/PROBABLE triggers fallback to standard shared-lineup baseline, skip if no standard tier qualifies. Gate 2 (Minimum Sample): n < 10 games → Without-Star data is supplementary only, cannot be sole qualifying path. Both gates must pass independently. `build_prompt()` (fallback) is NOT updated. Motivated by LeBron PTS T15 miss on 2026-03-21: Luka listed QUES, Without-Luka n=6 used as primary qualifier, auditor classified model_gap_rule.)
**Last updated (prior):** March 22, 2026 (`build_pick_prompt()` in `analyst.py` now contains a `WITHOUT-STAR BASELINE — TWO REQUIRED GATES` block inserted between RETURN FROM INJURY and MINUTES FLOOR sections. Gate 1 (Confirmed-OUT): Without-Star baseline may only be the primary tier qualifier when the star is confirmed OUT in today's injury report — QUES/GTD/PROBABLE triggers fallback to standard shared-lineup baseline, skip if no standard tier qualifies. Gate 2 (Minimum Sample): n < 10 games → Without-Star data is supplementary only, cannot be sole qualifying path. Both gates must pass independently. `build_prompt()` (fallback) is NOT updated. Motivated by LeBron PTS T15 miss on 2026-03-21: Luka listed QUES, Without-Luka n=6 used as primary qualifier, auditor classified model_gap_rule.)
**Last updated (prior):** March 21, 2026 (Three `analyst.py` runtime bugs fixed: (1) `SCOUT_OMITTED_JSON` module-level constant referenced `TODAY_STR` before it was defined — NameError on import; moved below `TODAY_STR` block. (2) `build_review_context()` line 3217: `(s.get("bounce_back") or {}).get(prop, {})` returns `None` when the key exists but its value is `None` — `.get(prop, default)` only fires when key is absent; fixed with `or {}` after `.get(prop)`. (3) `build_review_context()` line 3205: `volatility[prop]` is a dict `{label, sigma, n}` not a string — was doing `.get(prop, "unknown")` and embedding the raw dict in the review card; fixed with `((s.get("volatility") or {}).get(prop) or {}).get("label", "unknown")`. `call_scout()` return type annotation corrected to `tuple[list[dict] | None, list[dict]]`.)
Session 7 continued: Two analyst prompt bug fixes applied to both `build_prompt()` and `build_pick_prompt()`: (1) BLOWOUT_RISK secondary scorer skip direction corrected — rule body said "large underdog (spread of +8 or worse)" which was inverted; fixed to BLOWOUT_RISK=True (favored side only) with explicit CRITICAL DIRECTION CHECK; (2) New 3PM extreme blowout hard skip — BLOWOUT_RISK=True AND spread_abs ≥ 19 → skip all 3PM regardless of trend direction (including trend=up); additive to existing trend=down rule; reuses `3pm_blowout_trend_down` skip_reason. Auto-review badge rendering bug fixed — `build_site.py` replaced `auto_review_keys` set with `review_lookup` dict; game-time annotation loop now attaches `human_verdict`, `trim_reasons`, `auto_reviewed` directly to pick objects so JS badge logic has the fields it needs. Scout omitted block persisted to `data/scout_omitted_YYYY-MM-DD.json` via `save_scout_omitted()`. Teammate absence splits: `compute_teammate_absence_splits()` in `quant.py`, `Without [X]` line in `build_quant_context()`, `KEY RULES — TEAMMATE ABSENCE USAGE ABSORPTION` in `build_pick_prompt()` only. Session 7: Review Agent — adversarial pick stress-test as Stage 3 of analyst pipeline. Four new functions: `build_review_context()`, `build_review_prompt()`, `call_review()`, `apply_review_flags()`. Writes `data/picks_review_YYYY-MM-DD.json` in existing schema. Manual review files take priority. Non-fatal failure. `build_site.py` auto-review badges (`🤖 Auto-Review` / `🤖 Stay Away`). `analyst.yml` commits picks_review files. Session 6: Scout → Pick two-stage analyst pipeline. Replaced single-call analyst with two-stage LLM pipeline: Scout (context-heavy, no rules, shortlist 20–25 players) → Pick (rules-heavy, filtered quant context, shortlisted players only). `SCOUT_MAX_TOKENS = 4096`, `build_scout_prompt()`, `call_scout()`, `build_pick_prompt()` added; `main()` refactored with fallback to original single-call path if Scout fails or <5 players match. Scout uses Opus on large slates, Sonnet otherwise; Pick always Sonnet. All legacy functions preserved byte-for-byte. Output schemas unchanged. Session 5: Six targeted analyst.py prompt fixes — all prompt-only, no quant/schema/workflow changes. (1) JSON repair layer in `call_analyst()` — `_repair_json()` two-step fallback (json_repair lib + char sanitization) before sys.exit(1); `json-repair` added to analyst.yml pip install. (2) Three prompt rules (2026-03-17): `volatile_ast_t6` hard skip in VOLATILITY block, 3PM confidence ceiling at 80% at end of SEQUENTIAL GAME CONTEXT block, VOLATILE neutralizes absence-driven upside in VOLATILITY block. (3) Four prompt rules (2026-03-18): elite scorer blowout exemption capped at spread_abs < 15, VOLATILE + AST >= T6 hard SKIP in AST rules block, iron_floor B2B road gate for non-primary BHs (AST < 6.0), 3PM top_pick requires iron_floor + 80% cap. (4) fg_margin_thin redesign: full step-down cascade T20→T15→T10 before skipping; `fg_margin_thin_no_valid_tier` retired → `fg_margin_thin_tier_step`. (5) T25 blowout hard skip + 3PM 75% floor: `PTS T25 BLOWOUT HARD SKIP` in TIER CEILING RULES (spread_abs >= 15, favored side = hard skip T25/T30, `blowout_t25_skip`); `3PM CONFIDENCE FLOOR` in SELECTION RULES (75% minimum for 3PM, no skip record needed). (6) filter_self_skip_picks: 7 named hard-gate patterns added to SKIP_CONCLUSIONS (16 total) — `HARD GATE FIRES`, `BLOWOUT_SECONDARY_SCORER SKIP fires`, named rule skip phrases, `mandatory skip`, `Record as skip`. Session 4: Session 3: Opportunity Flags overhaul — per-player card collapse, `qualifying_tiers`/`upgrade_tiers` schema, upgrade card design, absent player self-exclusion guard, dedup by `name_lower` only, `card_type` ("new_pick"/"upgrade"/"mixed"). Picks Review integration — `human_verdict`/`trim_reasons` tagging in `auditor.py` via `load_picks_review()`+`apply_human_verdicts()`, `human_flag_precision` block in `audit_summary.json`, parlay `trim`/`manual_skip` guard in `parlay.py`, `⚠ Caution`/`⚠ Flagged` frontend review badges in `build_site.py`. Also: analyst meta-framework for rule conflicts, leaderboard injection, `reconcile_pick_values` hardening + elite scorer blowout exemption, floor gate skip enforcement, return-from-injury SHORT_SAMPLE rule, voided pick grading fix, whitelisted teammates grounding, `lineup_update.py` comprehensive overhaul, 3PM blowout hard skip, parlay one-player-per-card cap, T2 REB deprecation.
Session 2: Analyst-driven Top Picks — `top_pick` field in picks schema, `## TOP PICKS` prompt section, `get_top_picks()` flag-first with 85%+ fallback; Results tab redesigned to 3 stat cards + Props card + chart + drawers; `load_yesterday_summary()` + `AUDIT_SUMMARY_JSON`; post_game_reporter.py Bug Fixes — `result=="MISS"` filter fix, `espn_fetch_ok` scoping fix. Session 1: M2 Defensive Recency Split complete — `compute_opp_defense_recency()`,
`def_recency` field in player_stats.json, `DEF↑`/`DEF↓` header annotation, annotation-only.
March 11 additions: P6 Skip Validation System, P7 Team Momentum Indicator, three analyst prompt
rule hardenings, Post-Game Reporter Brave Search web narrative layer. March 10: P5 Lineup Update
Agent complete, Rotowire session login + proj_min/USG_SPIKE/OPP annotations, knowledge staleness
awareness block, analyst Opus hybrid.)

---

## Mental Model: Data Flow

```
player_game_log.csv + team_game_log.csv + nba_master.csv
    ↓ quant.py
player_stats.json          (one entry per whitelisted player playing today)
team_defense_narratives.json  (per-team last-15g PPG allowed + rank, auto-generated daily)
    ↓ pre_game_reporter.py → pre_game_news.json
         player_notes, game_notes (Claude-summarised ESPN news)
         suggested_context_updates (Claude conflict flags)
         staleness_flags (deterministic date-based stale fact flags — Pass 1, no LLM)
    ↓ analyst.py (two-stage Scout → Pick pipeline, with single-call fallback)
         reads lineups_today.json (written by rotowire_injuries_only.py, refreshed at analyst start)
         lineups_today.json optionally contains projected_minutes + onoff_usage per team (when Rotowire creds present)
         load_lineup_context() builds per-team lookup → proj_min/USG_SPIKE/OPP annotations in quant context blocks
         calls write_analyst_snapshot() → stores snapshot_at_analyst_run in lineups_today.json
         OUT/DOUBTFUL pre-filter applied to player_stats before prompt building
         Stage 1 — Scout: all context (quant, logs, news, lineups, profiles, leaderboard), no rules → shortlist 20–25 players
         Stage 2 — Pick: shortlisted quant context + full rulebook + audit feedback → picks + skips
         Fallback: if Scout fails or <5 shortlisted players match quant stats → original single-call build_prompt() path
         → picks.json (appended, result=null until auditor runs)
    ↓ parlay.py                → parlays.json (appended; OUT/DOUBTFUL excluded)
    ↓ lineup_watch.py          → picks.json (injury_status_at_check + voided/risk fields, hourly)
    ↓ lineup_update.py         → picks.json (lineup_update sub-object on affected picks, hourly)
                               → opportunity_flags.json (teammate suggestions when qualifying absence detected, hourly)

    ↓ post_game_reporter.py    → post_game_news.json (ESPN exit/DNP news for yesterday's picks)
    ↓ picks_review_YYYY-MM-DD.json (human-produced, committed before auditor.yml runs — see below)
    ↓ auditor.py (next morning)
picks.json + parlays.json  (result fields filled; human_verdict + trim_reasons tagged from review file)
audit_log.json + audit_summary.json  (written; human_flag_precision block in audit_summary)
    ↓ season_context_updater.py (date-gated to PLAYOFFS_R1_DATE — playoff-only)
nba_season_context.md  (in-place patch: new series diary entries, injury-bullet updates, timestamp bump)
    ↓ build_site.py
site/index.html  (deployed to GitHub Pages)
```

Quant runs twice per day: once in `ingest.yml` (for freshness after box scores land), once at
the start of `analyst.yml` (to ensure the analyst sees today's correct data). This is intentional.

`rotowire_injuries_only.py` runs at the START of `analyst.yml` (before quant re-run) to ensure
injuries and lineups are fresh before picks are generated.

---

## `data/playoff_career_log.csv` (21 columns)

Populated by `ingest/espn_playoff_backfill.py` (one-time local run) and a future
daily ingest integration (separate prompt). One row per player per game across
**both regular season and playoff games**, distinguished by the `season_type`
column (`regular` | `playoff`). Covers seasons 2021–2025 for active whitelisted
players. Upsert idempotent on `(player_id, game_id, season_type)`.

Columns (in order): `season, season_type, game_id, game_date, team_abbrev,
opp_abbrev, home_away, player_id, player_name, started, minutes, pts, reb, ast,
tpm, fgm, fga, fg3m, fg3a, ftm, fta`.

Source-format notes:
- `team_abbrev` / `opp_abbrev` are already normalized to Rotowire-standard codes
  via `to_roto_code()` (GSW not GS, NOP not NO, NYK not NY, SAS not SA, UTA not
  UTAH, WAS not WSH, PHX not PHO).
- `tpm` is duplicated from `fg3m` for parity with `player_game_log.csv`.
- Shooting fields (`fgm/fga`, `fg3m/fg3a`, `ftm/fta`) are parsed from ESPN's
  compound "X-Y" format (e.g. `FG="11-21"` → fgm=11, fga=21).
- `started` is blank — ESPN gamelog endpoint does not expose starter info at
  this layer. Kept as a placeholder for future enrichment.
- Rows for "EAST"/"WEST" team_abbrev are All-Star game appearances (ESPN
  classifies them under Regular Season). Harmless — will be filtered by the
  whitelist tuple filter in any downstream consumer.
- 5 active whitelisted players are missing from `player_dim.csv` due to name
  normalization (hyphens, apostrophes, "Jr.") — KAT, SGA, Fox, NAW, Jabari
  Smith Jr. These need manual `player_dim.csv` mapping before their playoff
  history is available downstream.

Downstream consumer: future `compute_playoff_splits()` in `quant.py`
(not yet implemented — separate prompt). Will compute career playoff vs
regular-season tier deltas per player, surfaced as annotation-only context.

---

## Current `player_stats.json` Schema (all keys, in order)

```
team, whitelisted_teammates,  ← sorted list of other active whitelisted players on same team playing today; [] when none
opponent, games_available, last_updated,
on_back_to_back,   ← player-level (2026-03-15): True only if team in b2b_teams AND player's most recent game_date == yesterday; rested players (OUT/load mgmt yesterday) get False even if team played
rest_days, games_last_7, dense_schedule,
b2b_hit_rates, today_spread, spread_abs, blowout_risk,
tier_hit_rates, matchup_tier_hit_rates,
h2h_splits,           ← {opponent, games, PTS:{tier_str:{hits,n,rate}}, REB:{...}, AST:{...}, 3PM:{...}} or null (added 2026-04-09); per-opponent tier hit rates from full season game log vs today's specific opponent; min H2H_MIN_GAMES=2 to emit (else null); DNP exclusion applied once — "games" count is shared across all tiers and all stats (sample invariant); annotation-only, no directive rules
spread_split_hit_rates,
best_tiers, trend, home_away_splits,
minutes_trend, avg_minutes_last5, minutes_floor,   ← {floor_minutes, avg_minutes, n}; null if <5 games
raw_avgs,
opp_defense,          ← team-level, kept for auditor context injection
game_pace, teammate_correlations,
bounce_back,          ← player-level post-miss profiles + iron_floor + miss anatomy fields
volatility,           ← {PTS/REB/AST/3PM: {label, sigma, n}}
positional_dvp,       ← {position, pts_rating, reb_rating, ast_rating, tpm_rating, n, source}
ft_safety_margin,     ← {label, margin, breakeven_fg_pct, season_fg_pct, n}; null if insufficient FT data
team_momentum,        ← {team: {l10_wins, l10_losses, l10_pct, l10_margin, tag}, opponent: {...}}; null if no data
def_recency,          ← "soft" | "tough" | null — opponent's L5 vs L15 PTS allowed divergence; null when <3 L5 games or within threshold
shooting_regression,  ← {fg_hot, fg_cold, fg_pct_l5, fg_pct_l20, n_l5, n_l20}; null if insufficient data
playoff_profile,      ← {playoff_games, seasons_with_data, deltas:{pts,reb,ast,tpm,fg_pct}, playoff_avgs, regular_avgs} or null (added 2026-04-09); career playoff vs regular-season deltas from data/playoff_career_log.csv (2021–2025); null when <PLAYOFF_MIN_GAMES (5) career playoff games or player absent from backfill; annotation-only — surfaced by analyst build_quant_context() as "PLAYOFF PROFILE" line, gated behind PLAYOFFS_R1_DATE (2026-04-18, inclusive); no directive rules
profile_narrative,    ← string or null; live portrait for ≥10 non-DNP games + qualifying PTS best tier
key_teammate_absent   ← {teammate_name, teammate_avg_pts, n_games, raw_avgs, tier_hit_rates, absence_trend} or null (added 2026-03-20/25); absence_trend is per-stat {PTS/REB/AST/3PM: "up"|"stable"|"down"} dict computed as L5 vs full absence-window mean; empty {} when n_games < TREND_SHORT_WINDOW (5); top-level trend dict is overridden with these values in build_player_stats() when key teammate is absent and absence_trend non-empty — prevents regime-crossing artifact; null when <3 without-teammate games
star_absence_lift     ← {star_name, star_ppg, n_without, n_with, key_deltas, qualifier, cannib_mechanism} or null (added 2026-04-11 entry 62, H26 Part 1 of 2; `cannib_mechanism` added entry 69, Spec 1 H26×H33); per-player with-vs-without deltas at H26 key tiers (PTS T15/T20/T25, AST T4/T6) when team's OVERALL leading scorer is absent; companion to key_teammate_absent but uses full player_log (not whitelist-filtered) team leader with ≥20 games. `key_deltas` is a dict of f"{STAT}_T{tier}" → delta_pp (positive = lift without star); `qualifier` is one of STRONG_PERSONAL_SIGNAL (mean delta > +3pp), NEUTRAL_PERSONAL_DATA (±3pp), PERSONAL_DRAG_WARNING (< −3pp — e.g. Jalen Green without Booker), POPULATION_ONLY (< 3 without-star games). `cannib_mechanism` is `{PTS: {cannib_idx, label}, AST: {cannib_idx, label}}` or `None` — H33 data for this player↔star pair; negative cannib_idx = star's usage suppresses this player (freed ceiling when OUT); positive = synergy (may decline when OUT). Null when player IS the star, has fewer than MIN_GAMES total team games, or no H33 data exists for the pair. Surfaced by analyst `build_quant_context()` as a three-line `STAR_ABSENT_LIFT: ...` annotation (population + per-player + H33 mechanism) ONLY when the star_name is in today's `_out_set` (gated on injuries_today.json). Annotation-only — no directive rules; the existing WITHOUT-STAR BASELINE two-gate rule still governs tier qualification. Downstream consumer (Part 2, shipped): skip re-evaluation in lineup_update.py when a star teammate flips to confirmed OUT post-morning.
```

`opp_defense` is still written to `player_stats.json` even though the analyst no longer reads
`opp_today=` from the quant context block — the auditor reads quant context from pick object
fields directly (not from `player_stats.json`), but `opp_defense` remains for other consumers.

`bounce_back` per-stat fields: `{post_miss_hit_rate, lift, iron_floor, n_misses,
near_miss_rate, blowup_rate, typical_miss}`. The Miss Anatomy fields (`near_miss_rate`,
`blowup_rate`, `typical_miss`) are null when fewer than 5 misses — they feed Player Profiles
conditional rendering. Analyst wiring for directive rules is **deferred** pending backtest.

---

## Current Quant Context Block Format (what the Analyst actually sees per player)

```
Jalen Brunson (vs BOS | spread_abs=5.5 rest=1d L7:4g proj_min=34 [USG_SPIKE:+7.2pp vs J.Holiday] DEF↑):
  ⚠ OPP: Jayson Tatum OUT (proj=0min)
  DvP [PG]: PTS=soft REB=mid AST=tough 3PM=soft (n=52)
  Momentum — NYK: 7-3 L10 avg_margin=+5.2 [hot] | BOS: 4-6 L10 avg_margin=-1.8
  PTS: tier=25 overall=80% vs_soft=85%(14g) vs_tough=62%(11g) competitive=79%(18g) blowout_games=71%(7g) trend=up bb_lift=1.18(6miss) [consistent]
  AST: tier=6 overall=72% vs_soft=n/a vs_tough=68%(11g) competitive=74%(18g) blowout_games=n/a trend=stable [VOLATILE]
```

**Header flags:** `B2B`, `rest=Xd`, `DENSE`, `L7:Xg`, `BLOWOUT_RISK=True`, `spread={signed:+.1f}(abs={abs:.1f})` — negative = this team favored, positive = underdog (2026-03-25: replaces bare `spread_abs=X.X`; falls back to `spread_abs=X.X` when signed value unavailable), `proj_min=N` (Rotowire projected minutes, when creds present), `[USG_SPIKE:+N.Npp vs X.Name]` (usage spike ≥5pp + ≥100 min sample, when creds present), `DEF↑` (opponent defense trending soft — L5 ≥8% above L15 PTS allowed), `DEF↓` (opponent defense trending tough — L5 ≥8% below L15 PTS allowed); omitted when neutral or insufficient data
**After DvP line (optional):** `Momentum —` line showing L10 W-L record, avg point margin, and hot/cold tag for the player's team and opponent; omitted when neither team's momentum can be computed; "neutral" tag teams show no tag (only [hot] and [cold] labelled)
**After Momentum line (optional):** `Teammates (active/whitelisted): Name1, Name2` — other active whitelisted players on the same team playing today; omitted when none; used to ground Analyst teammate references and prevent stale training knowledge hallucinations
**After Teammates line (optional):** `⚠ OPP: Name OUT (proj=0min)` — opponent player with 0 projected minutes per Rotowire; capped at 3 entries; only emitted when Rotowire creds present
**DvP line:** one per player (not per stat), covers all 4 stats, shows `(team-lvl)` tag when
the positional sample had < 10 games. `n=` is the number of player-game observations.
**Stat line fields (in order):** `tier` · `overall` · `vs_soft` · `vs_tough` · `competitive` ·
`blowout_games` · `trend` · `b2b=` (only when B2B) · `bb_lift=` or `[iron_floor]` · `[VOLATILE]` or `[consistent]`
**PTS stat line may also show:** `[FG_HOT:+X%]` or `[FG_COLD:−X%]` (shooting regression flag)

---

## Current `build_prompt()` Section Order

1. Task framing:
   - `Today is {TODAY_STR}.`
   - `## IMPORTANT: YOUR TRAINING KNOWLEDGE IS POTENTIALLY YEARS OUT OF DATE` — epistemic calibration block; distinguishes perishable knowledge (player roles, rosters, team systems, H2H history, season narratives — do NOT rely on training data) from durable knowledge (basketball principles, tier logic, role archetype reasoning — apply freely); instructs Claude to trust injected data over training priors on any named-player or named-team fact
   - `## YOUR TASK` + tier system intro
2. Hit definition (`>=`, exact match = HIT)
3. Tier ceiling rules with backtest evidence (REB T8+, AST T6+, PTS T25+, PTS T30 invalid)
4. `## TODAY'S GAMES` (JSON array)
5. `## CURRENT INJURY REPORT` (filtered to today's teams only)
5a. `## PROJECTED LINEUPS` (from `lineups_today.json`; freshness-checked vs TODAY_STR; fallback "unavailable" line if missing or stale)
6. `## PRE-GAME NEWS` (conditional — only injected when `pre_game_news.json` has content; critical context flags prepended with ⚠ warning)
7. `## SEASON CONTEXT` (SEASON FACTS only — `## TEAM DEFENSIVE PROFILES` section is stripped from the file at load time; manually maintained)
8. `## PLAYOFF PICTURE` — auto-generated from `data/standings_today.json` (bucketed: safe / contending / playin / bubble / eliminated)
8b. `## PLAYOFF CONTEXT — POSTSEASON MODE` (conditional — fires on/after `PLAYOFF_START_DATE = "2026-04-14"`; annotation-only behavioral framing: rotation tightening, pace compression, play-in intensity, series dynamics, data window caution; no directive rules)
8c. `## WHITELISTED PLAYER RANKINGS — SEASON vs L20` — built by `build_player_leaderboard()` from already-loaded `game_log`; top 15 per stat (PTS/REB/AST/3PM), season avg + L20 avg with ↑/↓/→ arrow (±3 rank threshold); "" on any exception
9. `## TEAM DEFENSIVE PROFILES` — auto-generated from `data/team_defense_narratives.json` (last 15g PPG allowed + rank, updates daily via quant.py)
9a. `## SERIES CONTEXT — PLAYOFFS` (conditional — from `data/playoff_matchup.json` via `load_series_context()`; per-series performance data + season H2H; only for active series with game today)
9b. `## PLAYOFF PLAYER ADJUSTMENTS` (conditional — built by `build_playoff_adjustments()`; date-gated to `PLAYOFFS_R1_DATE = "2026-04-18"` (inclusive), empty during regular season AND play-in; per-player directional guidance for 20 players synthesized from H27–H32 backtest suite; annotation-only, no directive rules; positioned between `series_context_section` and `## PLAYER RECENT PERFORMANCE` in `build_prompt`/`build_scout_prompt`, and inline after `build_playoff_context(TODAY_STR)` in `build_pick_prompt`)
10. `## PLAYER RECENT GAME LOGS` (last 10 games per whitelisted player)
11. `## QUANT STATS — PRE-COMPUTED TIER ANALYSIS`
    - **KEY FRAMEWORK — HOW TO REASON WHEN RULES CONFLICT** (inserted before all KEY RULES blocks): 5-level priority order (hard skips → mandatory tier steps → confidence penalties → caps → positive signals); PENALTY STACK LIMIT (>3 independent penalties → re-examine or skip; B2B + DENSE double-counting example); TIER_WALK FORMAT standard (✓ marks, step notation, clean confidence chain, no skip conclusions in emitted picks); SANITY CHECK 4-point gate (tier vs. actual floor, honest confidence, self-skip consistency, smell test); ON COMPLEX SLATES (evaluate independently, quality over volume); PLAYER TIER CONTEXT (leaderboard as ground truth for elite status, anchors 27.0 PPG exemption); CONFIDENCE THRESHOLD IS A FLOOR, NOT A TARGET (no rounding up from sub-70% assessment)
    - KEY RULES — MATCHUP QUALITY (DvP + vs_soft/vs_tough interaction)
    - OPPONENT DEFENSE — POSITIONAL DvP (stat-specific rules, REB/3PM exclusions)
    - SELECTION RULES (offensive-first REB floor rule, tier ceiling conditions, tier walk-down discipline; **LINEUP CONTEXT** rule — use ## PROJECTED LINEUPS as ground truth for who is playing)
    - KEY RULES — REST & FATIGUE (B2B, DENSE, rest_days; **RETURN FROM INJURY — SHORT SAMPLE INSTABILITY** for `[SHORT_SAMPLE:Ng]` players: mandatory REB/AST one-tier step-down, PTS -5% + no T20+ unless ≥7 games)
    - KEY RULES — SEQUENTIAL GAME CONTEXT (REB slump-persistent, 3PM cold streak, **3PM trend=down mandatory step-down**; **3PM blowout hard skip** — trend=down AND BLOWOUT_RISK=True → skip all 3PM; **3PM extreme blowout hard skip** — BLOWOUT_RISK=True AND spread_abs ≥ 19 → skip all 3PM regardless of trend direction)
    - KEY RULES — SPREAD / BLOWOUT RISK (BLOWOUT_RISK flag, spread_abs > 13 cap; **BLOWOUT_RISK secondary scorer skip for FAVORED players** — BLOWOUT_RISK=True on the player's own team → skip non-primary scorer PTS; CRITICAL DIRECTION CHECK: does NOT apply to underdog players; ELITE SCORER BLOWOUT EXEMPTION for raw_avgs PTS ≥ 27.0; **ROAD UNDERDOG NEAR-THRESHOLD PTS PENALTY** — away team + spread_abs 4–7 (underdog) + raw_avgs PTS − pick_value ≤ 5 → -5% confidence; iron_floor exempt; added 2026-03-26)
    - KEY RULES — VOLATILITY (−5% for VOLATILE, Top Pick gate, iron_floor override)
    - KEY RULES — HIGH CONFIDENCE GATE 81%+ (Conditions A/B/C)
    - INJURY EXCLUSION (HARD RULE) — players removed from quant context by Python pre-filter; do not pick any player absent from ## QUANT STATS
    - `{quant_context}` — the per-player blocks
11a. `## FANDUEL MARKET AVAILABILITY` (conditional — from `odds_available.json` via `format_available_markets()`; omitted when gate disabled)
12. `## PLAYER PROFILES — LIVE STATISTICAL PORTRAITS` (from `profile_narrative` fields in `player_stats.json`)
13. `## AUDITOR FEEDBACK FROM PREVIOUS DAYS` (last 5 entries)
14. `## ROLLING PERFORMANCE SUMMARY` (from `audit_summary.json`, blank < 3 days)
15. `## ANALYSIS APPROACH` (3-line per-player reasoning format)
16. `## OUTPUT FORMAT` (strict JSON schema — includes `tier_walk` and `iron_floor` fields)

**Hard gates enforced in Python before prompt building (not just in prompt rules):**
- **Market availability gate** — `load_available_markets()` loads `odds_available.json`; `format_available_markets()` injects `## FANDUEL MARKET AVAILABILITY` section listing per-player available tiers; prompt rules enforce priority 0 unconditional skip (`no_market`) when player+prop+tier has no FanDuel market. Gate disabled entirely (no section injected) when file missing/stale. Added 2026-04-07.
- OUT/DOUBTFUL players removed from `player_stats` dict via `load_out_players()` — Claude never sees their stats
- Same filter applied in `parlay.py` `load_todays_picks()` — cannot appear in parlay legs
- `min_floor confidence cap` — PTS pick with `floor_minutes < 24` → confidence capped at 84%
- `BLOWOUT_RISK secondary scorer skip` — BLOWOUT_RISK=True (player's team is the heavily FAVORED side) AND spread_abs >= 13.5 AND non-primary scorer → PTS pick skipped (`blowout_secondary_scorer`). **Direction corrected 2026-03-20.** **Threshold history:** original > 8 → raised to ≥ 15 in build_pick_prompt only (2026-03-22, H19 Finding 1) → lowered to >= 13.5 in BOTH prompts (2026-04-11, entry 61). At spread_abs 8–13, standard BLOWOUT_RISK confidence penalty applies instead of a hard skip — H19 backtest (n=140, lift=1.083) showed secondary scorers hit at 80.7% in that range. **Two exemptions added 2026-04-11:** (a) `T10 FLOOR EXCEPTION` — T10 PTS picks are never hard-skipped by this rule; (b) `CO-PRIMARY SCORER GATE` — any player with raw_avgs PTS >= 22.0 is always PRIMARY regardless of teammate tier comparison (Fox + Wembanyama example: both above 22.0 → both primaries, rule fires for neither). Pre-existing tier-based tiebreaker retained as fallback when co-primary gate doesn't apply. Prompt-only — no Python change.
- `AST T4+ hard gate` — PF/C or raw_avgs AST < 4.0 → opponent AST DvP must be "soft"; skip on mid/tough. **Exception (2026-03-15):** players with raw_avgs AST ≥ 8.0 APG are exempt — elite playmaker profile, gate not designed for this case (e.g. Jokic). Prompt-only — no Python change.
- `reb_floor_skip` — pick_value must be strictly below 3rd-lowest L10 REB value; exact match = skip. **Exception (2026-03-15):** T4 (minimum valid tier) is exempt from the exact-match gate — no lower tier to step to; validate on hit rate merit alone. Gate fires normally for pick_value ≥ 6. Prompt-only — no Python change.
- `3PM hard skip` — trend=down AND avg_minutes_last5 ≤ 30 → skip all 3PM picks including T1
- `3PM blowout hard skip` (March 14, 2026) — trend=down AND BLOWOUT_RISK=True → skip all 3PM including T1; overrides [iron_floor] (prompt rule only; no Python pre-filter). **RETIRED for spread_abs 8–18 (2026-03-22, H19 Finding 2)** — in `build_pick_prompt()` only, this hard skip is replaced with a note deferring to the trend=down step-down rule. `build_prompt()` fallback retains original behavior unchanged.
- `3PM extreme blowout hard skip` (March 20, 2026) — BLOWOUT_RISK=True AND spread_abs ≥ 19 → skip all 3PM for ALL players on favored team regardless of trend direction; reuses `3pm_blowout_trend_down` skip_reason (prompt rule only; no Python pre-filter). **Unchanged — still active.**

**Parlay concentration cap:** max 1 parlay per player per day (March 14, 2026 — tightened from 2). Enforced in `enforce_concentration_cap()` (`> 1` threshold) and `build_parlay_prompt()` AVOID section.

---

## Key Design Decisions (and why)

**Why two whitelist load functions?**
`load_whitelist()` returns a `set` of `(name_lower, team_upper)` tuples — used in three places
for DataFrame mask filtering. Changing the return type would break all three callers. Instead,
`load_whitelist_positions()` returns a separate `{name_lower: position}` dict used only by the
new positional DvP code. Zero risk to existing filtering logic.

**Why is `player_games` sorted ascending before passing to `compute_volatility()`?**
`grp` in `build_player_stats()` is always sorted newest→oldest (the `grp.sort_values("game_date",
ascending=False)` at line ~967). If you called `.to_dict("records")` directly, `played[-window:]`
would return the *oldest* 20 games. The ascending sort at the call site ensures `[-window:]`
correctly yields the most recent games.

**Why does `opp_defense_rating` still appear in `picks.json` output schema?**
The analyst prompt's OUTPUT FORMAT section still asks for `"opp_defense_rating"` in each pick.
The Analyst derives this from the DvP line's PTS rating (since the prompt removed `opp_today=`
from stat lines, the DvP PTS rating is the closest proxy). This field is used by `build_site.py`
for defense pills on pick cards. A future cleanup could rename it to `dvp_pts_rating`.

**Why is the tier walk-down taught by example in the prompt instead of rules?**
Backtesting showed the system was picking based on raw averages rather than tier hit rates.
An explicit worked example (PTS: check ≥20 → 40% → skip; check ≥15 → 80% → pick) was more
effective than abstract rules at anchoring Claude to the right selection logic.

**Why does Quant NOT run LLM calls?**
All signal is pre-computed deterministically. This keeps cost at zero for the heaviest compute
step, makes debugging trivial (inspect the JSON), and separates concerns cleanly. The LLM only
runs in analyst, parlay, auditor, and lineup_update.

**Why does positional DvP rank *within position groups* rather than globally?**
A PG averaging 18 pts/game against a team is not directly comparable to a C averaging 18 pts.
Ranking teams separately within each position group (all PG allowed averages ranked against each
other across 30 teams) gives a meaningful "soft/mid/tough for PGs" label. Global ranking would
mix apples and oranges.

**Why is there a 10-game minimum for positional DvP cells?**
With only ~12 whitelisted players per position across 30 teams, some cells will have very few
observations early in a season. Below 10, the variance is too high to trust the rating — the
system falls back to team-level gracefully and marks `source: "team_fallback"`.

**Why does the auditor read quant context from pick object fields rather than player_stats.json?**
The auditor runs the morning after picks are made. `player_stats.json` is overwritten by quant
each day — if the auditor read it, it would see today's (not yesterday's) quant context, which
is wrong. `load_player_stats_for_audit()` was removed (March 8) to eliminate this confabulation
bug. The auditor now reads `reasoning`, `hit_rate_display`, `tier_walk`, `opponent` directly
from each pick object — fields that were written at pick time.

**Why was `PLAYER_WINDOW` raised from 10 to 20?**
Backtest 2 showed w20 improved REB T8 by +7.8pp (to 71.0%), crossing the 70% threshold that
makes it a valid pick. The tradeoff was ~25% fewer total selections — estimated ≥8 picks/day on
typical slates, above the parlay minimum.

**Why does `build_team_defense_narratives()` omit perimeter and pace clauses?**
The current `team_game_log.csv` schema lacks `fg3_pct_allowed` and `possessions` columns.
`build_team_defense_narratives()` checks `has_3p` and `has_pace` boolean flags at runtime and
omits those clauses when the columns are absent. All 30 teams still get the PPG rank line. The
function is structured to add these clauses automatically if/when the schema expands.

**Why do staleness flags go into both `data/context_flags.md` AND `pre_game_news.json`?**
`context_flags.md` is a human-readable daily report for manual review. `pre_game_news.json`
carries the same flags structured as a list under `"staleness_flags"` for programmatic access
if needed. The analyst reads `pre_game_news.json` (via `load_pre_game_news()`) — not the .md
file directly — so both formats serve different consumers.

**Why is `data/context_flags.md` NOT in `context/context_flags.md`?**
The `CONTEXT_FLAGS_MD = DATA / "context_flags.md"` constant was established when the file was
first created. The flags file lives in `data/`, not `context/`. `analyst.py` reads flags via
`load_pre_game_news()` which reads `pre_game_news.json` — the `.md` file is human-readable only.

**Why does `lineup_update.py` use `snapshot_at_analyst_run` rather than the previous hourly cycle?**
The comparison baseline must be "what was true when picks were made," not "what was true an hour
ago." If the previous cycle is used, a change that happened before the first hourly run would
never appear as a change. Using the morning snapshot means every hourly run independently compares
against the same ground truth — and `direction=unchanged` is still written as audit evidence.

**Why does `lineup_update.py` overwrite the `lineup_update` sub-object each run?**
The latest Claude assessment is always the most current. Writing idempotently means the frontend
always shows the most recent reasoning without needing to track "has this been processed" state.
Original morning fields (`confidence_pct`, `reasoning`, `pick_value`, `tier_walk`) are never
touched — the sub-object is additive, not destructive.

---

## Key Backtest Verdicts (applied to production)

| Signal | Verdict | Applied |
|--------|---------|---------|
| Trend (L5 vs L20) | Noise for PTS/REB/3PM; weak for AST | Data shown, not directive |
| Home/away | Noise | Data shown, not directive |
| Opp defense (team-level) | Noise for REB, noise for 3PM, weak for PTS/AST | REB/3PM excluded in prompt |
| B2B (aggregate) | Noise (directionally −3%) | Quantified per-player instead |
| Bounce-back (league-wide) | Slump-persistent (lift=0.89); REB worst (lift=0.83) | Player-level profiles instead |
| Player-level bounce-back | Strong for specific combos (16 iron-floor) | bb_lift + iron_floor in context |
| 3PM cold streak (severe) | Decline: 68.3% next game (lift=0.87, n=161) | −5% confidence or skip rule |
| Mean reversion (cold streaks) | Null for PTS/REB/AST; decline for 3PM | 3PM rule only |
| Recency decay weighting | Marginal (+2.1pp over 31 days) | Not applied |
| PTS T20 | 69.6% at w20 (borderline concern) | Ceiling rule in prompt |
| REB T8 | 63.2%(w10) → 71.0%(w20) | Requires w20 to be valid |
| AST T6 | 65.1% — below threshold | Ceiling rule in prompt |
| 3PM T2 | 71.4%(w10) → 77.3%(w20) | Valid pick tier |
| Post-blowout bounce-back (H6) | NOISE — lift 0.955–0.988 across all stats | Closed March 7, 2026 |
| Opponent schedule fatigue (H7) | NOISE — opp B2B lift 0.977–1.025; dense=0 instances | Closed March 7, 2026 |
| Positional DvP (H8) | REVERT — lift_advantage negative for PTS/REB/AST (−0.051/−0.052/−0.060); 3PM KEEP (+0.106) but 3PM DvP already excluded from prompt. Team-level opp defense outperforms positional splits — position-splitting dilutes cell sizes and flattens signal. | Prompt cleanup pending; `DvP [POS]` annotation line to be removed; implementation is currently annotation-only so no picks at risk |
| Opponent team hit rate (H15) | **Second run complete (March 22, n=538):** HOU confirmed system-wide suppressor (61.9%, n=21, −23.4pp). MIN×AST at 55.6% (n=9, −29.5pp) — active scrutiny, below formal ≥15 gate. SAS floor compression (n=6, mean −5.0). CHI/BOS amplifiers (95%+) are small-sample noise (n=5–11). `nba_season_context.md` updated: HOU suppressor note added, MIN×AST upgraded to active scrutiny, SAS note updated. Re-run at ≥600 picks (end of season). | HOU note in season context; watch MIN×AST for ≥15 picks |

---

## Live Agent Function Inventory (non-obvious additions only)

### `quant.py`
- `load_whitelist()` → `set` of `(name_lower, team_upper)` tuples — **updated 2026-03-25**: uses iterrows loop emitting tuples for BOTH `team_abbr` (standard NBA) AND `team_abbr_alt` (ESPN short abbrev) when alt is non-empty; fixes 10 players invisible since launch (SAS/SA, NYK/NY, GSW/GS, UTA/UTAH); DO NOT revert to set-comprehension pattern (would break alt-abbrev matching again)
- `load_whitelist_positions()` → `{name_lower: position}` — new, used only by positional DvP
- `_ordinal(n)` → `str` — converts int to ordinal string (`1→"1st"`, `11→"11th"`, `21→"21st"`); handles 11th/12th/13th exception
- `compute_tier_hit_rates(games_df, stat)` → `{str(tier): float}` — uses `>=` grading
- `compute_volatility(game_log_list, stat, tier, window=20)` → `{label, sigma, n}` — takes records NOT DataFrame; must be sorted oldest→newest at call site
- `compute_matchup_tier_hit_rates(all_games_df, opp_defense_dict, stat)` → matchup splits
- `compute_h2h_splits(grp, opponent)` → per-opponent tier hit rates from full season game log; returns `{opponent, games, PTS:{...}, REB:{...}, AST:{...}, 3PM:{...}}` or `None` when `< H2H_MIN_GAMES` (2). DNP exclusion applied once to the filtered H2H subset — `games` count is shared across all tiers and all stats. Annotation-only — no directive rules. Added 2026-04-09.
- `load_playoff_career_data() → pd.DataFrame | None` — reads `data/playoff_career_log.csv` once at the top of `build_player_stats()`; filters zero-minute rows (DNP-equivalent in playoff data); returns `None` gracefully when the file is missing or empty. Added 2026-04-09.
- `compute_playoff_splits(player_name, playoff_df) → dict | None` — career playoff vs regular-season averages + deltas for PTS/REB/AST/3PM/FG% from `playoff_career_log.csv` (2021–2025). Regular-season comparison restricted to seasons where the player also has playoff data (same-season apples-to-apples). FG% computed from aggregate FGM/FGA totals, not per-game mean. Returns `None` when `playoff_df is None`, player absent, or `playoff_games < PLAYOFF_MIN_GAMES` (5). Shape: `{playoff_games, seasons_with_data, deltas, playoff_avgs, regular_avgs}`. Annotation-only — no directive rules. Added 2026-04-09.
- `compute_spread_split_hit_rates(player_log_df, game_spreads_dict, stat)` → competitive/blowout splits
- `compute_b2b_hit_rates(player_log_df, b2b_game_ids_dict, stat)` → B2B tier hit rates
- `compute_positional_dvp(player_log_df, position_map_dict)` → `{team: {position: {avgs + ratings}}}`
- `compute_shooting_regression(grp)` → `{fg_hot, fg_cold, fg_pct_l5, fg_pct_l20, n_l5, n_l20}` — P3 feature; ±8% threshold for HOT/COLD flag
- `compute_ft_safety_margin(grp)` → `{label, margin, breakeven_fg_pct, season_fg_pct, n}` — H11 feature; null if insufficient FT data
- `compute_minutes_floor(grp, window=10)` → `{floor_minutes, avg_minutes, n}` or `None` — lower bound on expected minutes
- `build_bounce_back_profiles(player_log_df, whitelist_set)` → `{player: {stat: {post_miss_hit_rate, lift, iron_floor, n_misses, near_miss_rate, blowup_rate, typical_miss}}}`
- `build_team_defense_narratives(team_log)` → `dict[str, str]`; writes `data/team_defense_narratives.json`; one auto-generated narrative per team (PPG allowed + rank + perimeter/pace clauses when columns available)
- `build_player_profiles(player_stats_dict)` → mutates `profile_narrative` key in-place for each player meeting eligibility; called at end of `main()` after `build_player_stats()` writes the JSON
- `build_team_momentum(master_df, teams_today) → dict[str, dict]` — computes L10 W-L record, avg point margin, and hot (≥7W) / cold (≤3W) / neutral tag for each team in `teams_today`; uses completed games only (both scores present, date < TODAY_STR); returns `{}` if master_df is None/empty; tag is always one of "hot"/"cold"/"neutral"
- `compute_opp_defense_recency(team_log) → dict[str, str | None]` — one entry per team appearing as opponent today; value is `"soft"` (L5 PTS-allowed ≥8% above L15), `"tough"` (L5 ≥8% below L15), or `None` (neutral / insufficient data); requires `n_l15 ≥ 3` and `n_l5 ≥ DEF_RECENCY_MIN_L5 (3)` to compute; mirrors `build_opp_defense()` self-join pattern
- `compute_teammate_absence_splits(player_log, player_name, team, whitelist)` → `{teammate_name, teammate_avg_pts, n_games, raw_avgs, tier_hit_rates, absence_trend}` or `None` — computes player's raw avgs and tier hit rates in games where their top-PPG whitelisted teammate was absent (min 3 qualifying games); stored as `key_teammate_absent` field in `player_stats.json`; used by `build_quant_context()` to emit `Without [X]` line; added 2026-03-20. **`absence_trend` field (added 2026-03-25):** per-stat `up/stable/down` dict computed as L5 vs full absence-window mean (same logic as `compute_trend()` but scoped to absence regime only); only populated when `n_games >= TREND_SHORT_WINDOW` (5); empty dict `{}` otherwise. `build_player_stats()` overrides the top-level `trend` dict with `absence_trend` values **only when the key teammate is confirmed OUT or OFS in `injuries_today.json`** (activation guard added 2026-03-25). If the teammate is healthy/playing, the absence data is still written to `player_stats.json` as historical context but the trend label is not overridden. Gate condition: `_teammate_name in players_out_today`. Safe fallback: empty `players_out_today` (file missing) means override never fires.
- `compute_star_absence_deltas(player_log, player_name, team, star_name, star_ppg, star_absent_game_ids)` → `{star_name, star_ppg, n_without, n_with, key_deltas, qualifier}` or `None` — added 2026-04-11 (H26 Part 1 of 2). Companion to `compute_teammate_absence_splits()` — that function uses top WHITELISTED teammate with absolute without-star rates for the WITHOUT-STAR BASELINE rule; this function uses team's OVERALL leading scorer (full player_log, ≥`SA_MIN_STAR_GAMES`=20 games, not whitelist-filtered) and produces with-vs-without DELTAS at H26 key tiers (PTS T15/T20/T25, AST T4/T6). Both fields coexist unchanged. Returns `None` when player IS the star or has fewer than `MIN_GAMES` total active games. `key_deltas` is a dict of f"{STAT}_T{tier}" → delta_pp (positive = lift without star). Qualifier classification: `STRONG_PERSONAL_SIGNAL` (mean delta > +3pp across qualifying tiers), `NEUTRAL_PERSONAL_DATA` (mean within ±3pp), `PERSONAL_DRAG_WARNING` (mean < −3pp — e.g. Jalen Green without Booker), `POPULATION_ONLY` (< `SA_MIN_CONDITION_N`=3 without-star games, no per-player signal). Uses `.isin()` for game_id filtering (sort-direction independent); DNP exclusion via `dnp != "1"` coerced numeric. **Team-stars pre-computation** in `build_player_stats()` placed after `_rest_cache` block — iterates `teams_today`, for each team finds full-log player with highest PTS avg and ≥20 non-DNP games, builds `(star_name, star_ppg, absent_game_ids_set)` tuple in `_team_stars[team]`. Per-player loop looks up `_team_stars.get(team)` and calls `compute_star_absence_deltas()` after the existing `compute_teammate_absence_splits()` + trend override block; self-star guard prevents a leading scorer from showing lift for themselves. Output wired as `star_absence_lift` field in `stats_out[player_name]` dict. Runtime invariant verified: 49/49 players pass `n_with + n_without == total player non-DNP games on team`. **H26 backtest constants** (also in `agents/quant.py`): `SA_KEY_TIERS` (PTS 15/20/25, AST 4/6), `SA_POPULATION_DELTAS` (PTS_T15 +11.3, PTS_T20 +12.7, PTS_T25 +10.5, AST_T4 +9.6, AST_T6 +6.7), `SA_MIN_STAR_GAMES`, `SA_MIN_CONDITION_N`.
- `load_players_out_today() → set[str]` — reads `injuries_today.json`, returns set of player names with status OUT or OFS; QUES/DOUBT/PROB/GTD excluded (those players may still play); returns empty set if file missing; added 2026-03-25
- `build_player_stats(player_log, b2b_teams, opp_defense, game_pace, todays_games, teammate_correlations, whitelist, game_spreads=None, master_df=None, b2b_game_ids=None, positional_dvp_data=None, position_map=None, team_momentum=None, opp_defense_recency=None, players_out_today=None)` → full `player_stats.json` dict

### `ingest/rotowire_injuries_only.py`
- `ROTOWIRE_LOGIN_URL` — constant for Rotowire login endpoint
- `login_rotowire(session: requests.Session) → bool` — reads `ROTOWIRE_EMAIL`/`ROTOWIRE_PASSWORD` env vars; POSTs to login URL; returns `False` (not crash) if creds missing or HTTP != 200; returns `True` when session is authenticated
- `fetch_rotowire_html(session: requests.Session | None = None) → str | None` — refactored to accept optional session; `requester = session if session is not None else requests`; backward-compatible for callers without session
- `fetch_rotowire_minutes_html(session: requests.Session) → str | None` — fetches `ROTOWIRE_MINUTES_URL` (`/basketball/projected-minutes.php`); returns raw HTML or None on error/non-200; used by `main()` to feed premium parsers from the correct page (the lineups page does not server-render those panels)
- `ROTOWIRE_MINUTES_URL = "https://www.rotowire.com/basketball/projected-minutes.php"` — constant added after `ROTOWIRE_LOGIN_URL`; this page is a separate URL from the lineups page, with different HTML structure
- `parse_rotowire_lineups(html) → dict` — **(rewritten 2026-03-25)** anchors on `data-team` attribute in On/Off Court Stats `<button>` elements (same anchor used by `parse_rotowire_injuries`); slices raw HTML between consecutive button matches to isolate each team's Expected Lineup section; walks `<li>` elements within the section looking for position labels (PG/SG/SF/PF/C) as first word; reads player names from `a_tag.get("title")` (full name, reliable) NOT from link text (may be abbreviated, e.g. "R. Hachimura"); detects inline injury status keywords (Ques/Doubt/Out/Prob/GTD etc.) from `<li>` text; deduplicates by position; returns `{team_abbr: {"starters": [{name, position, injury_status}], "confirmed": bool}}`
- `parse_projected_minutes(soup: BeautifulSoup) → dict[str, list[dict]]` — **(rewritten 2026-03-25; full-coverage hardening 2026-04-15)** anchors on team logo `<img>` tags with `src` matching `/100{ABBREV}.png` pattern; walks up ancestors to find containers with ≥2 player links; handles "Subscriber Exclusive" gating with `break` (skip team); extracts `{name, minutes, section, injury_status}` per player; deduplicates by name; returns `{}` on any exception (graceful degradation). **2026-04-15: now called on BOTH the dedicated `projected-minutes.php` HTML AND the main `nba-lineups.php` HTML.** Rotowire splits coverage — dedicated page carries AWAY-team panels, lineups page carries HOME-team panels (premium-gated); previously only the dedicated page was parsed (~50% team coverage on a normal slate). Two defensive filters added to safely operate on the lineups page where Expected Lineup panels also contain 2+ player links and would false-match the logo-anchor heuristic: (a) the candidate ancestor must contain the literal text `"starters"` (case-insensitive) — Expected Lineup panels use position labels (PG/SG/SF/PF/C) and lack STARTERS/BENCH headers; (b) each player's enclosing `<li>` must contain a digit — Projected Minutes entries always carry a minute integer (including 0 for OUT players), Expected Lineup entries never do; (c) a post-parse validation requires at least one player with `minutes > 0` before accepting a team (if not, parser continues walking up ancestors). Diagnostic logging: `[injuries] projected_minutes: parsed N teams, M players with minutes > 0` plus `[injuries] projected_minutes teams found: ...` on successful parses. **Pre-existing bug (unrelated, unfixed):** the NavigableString section-header detection is dead code because `hasattr(NavigableString, 'name')` returns True in bs4, so every player is tagged `section="STARTERS"` in production regardless of source panel — downstream consumers only read `minutes` so no downstream impact.
- `parse_onoff_usage(soup: BeautifulSoup) → dict[str, list[dict]]` — **(rewritten 2026-03-25)** intentionally returns `{}` with diagnostic log. On/Off Court Stats data is loaded via JavaScript after a button click — not present in the server-rendered HTML fetched by `requests.get()`. A future Playwright-based implementation could populate this; see `docs/ROADMAP_offseason.md`. Function signature preserved so callers in `main()` require no changes
- `write_lineups_json(lineups, asof_date, built_at_utc, projected_minutes: dict | None = None, onoff_usage: dict | None = None)` — extended signature; merges `projected_minutes` and `onoff_usage` dicts into per-team payload before atomic write to `data/lineups_today.json`
- Guard condition in `main()`: if parsed 0 teams but existing file has teams for today, keeps existing (protects against partial scrape overwriting good data)
- Session flow in `main()`: creates `requests.Session()`; calls `login_rotowire(session)`; passes session to `fetch_rotowire_html(session)` (lineups page); when authenticated, makes a second fetch via `fetch_rotowire_minutes_html(session)` (minutes page) → builds separate `soup_minutes` for `parse_projected_minutes()` and `parse_onoff_usage()`. **2026-04-15 auth fix:** `login_rotowire()` now POSTs with `data={"email": email, "password": password}` (primary) and falls back to `data={"username": email, "password": password}` if the primary returns non-200. `session.cookies.clear()` between attempts. Root cause of the projected-minutes ~50% coverage bug: the scraper had been sending `"username"` as the field name; the endpoint returned HTTP 200 but granted only a non-premium cookie, silently gating half the dedicated page. `post_game_reporter.py` was always using `"email"` and authenticating premium content correctly — same login URL, different field. Post-login log prints cookie count + sorted cookie names so any future regression is visible. An earlier same-day dual-parse workaround (parse both dedicated page and lineups page, merge) has been reverted — no longer needed once auth is correct. `main()` emits a `COVERAGE GAP` log line when teams in parsed `lineups` are missing from `projected_minutes`, so the dedicated-page result is held accountable.
- Output: `data/lineups_today.json` keys: `asof_date`, `built_at_utc`, `source`, per-team dicts with `starters` + `confirmed`; optional `projected_minutes` + `onoff_usage` per team when Rotowire creds present and scrape succeeds; `snapshot_at_analyst_run` key added later by analyst.py

### `analyst.py`
- `_call_with_overload_retry(fn, max_retries=4, base_delay=10)` — retries `fn()` on `anthropic.APIStatusError` containing `overloaded_error`; exponential backoff (10s/20s/40s/80s); raises last exception if all retries exhausted; non-overload exceptions raise immediately with no retry; added 2026-03-31
- `_ABBR_NORM` dict + `_norm_team(abbr)` + `_extract_last(raw_name)` — normalization helpers (mirrors `lineup_watch.py` and `build_site.py`); `_extract_last` handles "J. Brunson" style Rotowire names
- `load_out_players() → set[tuple[str, str]]` — reads `injuries_today.json`; returns `{(last_name_lower, norm_team_upper)}` for all OUT/DOUBTFUL players; used in `main()` to pre-filter `player_stats` before any prompt building; Claude never receives stats for excluded players
- `format_lineups_section(lineups_path=LINEUPS_JSON, today_teams=None) → str` — reads `lineups_today.json`; staleness-checks `asof_date == TODAY_STR`; loads game pairings from MASTER_CSV; loads injuries for key absences; returns formatted `## PROJECTED LINEUPS` block (starters + key absences per game pair); falls back to `"[Lineup data unavailable — injury report only]"` if file missing/stale
- `write_analyst_snapshot(lineups_path, picks_run_at_iso) → None` — writes `snapshot_at_analyst_run` key into `lineups_today.json` capturing starters + confirmed flag per team at pick time; guard logic (2026-03-25 fix): refreshes if today's date differs from existing snapshot OR if existing `teams` is empty — safe to re-run within the same day, will not overwrite a valid same-day snapshot; atomic write via `.tmp` rename; called in `main()` after `format_lineups_section()`, before `build_prompt()`
- `load_player_stats()` → reads `player_stats.json`, returns dict
- `load_lineup_context() → dict[str, dict]` — reads `lineups_today.json`; staleness-checks `asof_date == TODAY_STR`; skips metadata keys (`asof_date`, `built_at_utc`, `source`, `snapshot_at_analyst_run`); builds `{norm_team: {"projected_minutes": {name_lower: {minutes, section}}, "onoff_usage": {name_lower: {usage_pct, usage_change, minutes_sample, absent_players}}}}`; returns `{}` silently if missing/stale; logs team count on success
- `build_player_leaderboard(game_log: pd.DataFrame, whitelist: set) → str` — computes season avg and L20 avg per-stat rankings for all whitelisted players (top 15 per stat, PTS/REB/AST/3PM); L20 rank shown with ↑/↓/→ arrow (±3 spot threshold); returns `## WHITELISTED PLAYER RANKINGS — SEASON vs L20` block; fully graceful — returns `""` on any exception; placed immediately before `build_quant_context()` in source; wired in `main()` as `leaderboard = build_player_leaderboard(game_log, whitelist)`; injected between PLAYOFF PICTURE and TEAM DEFENSIVE PROFILES via `leaderboard_section` variable in `build_prompt()`; added 2026-03-16
- `load_players_out_today() → set[str]` — reads `injuries_today.json`, returns full player names with OUT/OFS status; QUES/DOUBTFUL excluded; used to gate `Without [X]` context line in `build_quant_context()`; added 2026-03-26
- `build_quant_context(player_stats: dict, lineup_context: dict | None = None, players_out_today: set[str] | None = None) → str` — updated signature (2026-03-26: added `players_out_today` param); DvP line per player + stat lines with vol_tag + FG_HOT/FG_COLD; per-player annotations: `proj_min=N`, `[USG_SPIKE:+N.Npp vs X.Name]`, `⚠ OPP: Name OUT (proj=0min)` lines; `Without [X] (their avg=Ypts, n=Ng): ...` line after teammates line — **now only rendered when `teammate_name` is confirmed OUT/OFS in `players_out_today`** (gated 2026-03-26; was unconditional on `n_games ≥ 3` before). **H2H splits annotation (added 2026-04-09):** after the per-stat `stat_parts` loop, appends `  H2H vs {OPP} ({N}g): {STAT} T{tier} {hits}/{n}({pct}%) | ...` — reads `s["h2h_splits"]`, shows the highest qualifying tier (≥70% rate) per stat; no line rendered when `h2h_splits` is null or when no stat has a qualifying tier.
- `load_audit_summary()` → reads `audit_summary.json`, returns "" if < 3 entries
- `load_available_markets() → dict | None` — reads `data/odds_available.json`; returns parsed dict if file exists and `date == TODAY_STR`; returns `None` (gate disabled) on missing file, stale date, or any exception; all failure paths print diagnostic and exit gracefully
- `format_available_markets(markets_data: dict | None, player_stats: dict) → str` — formats `## FANDUEL MARKET AVAILABILITY` prompt section from `odds_available.json` data; normalizes player names via `_norm_name()` (lowercase, punctuation stripped); filters to players present in `player_stats` (today's active whitelisted players only); returns `""` when `markets_data is None` (gate disabled); per-player output lists available tiers per prop type with implied probabilities
- `load_season_context()` → reads `context/nba_season_context.md`; strips HTML comment header; **strips `## TEAM DEFENSIVE PROFILES` section** (truncates at that heading — file unchanged, only in-memory text is modified); returns "" if missing
- `load_pre_game_news()` → reads `pre_game_news.json`; formats critical flags + player_notes + game_notes + monitor flags; returns "" if no notable items
- `load_series_context() → str` — reads `data/playoff_matchup.json` written by `agents/playoff_matchup.py`; returns pre-formatted `context_block` string for prompt injection; returns `""` if file not found, `mode != "playoffs"`, or `date != TODAY_STR`; never raises; added 2026-04-07
- `build_playoff_context(today_str: str) → str` — returns `## PLAYOFF CONTEXT — POSTSEASON MODE` block when `today_str >= PLAYOFF_START_DATE` ("2026-04-14"); returns `""` during regular season. Annotation-only behavioral framing (rotation tightening, pace compression, play-in intensity, series dynamics, data window caution). Called inline in all 3 prompt builders — no parameter wiring needed. Added 2026-04-09
- `build_playoff_rule_modifications(today_str: str) → str` — date-gated `PLAYOFF_START_DATE`. Returns `## PLAYOFF RULE MODIFICATIONS — ACTIVE OVERRIDES` block with 3 directive overrides: (1) MIN_FLOOR 84% PTS cap SUSPENDED when Rotowire `projected_minutes ≥ 30` (L10 floor understates playoff floor when contaminated by regular-season rest games); (2) VOLATILE confidence deduction reduced from -5% to -3% (iron_floor interaction, VOLATILE+AST T6 skip, weak-combo skip rules unchanged — only base deduction changes); (3) `team_momentum` tag (hot/cold/neutral) is INFORMATIONAL ONLY, not a confidence modifier. Block is ~2,435 chars / 37 lines. Injected in both Scout and Pick prompts directly after `{build_playoff_blowout_override(TODAY_STR)}` and before `KEY RULES — VOLATILITY:`. Returns `""` during regular season. Added 2026-04-15.
- `build_playoff_adjustments(today_str: str) → str` — returns `## PLAYOFF PLAYER ADJUSTMENTS` block when `today_str >= PLAYOFFS_R1_DATE` ("2026-04-18", inclusive); returns `""` during regular season AND during play-in (4/14–4/17). Sibling function to `build_playoff_context` but gated one step stricter because play-in teams have thin/no career playoff history. Per-player directional guidance for 20 players synthesized from the full H27–H32 backtest suite (career playoff 2021–2025 from H28, minutes elasticity from H30, confidence calibration from H29, consistency index from H32, series progression from H31, blowout resilience from H27). Block is 4,376 chars / 53 lines. Wired into `build_prompt()` + `build_scout_prompt()` via new `_playoff_adj` + `playoff_adjustments_section` section variables (injected between `{series_context_section}` and `## PLAYER RECENT PERFORMANCE`), and into `build_pick_prompt()` via inline `{build_playoff_adjustments(TODAY_STR)}` call directly after the existing `{build_playoff_context(TODAY_STR)}` inline call. `build_review_prompt()` untouched per spec — Review reviews existing picks, doesn't generate new ones. Annotation-only: block header explicitly says "annotation-only — use to inform confidence direction alongside quant data, not as hard overrides". Added 2026-04-11.
- `render_playoff_picture(standings_path=STANDINGS_JSON)` → reads `data/standings_today.json`; formats bucketed `## PLAYOFF PICTURE` string; returns fallback string if file missing/stale
- `format_team_defense_section(narratives_path=TEAM_DEFENSE_NARRATIVES_JSON)` → reads `data/team_defense_narratives.json`; validates `as_of == TODAY_STR`; returns `## TEAM DEFENSIVE PROFILES (last 15 games — auto-generated DATE)` block sorted alphabetically; always returns non-empty string (fallback warning if file missing/stale)
- `build_prompt(games, player_context, injuries, audit_context, season_context, quant_context="", audit_summary="", pre_game_news="", player_profiles="", playoff_picture="", team_defense="", lineups_section="")` → full system prompt string; preserved as fallback path for Scout failure
- `build_scout_prompt(games, player_context, injuries, season_context, quant_context, pre_game_news, player_profiles, playoff_picture, team_defense, leaderboard, lineups_section)` → Scout stage prompt; all contextual data, NO rules; output schema: `{"slate_read": str, "shortlist": [...]}`; targets 20–25 players; added 2026-03-20. **Omitted block removed 2026-03-25** — never populated correctly (6 days of empty `[]` or sentinel); Review agent handles coverage concerns.
- `call_scout(prompt, model=MODEL) → list[dict] | None` — calls Claude for Scout shortlist; returns shortlist on success, `None` on any failure (triggers fallback); never calls `sys.exit()`; uses `SCOUT_MAX_TOKENS = 12288` (**raised from 4096 on 2026-04-10** to accommodate 20-25 structured shortlist entries at ~450 tokens each — the old 4096 cap physically limited the Scout to ~8–9 entries, causing aggressive pre-filtering and strong candidates being dropped); streaming API; same JSON repair pattern as `call_analyst()`; added 2026-03-20. **(Simplified 2026-03-25):** return type changed from `tuple[list[dict] | None, list[dict]]` to `list[dict] | None`; omitted extraction removed; `save_scout_omitted()` removed. **(Overload retry 2026-03-31):** streaming call wrapped with `_call_with_overload_retry()` — retries up to 4 times on `overloaded_error` (10s/20s/40s/80s exponential backoff) before falling back to single-call mode. **(Shortlist undersize fallback 2026-04-10):** in `main()`, immediately after the shortlist is extracted, if `active_count >= 40` and `len(shortlist_names) < 12` the run falls back to single-call `build_prompt()` mode — this catches cases where the Scout under-delivers on large slates despite the larger token budget (model pre-filtering, JSON repair path, etc.). Gated on 40+ active players so normal-slate runs are unaffected.
- `build_pick_prompt(scout_shortlist, games, injuries, quant_context, audit_context, audit_summary)` → Pick stage prompt; contains full rulebook (all KEY RULES, TIER CEILING RULES, KEY FRAMEWORK, SELECTION RULES, etc.) copied verbatim from `build_prompt()`; receives Scout shortlist as `## SCOUT SHORTLIST` section + filtered quant context (shortlisted players only); no redundant context blocks (no game logs, season context, profiles, leaderboard, lineup section); added 2026-03-20
- `build_review_context(picks, player_stats)` → compact per-pick vulnerability card string; extracts risk-relevant quant fields (volatility, B2B hit rates, blowout risk, minutes floor, bounce_back, FT safety margin, def_recency, team_momentum); one card per pick; added 2026-03-20
- `build_review_prompt(picks, review_context, audit_summary)` → Review stage prompt; adversarial stress-test framing; miss pattern extraction from audit_summary; calibration guidance (2–4 flags per typical slate); output schema: `[{player_name, team, prop_type, pick_value, verdict, vulnerability, confidence_in_flag}]`; added 2026-03-20
- `call_review(prompt, model=MODEL) → list[dict] | None` — calls Claude for Review verdicts; returns parsed list on success, `None` on any failure (non-fatal — picks already saved); never calls `sys.exit()`; max_tokens=4096; streaming API; same JSON repair pattern as `call_analyst()`; added 2026-03-20
- `apply_review_flags(verdicts, picks, review_path)` → converts Review verdicts to `picks_review_YYYY-MM-DD.json`; verdict mapping: clean→keep, concern→trim, stay_away→manual_skip; writes `source: "auto"` on every entry; skips write if file already exists (manual review priority); added 2026-03-20
- Output schema: Claude emits `{"picks": [...], "skips": [...]}` JSON object. `call_analyst()` returns `tuple[list[dict], list[dict]]` — picks and skips. Falls back to flat-array parse for robustness. **(Overload retry 2026-03-31):** streaming call wrapped with inline retry loop — retries up to 4 times on `overloaded_error` (10s/20s/40s/80s exponential backoff) before raising; `raw_chunks` reset on each retry attempt.
- Pick output fields: `date, player_name, team, opponent, home_away, prop_type, pick_value, direction, confidence_pct, hit_rate_display, trend, opp_defense_rating, tier_walk, walked_tier, iron_floor, top_pick, is_skip, reasoning`
- `walked_tier` field: integer — final tier after all step-downs; set by analyst, verified by `reconcile_pick_values()`. Must equal `pick_value`. When they disagree and `walked_tier` is a valid lower tier, Python corrects `pick_value` downward. Added 2026-04-09.
- `is_skip` field: `true` when Analyst concluded skip for any reason; `false` for all genuine picks. Primary gate in `filter_self_skip_picks()` — checked before regex fallback. `save_picks()` defaults to `false` when field absent. Added 2026-03-25 to eliminate regex whack-a-mole for skip detection.
- `morning_implied_prob` field: float — FanDuel market implied probability from the morning odds snapshot. **Set by `ingest/odds_today.py main()` during the morning `analyst.yml` run, inside the odds-match block immediately after `market_implied_prob`** (fix 2026-04-10 — previously this field was written only by `pretip_sweep()`, and pretip was failing to execute on time, so CLV was never populated). Same `if "morning_implied_prob" not in pick` guard in both `main()` and `pretip_sweep()` — main sets the baseline, pretip leaves it alone. `None`/absent when the pick wasn't matched to any FanDuel market in the morning run. Used by auditor for CLV calculation. Added 2026-04-09, source corrected 2026-04-10.
- `clv_pp` field: float — Closing Line Value in percentage points. `market_implied_prob (pretip) − morning_implied_prob`. Positive = beat the close (market moved toward our OVER position). Computed by `auditor.py` `save_audit()` for graded HIT/MISS picks with both morning and pretip data. Added 2026-04-09.
- Skip output fields: `date, player_name, team, opponent, prop_type, tier_considered, direction, skip_reason, rule_context` (rule-specific dict)
- Skip reasons: `no_market` (no FanDuel alternate market exists for this player+prop+tier — unconditional hard skip, priority 0; added 2026-04-07), `min_floor_tier_step`, `volatile_weak_combo`, `blowout_secondary_scorer`, `3pm_trend_down_tough_dvp`, `3pm_trend_down_low_minutes`, `3pm_blowout_trend_down` (scoped exclusively to spread_abs ≥ 19 unconditional 3PM hard skip — 2026-03-25; do NOT use for penalty-driven confidence floor failures), `ast_hard_gate`, `fg_margin_thin_tier_step`, `reb_floor_skip`, `merit_below_floor` (confidence after all penalties falls below prop-type floor: 75% 3PM, 78% REB, 70% general — added 2026-03-25 to fix skip_validation table pollution), `fg_cold_tier_step`, `volatile_ast_t6`, `blowout_t25_skip`
- `volatile_weak_combo` (2026-03-31 update): **PTS props only**, triggers on VOLATILE + exactly 7/10 (70%) hit rate + T25 or higher. **(Raised 2026-03-31):** tier threshold raised from T20+ to T25+; full-season skip archive showed 0/3 correct skips at T20 post-narrowing (JJ actual 20, Mitchell actual 28, Wembanyama actual 41). **(Narrowed 2026-03-26):** previously fired on `(7/10 OR 8/10) + T15+`; 8/10 removed from trigger (80% - 5% = 75%, a legitimate pick; KD T20 actual 30 and Sengun T15 actual 30 were false skips). T15–T24 VOLATILE picks with 7/10 or 8/10 proceed under standard -5% treatment. Do NOT apply to AST picks below T6 (governed by the VOLATILE AST skip rule). Three exceptions: **(a)** player has [iron_floor] on this stat AND trend=up; **(b)** stat is AST AND raw_avgs AST ≥ 6.0 AND [iron_floor] is true on AST — elite passers with confirmed structural floor are not weak-combo candidates; **(c)** Victor Wembanyama PTS — established blowout exemption extends to volatile_weak_combo on PTS (standard -5% treatment, not hard skip). Both `build_prompt()` and `build_pick_prompt()` updated identically.
- `fg_margin_thin_no_valid_tier` RETIRED (2026-03-19) — renamed `fg_margin_thin_tier_step`; rule now applies full cascade (T20→T15→T10) before skipping. Only skips PTS when T10 also fails. 100% false skip rate on graded instances motivated the fix.
- `blowout_t25_skip` (2026-03-19, refined 2026-04-11): spread_abs >= 15 AND favored side → hard skip PTS T25 and T30 for NON-ELITE scorers only. **Elite exemption added 2026-04-11 (entry 61):** players with `raw_avgs PTS >= 27.0` are exempt from the hard skip and proceed through normal T25 evaluation where the 80% hit rate bar (8+/10) still applies and the existing spread_abs >= 15 confidence cap (74% max) still constrains the pick. Rationale: H27 backtest (Apr 11, n=68) — population T25 at 58.8% MARGINAL, not SUPPRESSED; elite scorers with 8+/10 individual rates significantly outperform. Non-elite scorers remain hard-skipped. T20 and below still subject to existing 74% cap rule.
- **Analyst prompt rules updated 2026-03-20:** Two blowout rule fixes applied to BOTH `build_prompt()` AND `build_pick_prompt()` (any rulebook change must update both identically): (1) **BLOWOUT_RISK Secondary Scorer Skip — direction correction:** Rule body was inverted — it said "player's team is the large underdog" but BLOWOUT_RISK=True means the player's team IS the favored side. Rewritten to correctly reference BLOWOUT_RISK=True (favored side); `CRITICAL DIRECTION CHECK` paragraph added prohibiting application to underdog players. Motivated by Jalen Green PTS skip on 2026-03-19 (PHX +9.5 underdog — rule misfired on wrong team). `BLOWOUT_SECONDARY_SCORER SKIP fires` pattern in `filter_self_skip_picks()` now correctly targets favored-side scenarios. (2) **3PM extreme blowout hard skip (spread_abs ≥ 19):** BLOWOUT_RISK=True AND spread_abs ≥ 19 → skip ALL 3PM picks regardless of trend direction (even trend=up). Additive to existing trend=down rule (fires at spread_abs ≥ 8). Reuses `3pm_blowout_trend_down` skip_reason. Motivated by SGA 0-for-3 in 29-point OKC blowout win on 2026-03-18 despite trend=up. Applied to `agents/analyst.py` only.
- **Analyst prompt rules updated 2026-03-19:** Two additional rules in `build_prompt()`: (1) `PTS T25 BLOWOUT HARD SKIP` in TIER CEILING RULES — spread_abs >= 15 + favored side = hard skip T25/T30, skip_reason=blowout_t25_skip. Cross-reference note added to ELITE SCORER BLOWOUT EXEMPTION exception clause. (2) `3PM CONFIDENCE FLOOR` in SELECTION RULES — 3PM minimum confidence is 75% (not system-wide 70%); picks landing 71–74% after all adjustments do not emit (no skip record needed). `agents/analyst.py` only.
- `3pm_blowout_trend_down` (March 14, 2026, refined 2026-04-11): trend=down AND BLOWOUT_RISK=True → originally hard-skipped all 3PM including T1, overriding [iron_floor]. **Relaxed in `build_pick_prompt()` (2026-03-22, H19 Finding 2)** — step-down at spread_abs 8–18 instead of hard skip; skip_reason enum retained for the ≥19 unconditional case. **Relaxed in `build_prompt()` monolithic (2026-04-11, entry 61 Edit D)** — now aligned with Pick prompt: step-down at spread_abs 8–18, unconditional hard skip at spread_abs >= 19. **T1 iron_floor exception added to both prompts (2026-04-11 Edit C):** if the step-down reaches T1 and the player has iron_floor OR a 9+/10 hit rate at T1, hold at T1 with a -5% confidence penalty instead of skipping. Fox and Wembanyama 3PM T1 false-skips on 4/10 motivated this. The trend=down + tough DvP and trend=down + limited minutes hard skips still override the iron_floor exception when their conditions are met.
- **Analyst prompt rules updated 2026-03-18:** Four new rules added to `build_prompt()`: (1) elite scorer blowout exemption capped at spread_abs < 15 — at spread_abs >= 15, full 74% blowout cap applies to ALL players regardless of raw_avgs PTS; exemption unchanged for spreads 8–14. (2) VOLATILE + AST >= T6 = hard SKIP — added to AST rules block after the ast_hard_gate section; separate from and in addition to the VOLATILITY block volatile_ast_t6 rule. (3) iron_floor does NOT waive B2B road gate for non-primary BHs (raw_avgs AST < 6.0) — IRON-FLOOR B2B ROAD GATE added to REST & FATIGUE block; all four conditions required: on_back_to_back=True, home_away="A", opp_defense=tough, raw_avgs AST < 6.0. (4) 3PM top_pick requires iron_floor — 3PM TOP-PICK RESTRICTION added to TOP PICKS block; without iron_floor, 3PM capped at 80% and excluded from top_pick regardless of hit rate.
- **Three new prompt rules added 2026-03-17** (KEY RULES — VOLATILITY and KEY RULES — SEQUENTIAL GAME CONTEXT blocks):
  - **`volatile_ast_t6`** (HARD SKIP): [VOLATILE] + AST tier >= T6 → skip AST pick entirely, no lower-tier fallback. Elite playmaker exemption (>=8.0 APG) does NOT override — that exemption covers the low-volume position gate only, not floor instability. Exception: iron_floor AND trend=up at exactly T6 is exempt; T8+ skips regardless of iron_floor. Added to HARD SKIPS named skip list in priority framework. Audit evidence: Luka Doncic AST T6, actual 4 (exact floor value). Location: VOLATILITY block, after VOLATILE PTS skip exception paragraph.
  - **3PM confidence ceiling at 80%**: confidence_pct capped at 80 for all 3PM picks where iron_floor=false. Applied after all other penalties and caps. top_pick=true ineligible on non-iron-floor 3PM. Audit evidence: Austin Reaves 3PM T1 at 84%, actual 0. Location: end of SEQUENTIAL GAME CONTEXT block, before INJURY STATUS ON SHOOTING PROPS.
  - **VOLATILE neutralizes absence-driven upside**: [VOLATILE] on a stat → do not raise confidence citing teammate/opponent OUT. Treat as direction unchanged. Genuine role-change reassignments (primary ball-handler absent → VOLATILE player takes all creation duties) evaluated separately with explicit floor-change reasoning; iron_floor overrides. Audit evidence: Kevin Durant PTS (VOLATILE) 74%→79% via Sengun OUT amendment — scored 18 on T20. Location: VOLATILITY block, after volatile_ast_t6.
- `save_picks(picks: list[dict]) → list[dict]` — runs three post-processors in sequence: (1) `filter_self_skip_picks()`, (2) `reconcile_pick_values()`, (3) `enforce_market_gate()` (added 2026-04-09). Appends survivors to `picks.json`, logs each saved pick, and **returns the filtered list** (fixed 2026-03-25). All three call sites in `main()` capture the return value: `picks = save_picks(picks)` — ensures the Review stage (Stage 3) only receives picks that survived all filters.
- `save_skips(skips: list[dict]) → None` — writes `data/skipped_picks.json`; initialises null grading fields (`actual_value`, `would_have_hit`, `skip_verdict`, `skip_verdict_notes`); logs each skip reason to stdout
- `main()` now unpacks `picks, skips = call_analyst(...)` and calls both `picks = save_picks(picks)` and `save_skips(skips)`
- `VALID_TIERS` constant — `{"PTS": [10,15,20,25,30], "REB": [4,6,8,10,12], "AST": [2,4,6,8,10,12], "3PM": [1,2,3,4]}` — defined after `LARGE_SLATE_THRESHOLD`; used by `reconcile_pick_values()`; T2 REB removed 2026-03-14 (no betting market at mainstream books)
- `PLAYOFF_START_DATE = "2026-04-14"` — date-gate for `build_playoff_context()`; postseason context block fires on/after this date; added 2026-04-09
- `filter_self_skip_picks(picks: list[dict]) → list[dict]` — **first step of `save_picks()`** (runs before `reconcile_pick_values()`); scans each pick's `tier_walk` for unambiguous skip-conclusion language; **SKIP_CONCLUSIONS now has 16 patterns**: 5 confidence-threshold patterns ("below 70% skip", "→ 65% skip", "confidence = N% skip", etc.) + 4 floor gate failure patterns (`floor gate fails`, `no variance buffer + floor gate/skip`, `3rd-lowest equals pick value + skip`, `SKIP: 3rd-lowest`) + 7 named hard-gate patterns (2026-03-19): `HARD GATE FIRES`, `BLOWOUT_SECONDARY_SCORER SKIP fires`, `hard gate fires` (case-insensitive), named rule skip phrases (volatile_weak_combo, ast_hard_gate, blowout_secondary_scorer, 3pm_blowout_trend_down, reb_floor_skip, blowout_t25_skip, fg_margin_thin_tier_step + fires/skip), `SKIP T{N} AST/PTS/REB/3PM` + hard gate/mandatory skip/no valid tier, `mandatory skip`, `Record as skip`; hard-gate patterns cover rules that fire unconditionally before confidence arithmetic; removes picks whose own reasoning concludes skip but were emitted anyway; conservative — override signals ("proceed", "exception applies", "iron_floor overrides", etc.) appearing AFTER the skip language leave the pick intact; filtered picks logged to stdout as `[analyst] SELF_SKIP_FILTERED:` (not written to `skipped_picks.json`); added 2026-03-15; floor gate patterns added 2026-03-16; hard-gate patterns added 2026-03-19. Prototype failures: Pritchard PTS T10 (confidence threshold), Thompson REB T4 (floor gate), Banchero AST (HARD GATE FIRES / ast_hard_gate), Jalen Green PTS (BLOWOUT_SECONDARY_SCORER SKIP fires).
- `reconcile_pick_values(picks: list[dict]) → list[dict]` — **second step of `save_picks()`** (runs after `filter_self_skip_picks()`); ensures `pick_value` matches the analyst's declared final tier. **Primary path (added 2026-04-09):** reads `walked_tier` integer field directly — if it disagrees with `pick_value` and is a valid lower tier, corrects `pick_value` downward. No one-tier-step guard needed (explicit analyst declaration). **Fallback path:** when `walked_tier` is missing (transition period), attempts regex parsing of `tier_walk` text — rightmost valid tier after step keywords within 20-char gap; one-tier-step guard retained for regex path only. Never raises `pick_value`. On correction: appends `[reconciled: pick_value corrected {old}→{new} via walked_tier]` (or `by regex fallback`) to `tier_walk`.
- `enforce_market_gate(picks: list[dict]) → list[dict]` — **third step of `save_picks()`** (runs after `reconcile_pick_values()`; added 2026-04-09); cross-checks every pick against `odds_available.json` via `load_available_markets()`; rejects picks where the player or player+prop+tier has no FanDuel market; logs `MARKET_GATE_REJECT` per removal; gate disabled entirely when `odds_available.json` is missing or stale — never blocks picks due to absent odds data. Catches Claude prompt compliance failures (e.g. Pritchard PTS T10 emitted despite zero FanDuel markets).

### `agents/lineup_update.py` (new — March 10, 2026; overhauled March 12, 2026; fixes applied March 14, 2026)
- Constants: `MODEL = "claude-sonnet-4-6"`, `MAX_TOKENS = 2048`, `CUTOFF_MINUTES = 20`
- `_ABBR_NORM` dict + `_norm(abbr)` — same normalization pattern as rest of codebase
- `OPPORTUNITY_FLAGS_JSON = DATA / "opportunity_flags.json"` — cumulative suggestion file
- `SKIPPED_PICKS_JSON = DATA / "skipped_picks.json"` — read by `load_morning_skips()` for annotation
- `ESPN_SCOREBOARD_URL` — ESPN scoreboard API used by `fetch_live_spreads()`
- `_OPPORTUNITY_TRIGGER_TAGS = {"defensive_anchor", "rim_anchor", "high_usage"}` — role tags; **used only by `classify_absent_player()` / amendment path now**; opportunity surfacing no longer uses tag gating (redesign 2026-03-15)
- `load_game_map() → dict[str, str]` — `{norm_team_abbr: game_time_utc}` from `nba_master.csv`; used to check tip-off cutoff per pick
- `game_is_actionable(game_time_utc, now_et) → bool` — True if tip-off > CUTOFF_MINUTES away; True on parse failure (safe default — don't skip on bad data)
- `compute_lineup_diff(lineups, injuries, today_picks=None) → list[dict]` — **TWO-SOURCE detection (March 14, 2026):** Source 1 (snapshot starters): players in `snapshot_at_analyst_run` now OUT/DOUBTFUL in injury report or silently dropped from starters. Source 2 (picks-based): players with open (ungraded) picks today who appear OUT/DOUBTFUL in injuries OR have `voided=True` in picks (lineup_watch already confirmed OUT). Both sources run; results merged and deduplicated via `(player_name_lower, norm_team)` 2-tuple. Each change dict carries a `"source": "snapshot" | "picks"` field. Root cause fixed: stars who aren't in Rotowire projected starters (load management, post-snapshot status shifts, bench players) were completely invisible to the original snapshot-only diff.
- `get_affected_picks(today_picks, changes, game_map, now_et) → list[dict]` — returns open today picks (result=None, not voided) where `team` OR `opponent` in changed teams AND game still actionable
- `build_rotowire_context(lineups, changed_teams) → str` — reads `projected_minutes` and `onoff_usage` from `lineups_today.json` for each team in `changed_teams`; formats starters/bench/out projected minutes and on/off usage deltas as a plain-text block; returns `""` when no Rotowire data present (graceful no-op on unauthenticated runs)
- `load_player_stats() → dict` — reads `data/player_stats.json`; returns `{}` gracefully if missing
- `build_pick_quant_summary(player_name, prop_type, player_stats) → str` — builds a one-line quant summary for a specific pick (tier hit rate, vs_soft/vs_tough, trend, volatility tag); injected as context beneath each pick in the Claude prompt
- `classify_absent_player(player_name, team, player_stats, today_picks=None) → dict` — derives role tags `{role_tags, avg_pts, avg_reb, avg_ast, avg_min}` from `player_stats.json`; tag logic: high_usage (avg_pts ≥ 20 or raw_avgs usage proxy), rim_anchor (avg_reb ≥ 7 and C/PF or raw_reb ≥ 9), defensive_anchor (avg_reb ≥ 6 and avg_pts < 15 proxy), perimeter_threat (avg_tpm ≥ 1.5), otherwise role_player. **Picks-based fallback (March 14, 2026):** if `player_stats` lookup finds nothing AND `today_picks` provided, infers tags from pick values — PTS pick → `high_usage`, REB pick ≥ 6 → `rim_anchor + defensive_anchor`, AST pick ≥ 4 → `primary_creator`, 3+ picks → `high_usage`; logs `[lineup_update] classify: {name} not in player_stats — inferred from picks: {tags}`. Prevents opportunity scan from silently failing when absent player was pre-filtered from player_stats at analyst run time.
- `build_absent_player_profiles(changes, player_stats) → str` — builds a `## ABSENT PLAYER PROFILES` block for the Claude prompt listing each absent player with role tags and key stats
- `call_lineup_update(affected_picks, changes, rotowire_context="", player_stats=None) → list[dict]` — single Claude call with 4-step reasoning framework (identify role, determine direction, calibrate magnitude using quant, apply default rule); system prompt now uses absent player profiles and per-pick quant context; `rotowire_context` injected when non-empty; returns `[{player_name, prop_type, direction, revised_confidence_pct, revised_reasoning}]`
- `apply_amendments(all_picks, amendments, affected_picks, changes, now_iso, player_stats=None) → tuple[int,int,int]` — writes `lineup_update` sub-objects in-place to `all_picks`; returns `(n_amended, n_up, n_down)`; keyed by `(player_name_lower, prop_type)`. **Two deterministic gates (2026-04-01):** **Gate 1 (Sub-70% auto-skip):** if `direction=="down"` and `revised_confidence_pct < 70`, the pick is immediately voided (`voided=True`, `void_reason` references revised confidence); the `lineup_update` sub-object remains visible on the voided pick for audit trail; fixes SGA AST T4 miss 2026-03-30 (amended to 68%, ran anyway). **Gate 2 (B2B <5g upside block):** if `direction=="up"` and `player_stats[player].b2b_hit_rates[prop]` is null AND `on_back_to_back` is true, override to `direction="unchanged"` with original confidence preserved; opponent lineup changes do not resolve B2B sample uncertainty; fixes Harden AST T4 + REB T6 misses 2026-03-31 (both null B2B, both amended up, both missed). Gate 2 uses name-based lookup into `player_stats` dict (title-case keys). Both gates fire before the normal `lineup_update` dict is written.
- `load_game_log() → pd.DataFrame | None` — reads `data/player_game_log.csv`; returns None gracefully on missing/error
- `fetch_live_spreads() → dict[str, float | None]` — fetches current spreads from ESPN scoreboard API; returns `{norm_team: signed_spread}`; negative = favored; returns `{}` on any error (graceful degradation — spread delta omitted from cards without blocking run)
- `load_morning_skips() → dict[str, list[str]]` — reads `data/skipped_picks.json`; filters to TODAY_STR; returns `{player_name_lower: [skip_reason, ...]}` for opportunity card annotation
- `compute_without_player_rates(teammate_name, absent_player_name, game_log) → dict` — looks up historical games where absent player had dnp=="1" or minutes in ("","0"); computes hit rates at each tier for the teammate in those games; returns `{stat: {tier, hit_rate, n}}` for qualifying tiers (≥0.70 and n≥3); returns `{}` when insufficient history
- `build_opportunity_suggestions(changes, today_picks, player_stats, game_log, now_iso, injuries=None) → list[dict]` — **(overhauled 2026-03-16; injuries filter added 2026-04-11; odds enrichment added 2026-04-11)** for each confirmed absence (new_absence), scans ALL whitelisted teammates AND opponents; **skips absent player from their own scan** (self-exclusion guard); **skips candidate players listed as OUT/DOUBTFUL/OFS in the injuries dict** (injury filter added 2026-04-11 after the 4/10 incident where SGA, Holmgren, Hartenstein, J. Williams all surfaced as opportunities despite being OUT for rest — `injuries` param is optional with default `None` for backward compatibility; `main()` passes the in-scope injuries dict); **loads `data/odds_available.json` once per run (date-gated) and annotates each matching tier entry with `market_implied_pct` and `market_odds` from FanDuel** (odds enrichment added 2026-04-11; lookup keyed by `_norm_odds_name(tm_name)` matching `ingest/odds_today.py` normalization; graceful no-op on missing/stale file or no-match player/tier); collapses all qualifying props into one card per player (was one card per player×prop); builds `qualifying_tiers` dict (props ≥70% where player has no morning pick) and `upgrade_tiers` dict (props where quant best tier > morning pick tier, carries `morning_tier`/`morning_confidence_pct` sub-fields); determines `card_type` ("new_pick"/"upgrade"/"mixed"); skips player if both dicts empty; deduplicates by `name_lower` only; uses `triggered_by` (string, not `triggered_by_player`). Suggestion schema: `{date, generated_at, triggered_by (str), triggered_by_team, side ("teammate"|"opponent"), player_name, team, card_type, qualifying_tiers {stat: {tier, hit_rate_pct, trend, volatility, [without_player_hit_rate_pct, without_player_n], [market_implied_pct, market_odds]}}, upgrade_tiers {stat: {tier, hit_rate_pct, trend, volatility, morning_tier, morning_confidence_pct, [without_player_hit_rate_pct, without_player_n], [market_implied_pct, market_odds]}}, spread_delta, morning_context, reasoning}`.
- `_norm_odds_name(name) → str` — normalizes player names for `odds_available.json` lookup. Uses `re.sub(r"[^a-z0-9 ]", "", name.lower()).strip()` — strips hyphens, apostrophes, periods, keeps only alphanumerics and spaces. Matches the pattern in `ingest/odds_today.py` (Jaylen Brown → `jaylen brown`, Karl-Anthony Towns → `karlanthony towns`, De'Aaron Fox → `deaaron fox`, C.J. McCollum → `cj mccollum`). DIFFERENT from `_norm()` which handles team abbreviations — the two helpers coexist. `re` imported inline inside the function body. Added 2026-04-11.
- `save_opportunity_flags(suggestions) → None` — appends to `data/opportunity_flags.json`; deduplicates by `(date, player_name_lower, triggered_by_lower)` — one card per player per triggering absence; atomic write via `.tmp` rename
- `build_skip_reconsiderations(changes, player_stats, game_log, now_iso) → list[dict]` — added 2026-04-11 (H26 Part 2 of 2). Re-evaluates morning `merit_below_floor` PTS/AST skips when a team's leading scorer is confirmed OUT in today's afternoon changes. Reads `star_absence_lift` field from `player_stats.json` (Part 1 dependency — gracefully no-ops if field missing). PTS and AST only — REB/3PM excluded (no H26 signal). **PERSONAL_DRAG_WARNING guard** fires before any reconsider logic with explicit `continue` — invariant that prevents the Jalen Green / Booker class of misses where per-player data shows a drag and population lift must NOT apply. Two reconsideration paths: (A) `compute_without_player_rates()` gives per-player without-star tier hit rate ≥ 70% (preferred); (B) population-delta fallback using `_SA_POPULATION_DELTAS` module constant (mirror of `quant.py` `SA_POPULATION_DELTAS`, inline comment documents both must update together) when no per-player data, only when qualifier is in {STRONG_PERSONAL_SIGNAL, POPULATION_ONLY, NEUTRAL_PERSONAL_DATA} and per-player `key_deltas[pop_key] > 0` OR qualifier is not NEUTRAL_PERSONAL_DATA. Emits `skip_reconsideration` cards to `data/opportunity_flags.json` via the existing `save_opportunity_flags()` pipeline — card schema mirrors `build_opportunity_suggestions()` cards (same dedup fields `qualifying_tiers={}`, `upgrade_tiers={}` required) plus new fields `prop_type`, `tier_considered`, `original_skip_reason="merit_below_floor"`, `without_star_hit_rate_pct`, `without_star_n`, `star_absence_qualifier`, `reasoning` (semicolon-joined, starts with "<star> confirmed OUT", ends with "original skip: merit_below_floor"). Wired in `main()` immediately after the existing `build_opportunity_suggestions()` block with parallel `has_absences and not debug` / `has_absences and debug` branches — non-debug calls `save_opportunity_flags(recons)`, debug prints cards to stdout without saving. Reuses in-scope `game_log` via `game_log if game_log is not None else load_game_log()` defensive ternary. Runtime-tested with synthetic fixture reproducing Tatum/Brown case: Tatum PTS T20 and Pritchard AST T4 correctly reconsidered (+27.3pp and +27.9pp player-specific deltas), Derrick White REB filtered by PTS/AST-only filter, Jalen Green PTS filtered by DRAG_WARNING guard, volatile_weak_combo and wrong-date skips filtered by their respective gates. **H26 downstream loop closed** — Part 1 annotation (entry 62) + Part 2 reconsideration (entry 63) both shipped 2026-04-11.
- `main()` — accepts `--debug` flag (argparse). Debug mode: prints full diagnostic (snapshot teams found, changes detected with source labels, affected picks count, opportunity suggestions formatted as `[card_type] new=STAT Ttier hit% upgrade=STAT T{old}→T{new} ← absent_player (side)`) without making LLM call or writing any files. Picks are loaded BEFORE `compute_lineup_diff()` call so `today_picks` can be passed as second detection source. Run with: `python agents/lineup_update.py --debug`. **Skip reconsideration block** (added 2026-04-11, H26 Part 2) runs immediately after the opportunity-surfacing block, gated on the same `has_absences` flag — calls `build_skip_reconsiderations()` and saves cards via the same `save_opportunity_flags()` pipeline with `card_type="skip_reconsideration"`.

**`lineup_update` sub-object schema (written to affected picks):**
```json
{
  "triggered_by":           ["string — detail of each relevant change"],
  "updated_at":             "ISO timestamp",
  "direction":              "up" | "down" | "unchanged",
  "revised_confidence_pct": number,
  "revised_reasoning":      "string, max 20 words"
}
```
Sub-object is overwritten on each hourly run. `direction=unchanged` is still written (audit evidence).
Original `confidence_pct`, `reasoning`, `pick_value`, `tier_walk` fields are NEVER modified.

### `auditor.py`
- **Path constants:** `MASTER_CSV = DATA / "nba_master.csv"` added; `SKIP_ARCHIVE_JSON = DATA / "skipped_picks_archive.json"` added (2026-03-23) — persistent append-log for graded skips, never overwritten.
- `save_skip_archive(graded_skips: list[dict]) -> None` — appends fully graded skip records to `data/skipped_picks_archive.json` after each auditor run; de-duplicates on re-run by date (same pattern as `audit_log.json`); called inside the `if skips:` block in `save_audit()` after the `SKIPPED_PICKS_JSON` write; no-op when `graded_skips` is empty; added 2026-03-23.
- `build_absence_context(graded_picks)` → returns `## YESTERDAY'S NOTABLE ABSENCES` block (players voided or OUT at check time) or ""
- `load_game_results() → dict[str, dict]` — reads `nba_master.csv` for YESTERDAY_STR; returns `{team_abbrev: {home, away, home_score, away_score, margin, winner}}` keyed by BOTH home and away team abbrev (O(1) lookup from either side); skips rows where scores can't be cast to int; returns `{}` on any exception; logs game count. Motivating failure: Durant and Jokic played in the same game (HOU/DEN 36-pt blowout, March 11) — auditor couldn't identify the game context without a Brave Search hit, causing asymmetric miss classification.
- `build_game_results_block(game_results: dict) → str` — deduplicates via `seen` set on `{home}_{away}` key; labels each game `BLOWOUT` (margin ≥ 20), `COMPETITIVE` (10–19), or `CLOSE` (<10); sorts lines alphabetically; returns `## GAME RESULTS — YESTERDAY\n...` block; returns `""` if no games
- `save_audit_summary(audit_log, all_skips=None)` → writes `data/audit_summary.json`; per-prop and overall denominators exclude `injury_event` picks; `injury_exclusions` key in both per-prop and overall dicts; when `all_skips` provided, rolls up per-rule false skip rates into `"skip_validation"` key in the summary; `overall.voided` accumulates `voided_picks` across all audit entries (older entries without the field return 0 via `.get()`); `"human_flag_precision"` block (added 2026-03-16) reads `picks.json` directly, groups all graded picks by `human_verdict`, computes `{hits, misses, total, hit_rate_pct}` per verdict type across full season — accumulates automatically without explicit audit_log entries; `"clv_summary"` block (added 2026-04-09) reads all picks with `clv_pp` field, computes beat_close/lost_close/no_movement splits (±0.5pp threshold), avg CLV pp, and hit rate per CLV direction; CLV only populated for picks that had both `morning_implied_prob` and `market_implied_prob` (pretip sweep ran for their game)
- `load_picks_review(date_str: str) → dict[tuple, dict]` — reads `data/picks_review_{date_str}.json`; returns `{(player_name_lower, prop_type, pick_value): entry_dict}`; returns `{}` gracefully when file absent (prints no-op log); called in `main()` with `YESTERDAY_STR` after `grade_picks()`
- `apply_human_verdicts(graded_picks, review) → list[dict]` — tags each pick in-place with `human_verdict` ("keep"/"trim"/"manual_skip"/None) and `trim_reasons` (list/[]); returns same list; no-op (returns unchanged) when `review` is empty; called in `main()` after `load_picks_review()`
- `build_audit_prompt()` — splits `graded_picks` into `voided_picks` (player confirmed OUT, `voided=True`) and `active_picks` at the top of the function before any counting; all hit/miss/no_data counts, prop_breakdown, conf_breakdown, and `hits_and_misses` use `active_picks` only; `build_absence_context()` still receives full `graded_picks` (voided players ARE the absences it surfaces); OUTPUT FORMAT schema now emits both `"total_picks"` (active only) and `"voided_picks"`; summary line shows `active | voided | hits | misses` separately. Motivating fix: voided picks were inflating total_picks and appearing as NO_DATA, docking the system's hit rate for picks correctly voided before tip-off.
- `load_skipped_picks() → list[dict]` — reads `data/skipped_picks.json`; returns `[]` gracefully if missing
- `build_game_log_rows_for_yesterday() → dict[str, dict]` — reads `player_game_log.csv` for YESTERDAY_STR; returns `{player_name_lower: row_dict}` for non-DNP rows; used by `grade_skips()`
- `grade_skips(skips, game_log_rows) → list[dict]` — pure Python; fills `actual_value`, `would_have_hit`, `skip_verdict` (`false_skip` / `correct_skip` / `no_data`), `skip_verdict_notes` on each skip record in-place; returns same list
- `save_audit_report(audit_entry, graded_picks, graded_parlays, skips=None)` → extended signature; when `skips` provided, appends `## Skip Validation` table to the daily markdown report in `data/audit_reports/`; `save_audit()` callsite passes graded skips
- Miss classification taxonomy (6 types): `selection_error` / `model_gap_signal` / `model_gap_rule` / `variance` / `injury_event` / `workflow_gap` — written to `miss_classification` in `miss_details`. `model_gap_signal` = system lacks the signal entirely (no quant field or rule exists that could have caught this); `model_gap_rule` = signal existed in quant data/context at pick time but the analyst rule didn't correctly handle the combination. The legacy `model_gap` value is no longer valid.
- `build_audit_prompt(..., game_results_block: str = "")` — extended signature; `{game_results_block}` injected into prompt between season context and playoff_picture_section. Injects `{absence_block}` before `{news_block}`, ⚠ INJURY LANGUAGE IN NEWS flag on news_lines entries where detected. STEP 0 — ESTABLISH GAME CONTEXT inserted as first step of PICK ANALYSIS TASK: auditor looks up each player's team in `## GAME RESULTS — YESTERDAY` to identify final score, margin, and game_script label (BLOWOUT/COMPETITIVE/CLOSE) before analyzing any individual miss; provides shared game-context evidence across all players from the same game without requiring Brave Search hits. STEP 6 reads `lineup_update` sub-objects and annotates amendment direction vs. outcome in `root_cause`. Does not change `miss_classification` — amendment and game context notes are contextual only. Existing STEP 1–6 numbering unchanged.
- **`load_player_stats_for_audit()` was REMOVED (March 8, 2026)** — eliminates yesterday's-data-for-today's-audit confabulation bug. Auditor now reads quant context from pick object fields (`reasoning`, `hit_rate_display`, `tier_walk`, `opponent`) written at pick time.
- `promote_injury_event_voids(graded_picks, audit_entry) → int` — added 2026-04-22. Called between `call_auditor()` and `save_audit()` in `main()`. Reads `audit_entry["miss_details"]`, builds a `(name_lower, prop_type, pick_value)` set of `injury_event` misses, and promotes matching graded picks to `voided=True, result=null, void_reason="injury_exit_mid_game"`. Recomputes daily counts (`total_picks`, `voided_picks`, `hits`, `misses`, `hit_rate_pct`) plus `prop_type_breakdown` and `confidence_calibration` per-band totals so the audit_log entry stays internally consistent. **Grading contract: voided picks always have `result=null`** — frontend `build_site.py` filters on `result in ("HIT","MISS")` so voided picks are automatically excluded from Overall/Yesterday/Playoffs stat cards. The miss still appears in `miss_details` for audit trail. Companion one-time `_retroactive_injury_void_patch()` runs at the start of `main()` — idempotently patches Wemby's two 2026-04-21 picks (PTS T20 + 3PM T1) to voided + recomputes audit_log[2026-04-21] from `22/18/4/0 = 81.8%` to `20/18/2/2 = 90.0%`.

### `post_game_reporter.py` (runs before auditor each morning)

**Architecture (rewritten 2026-04-07):** Three-source intelligence pipeline with Claude LLM classification. Replaces prior Brave Search + deterministic phrase-matching approach. Sources: (1) ESPN athlete news per player, (2) ESPN game recap pages per `game_id`, (3) Rotowire authenticated news feed. Claude receives all available source blocks per missed-pick player and classifies in a single batch call.

**Kept unchanged from prior version:**
- `load_yesterdays_player_names()` → set of lowercase names from yesterday's picks
- `load_yesterdays_missed_pick_names()` → subset where `result == "MISS"` **only** — `None` (ungraded) excluded
- `load_yesterdays_picks_with_status()` → `{name_lower: highest_severity_status}` from `picks.json`
- `load_athlete_id_map()` → `{player_name_norm: player_id}` from `player_dim.csv`
- `load_yesterday_game_rows(player_names)` → `{name: row_dict}` from `player_game_log.csv`
- `_get_miss_pick_meta(player_name_lower)` → `{prop_type, pick_value, actual_value, team}` from picks
- `should_fetch(game_row, is_missed_pick=False)` → `(bool, reason)` — efficiency gate, output used for logging labels
- `parse_minutes(raw)` → `float` — extracts minutes from game log
- `is_dnp_row(row)` → `bool` — checks dnp flag
- `fetch_espn_news(athlete_id)` → `(news_items, fetch_ok)` — per-player ESPN athlete news API
- `_write_empty()` → writes empty `post_game_news.json` on early exit

**New functions (added 2026-04-07):**
- `load_yesterdays_game_ids(player_teams: set[str]) → dict[str, str]` — reads `nba_master.csv` for YESTERDAY_STR; returns `{game_id: "AWAY @ HOME"}` for games involving relevant teams; includes `_ABBR_NORM` dict for team abbreviation normalization
- `fetch_espn_recaps(game_ids: dict) → dict[str, str]` — fetches ESPN recap pages via `ESPN_RECAP_URL` template (`https://www.espn.com/nba/recap/_/gameId/{game_id}`); parses via BeautifulSoup `div.Story__Body` with `<article> <p>` fallback; truncates to `RECAP_MAX_CHARS = 3000`; returns `{game_id: recap_text}`; graceful per-game error handling
- `login_rotowire() → requests.Session | None` — authenticates using `ROTOWIRE_EMAIL`/`ROTOWIRE_PASSWORD` env vars; same pattern as `ingest/rotowire_injuries_only.py`; returns `None` if creds missing or login fails
- `fetch_rotowire_news(session, player_names) → dict[str, list[str]]` — fetches `ROTOWIRE_NEWS_URL` (`https://www.rotowire.com/basketball/news.php`); finds news divs via regex class matching (`news-update|news_item|news-item`); matches players by last name; truncates blurbs to 500 chars; returns `{player_name_lower: [blurb, ...]}}`
- `call_claude_classify_events(players_context: list[dict]) → dict[str, dict]` — single Claude API call (`claude-sonnet-4-6`, max_tokens=2048); system prompt includes explicit handling of phrasing variations ("won't return", "did not finish", etc.); classifies `event_type` (injury_exit/dnp/minutes_restriction/no_data) + `confidence` (confirmed/inferred) + `detail` + `web_narrative` per missed-pick player; JSON output parsed with graceful fallback; returns `{player_name_lower: {event_type, detail, confidence, web_narrative}}`

**Removed functions (2026-04-07):** `news_contains_injury_language()`, `classify_from_news()`, `fetch_web_narratives()`, `call_claude_summarise_narratives()`, `_extract_url()`. Removed constants: `BRAVE_SEARCH_URL`, `INJURY_SCAN_TERMS`, `_INJURY_EXIT_TERMS`, `_DNP_TERMS`, `_RESTRICTION_TERMS`, `WEB_SEARCH_MODEL`, `WEB_SEARCH_TOKENS`.

**`main()` flow (6 steps):** (1) Fetch ESPN athlete news per player → `espn_news_map`, `espn_fetch_status`. (2) Fetch ESPN recaps per game via `load_yesterdays_game_ids()` + `fetch_espn_recaps()` → `recaps`. (3) Fetch Rotowire news via `login_rotowire()` + `fetch_rotowire_news()` → `rw_news`. (4) Build per-player context blocks for missed-pick players; call `call_claude_classify_events()` → `claude_results`. (5) Build `players_out` dict: missed picks use Claude results; non-missed picks get `event_type="normal"`. Fallback: if Claude fails entirely, deterministic DNP detection from box score data (minutes=0 or dnp flag → `event_type="dnp"`, else `"no_data"`). (6) Write JSON output.

- Writes `data/post_game_news.json`: `{date, generated_at, players: {name: {event_type, detail, minutes_played, source_url, confidence, injury_status_at_check, injury_language_detected, injury_scan_term, espn_fetch_ok, web_narrative}}, fetch_errors}`
- Legacy fields preserved for auditor compatibility: `injury_language_detected` (always `False`), `injury_scan_term` (always `None`)
- `espn_fetch_ok`: `true/false` when ESPN athlete news attempted, `null` when no athlete ID
- `web_narrative`: Claude-generated narrative string for missed-pick players, `null` for non-missed picks
- `source_url`: first ESPN news link if available, else `null`
- `confidence`: `"confirmed"` or `"inferred"` from Claude classification; `null` for non-missed picks
- **Graceful degradation:** ESPN recap failure → Claude classifies without recap. Rotowire failure → Claude classifies without Rotowire. Claude failure → fallback deterministic DNP detection from box scores. All failures logged, never crash.
- **`BRAVE_API_KEY` no longer required** by this agent (removed as a dependency).

### `season_context_updater.py` (runs in auditor.yml after auditor.py — NEW 2026-04-21)
- Model: `claude-sonnet-4-6` | MAX_TOKENS: 4096
- Date-gated: no-op before `PLAYOFFS_R1_DATE = "2026-04-18"`
- Key inputs: `data/nba_master.csv` (yesterday's completed postseason games with season_type==3), `data/player_game_log.csv` (stat lines for whitelisted players + any non-whitelisted 25+ pt performers), `data/post_game_news.json` (narratives from post_game_reporter.py), `data/injuries_today.json` (current team-keyed injury status), `context/nba_season_context.md` (read + patched in place)
- Key output: `context/nba_season_context.md` (in-place patch — appends series diary entries, patches injury bullets, bumps timestamp)
- Core flow:
  1. `load_yesterday_playoff_games()` — filters to completed postseason games
  2. `find_series_sections()` — regex on `##### (N) TEAM vs (N) TEAM` headers; returns per-section dicts with `header_start`, `section_end`, `last_game_entry_end` (insertion point), and `game_count`
  3. `match_game_to_series()` — symmetric home/away match against `team1`/`team2`
  4. `load_player_stat_lines()` — per-game stats, sorted by pts desc
  5. Single Claude call with full document in `<season_context>` tags as ground truth (combats stale training priors), structured game data, post-game narratives, relevant injuries
  6. `parse_llm_response()` — 4-strategy parse: direct → strip fences → `json_repair` → brace extraction
  7. `apply_diary_entries()` — bottom-up insertion to preserve offsets; inserts after last existing `**Game N**` paragraph
  8. `apply_injury_updates()` — substring-match bullets by `search_line`, replace with `full_replacement`; graceful skip on missing
  9. `update_timestamp()` — regex replace `*Last updated: YYYY-MM-DD.*`
  10. Safety net: refuses to write if updated content shorter than original

### `playoff_matchup.py` (runs between quant and pre_game_reporter — NEW 2026-04-07)
- Pure Python, no LLM call. Reads `data/playoff_bracket.json` (manually created by Oliver when bracket confirmed), `nba_master.csv`, `player_game_log.csv`, `player_whitelist.csv`
- **Graceful no-op:** exits immediately if `playoff_bracket.json` absent — safe to keep in workflow year-round
- Computes per-series: game results from `nba_master.csv` (on/after `playoff_start_date`), series record (home_wins/away_wins), series state tags (`dominant`/`favorable`/`even`/`trailing`/`desperate`), game-script implication notes
- Per-player (whitelisted players on series teams): series averages + tier hit counts (from completed playoff games) + season H2H averages + tier hits + home/away H2H split (from regular season games vs same opponent before playoffs)
- Season H2H always shown alongside series data — persistent baseline, not a fallback
- Writes `data/playoff_matchup.json`: `{date, generated_at, mode, round, season, series: [...], context_block: str}`
- `context_block` is pre-formatted text ready for direct injection into analyst prompt
- **Manual setup required:** Oliver creates `data/playoff_bracket.json` when R1 bracket is confirmed (~April 12–13); updates `round` field for R2/R3/Finals
- **Analyst integration:** `load_series_context()` reads `playoff_matchup.json`, returns `context_block` or `""`. Injected as `## SERIES CONTEXT — PLAYOFFS` in all 3 prompt builders
- Constants: `BRACKET_JSON`, `MATCHUP_JSON`, `TIER_THRESHOLDS`, `STAT_COLS`
- Key functions: `load_bracket()`, `load_whitelist()`, `load_today_teams()`, `load_series_game_results()`, `_compute_avgs_and_hits()`, `load_player_stats_for_matchup()`, `compute_series_state()`, `format_series_block()`, `format_context_block()`

### `pre_game_reporter.py` (runs between quant and analyst)
- Loads today's team abbrevs from `nba_master.csv`; loads active whitelist players on today's teams
- **`load_target_players(teams_today)` matches on BOTH `team_abbr` (Rotowire: NYK, SAS, GSW) AND `team_abbr_alt` (ESPN: NY, SA, GS)** (fixed 2026-04-10; previously only `team_abbr` was checked, silently dropping 10 NYK/SAS/GSW players because `nba_master.csv` uses ESPN short codes). Mirror of the 2026-03-25 whitelist-tuple fix in `quant.py`/`analyst.py`.
- Fetches ESPN athlete news per player + league-wide news (48h window)
- Filters to prop-relevant items; discards noise keywords (contract/fine/trade rumor)
- **Error tracking split into three lists** (fixed 2026-04-10): `no_id_errors` (athlete_id missing from `player_dim.csv`), `espn_errors` (ESPN HTTP/network failure), `no_news_players` (fetch succeeded but returned zero items). Combined `fetch_errors = no_id_errors + espn_errors` kept for backward compatibility. All three lists written as top-level keys in `pre_game_news.json`. Downstream consumers (analyst) read only `player_notes`/`game_notes`/`suggested_context_updates`/`staleness_flags` — new fields are additive and diagnostic-only.
- **`detect_staleness_flags(context_path, today)` → `list[str]`** — deterministic Pass 1; parses `## SEASON FACTS` section of `nba_season_context.md` for ISO dates (`YYYY-MM-DD`) and month-year dates (`Feb 2026`); applies three rules per line: (1) return/injury keyword + age >7d, (2) ISO date + no return keyword + age >5d, (3) trade/role keyword + age >60d; returns list of `⚠ CONTEXT FLAG: ...` strings; uses `re` + `datetime` only — no LLM
- **`_append_staleness_flags_to_md(staleness_flags)`** — appends a `## 🕐 DATE-BASED STALENESS FLAGS (auto-detected)` block to `data/context_flags.md` after the Claude report section
- **`call_claude_staleness_check(context_text, news_items)`** → `list[dict]` — Pass 2; single Claude call cross-referencing season context against today's news; returns list of `{player_or_team, current_context_fact, conflict, urgency, suggested_action}` flag dicts
- **`write_context_flags_md(flags)`** — writes full structured markdown report to `data/context_flags.md` (OVERWRITES — critical/monitor urgency tiers with `##` headers); `_append_staleness_flags_to_md()` is called AFTER this to append Pass 1 flags
- **`run_context_staleness_check(filtered_items, all_raw_items)`** → `tuple[list[dict], list[str]]` — orchestrates both passes; Pass 1 always runs first; Pass 2 (Claude) fires when `filtered_items > 0` OR context file date is stale; deduplicates Pass 1 flags against Pass 2 `player_or_team` tokens; returns `(claude_flags, staleness_flags)`
- **`write_output(..., context_flags=None, staleness_flags=None, no_id_errors=None, espn_errors=None, no_news_players=None)`** — writes `data/pre_game_news.json`; keys: `suggested_context_updates` (Claude flags), **`staleness_flags`** (Pass 1 flags — always present, `[]` when none), **`fetch_errors`** (legacy combined list, `= no_id_errors + espn_errors`), **`no_id_errors`** / **`espn_errors`** / **`no_news_players`** (diagnostic split added 2026-04-10, all default to `[]`)
- Writes `data/pre_game_news.json`; always writes (empty) even with no news

### `lineup_watch.py` (standalone, runs after each injury refresh)
- Reads `picks.json` + `injuries_today.json`
- Writes `injury_status_at_check` (OUT / DOUBTFUL / QUESTIONABLE / NOT_LISTED) + `injury_check_time` (ISO PT) to **ALL** of today's picks on every run
- OUT → `voided=True` + `void_reason`; DOUBTFUL → `lineup_risk="high"`; QUESTIONABLE → `lineup_risk="moderate"`
- Severity is sticky upward — picks never downgraded
- Name matching uses last-name + team-abbrev key to handle Rotowire abbreviated player name format
- Stale flag clearing: status improvements between hourly runs (e.g., DOUBTFUL → NOT_LISTED) now remove prior `voided`/`lineup_risk` flags
- **Sticky void guard (March 14, 2026):** CLEAR block guards against clearing confirmed voids. If `voided=True` AND `void_reason` is set, the pick is never un-voided — Rotowire post-game roster resets (which move all players back to PROB/unlisted) cannot undo a confirmed OUT. Speculative `lineup_risk` flags (DOUBTFUL/QUESTIONABLE) still clear normally when status improves.

---

## Known Edge Cases and Gotchas

**Review file staleness guard in `apply_review_flags()` (2026-04-11):** `agents/analyst.py` distinguishes auto-generated review files (every entry has `source: "auto"`) from manual ones (any entry lacks that marker). Manual files — including mixed auto+manual files — are NEVER overwritten (manual review always takes priority). Auto files are inspected: if their pick-key set covers all current picks, they're kept (no API-burn regeneration); if there's any uncovered current pick, the file is treated as stale and regenerated. Keys: `(player_name.lower(), prop_type, pick_value)`. This prevents same-day analyst re-runs with different pick sets from leaving new picks without review coverage. Triggered by the 4/10 Scout token fix expanding the shortlist from 8 to 30+ picks on a re-run — the old 15-pick auto review was preserved by the pre-fix existence guard, leaving 18 new picks without review badges. Any new code modifying `apply_review_flags()` MUST preserve the manual-file never-overwrite invariant — any entry without `source: "auto"` is authoritative.

**picks.json cross-workflow merge guard (2026-04-10):** `injuries.yml` commit steps 2 and 3 ("Commit picks status" after `lineup_watch.py`, "Commit lineup amendments" after `lineup_update.py`) run `ingest/merge_picks.py` to prevent clobbering picks.json when `analyst.yml` pushes new picks between the injuries checkout and commit. The guard compares today-pick counts: if origin (post-`git pull`) has MORE today-picks than local, origin is used as base with local's injury mutations overlaid onto matching picks. Otherwise local wins (no-op). Mutation fields carried from local → origin: `voided`, `void_reason`, `lineup_risk`, `injury_status_at_check`, `injury_check_time`, `lineup_update`. Match key: `(player_name.lower(), prop_type, pick_value, date)`. On any error, falls back to copying local → output (preserves pre-guard behavior — the guard can never regress). **The first commit step ("Commit injuries") is unchanged** because it doesn't touch picks.json. **Known limitations** (flagged in the resolved entry): analyst re-runs that *reduce* pick count bypass the guard (origin has fewer, so "origin > local" is false and local wins) — these are manually recoverable via the same `merge_picks.py` script run offline. Shipped after the 4/10 incident where re-run #3's 33 picks were clobbered back to 15 by an in-flight lineup_update commit 6 seconds later. Recovery commit: `a131c1f`. Any new `injuries.yml` commit step that touches picks.json MUST use the merge guard pattern: save origin after `git pull --rebase`, call `merge_picks.py`, clean up temp files.

**Player name lookups against `player_dim.csv` MUST use `_norm_name()` — raw `.lower()` silently drops 5 players (2026-04-09):** `data/player_dim.csv` stores `player_name_norm` with hyphens replaced by spaces and apostrophes/periods stripped (e.g. `karl anthony towns`, `deaaron fox`, `jabari smith jr`). Callers that query the lookup map with raw `whitelist_name.lower()` or `picks_player.lower()` will miss: **Karl-Anthony Towns**, **Shai Gilgeous-Alexander**, **De'Aaron Fox**, **Nickeil Alexander-Walker**, **Jabari Smith Jr.** The bug is silent — the player is skipped, no error raised. Any new code doing athlete ID lookups against `player_dim.csv` MUST apply the `_norm_name()` normalization (hyphens → space, apostrophes/periods removed, whitespace collapsed, lowercase). The helper is currently copied into `ingest/espn_playoff_backfill.py`, `agents/pre_game_reporter.py`, and `agents/post_game_reporter.py`. Do NOT apply `_norm_name()` to headline-substring-match sets (e.g. `target_names_lower` in `pre_game_reporter.py`) — those need the readable form for matching news headlines, not the dim form.

**`player_stats.json` nullable per-stat fields — `.get(prop, default)` does NOT protect against explicit None (2026-03-21):** Several quant fields store per-stat sub-dicts but write explicit `None` when data is insufficient (e.g. `bounce_back["REB"] = None`, `b2b_hit_rates["3PM"] = None`). The pattern `(s.get("field") or {}).get(prop, {})` is NOT safe for this case — `.get(prop, default)` only fires when the key is absent; when the key exists with a `None` value the default is bypassed and `None` is returned. The correct pattern is `(s.get("field") or {}).get(prop) or {}`. This bit `build_review_context()` on `bounce_back` (line 3217). Apply the `or {}` form any time a per-stat quant field can be explicitly `None`.

**`build_review_context()` volatility field shape (2026-03-21):** `player_stats.json["volatility"][prop]` is a dict `{label, sigma, n}`, not a string. Any code reading this field must do `.get(prop) or {}` then `.get("label")` to extract the label string. The pattern `.get(prop, "unknown")` embeds the raw dict representation into output instead of the label. Fixed at line 3205.

**Date-stamped module constants must follow `TODAY_STR` definition (2026-03-21):** Module-level path constants that embed `TODAY_STR` must be defined AFTER the `TODAY` / `TODAY_STR` block (lines ~46–47). Placing them above causes a `NameError` on import. All date-stamped paths in the file are in functions or after the date block — keep this invariant. (`SCOUT_OMITTED_JSON` was the original example; removed 2026-03-25 when the omitted block was dropped.)

**`call_analyst()` JSON repair fallback (added 2026-03-17):** `parse_claude_response()` (implemented inside `call_analyst()`) now has a two-step repair fallback that fires when `json.loads(raw)` raises an exception — triggered by a 2026-03-17 run failure where Claude produced structurally valid JSON with a bad character inside a `tier_walk` string field. Step 1: `json_repair` library (`from json_repair import repair_json`). Step 2: character sanitization — char-by-char scan that replaces literal `\n`/`\r`/`\t` inside JSON string values with their escaped forms. Both `except` blocks (object-format path and flat-array fallback path) call `_repair_json(raw)` before `sys.exit(1)`. The happy path (valid JSON) is completely unchanged. Repair logs `[analyst] WARNING: JSON repair via ... succeeded` so the pick run continues with visible evidence in the Actions log. `json-repair` is installed in `analyst.yml` pip install step.

**Team abbreviation variants:** `nba_master.csv` uses legacy 2-char forms (`GS`, `SA`, `NO`,
`UTAH`, `WSH`). `_ABBR_NORM` dict + normalization helpers exist in THREE places: `analyst.py`
(`_norm_team()`), `parlay.py` (`_norm_team()`), and `build_site.py` (`normAbbr()` JS). Also in
`lineup_update.py` (`_norm()`). Keep all in sync if abbreviations need updating.
`analyst.py` copies raw abbrevs from `nba_master.csv` into `picks.json` — this is the root
cause of historical game grouping issues (all now resolved in `build_site.py`).

**Player name case:** Whitelist CSV uses title case ("Cade Cunningham"). All filtering uses
`.strip().lower()` before set membership checks. The `position_map` dict is also keyed by
lowercase. The `player_name` variable in the `build_player_stats()` loop comes from
`log.groupby("player_name")` — raw case from game log CSV (also title case).

**DNP rows:** `player_game_log.csv` keeps DNP rows with `dnp="1"`. All hit rate computations
and volatility exclude them via `df[df["dnp"] != "1"]` or equivalent. Missing this filter
anywhere would silently corrupt hit rates (0 pts on a DNP would count as a miss at every tier).

**`player_stats.json` is overwritten daily** — it only contains players playing TODAY. It is
not a historical archive. The auditor no longer reads `player_stats.json` at all (the function
was removed March 8 to fix the date-gate bug). The auditor reads quant context from pick fields.

**`compute_positional_dvp` uses full season history,** not just the last 15 games (unlike
`build_opp_defense()` which uses `OPP_WINDOW=15`). This is intentional — position-specific
cells need larger samples to clear the 10-game minimum reliably.

**`spread_split_hit_rates` coverage is limited** — only 829/935 rows in nba_master.csv have
spread data. Cells with < 5 games show `n/a` in the quant context. Accumulates going forward.

**The `opp_defense_rating` field in `picks.json` output** — the analyst's prompt no longer
shows `opp_today=` per stat line (replaced by the DvP line), but the OUTPUT FORMAT schema
still requests `opp_defense_rating`. Claude derives this from the DvP PTS rating. It's read
by `build_site.py` for defense pills. Functionally fine; cosmetically inconsistent naming.

**`audit_summary.json` starts empty** — `save_audit_summary()` only accumulates going forward.
The analyst prompt shows "Insufficient audit history yet (need 3+ days)" until 3+ days exist.
The `prop_type_breakdown` and `confidence_calibration` fields are absent from pre-March audit
entries; `save_audit_summary()` handles missing fields with `.get()` defaults.

**`injury_status_at_check` field in `picks.json`** — written by `lineup_watch.py` on every
hourly run to ALL of today's picks (not just open ones). Values: `OUT / DOUBTFUL / QUESTIONABLE /
NOT_LISTED`. Used by the auditor's STEP 2 to distinguish `injury_event` (player was
NOT_LISTED/QUESTIONABLE at pick time, exited mid-game) from `workflow_gap` (player was
OUT/DOUBTFUL pre-game but wasn't voided). If `lineup_watch.py` hasn't run yet when the auditor
grades, the field will be absent — auditor handles gracefully.

**`post_game_news.json` fetch errors** — `fetch_errors` list in the output names players whose
ESPN API call failed. The auditor receives this in `## POST-GAME NEWS CONTEXT` and should note
the gap, not infer absence from missing data. Players without `athlete_id` in `player_dim.csv`
are also silently skipped for news fetch (box score inference used instead).

**`iron_floor` field in `picks.json`** — as of March 7, 2026, `iron_floor` is written to all
new picks. Pre-March picks don't have the field; `build_site.py` badge logic handles `undefined`
gracefully. The field is `true` only when the quant stat line showed `[iron_floor]` — Claude is
instructed to copy it directly from the context, not derive it.

**Voided pick grading contract (fixed March 14, 2026)** — `grade_picks()` in `auditor.py` now guards against grading voided picks. Two rules: (1) Fix 1A — `voided=True` picks are passed through with `result=None, actual_value=None` (no box score lookup). (2) Fix 1B — a second pass after the main loop promotes any pick with `result=="MISS" AND actual_value==0.0 AND void_reason set AND not voided` to `voided=True, result=None` (post-hoc late-DNP detection). `build_audit_prompt()` already filters `voided/active` correctly; `save_audit_summary()` denominator already excludes voided picks. The repair script `scripts/repair_void_grading.py` patched historical data for 2026-03-12 and 2026-03-13.

**`top_pick` field in `picks.json`** — as of March 12, 2026 (Session 2), `top_pick` is emitted by the analyst for 2–4 picks per day. Older picks without the field are treated as `false` by JS. `get_top_picks()` in `build_site.py` uses `top_pick=True` as primary gate; falls back to `confidence_pct >= 85` for backwards-compatibility. The `past_top_picks` history filter is `p.get("top_pick") is True OR p.get("confidence_pct", 0) >= 85` so historical picks still populate the Top Picks History drawer. Criteria live in the `## TOP PICKS — FINAL SELECTION STEP` section of `build_prompt()`.

**Whitelist filter is a `(name, team)` tuple set**, not name-only. This prevents traded players
from appearing under their old team. If a player is traded mid-season, update `team_abbr` in
`player_whitelist.csv` and set the old entry `active=0`. Do not delete old rows.

**`load_whitelist()` emits alt-abbrev tuples (2026-03-25):** Both `quant.py` and `analyst.py`
`load_whitelist()` now emit tuples for BOTH `team_abbr` (standard NBA) AND `team_abbr_alt` (ESPN
short) when alt is non-empty. Required because `player_game_log.csv` uses ESPN abbrevs (SA, GS,
NY, UTAH) while the whitelist uses standard NBA abbrevs (SAS, GSW, NYK, UTA). Without this, 10
players were invisible to quant and analyst since launch. Affected teams: SAS/SA, NYK/NY,
GSW/GS, UTA/UTAH. `load_injuries()` in `analyst.py` also normalizes `teams_today` through
`_ABBR_NORM` for the same reason (injury data for all four teams was silently dropped).

**`data/context_flags.md` write order** — `write_context_flags_md(claude_flags)` OVERWRITES the
file each run with the Claude-format structured report. `_append_staleness_flags_to_md()` is
called AFTER to append Pass 1 deterministic flags as a plain `## 🕐 DATE-BASED STALENESS FLAGS`
block. If there are no Claude flags, `write_context_flags_md([])` writes a "no conflicts" stub,
then staleness flags are still appended if any exist.

**`detect_staleness_flags()` section boundary** — the function stops at BOTH
`## TEAM DEFENSIVE PROFILES` AND `## PERMANENT ABSENCES` (whichever comes first). This prevents
the static TEAM DEFENSIVE PROFILES content (still in the file as a reference — not injected by
agents) from generating false staleness flags for old team defense notes.

**`format_team_defense_section()` always non-empty contract** — returns the fallback warning
string (including `##` header) on missing file, stale file (`as_of != TODAY_STR`), or empty
narratives. The analyst always sees a `## TEAM DEFENSIVE PROFILES` section even if it's just
the warning line. This means `team_defense_section` is never injected as a bare empty string.

**`load_season_context()` TEAM DEFENSIVE PROFILES stripping** — the static `## TEAM DEFENSIVE
PROFILES` section remains in `context/nba_season_context.md` as a human reference but is
stripped in-memory before injection. The file itself is never modified by any agent.

**`lineups_today.json` guard condition** — `rotowire_injuries_only.py` will NOT overwrite a
good existing file with 0 teams parsed. If a scrape returns 0 starters (e.g., Rotowire is slow
pre-noon), the existing file for today's date is preserved. Only non-zero parses overwrite.

**`snapshot_at_analyst_run` in `lineups_today.json`** — written by `write_analyst_snapshot()`
in `analyst.py` at pick time. Guard logic (2026-03-25): skips write only when `written_at` date
matches today AND `teams` is non-empty — prevents redundant overwrites within the same day while
allowing a refresh when a prior run left `teams: {}` (stale empty snapshot bug). Prior guard was
`if raw.get("snapshot_at_analyst_run"): return` which blocked all refreshes once any snapshot
existed, causing `lineup_update.py` to skip for 13 days. `lineup_update.py` treats absence of
this key (or empty `teams`) as a skip condition.

**`lineup_update.py` no-op conditions** — five conditions will silently skip the LLM call:
(1) `lineups_today.json` missing; (2) `snapshot_at_analyst_run` key absent; (3) no starter
changes detected (most common daily case); (4) no open non-voided picks for changed teams;
(5) all affected picks past tip-off cutoff. The agent logs the reason and exits cleanly.

**`_extract_last()` in `analyst.py`** — handles Rotowire-abbreviated name format "J. Brunson"
by detecting a pattern of `[char]. [rest]` and taking everything after the `. `. For normal
names (e.g., "Jalen Brunson"), takes the last space-separated token. Both normalize to lowercase.

---

## Prompt Signals and Their Reliability Status

| Signal shown to Analyst | Where in context | Reliability |
|--------------------------|-----------------|-------------|
| Tier hit rates (last 20g) | Stat line `overall=` | Primary — use as floor |
| vs_soft / vs_tough (full season) | Stat line | Secondary — confirm DvP direction |
| competitive / blowout_games splits | Stat line | Confirm spread risk |
| DvP [POS] rating | DvP line per player | Primary defense signal for PTS/AST |
| B2B rate at tier | Stat line `b2b=` | Primary on B2B nights |
| bb_lift / iron_floor | Stat line | Trust iron_floor; treat bb_lift as mild |
| [VOLATILE] / [consistent] | Stat line suffix | Directional (no confirmed backtest yet) |
| [FG_HOT] / [FG_COLD] | PTS stat line suffix | Shooting regression — ±8% threshold; unvalidated |
| trend (up/stable/down) | Stat line | Noise — data shown, no directive weight |
| BLOWOUT_RISK | Header flag | Confirmed directionally; −1 tier rule + secondary scorer skip at spread_abs ≥ 15 (favored team only — BLOWOUT_RISK=True means player's team is the heavily favored side). At spread_abs 8–14: confidence penalty only, no hard skip. H19 backtest applied 2026-03-22. |
| Without [X] (key_teammate_absent) | After Teammates line | Player's raw avgs + tier hit rates in games where top-PPG whitelisted teammate was absent (n≥3); directive — Pick stage uses absence baseline as primary evaluation when teammate is OUT/DOUBTFUL; added 2026-03-20. **Two-gate eligibility rule in `build_pick_prompt()` (added 2026-03-22):** Gate 1 — star must be confirmed OUT (not QUES/GTD) to use as primary qualifier; Gate 2 — n ≥ 10 games required to use as primary qualifier. Both gates must pass. n < 10 or QUES status → supplementary context only, fall back to standard baseline. **`absence_trend` override (added 2026-03-25):** When key teammate is absent and n_games ≥ 5, the top-level `trend=` value shown in the stat line is overridden with within-absence-window trend (L5 vs full absence-window mean). This means the `trend` label in the stat line reflects the player's trajectory WITHIN the without-star regime, not against an L20 baseline that includes higher-usage shared-lineup games — prevents false `trend=up` on a player declining within the absence window. |
| spread_abs > 13 | Header flag | Cap at 80% confidence (elite scorer exempt if raw_avgs PTS ≥ 27.0) |
| DENSE schedule | Header flag | −5–10% confidence |
| REB opp defense | DvP line REB field | Excluded — not a valid signal |
| 3PM opp defense | DvP line 3PM field | Excluded — noise (lift variance 0.053) |
| 3PM cold streak | Inferred from trend + recent logs | Decline signal (lift=0.87, n=161) |
| 3PM trend=down | Stat line `trend=down` | **Mandatory step-down rule** — pick one tier lower; skip if below T1 |
| PROJECTED LINEUPS | ## PROJECTED LINEUPS section | Ground truth for starter availability — treat as LINEUP CONTEXT backstop |
| PLAYOFF PICTURE | ## PLAYOFF PICTURE section | Situational awareness only — no hard rules |
| TEAM DEFENSIVE PROFILES | ## TEAM DEFENSIVE PROFILES section | Rolling 15g — replaces static file section |
| proj_min=N (player header) | Quant context block header | Rotowire projected minutes; contextual only — no directive rules; absent when Rotowire creds not present |
| [USG_SPIKE:+Npp vs X.Name] (player header) | Quant context block header | Usage boost ≥5pp with ≥100 min sample when named player absent; treat as contextual positive signal |
| ⚠ OPP: Name OUT (proj=0min) | After Momentum line | Opponent key player with 0 projected minutes per Rotowire; supports matchup assessment; absent when creds not present |
| Momentum — line | After DvP line | L10 W-L record + avg margin + hot/cold tag; annotation only — no directive rules; unbacktested; treat as situational awareness |
| DEF↑ / DEF↓ (player header) | Quant context block header | Opponent's L5 vs L15 PTS-allowed divergence ≥8%; annotation only — no directive rules; absent when neutral or <3 L5 games |
| [SHORT_SAMPLE:Ng] (player header) | Quant context block header | `games_available < 8` — player recently returned from injury or missed extended stretch; L10 floor unreliable; triggers RETURN FROM INJURY rule: mandatory REB/AST one-tier step-down, PTS -5% + no T20+ unless ≥7 games; absent when games_available ≥ 8; added 2026-03-16 |
| H2H vs OPP line (`h2h_splits`) | Indented line after player stat lines | Per-opponent tier hit rates from full season game log vs today's specific opponent; min H2H_MIN_GAMES=2 to render. Shows highest qualifying tier (≥70% rate) per stat: `H2H vs BOS (4g): PTS T20 3/4(75%) | AST T6 3/4(75%)`. **Annotation only — no directive rules.** Analyst reads as context and applies judgment on small samples. Samples are qualitatively different from noise in divisional/playoff matchups where the opponent is seen 4+ times with deliberate defensive attention. Added 2026-04-09. |
| PLAYOFF PROFILE line (`playoff_profile`) | Indented line after H2H annotation | Career playoff vs regular-season deltas for PTS/REB/AST/3PM/FG% from `data/playoff_career_log.csv` (2021–2025). Format: `PLAYOFF PROFILE (61g, 5 seasons): PTS +3.2 | REB +1.1 | AST -0.3 | 3PM +0.1 | FG% +1.3%`. `(small sample)` suffix when `playoff_games < 10`. **Date-gated to `PLAYOFFS_R1_DATE = "2026-04-18"` (inclusive) — no rendering during regular season or play-in.** Min PLAYOFF_MIN_GAMES=5 career playoff games to render. **Annotation only — no directive rules.** Added 2026-04-09. |

---

## Active Improvement Queue (as of March 25, 2026)

| ID | Name | Status | Files |
|----|------|--------|-------|
| Season Context 0 | Standings Snapshot | ✅ DONE (March 2026) | `espn_daily_ingest.py`, `analyst.py`, `auditor.py` |
| Season Context 1 | Auto-Generated Team Defense Narratives | ✅ DONE (March 2026) | `quant.py`, `analyst.py` |
| Season Context 2 | Staleness Detection in pre_game_reporter | ✅ DONE (March 8, 2026) | `pre_game_reporter.py` |
| Season Context 3 | Restructure SEASON FACTS into decay tiers | Manual edit — no code needed | `context/nba_season_context.md` |
| P3 | Shooting Efficiency Regression | ✅ DONE (March 7–8, 2026) | `espn_player_ingest.py`, `quant.py`, `analyst.py` |
| P4 | Tier-Walk Audit Trail | ✅ DONE (March 6, 2026) | — |
| P5 | Afternoon Lineup Update Agent | ✅ DONE (March 10, 2026) | `analyst.py`, `agents/lineup_update.py` (new), `injuries.yml`, `build_site.py`, `AGENTS.md` |
| P6 | Skip Validation System | ✅ DONE (March 11, 2026) | `analyst.py`, `auditor.py`, `analyst.yml`, `auditor.yml` |
| P7 | Team Momentum Indicator | ✅ DONE (March 11, 2026) | `quant.py`, `analyst.py` |
| #1 | Teammate Absence Delta | Deferred to next season (insufficient DNP sample) | `quant.py`, `analyst.py` |

**Next backtests to run:**
- **H9 — H2H Splits:** Run ~mid-April 2026
- **H14 — Elite Opposing Rebounder:** COMPLETE — NO_SIGNAL (Mar 22, 2026, n=1,709). thresh=10.0 delta=−0.5pp; H14b team REB flat. No rule change. CLOSED.
- **H15 — Opp Team Hit Rate:** COMPLETE third run (Mar 31, ≥600 picks). Suppressors confirmed: HOU (65.2%, n=23), PHX (75.0%, n=24), PHI (64.7%, n=17; game-script caveat). Amplifier confirmed: IND (100.0%, n=23). MIN×AST 63.6% n=11 — below ≥15 gate, monitor through playoffs. `nba_season_context.md` updated. No new prompt rules — context guidance only. CLOSED unless MIN×AST clears ≥15 picks.
- **H16 — 3PA Volume Gate:** Re-run at ~150+ 3PM picks (`--mode 3pa-volume-gate`). Currently 99/150 graded 3PM picks as of Mar 22 — ETA ~April 1–3.
- **H18 — Wembanyama Rim Deterrent:** CLOSED after research phase (Mar 22, 2026). Q2: miss players split 4 interior/balanced vs 2 perimeter; Miller (motivating case) classifies as PERIMETER (49.77% 3PA rate). Q3: opponents take +0.52 more 2PA vs SA (opposite of hypothesis). Pattern does not hold. No backtest warranted.
- **H17 — Spread Context:** COMPLETE second run (Mar 22, n=538) — NOISE confirmed. CLOSED. `--mode spread-context`. No rules warranted.
- **H20 — Losing-Side AST:** COMPLETE — NO_SIGNAL (Mar 22, 2026, n=54). `--mode losing-side-ast` implemented in `backtest.py`. Rule not shipped.
- **H21 — Miss Anatomy:** COMPLETE — NOISE (Mar 22, 2026). PTS delta 0.6pp, REB delta 2.0pp, AST delta 0.8pp — all below 4pp threshold. Rule NOT shipped. `near_miss_rate`/`blowup_rate` remain in quant for Player Profiles only.
- **H26 — Star Absence Teammate Impact:** IMPLEMENTED — **strong signal** (Apr 10, 2026, 2026-season data, 14 teams / 31 obs). `--mode star-absence` in `backtest.py`. Weighted-avg deltas: PTS_T15 +11.3pp, PTS_T20 +12.7pp, PTS_T25 +10.5pp, AST_T4 +9.6pp, AST_T6 +6.7pp — all cross +3pp SIGNAL threshold. Constants: `SA_MIN_STAR_GAMES=20`, `SA_MIN_ABSENT_GAMES=5`, `SA_MIN_CONDITION_N=3`. Output: `data/backtest_star_absence.json`. Motivating BOS case (Tatum +27.3pp T20, Pritchard +71.2pp T20 when Brown OUT) validated in data. Downstream consumers (skip re-evaluation in `lineup_update.py`, absence-driven analyst confidence rule) deferred pending multi-season verification and confirmed-OUT gate alignment with existing Without-Star Baseline rule.

**Confidence calibration check:** After 20+ audit days (~late March), compare per-band actual hit rates in `audit_summary.json` against stated confidence bands (70–75 / 76–80 / 81–85 / 86+).

---

## Files To Touch Most Often (by task type)

| Task | Files |
|------|-------|
| New quant signal | `quant.py` only (add function + wire in `build_player_stats()` + `main()`) |
| New prompt rule | `analyst.py` `build_prompt()` AND `build_pick_prompt()` — both functions contain the full rulebook and must be updated identically for any rule change |
| New context shown to analyst | `quant.py` `build_player_stats()` + `analyst.py` `build_quant_context()` |
| New pick output field | `analyst.py` OUTPUT FORMAT schema + `build_site.py` pick card renderer |
| Whitelist change | `playerprops/player_whitelist.csv` only (toggle `active` flag) |
| Season context update | `context/nba_season_context.md` only |
| Team defense narratives | `quant.py` `build_team_defense_narratives()` only |
| Backtest | `agents/backtest.py` — standalone, reads CSVs, writes JSON, no production impact |
| Frontend change | `build_site.py` only — triggers on next injury refresh or analyst run |
| Lineup update rules | `agents/lineup_update.py` `call_lineup_update()` system/user prompt; also `classify_absent_player()` role_tag thresholds for absence impact tuning |
| Daily picks review | `data/picks_review_YYYY-MM-DD.json` only — human-produced in Claude chat session, committed before auditor.yml runs |

---

## data/picks_review_YYYY-MM-DD.json

Human-produced daily picks review file. Written in Claude chat sessions each morning after reviewing the day's picks. Committed to the repo **before `auditor.yml` runs** so the auditor can join against it.

**Schema:** `[{date, player_name, team, prop_type, pick_value, verdict, trim_reasons}]`

**Verdicts:** `"keep"` (clean pick) | `"trim"` (marginally weak; exclude from parlay core) | `"manual_skip"` (should not have been filed; workflow/analyst error)

**Join key:** `(player_name.strip().lower(), prop_type, pick_value)` — date is for reference only (all entries should match yesterday's date when the auditor reads the file)

**Consumers:**
- `auditor.py` — `load_picks_review(YESTERDAY_STR)` + `apply_human_verdicts()` → writes `human_verdict` and `trim_reasons` fields to `picks.json`; `save_audit_summary()` rolls up `human_flag_precision` block tracking season hit/miss rates by verdict type
- `parlay.py` — `load_todays_picks_review()` (TODAY_STR) → excludes `manual_skip` picks from candidate pool; allows max 1 `trim` pick per card only with ≥2 clean anchors
- `build_site.py` — renders amber `⚠ Caution` badge on `trim` picks, red `⚠ Flagged` badge on `manual_skip` picks, with inline `trim_reasons`

**Graceful degradation:** All three consumers return no-op (empty/unchanged) when the file is absent for the date — no crash, no silent error.

**NOT automated** — `data/picks_review_YYYY-MM-DD.json` is never written by any agent. `analyst.py`, `quant.py`, `lineup_update.py`, `lineup_watch.py` do not read or write this file.

---

## What Has NOT Been Done (common assumption errors)

- **Cunningham/Ball misclassification fix is implemented but unverified in production** — injury_exit promotion in `post_game_reporter.py` and auditor STEP 2 fast path added 2026-03-18. Two paths: (1) `_INJURY_EXIT_TERMS` extended to catch "spasms", "ruled out for the remainder", "hard fall" etc. directly in `classify_from_news()`; (2) new promotion block: `event_type=="minutes_restriction"` AND `from_news=False` AND `inj_detected=True` AND `minutes < MINUTES_LOW_THRESHOLD` → promoted to `injury_exit`. Auditor: STEP 2 opens with FAST PATH — if `injury_exit` in POST-GAME NEWS CONTEXT, classify immediately as `injury_event` without game-script reasoning; `post_game_event="injury_exit"` annotation stamped on pick dicts in `build_audit_prompt()`. Confirm working after next low-minutes in-game injury exit is processed by the pipeline.
- **Positional DvP — H8 verdict: REVERT (March 12, 2026)** — backtest ran against full season game logs. Positional lift_advantage was negative for PTS (−0.051), REB (−0.052), and AST (−0.060). Team-level opp defense outperforms positional splits — position-splitting dilutes cell sizes and flattens signal, particularly for frontcourt REB (team-level lift 1.077 vs positional 0.980). 3PM was KEEP (+0.106) but 3PM DvP is already excluded from prompt rules. **Prompt cleanup is PENDING** — `DvP [POS]` line in `build_quant_context()` to be removed; implementation is annotation-only so no picks are at risk in the interim. Do not treat positional DvP as a validated signal.
- **Volatility scores have not been backtested** — σ thresholds (0.3/0.4) are reasonable priors
  but not empirically validated against this dataset. The auditor will accumulate evidence over
  time via the `reasoning` field (volatility flag instruction).
- **Shooting regression signal has not been backtested** — ±8% threshold is an untested prior;
  validate via audit accumulation after 30+ days of flagged picks. HOT misses clustering in
  `model_gap` confirms mechanism; `variance` clustering suggests penalty is overcorrecting.
- **Miss Anatomy analyst wiring is NOT done** — `near_miss_rate` and `blowup_rate` fields are
  live in `player_stats.json` and feeding Player Profiles conditional rendering. The directive
  prompt rule (confidence modifier or tier-drop on high `blowup_rate`) is explicitly NOT shipped
  until the backtest validates the signal.
- **`auditor.py` does NOT detect `lineup_update` picks** — picks that were re-reasoned by
  `lineup_update.py` are graded identically to morning picks. The audit log does not distinguish
  "revised up/down" from "original." A future P5 audit integration would tag these and evaluate
  whether re-reasoned picks outperform their morning originals. Revisit after ~20 days of
  amendment data accumulates.
- **`lineup_update.py` LLM cost** — each hourly run that detects changes will call Claude (one
  call, up to 2048 tokens). On a typical day with one morning absence detected, this adds ~$0.01.
  On a busy injury day with 3+ changes, cost is still negligible. Monitor if cost becomes visible.
- **`lineup_update.py` has no audit integration** — `lineup_update` sub-objects are written
  but the auditor's STEP 2 and miss classification do not yet check for them. A pick that was
  revised DOWN and then missed would still be classified via the normal root-cause logic without
  noting the amendment context. Acceptable for now.
- **`opportunity_flags.json` has no grading integration** — the auditor does NOT read `opportunity_flags.json`
  and does NOT compare suggestion tiers against actual game results. Implement after ~2 weeks of suggestion
  data accumulates to verify the surfacing mechanism is working before adding grading infrastructure.
  The follow-on design (Option 2 from spec) would log `opportunity_miss` events to `audit_log.json` when
  a surfaced suggestion would have hit at the suggested tier.
- **Skip Validation has no analyst prompt feedback loop yet** — `audit_summary.json["skip_validation"]`
  accumulates per-rule false skip rates, but the analyst prompt does NOT yet read or act on this
  data. A future enhancement would inject high-false-skip-rate rules into the analyst prompt
  (e.g., "skip rule X has 35% false skip rate — consider loosening"). Evaluate after ~20 days of
  skip data accumulates.
- **Season Context Improvement 3 (SEASON FACTS decay tier restructure) is NOT done** — this is
  a manual edit to `context/nba_season_context.md`. No code required. Do after verifying that
  Improvements 0–2 are running cleanly in production.
- **`team_defense_narratives.json` perimeter and pace clauses are NOT active** — the current
  `team_game_log.csv` schema lacks `fg3_pct_allowed` and `possessions` columns, so only the
  PPG rank line is generated. Perimeter/pace clauses will auto-activate when schema expands.
- **Staleness flags do NOT auto-update `context/nba_season_context.md`** — `detect_staleness_flags()`
  is read-only. Flags are surfaced for human action only — the analyst sees them as ⚠ warnings
  in `pre_game_news.json`, but no agent modifies the context file automatically.
- **`lineups_today.json` IS committed by `analyst.yml`** — added to the commit loop (alongside
  `picks.json`, `parlays.json`, `player_stats.json`) so that the `snapshot_at_analyst_run` key
  written by `write_analyst_snapshot()` persists into the hourly `injuries.yml` runs. Without
  this, `lineup_update.py` would always exit with "no snapshot found — skipping" because each
  hourly checkout started from a clean repo with no `lineups_today.json`. `injuries.yml` does
  NOT commit the file — it only reads and updates it in-place. The file is overwritten fresh
  each morning by the next day's analyst run, so it does not accumulate across days.
  Note: `injuries_today.json` remains uncommitted (refreshed every hourly run, no snapshot
  dependency) — the analogy to `lineups_today.json` is imperfect.
  Note: `write_lineups_json()` in `rotowire_injuries_only.py` now preserves `snapshot_at_analyst_run` across hourly rewrites — fixed March 12, 2026. Prior to this fix, every successful Rotowire parse overwrote the file without the snapshot key, causing `lineup_update.py` to always skip despite the key being committed.
- **`skipped_picks.json` is committed by BOTH `analyst.yml` AND `auditor.yml`** — `analyst.py`
  writes it fresh each morning (null grading fields). `auditor.py` reads it, calls `grade_skips()`
  to fill `would_have_hit` / `skip_verdict` / `skip_verdict_notes` / `actual_value`, then writes
  it back. Both commit steps include it. The auditor's graded version is what `save_audit_summary()`
  rolls up into `audit_summary.json` under `"skip_validation"`. `save_audit_report()` renders the
  per-day skip table in the markdown archive report. This file accumulates only the current day's
  skips (overwritten each morning) — it is not a historical archive.

---

## Workflow Implementation Lesson (from P5 post-mortem, March 2026)

**When implementing a feature that spans multiple workflows, explicitly audit every file the
feature reads at runtime and verify it will be present in each workflow's checkout.**

The P5 Lineup Update Agent (lineup_update.py) was implemented correctly in isolation — the
Python logic, snapshot writing, and diff computation all worked as designed. The blindspot was
`lineups_today.json`: analyst.py wrote it, lineup_update.py read it, but analyst.yml never
committed it. Every hourly injuries.yml run checked out a clean repo, found no
`lineups_today.json`, and silently skipped with "no snapshot found." The feature ran for
multiple days producing zero output with no visible error.

**Checklist for any future cross-workflow feature:**
1. List every file the new agent reads (not just writes).
2. For each file: which workflow writes it? which workflow reads it? are they the same job, or different jobs running at different times?
3. If different jobs: is the file committed between them? If not, the reader will always see a missing or stale file.
4. Check that all commit steps (`git add` loops) in relevant `.yml` files include the new file.
5. Verify the "no file found" graceful-skip log line is actually the expected no-op, not a silent failure masking a missing commit step.
