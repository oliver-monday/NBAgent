# NBAgent — Roadmap: Resolved Issues

All resolved bugs, completed improvements, and closed hypotheses. See `docs/ROADMAP_active.md` for the active queue and `docs/BACKTESTS.md` for backtest details.

---

| Issue | Fix Applied |
|-------|-------------|
| **ESPN Athlete News Fetch Removed from Reporters (2026-04-16)** | Removed ESPN athlete news endpoint (`common/v3/athletes/{id}/news`) from both `agents/post_game_reporter.py` and `agents/pre_game_reporter.py`. Endpoint had 100% failure rate since mid-April — bare `except Exception` handlers masked the actual HTTP error. Removed: `ESPN_NEWS_URL`/`ESPN_ATHLETE_NEWS_URL` constants, `PLAYER_DIM_CSV` path constants, `_norm_name()`, `load_athlete_id_map()`, `fetch_espn_news()`/`fetch_player_news()` functions. Post-game reporter: Step 1 (ESPN athlete news loop) replaced with empty-dict initializations; downstream code (`espn_news_headlines`, `espn_fetch_ok`, `fetch_errors`) continues to function via empty dicts. Pre-game reporter: Step 2 (per-player athlete fetch loop) replaced with league-news-only path; `write_output()` signature simplified (removed `no_id_errors`/`espn_errors`/`no_news_players` params). Remaining news sources cover all use cases: Brave Search + ESPN recaps (web scrape) + Rotowire (post-game); ESPN league news `site/v2` endpoint + staleness checks (pre-game). 165 lines removed across both files. Re-enable if ESPN fixes the endpoint. |
| **Standings Injection Paused During Playoffs (2026-04-16)** | Date-gated `render_playoff_picture()` in `agents/analyst.py` to return `""` when `TODAY_STR >= PLAYOFF_START_DATE`. Standings are frozen post-regular-season and redundant with `nba_season_context.md`. Saves ~200 prompt tokens per run. Fully reversible for next season. `agents/analyst.py` only. |
| **AST Iron-Floor Role-Purity Gatecheck + Override 1 Step-Down Extension + Play-In Context (2026-04-16)** | Three changes across two files. `agents/analyst.py`: (1) New IRON-FLOOR AST ROLE-PURITY CHECK rule added to both `build_prompt()` and `build_pick_prompt()` immediately after the IRON-FLOOR B2B ROAD GATE block. When iron_floor is true on AST but the player's `raw_avgs PTS > 3× raw_avgs AST`, the iron_floor designation is downgraded to `soft_floor` for AST confidence purposes (−5pp confidence, tier remains valid). Does NOT apply when `raw_avgs AST ≥ 5.0` (primary playmakers retain full protection). Targets the Edgecombe AST T2 miss pattern (1 AST in 42 min, iron_floor 80% confidence, play-in game — 19 PTS, 11 REB but zero playmaking). Tier_walk must document: `"soft_floor (PTS/AST ratio > 3×, −5pp confidence)"`. (2) Override 1 in `build_playoff_rule_modifications()` extended from confidence-cap-only to BOTH the 84% min_floor cap AND the mandatory one-tier step-down at T15+. Both are suspended when Rotowire `projected_minutes ≥ 30`. Play-in evidence cited: Bane (min_floor 17.9, played 40), Garland (23.4, played 31), Curry (25.0, played 36), Maxey (played 42). `context/nba_season_context.md`: play-in results updated (PHI 7-seed East, POR 7-seed West, MIA/LAC eliminated), R1 matchups confirmed (BOS vs PHI, SAS vs POR, 1v8 seeds TBD Apr 17), series diary headers updated, R1 injury context for BOS-PHI added (Embiid OUT, PHI suppressor note, Maxey 31pts play-in), SAS-POR header added (POR upset PHX, SAS floor compression), injury landscape timestamp updated to 2026-04-16. |
| **Rotowire Projected Minutes: Full Coverage Fix — 2026-04-15** | Root cause was an **auth field-name mismatch**, not a page-split (an initial hypothesis based on screenshots of the same page rendering differently for authenticated browsers vs the scraper). `ingest/rotowire_injuries_only.py` `login_rotowire()` sent `data={"username": email, "password": password}` to `https://www.rotowire.com/users/login.php`; the endpoint returned HTTP 200 but granted only a non-premium session cookie, silently gating roughly half the projected-minutes page. Proof: `agents/post_game_reporter.py` uses the same login URL with `data={"email": email, ...}` and authenticates successfully for premium news fetches in production (confirmed working 2026-04-13). Fix: `login_rotowire()` now POSTs with `"email"` as the primary field name, falling back to `"username"` if `email` fails HTTP 200. Between attempts, `session.cookies.clear()` prevents a failed first attempt from poisoning the second. Post-login log shows cookie count + names so production ops logs surface any future auth regression at a glance. **Upstream `parse_projected_minutes()` hardening retained** (added earlier this day when troubleshooting the same bug under the page-split hypothesis): (a) candidate ancestor must contain the literal text `"starters"` (case-insensitive); (b) each player's enclosing `<li>` must contain a digit; (c) post-parse validation requires ≥1 player with `minutes > 0` before accepting a team. These filters are generally-correct hardening even for the dedicated page alone and provide defense-in-depth against future DOM changes. **Earlier same-day workaround reverted:** the dual-parse merge in `main()` (parse both dedicated page and lineups page, merge results) is no longer needed — correctly-authenticated dedicated page serves ALL teams in a single response. `main()` now goes back to the simple single-page parse path, with a new `COVERAGE GAP` log line that surfaces any mismatch between the parsed lineups slate and the teams returned by the projected-minutes page (so the real blocker — if any — stays visible). `agents/rotowire_minutes_diag.py` rewritten as a three-phase investigator: Phase 1 inspects the login page's form fields, Phase 2 runs dual login attempts (email vs username) from fresh sessions and counts teams/players/panels/premium-gate markers on each resulting projected-minutes fetch, Phase 3 prints a side-by-side comparison table with a "VERDICT" line naming the winning field. Writes `data/minutes_diag.json` for archival. **Pre-existing parser bug noted (out-of-scope, unfixed):** the NavigableString section-header detection branch in `parse_projected_minutes()` is dead code (`hasattr(NavigableString, 'name')` returns True in bs4), so every player has always been tagged `section="STARTERS"` in production regardless of source panel — downstream consumers only read the `minutes` value so no downstream impact. Enables PLAYOFF RULE MODIFICATIONS Override 1 (min_floor 84% PTS cap suspension when `projected_minutes ≥ 30`) to fire for all players, not just the ~half previously covered. `ingest/rotowire_injuries_only.py`, `agents/rotowire_minutes_diag.py`. |
| **Playoff Rule Modifications (Layer 1) — 2026-04-15** | Added `build_playoff_rule_modifications()` to `agents/analyst.py` — date-gated to `PLAYOFF_START_DATE` (active for both play-in and playoff games). Three directive overrides active during postseason: (1) MIN_FLOOR confidence cap (PTS 84% when floor_minutes < 24) SUSPENDED when Rotowire `projected_minutes ≥ 30`; (2) VOLATILE confidence deduction reduced from -5% to -3% (iron_floor interaction, VOLATILE+AST T6 skip, weak-combo skip rules unchanged); (3) `team_momentum` tag (hot/cold/neutral) is INFORMATIONAL ONLY — not a confidence modifier. Rationale: regular-season calibration data includes managed rest, DNPs, and low-stakes contests that don't exist in playoffs — the 84% cap understates actual playoff floor, the VOLATILE metric inflates from load-management variance, and regular-season momentum reflects schedule/rest artifacts not predictive of postseason. Block injected in both Scout and Pick prompts directly after `{build_playoff_blowout_override(TODAY_STR)}` and before `KEY RULES — VOLATILITY:`. `build_playoff_context()` "What does NOT change" line updated to remove VOLATILE penalties (no longer accurate) and to forward-reference PLAYOFF RULE MODIFICATIONS. Date-gate verified for 6 dates (pre-gate returns empty, gate day + later returns 2,435-char block). `agents/analyst.py` only. |
| **Pre-series intel for locked R1 matchups (2026-04-13)** | Pre-filled PLAYOFF SERIES DIARIES with pre-series intel for 4 locked R1 matchups: NYK-ATL (ATL hottest team, Brunson-Towns PnR vs ATL switching), CLE-TOR (post-Harden CLE is different team, TOR pace vs CLE set defense, Quickley hamstring variable), DEN-MIN (rivalry series, Jokic-Murray engine, Edwards health update, MIN×AST suppressor context), LAL-HOU (catastrophic injury mismatch, dual H15 suppressors, HOU league-best REB%). Sources: ESPN, NBA.com, The Athletic, CBS Sports, SI. `context/nba_season_context.md` only. |
| **L5/L10 window caution in playoff context (2026-04-13)** | Added paragraph to `build_playoff_context()` in `agents/analyst.py` warning that end-of-season rest patterns contaminate L5 trend signals for early playoff games. Stars sat 1-3 of last 5 games or played reduced minutes in meaningless contests — L5 averages and trend tags are unreliable for first 2-3 playoff games. Instructs analyst to weight L20 tier hit rates and full-season profiles over L5 indicators until L5 window refills with playoff-intensity data (~Game 3). `agents/analyst.py` only — prompt text. |
| **Playoff intelligence: Injury landscape + player adjustments + profiles refinement (2026-04-13)** | Three-file change. (1) `context/nba_season_context.md`: new `### PLAYOFF INJURY LANDSCAPE` section between bracket and series diaries — confirmed OUT players (Doncic hamstring, Reaves oblique, Embiid appendectomy), active injury concerns (Edwards knee, McDaniels knee, Quickley hamstring, Cunningham lung, Wemby rib, Curry knee, Gordon calf), R1 matchup injury context. (2) `agents/analyst.py`: `build_playoff_adjustments()` updated — Luka/Reaves/Embiid annotated as ⚠ OUT with DO NOT PICK directives; Edwards annotated with knee concern; 3 new entries added (Cade ramp-up, LeBron load risk, Wemby rib caveat). (3) `agents/injury_profiles.py`: stripped noisy auto-classifier (was 49/68 ELEVATED) — removed `classify_risk_tier()` function, `elevated_reasons`, `risk_tier` field, risk threshold constants. Now outputs `current_status` (OUT/DOUBTFUL/QUESTIONABLE/ACTIVE) from injury overlay only. `status_summary` replaces `tier_summary` in output. Clean data provider, not classifier. |
| **BRAVE_API_KEY added to auditor.yml (2026-04-13)** | Added `BRAVE_API_KEY: ${{ secrets.BRAVE_API_KEY }}` to the Post-Game Reporter step env block in `.github/workflows/auditor.yml`. Enables the newly implemented `fetch_brave_injury_search()` to access the Brave Web Search API in production. Without this, Brave search silently skipped (graceful degradation) and the 4/13 injury detection improvement was inert. `.github/workflows/auditor.yml` only — one line. |
| **New agent: Injury & Availability Profiles (2026-04-13)** | New `agents/injury_profiles.py` — pure Python agent computing per-player availability %, absence streaks/recency, minutes trends, B2B sit patterns, and risk tier classification (OUT/ELEVATED/MANAGED/CLEAR) overlaid with current `injuries_today.json`. Reads `player_game_log.csv`, `nba_master.csv`, `player_whitelist.csv`. Risk tiers: OUT (injury report), ELEVATED (<85% availability, recent absences, minutes declining, GTD status), MANAGED (>50% B2B sit rate), CLEAR (no concerns). Runs in `analyst.yml` after `playoff_matchup.py`. Output: `data/injury_profiles.json`. First run: 68 players, 49 ELEVATED (end-of-season rest patterns), 19 CLEAR, 0 OUT/MANAGED. Downstream consumer: future `## PLAYOFF INJURY LANDSCAPE` analyst context block (separate task). `agents/injury_profiles.py` (NEW), `.github/workflows/analyst.yml`. |
| **Post-Game Reporter: Brave injury search + likely-miss detection fix (2026-04-13)** | Added Brave Web Search as fourth source in `agents/post_game_reporter.py` — `fetch_brave_injury_search()` queries Brave API for `"{player} NBA injury"` on likely-miss players. New `compute_likely_misses()` replaces broken `load_yesterdays_missed_pick_names()` (which always returned empty set because reporter runs before auditor grades picks — `result` is always `None` at runtime). `compute_likely_misses()` compares box score actuals to pick thresholds pre-grading. Brave results injected as `BRAVE INJURY SEARCH:` block in Claude context alongside ESPN/Rotowire. System prompt updated to mention Brave as a source. Would have caught Quickley's hamstring exit on 4/12. `agents/post_game_reporter.py` only. |
| **Quickley miss reclassification + Bane root cause amendment (2026-04-13)** | Quickley 4/12 PTS T10 miss reclassified `model_gap_rule` → `injury_event` (hamstring exit Q2, did not return). Bane root cause amended to note season-finale organizational load management pattern (rested 2nd half vs CHI prior game, confirming deliberate minutes taper). Recommendations #0 and #3 updated. `audit_summary.json` miss_classification_totals corrected: model_gap_rule 35→34, injury_event 2→3. `data/audit_log.json` + `data/audit_summary.json` only. |
| **H15 opponent scheme notes updated in nba_season_context.md (2026-04-12)** | Full rewrite of OPPONENT SCHEME NOTES section with H15 fourth-run findings (815 picks). PHI confirmed suppressor (65.0%, −22.1pp, upgraded from "under review"). LAL new suppressor (76.5%, −10.7pp). PHX closed (82.4%, regressed). MIN×AST confirmed (75.0%, n=16, crossed ≥15 gate). DET×AST added as watch (50.0%, n=8). Every entry updated with PLAYOFF RELEVANCE tags for R1 matchup context. `context/nba_season_context.md` only. |
| **P2.3: Same-game parlay variance penalty (2026-04-12)** | `score_combination()` in `agents/parlay.py` now penalizes game-script concentration: `GAME_CONCENTRATION_PENALTY = -0.05` (probability scale, ×10 in composite) per leg beyond 2 from the same game. A 4-leg parlay with 3 same-game legs gets -0.5 composite penalty; 4 same-game legs gets -1.0. `concentration_penalty` field added to scored combination dict. Pushes toward multi-game diversification without blocking same-game stacks. Uses existing `games_set` computation loop — extended with `game_leg_counts` dict. `agents/parlay.py` only. |
| **P1.4: Game-in-series confidence modifier — H31 activation (2026-04-12)** | `game_in_series` and `series_phase` fields added to `agents/playoff_matchup.py` output — computed as `len(game_results) + 1` with phase labels (early G1-2, mid G3-4, late G5+). `format_series_block()` header shows `GAME N (PHASE)`. New `build_series_progression_rules(today_str)` in `agents/analyst.py` — date-gated to `PLAYOFFS_R1_DATE` AND `game_in_series >= 5`. Reads H31 `backtest_series_progression.json` for LATE_FADER/LATE_RISER flags. Renders per-player PTS confidence adjustments: LATE_FADER -5% (Harden, Curry, Edwards, etc.), LATE_RISER +3% (SGA, Durant, Kawhi, etc.). STABLE players: no adjustment. Wired into all 3 prompt builders via section variables (build_prompt, build_scout_prompt) and inline call (build_pick_prompt). Inert during Games 1-4 — activates automatically at Game 5. `agents/playoff_matchup.py` + `agents/analyst.py`. |
| **H15 fourth run + H16 rerun + H24/H25 new backtests (2026-04-12)** | **H15 (815 picks):** PHI (65.0%, n=20), HOU (65.2%, n=23), LAL (76.5%, n=34, NEW suppressor) confirmed. IND amplifier holds at 100% (n=38). PHX dropped below suppressor threshold (82.3%, n=34). MIN×AST at 75.0% (n=16) — crossed ≥15 gate but delta is −12.2pp (suppressor-level but prop-specific only). DET×AST at 50.0% (n=8) — new watch item. **H16 (132 3PM picks):** Insufficient data for volume gate verdict — low-volume bucket has n=2 at T1, n=0 at T2. High-vol/high-% hits at 88.7% vs high-vol/low-% at 78.0% (10.7pp gap, below 15pp actionable threshold). H16c mechanism validated: hit rate rises monotonically from 86.9% (≥1 3PA) to 95.0% (≥5 3PA). **H24 (134 picks with market data):** INSUFFICIENT DATA — only odds-enriched picks from 3/31-4/10 qualify (11 days). Zero picks in `market_much_lower` bucket (gap ≤ -15pp). The system's under-confidence means market almost always AGREES more than the system states — market gaps are overwhelmingly positive. Rerun after playoffs when 200+ odds-enriched picks exist. **H25 (324 reviewed picks):** PARADOX CONFIRMED — trim picks hit at 92.4% vs keep at 88.5% (+3.9pp). Triple flag (trim + conf ≤75 + VOLATILE) hits at 92.9% (n=56). Iron_floor trims: 100% (n=21). The review agent's trim signal is anti-predictive — it flags the system's safest picks. Trim badge removal (4/9) was correct. No escalation rule warranted. `agents/backtest.py` — 2 new modes (`market-disagreement`, `trim-escalation`), 4 runs complete. |
| **Lineup update injury field name fix + mobile movement-line overflow (2026-04-12)** | `compute_lineup_diff()` in `agents/lineup_update.py` used `row["player_name"]` but `injuries_today.json` uses `"name"` — `injury_map` was always empty, causing all changes to be classified as `starter_replaced` instead of `new_absence`, which prevented opportunity flag generation. Same class of bug as the `excluded_players` fix from earlier the same day. Fixed: `row["player_name"]` → `row["name"]` (2 occurrences). Also removed `white-space: nowrap` from `.movement-line` CSS in `agents/build_site.py` — movement indicator text was overflowing pick cards on mobile screens. |
| **P1.5: Playoff calibration early warning (2026-04-12)** | `_compute_playoff_calibration()` added to `agents/auditor.py`, called from `save_audit_summary()` between summary dict construction and file write. Compares playoff-only confidence band hit rates (picks with `date >= PLAYOFFS_R1_DATE`, non-voided, HIT/MISS) to regular-season baselines from `confidence_calibration_totals`. Warns (stdout `[PLAYOFF CALIBRATION WARNING]` + `recent_recommendations` in `audit_summary.json`) when any band diverges ≥`PLAYOFF_CAL_DIVERGE_PP` (10pp) with ≥5 picks. Minimum `PLAYOFF_CAL_MIN_PICKS` (15) graded playoff picks before check activates. Writes `playoff_calibration` key to `audit_summary.json` with per-band breakdown (`picks`, `hits`, `hit_rate_pct`, `regular_season_pct`, `divergence_pp`) + `_overall` sub-key. Purely diagnostic — no automatic rule changes. `agents/auditor.py` only. |
| **P1.2: Playoff blowout rule relaxation (2026-04-12)** | `build_playoff_blowout_override()` added to `agents/analyst.py`, date-gated to `>= PLAYOFFS_R1_DATE` (Apr 18). Raises all `spread_abs` thresholds by +3 points: secondary scorer skip at ≥16.5, 74% PTS cap at ≥15, elite exemption expiry at ≥18, 3PM extreme hard skip at ≥22. Reduces BLOWOUT penalty from −10% to −5%. Iron_floor fully neutralizes playoff blowout penalty. Injected into `build_prompt()` and `build_pick_prompt()` between SPREAD/BLOWOUT RISK and VOLATILITY sections via f-string call. Returns empty during regular season and play-in. `build_scout_prompt()` untouched (no rules). `agents/analyst.py` only — prompt text addition, no Python logic changes. |
| **P1.1: Per-player calibration wiring — H29 → odds layer (2026-04-12)** | `_get_calibrated_prob()` in `ingest/odds_today.py` now checks per-player actual hit rate from `data/backtest_confidence_calibration.json` (H29 output) before falling back to population calibration bands. Priority: (1) per-player rate if ≥10 graded picks, (2) population band from `audit_summary.json`, (3) stated confidence fallback. New `_load_per_player_calibration()` loader reads H29 JSON into `{norm_name: hit_rate}` dict (31 players qualified). `compute_edge()` passes `player_name` and `per_player` dict to the calibration function. New `calibration_source` field added to `bet_recommendation` sub-object: `"per_player"` / `"population_band"` / `"fallback"`. Log line shows source in parentheses. Directly improves Kelly sizing — Banchero goes from 85.1% (band) to 95.7% (per-player), Mitchell from 88.3% (band) to 65.2% (per-player). Graceful fallback when H29 file missing. `ingest/odds_today.py` only. |
| **Builder voided index fix (2026-04-12)** | `_idx` in `renderBuilder()` was set to the filtered-array index `i` instead of the original `DATA.today_picks` index, causing the wrong player to appear in the parlay card when voided picks preceded the clicked pick. Fix: tag `_origIdx` before filtering, use it as `_idx`. `agents/build_site.py` only — 2 lines. |
| **Opportunity flags OUT player exclusion fix (2026-04-12)** | Fixed `build_opportunity_suggestions()` in `agents/lineup_update.py`: `excluded_players` was always empty due to two bugs — (1) wrong field name (`row.get("player_name")` vs Rotowire's `"name"` field) and (2) Rotowire abbreviated name format (`"J. Johnson"`) never matching full names from player_stats. Now built from two sources: voided picks (full names from lineup_watch, primary) and raw injuries (Rotowire `"name"` field, secondary). `agents/lineup_update.py` only. |
| **Node.js 20→24 Workflow Migration (2026-04-12)** | Bumped `actions/checkout` v4→v6 and `actions/setup-python` v5→v6 across all 8 workflow files (analyst, auditor, build_site, diag, ingest, injuries, odds, odds_pretip). Eliminates Node.js 20 deprecation warning for these two actions. `upload-pages-artifact@v3` and `deploy-pages@v4` unchanged — no Node 24 versions released upstream yet; residual warning from `upload-pages-artifact`'s internal dependency on `upload-artifact@v4` is a known upstream issue. `.github/workflows/*.yml` only — no agent code, no data files. |
| **Parlay Builder UI + Spec 3 Cannibalization Badges (2026-04-12)** | `agents/build_site.py` only. **Spec 3: H33 cannibalization badges** — inline ⊖/⊕ badges between consecutive same-team same-stat parlay legs in `renderParlays()`, color-coded amber (cannibalization) or teal (synergy) with hover tooltip showing cannib_idx. Only MODERATE+ signals shown (weak pairs excluded at build time). **Custom Parlay Builder** — new interactive builder UI below system parlays on the Parlays tab. Click-to-add/remove legs from today's picks, grouped by game. Live-computed: combined probability, American odds, $10 payout, net calibrated edge. Inline H33 cannibalization warnings (⊖/⊕ with pp index) and Pearson correlation badges for same-team pairs. Copy-to-clipboard produces a formatted text summary. Pure client-side — no API calls, no runtime dependencies. **New Python functions:** `build_cannib_lookup()` reads `data/backtest_teammate_cannibalization.json` into a slim `{sorted_names|STAT: {idx, label}}` dict (55 MODERATE+ pairs). `build_corr_lookup(today_picks)` reads `teammate_correlations` from `player_stats.json` for today's players only (160 entries). Both injected as `cannib_lookup` and `corr_lookup` in the JS `DATA` object. **New CSS:** `.cannib-badge` (neg/pos variants), full `.builder-*` class suite (section, layout, pick rows, card, leg rows, stats, warnings, actions, responsive grid). **New JS:** `renderBuilder()`, `toggleBuilderLeg()`, `clearBuilder()`, `copyBuilder()` — state managed via `window._builderState` Set. Builder re-renders on every toggle for reactive UI. Not touched: `renderParlays()` structure (only the `forEach` loop gained `legIdx` and the badge insertion), all Python data prep functions, all other agents, all workflows. `agents/build_site.py` only. |
| **Parlay Track 1: Variety, Bold Card, Expanded Candidates (2026-04-12)** | `agents/parlay.py` only. `TARGET_MAX_ODDS` raised 600→900 (opens 4–5 leg ambitious plays). `TOP_N_TO_CLAUDE` raised 15→25 (broader candidate pool). Leg-count diversity enforcement added to `build_candidates()`: backfills ≥3 candidates with 4+ legs and ≥2 candidates with exactly 2 legs from the remaining pool when the top-N under-represents those counts. Prompt rewritten: `## YOUR TASK` gains MANDATORY VARIETY RULE (1× 2-leg + 1× 4+ leg minimum) and BOLD CARD concept (exactly one per day, `card_type: "bold"`, target +400–800, 4–5 legs, may include one speculative 70–74% leg with STRONG edge). `## SELECTION CRITERIA` updated: item 1 split core/bold odds targets, item 2 split core/bold floor confidence, item 5 mandates ≥2 different leg counts, item 6 emphasizes edge compounding. `## AVOID` extended with anti-monotony and anti-weak-bold bullets. `## OUTPUT FORMAT` gains `card_type` field ("core" or "bold"). Not touched: `score_combination()`, `CORR_BONUS`, `enforce_concentration_cap()`, `call_parlay_agent()`, `save_parlays()`, all other agents, all workflows. |
| **Spec 5: Skip Re-evaluation Path C — Cannibalization Release (2026-04-11)** | Path C added to `build_skip_reconsiderations()` in `agents/lineup_update.py`: when a skipped player was STRONGLY cannibalized by the absent star (H33 cannib_idx < −15pp), the skip is reconsidered even if Paths A (without-star rate ≥70%) and B (population delta positive) both fail. The −15pp threshold is stricter than the −8pp used in Specs 2/4 because skip reconsideration makes a stronger claim and needs higher-confidence evidence. Fires only as fallback — `if not reconsider:` gate ensures Path C never overrides A or B. Reuses `_load_cannib_data()` from Spec 4. Reasoning string: `"H33 cannibalization release: PTS was suppressed -X.Xpp by [star] → ceiling freed"`. PERSONAL_DRAG_WARNING guard (from Part 2 of H26) still fires first and prevents reconsideration for drag players. `agents/lineup_update.py` only — single insertion between Path B and the final continue gate. |
| **Spec 4: Opportunity Flags Cannibalization Priority (2026-04-11)** | Teammates cannibalized by absent player (H33 cannib_idx < −8pp) flagged as `priority: "high"` in opportunity_flags.json. New fields on suggestion cards: `cannib_freed` (bool), `cannib_idx` (float), `cannib_detail` (string). Suggestions sorted by priority (high first, then alphabetical) before save. Reasoning string includes `H33: PTS was cannibalized -X.Xpp by [player] → freed ceiling`. Teammate side only — opponent-side suggestions unaffected. Module-level `_load_cannib_data()` cached loader reading `data/backtest_teammate_cannibalization.json`. Debug logging shows ★ HIGH + H33 detail for cannibalized teammates. Graceful no-op when H33 file missing. `agents/lineup_update.py` only — `build_opportunity_suggestions()` enrichment + sort + debug logging. Not touched: `save_opportunity_flags()`, `compute_lineup_diff()`, `build_skip_reconsiderations()`, dedup logic, all other agents, all workflows. |
| **Spec 2: Auditor Cannibalization-Aware Parlay Grading (2026-04-11)** | `cannibalization_flags` field added to graded parlays in `grade_parlays()`: detects same-team, same-stat missed leg pairs with H33 cannib_idx < −8pp. Per-flag: `{player_a, player_b, stat, cannib_idx, label, note}`. Audit prompt item 6 (PARLAY MISS analysis) updated: LLM checks `cannibalization_flags` and references specific pair names + cannib index in `parlay_lessons` for Parlay agent feedback loop. Module-level `_load_cannib_data()` cached loader reading `data/backtest_teammate_cannibalization.json`. Graceful no-op when file missing. Completes the H33 learning loop: parlay agent blocks/penalizes pairs → auditor detects when MODERATE pairs slip through and fail → parlay_lessons teaches the parlay agent to avoid that construction → next morning's parlay run reads the lesson. `agents/auditor.py` only. |
| **Spec 1: H26 × H33 Star-Absence Cannibalization Mechanism (2026-04-11)** | `cannib_mechanism` field added to `star_absence_lift` in `player_stats.json`: per-stat `{cannib_idx, label}` from H33 data, looked up in `compute_star_absence_deltas()` via new `_load_cannib_data()` module-level cached loader reading `data/backtest_teammate_cannibalization.json`. "H33 mechanism" annotation line added to `STAR_ABSENT_LIFT` block in `build_quant_context()`: "was cannibalized → freed ceiling" / "independent → minimal change" / "had synergy → may decline without" with pp index. Prompt rule guidance added to WITHOUT-STAR BASELINE / TEAMMATE ABSENCE USAGE ABSORPTION sections in both `build_prompt()` and `build_pick_prompt()` explaining how to weight the mechanism (freed ceiling = expect higher-than-population lift; independent = expect lower; may decline = skepticism on lift). Annotation-only — no directive rules. `agents/quant.py` + `agents/analyst.py`. |
| **H33 Parlay Agent Integration: Teammate Cannibalization Context (2026-04-11)** | STRONG PTS/AST cannibalization pairs mechanically filtered from parlay candidates in `build_candidates()` (same pattern as `scoring_rivals`). MODERATE pairs get `CORR_BONUS` penalty (−0.06), SYNERGY pairs get boost (+0.08). `get_correlation_tag()` upgraded: same-stat same-team pairs check H33 data and override Pearson r when tier-level signal exists. `build_cannibalization_context()` generates `## SAME-TEAM SCORING CANNIBALIZATION` prompt block for today's relevant pairs (⊖ NEVER CO-PICK / ⊖ CAUTION / ⊕ SYNERGY per pair). `score_combination()` dominant correlation type extended with new tags. All data from `data/backtest_teammate_cannibalization.json` (H33 output). Module-level `_cannib_cache` loaded once per run. Graceful no-op when file missing. `agents/parlay.py` only. |
| **Ingest Team Abbreviation Normalization (2026-04-11)** | ESPN-style abbreviations (SA, NY, GS, UTAH, NO, WSH) normalized to standard NBA (SAS, NYK, GSW, UTA, NOP, WAS) at ingest write time. `ingest/espn_player_ingest.py`: added `_ABBR_NORM` map + `_norm_team()` helper, applied at 3 ESPN abbreviation extraction points (`infer_game_meta_from_summary` team_map, `parse_boxscore_players` team loop, `parse_boxscore_teams` team loop) so all downstream rows (`player_game_log.csv`, `team_game_log.csv`, `player_dim.csv`, `playoff_career_log.csv` via dual-write) get standard abbrevs. `ingest/espn_daily_ingest.py`: applied existing `to_roto_code()` (which already had the same map as `ESPN_TO_ROTO`) to `home_team_abbrev` and `away_team_abbrev` in the `nba_master.csv` row builder — previously only used for injury lookups and standings, not the actual CSV output. One-time backfill of all 5 existing CSV data files: `player_game_log.csv` (3,747 values), `team_game_log.csv` (978), `nba_master.csv` (489), `player_dim.csv` (48), `playoff_career_log.csv` (0 — already clean from backfill script's `to_roto_code()`). Post-backfill verification confirms zero ESPN abbreviations remain across all files. Eliminates the root cause of recurring silent player drops: `backtest.py` (10 players, discovered 4/11), `pre_game_reporter.py` (10 players, discovered 4/10), and originally the quant pipeline (10 players, fixed 3/25). The `team_abbr_alt` column in `player_whitelist.csv` is now a legacy field — no downstream consumer needs it, though existing alt-tuple `load_whitelist()` implementations in `quant.py`, `analyst.py`, and `backtest.py` are harmlessly redundant. H33 backtest re-run confirms SAS and NYK now appear with standard abbreviations (16 teams, 83 PTS pairs, 81 AST pairs). `ingest/espn_player_ingest.py`, `ingest/espn_daily_ingest.py`, 5 data CSV files (one-time backfill). |
| **Backtest: H33 teammate scoring cannibalization (2026-04-11)** | New `--mode teammate-cannibalization` in `agents/backtest.py`. Pairwise conditional tier hit rates between teammates — A's tier hit rate on nights when B hits vs misses B's best tier. Asymmetric cannibalization index. PTS + AST. League-wide. 14 teams, 68 PTS pairs, 58 AST pairs. Population PTS mean −0.6pp (centered). Key: HOU/LAL/DEN CANNIBALIZATION_CLUSTER, PHI SYNERGY_CLUSTER (Maxey/Embiid PTS cannibalize but Edgecombe synergizes), MIN INDEPENDENT. Parlay warning pairs output per team. SAS/NYK/GSW absent due to pre-existing backtest whitelist loader not emitting `team_abbr_alt` tuples — separate fix needed. `agents/backtest.py` only. |
| **Odds Layer 3: Frontend Line Movement Indicators (2026-04-11)** | New visual indicator on pick cards surfacing whether FanDuel's implied OVER probability has moved toward or away from each pick since the morning odds snapshot. All data fields (`morning_implied_prob`, `market_implied_prob`, `bet_recommendation.calibrated_prob`) were already being written onto each pick by `ingest/odds_today.py` `main()` (morning) and `pretip_sweep()` (hourly re-fetch) — this task makes them visible on the frontend without any Python, agent, workflow, or ingest changes. Card-face indicator for >3pp moves ("Market agrees ↑" teal or "Market disagrees ↓" amber with prob shift and edge strikethrough); odds drawer shows full detail for all >0.5pp moves. Scope: `agents/build_site.py` only. **Four edits inside `generate_html()`:** (1) **CSS block** after the existing `.edge-line` classes adds `.movement-line` (font-size 10px, margin-top 6px, nowrap, line-height 1.5), `.movement-line.agrees` (teal `#2dd4bf` matching existing POSITIVE edge tier color), `.movement-line.disagrees` (amber `#FF9800` matching existing opportunity-card "no edge" color), `.movement-line.significant` (font-weight 600), `.movement-line .edge-shift` (font-size 9px, opacity 0.8 for the secondary edge-shift span), `.movement-line .edge-shift s` (text-decoration-color inherit + opacity 0.6 for the strikethrough effect), and `.movement-drawer` (font-size 10px, muted color, monospace, line-height 1.6, opacity 0.75 — matches existing `.odds-sizing-body` aesthetic). (2) **New JS function `buildMovementLine(p)`** inserted immediately after `buildEdgeLine()` and before `displayConf()`. Returns empty string on three no-op conditions: `p.voided` (voided picks get no movement indicator regardless of data), `morning_implied_prob == null`, `market_implied_prob == null`. Computes `delta = current - morning` and absolute delta; returns empty string when `absDelta < 3.0` (dead zone for card face — smaller moves go to drawer only). For >3pp moves: `agrees = delta > 0` (market implied probability UP = market agrees with our OVER position), arrow `↑` or `↓`, label "Market agrees" or "Market disagrees", class `agrees` or `disagrees`. Prob shift rendered as rounded integers for compactness (`62→68%`). Edge shift computed from `bet_recommendation.calibrated_prob` when available: `morningEdge = calProb - morning`, `currentEdge = calProb - current`, rendered as a secondary `<span class="edge-shift">edge <s>+X.Xpp</s> → +Y.Ypp</span>` where the strikethrough `<s>` tag marks the stale morning edge. Final return: `<div class="movement-line significant {cls}">{label} {arrow}&nbsp; {probStr}{edgeStr}</div>`. (3) **`buildOddsSizing(p)` drawer extension** — new block inserted AFTER the existing `kelly_quarter` line and BEFORE the `alt_tiers` alt-tier rendering. Shows movement detail for ALL moves >0.5pp (including the 0.5–3pp range that doesn't render on card face). Uses underscore-prefixed local variables (`_morning`, `_current`, `_delta`, `_absDelta`, `_dir`, `_color`, `_mEdge`, `_cEdge`) to avoid any collision with enclosing scope. Gate: `_morning != null && _current != null && _absDelta >= 0.5`. Appends 3 lines unconditionally (`── Line Movement ──` header, directional line with inline color style, morning prob, current prob) plus 2 more conditional lines when `br.calibrated_prob != null` (morning edge, current edge). Directional line uses inline `style="color:{_color}"` so the accent stays consistent with the card-face indicator without needing another CSS class. (4) **Pick card template wiring** — added `${{buildMovementLine(p)}}` line between the existing `${{(function() {{ ... }})()}}` lineup update IIFE and the `${{buildOddsSizing(p)}}` drawer call. Placement is intentional: the movement indicator sits below the lineup-update amendment badge (high-priority contextual alerts) and above the odds drawer toggle (click-to-expand detail), so significant line movement is immediately visible at card-face level before any user interaction. **Thresholds:** card-face ±3pp (significant moves), drawer ±0.5pp (all meaningful moves). The ±3pp card-face threshold is calibrated to avoid noise — FanDuel alternate lines typically tick 0.5–2pp between morning and pretip even with no news; only moves ≥3pp indicate genuine sharp-money repricing or injury-driven shifts. The ±0.5pp drawer threshold captures everything above rounding noise for the user who wants full detail. **Semantic design:** "Market agrees" (teal ↑) means the market's OVER probability moved UP, which agrees with the analyst's OVER pick — but paradoxically the *edge* shrinks because the market is now closer to the system's calibrated probability. "Market disagrees" (amber ↓) means the market's OVER probability moved DOWN, disagreeing with our OVER pick — but the *edge* grows (assuming calibrated_prob is stable). The edge-shift strikethrough visualization makes the dual nature explicit: `edge +5.2 → +2.1pp` (strikethrough on +5.2) when the market agrees and compresses the edge, or `edge -3.1 → +0.8pp` when the market disagrees and widens into our favor. **Voided picks excluded** — even if `morning_implied_prob` and `market_implied_prob` are both populated, `p.voided` short-circuits the function to empty string. **Graceful degradation** — if either prob field is null (pretip didn't run, or pick wasn't matched in morning odds), the function returns empty string and the card renders exactly as before. Same for the drawer block. **Verification (8-point matrix, all pass):** Python `ast.parse` clean on `agents/build_site.py` (pre-existing `\d` SyntaxWarnings unrelated); `function buildMovementLine` count = 1 (definition); `movement-line` count = 7 (CSS + JS class references, well above the ≥4 requirement); `buildMovementLine(p)` count = 2 (function definition line + pick card template call site); `Line Movement` count = 1 (drawer section header); `absDelta < 3.0` = 1 (card-face threshold in `buildMovementLine`); `_absDelta >= 0.5` = 1 (drawer threshold in `buildOddsSizing`); `p.voided) return` ≥1 (voided guard in `buildMovementLine`). **End-to-end build test:** `python3 agents/build_site.py` ran cleanly and produced `site/index.html` (0 today's picks for 4/11 is expected — analyst hasn't run yet today, and no pretip data has been written to any pick for 4/11). **Untouched:** `buildEdgeLine()` function (unchanged, still shows current-edge badge based on `bet_recommendation.recommendation_tier`), `buildOddsSizing()` existing lines up through `kelly_quarter` (only the new block after kelly_quarter was added), all other JS functions (`displayConf`, `buildMicroStats`, `buildHitRate`, `toggleOddsSizing`, `toggleTierWalk`, `toggleLineupUpdate`, `renderPicks`, `renderBestBets`, `renderTopPicks`, `renderParlays`, `renderResults`, `renderAudit`, `renderResearch`, `renderPlayoffProfiles`, `renderPlayoffExplorer`, `renderCurrentSeasonExplorer`), Python code in `build_site.py` (all `build_*` data preparation functions, `get_top_picks`, `load_*` loaders, `main()` / `build_site()` entry point), `agents/analyst.py`, `agents/auditor.py`, `agents/quant.py`, `agents/parlay.py`, `agents/lineup_watch.py`, `agents/lineup_update.py`, `ingest/odds_today.py`, all `.yml` workflow files, `picks.json` schema (purely a frontend-visibility change reading fields that were already present). `agents/build_site.py` only. |
| **Prompt Text Audit: stale player stat references fixed (2026-04-11)** | Audited all named-player stat claims in `agents/analyst.py` prompt string literals inside `build_prompt()` and `build_pick_prompt()` against current-season ground truth extracted from `player_game_log.csv`. Scout prompt untouched (rules-free by design — no stat claims). **4 fixes applied** — all are EXAMPLE TEXT / ILLUSTRATIVE NUMBERS, no rule thresholds, logic, gate behavior, or skip_reason values changed. All thresholds (22.0, 27.0, 13.5, 74%, 15) preserved exactly as before. **Fix 1 — Co-primary scorer gate example (both prompts via `replace_all=true`, source byte-identical):** removed stale `Example: Fox (raw_avgs PTS ~25) and Wembanyama (raw_avgs PTS ~24)` — Fox averages 17.8 PPG on SA in 2025-26 (not ~25 — he was traded from SAC before this season), so he doesn't actually qualify for the co-primary gate. Replaced with `Example: on CLE, Mitchell (~26 PPG) and Harden (~23 PPG) are both above 22.0 — both are primaries, this skip fires for neither even when Harden's best qualifying PTS tier is lower than Mitchell's.` Added "Always verify against today's quant data, not these illustrative numbers." disclaimer. Ground-truth PPG: Mitchell 26.5, Harden 23.6 (both ≥22.0, same team, cleanly illustrative). **Fix 2 — Tiebreaker example (both prompts via `replace_all`):** the old Mitchell/Harden tiebreaker example was structurally invalid after the co-primary gate addition — both are above 22.0 so the co-primary gate would fire FIRST and the tiebreaker would never even be reached for them. Rewrote to a generic hypothetical illustrating the tier comparison path that actually executes when the candidate is below 22.0: "Example: on a team where the candidate player's best PTS tier is T10 and a teammate's best PTS tier is T15, the candidate is classified as secondary and BLOWOUT_SECONDARY_SCORER fires for their PTS pick." Also changed the gate condition text from "(both players have raw_avgs PTS < 22.0)" to "(the candidate player has raw_avgs PTS < 22.0)" — more accurate since the gate evaluates the candidate, not both-sides symmetry. Retained "A ball-handler role does not override this determination — the quant tier comparison is authoritative." **Fix 3 — Wembanyama SAS PPG precision (both prompts via `replace_all`):** tightened `As SAS's primary scorer (24.3 PPG team leader)` to `As SAS's primary scorer (~24.6 PPG team leader)` — ground truth is 24.6 (was 24.3, within rounding but now exact). SAS team context and "primary scorer" classification are factually correct. **Fix 4 — Elite scorer threshold example (both prompts via `replace_all`):** expanded `Example players currently meeting this threshold: Shai Gilgeous-Alexander.` to `Example players currently meeting this threshold: Shai Gilgeous-Alexander, Luka Doncic, Anthony Edwards (plus Tyrese Maxey and Kawhi Leonard hovering just above ~27.0). Always verify against today's quant data — the 27.0 gate reads live raw_avgs, not these examples.` Ground-truth PPG for all 5: SGA 30.2, Luka 32.0, Edwards 28.0, Maxey 27.4, Kawhi 27.2. All five are well clear of the 27.0 threshold. Broader grounding gives the analyst a richer mental model of "what a 27+ PPG player looks like" when the exemption is evaluated. **Dated historical references preserved untouched per spec:** Fox 3PM T1 / Wembanyama 3PM T1 "false-skipped on 2026-04-10" (dated audit event tied to a specific pick date with verifiable actual scores); Donovan Mitchell 3PM T1 miss on 2026-03-13; SGA 0-for-3 in OKC blowout on 2026-03-18; Barnes PTS T10 "raw_avg ~14, cushion=4, missed at 9 when TOR lost by 25" (dated audit narrative — `~14` was his L20 average at that pick's moment, not a current-season claim). Also untouched: Jokic T8/Barnes T4 volatile_weak_combo rationale (general rule phrasing, no stat numbers); "Mar 17 Jokic spread_abs=15.5, 25min, 8 PTS" (was already removed in earlier elite-exemption edit session). **Verification (4-point spec matrix + stale-claim inventory, all pass):** Python `ast.parse` clean; `Fox.*~25` pattern = 0; both `Example players currently meeting` lines (at 2333 and 3434) reference SGA/Luka/Edwards/Maxey/Kawhi (all verified ≥27.0 via `data/player_game_log.csv`); `raw_avgs PTS >= 27.0` count = 4 (unchanged — 2 in TIER CEILING RULES hard-skip block + 2 in ELITE SCORER exemption block across both prompts); `raw_avgs PTS >= 22.0` count = 2 (unchanged — co-primary gate in both prompts); `raw_avgs PTS < 22.0` count = 2 (unchanged — tiebreaker fallback text in both prompts); `spread_abs >= 13.5` = 4 (unchanged); `spread_abs >= 15` = 18 (unchanged). **Stale-claim removals verified:** `Fox (raw_avgs PTS ~25)` = 0; `Wembanyama (raw_avgs PTS ~24)` = 0; `Mitchell's best PTS tier is T20` = 0; `24.3 PPG` = 0. **New fix presence verified:** `on CLE, Mitchell (~26 PPG)` = 2 (one per prompt); `Shai Gilgeous-Alexander, Luka Doncic` = 2; `~24.6 PPG` = 2. **Ground-truth extraction method:** ran `pd.read_csv('data/player_game_log.csv')`, filtered to `dnp != '1'`, grouped by `player_name`, joined to active whitelist, computed PPG. Verified same-team pairs: CLE Mitchell (26.5) + Harden (23.6) both ≥22.0 ✓; ORL Banchero (22.2) + Bane (20.2) — Banchero above, Bane below (would trigger co-primary gate only for Banchero); DEN Jokic (26.9) + Murray (24.2) both ≥22.0 ✓; LAL Luka (32.0) + Reaves (22.8) both ≥22.0 ✓; PHI Maxey (27.4) + Embiid (26.2) both ≥22.0 ✓. CLE chosen for the example because the Mitchell/Harden case was the motivating scenario for the original 2026-03-31 PPG-tier tiebreaker rule — keeping it as the co-primary example preserves narrative continuity across the rule's history. **Not touched** per spec: rule thresholds, skip_reason enum values, Python logic, JSON schema, dated game-result rationale blocks, `build_scout_prompt()`, `build_review_prompt()`, `agents/auditor.py`, `parlay.py`, `backtest.py`, `quant.py`, `build_site.py`, `lineup_watch.py`, `lineup_update.py`, all `ingest/*.py`, all `.yml` workflow files, `context/nba_season_context.md`. `agents/analyst.py` only — 4 `replace_all=true` edits covering 8 hunks (4 examples × 2 prompts). |
| **H26 Skip Re-evaluation on Star Absence — Part 2 of 2 (2026-04-11)** | New `build_skip_reconsiderations()` function in `agents/lineup_update.py` re-evaluates morning `merit_below_floor` PTS/AST skips when a team's leading scorer is confirmed OUT during the afternoon. Closes the H26 downstream loop — Part 1 surfaced the star-absence signal as an annotation, this task acts on it by recovering picks that were skipped this morning at sub-70% confidence but would have crossed the floor with the star-absence lift. Motivating case: Tatum PTS skip at 67% confidence reconsidered when Brown is flagged OUT post-lunch. **Scope:** `agents/lineup_update.py` only. **Output target:** `opportunity_flags.json` via existing `save_opportunity_flags()` pipeline with `card_type="skip_reconsideration"` — no new JSON files, no new frontend work (existing opportunity-card renderer already handles the file). **New constant `_SA_POPULATION_DELTAS`** (PTS_T15 +11.3, PTS_T20 +12.7, PTS_T25 +10.5, AST_T4 +9.6, AST_T6 +6.7) — mirror of `agents/quant.py` `SA_POPULATION_DELTAS` copied to avoid a cross-module import of quant constants (inline comment documents that both must be updated together if the H26 deltas are recomputed). **New function `build_skip_reconsiderations(changes, player_stats, game_log, now_iso)`** inserted between `save_opportunity_flags()` and `build_pick_quant_summary()`. Control flow: (1) filter `changes` to `new_absence` only; (2) lower-cased `absent_names` set built from the absence player names; (3) early return `[]` if no absences OR `load_morning_skips()` empty OR `skipped_picks.json` missing/corrupt; (4) load skip records from `SKIPPED_PICKS_JSON` and filter to `date == TODAY_STR AND skip_reason == "merit_below_floor" AND prop_type in ("PTS", "AST")` (REB/3PM excluded — no H26 signal); (5) per-skip loop with all filters: player present in `player_stats`, `star_absence_lift` field present (graceful no-op if Part 1 not deployed), star name matches one of the absent names, **PERSONAL_DRAG_WARNING guard fires before any reconsideration logic and exits via `continue`** (invariant: when the per-player data shows a drag, reconsideration is structurally invalid regardless of other signals — prevents the Jalen-Green-without-Booker class of misses); (6) Path A `compute_without_player_rates()` call for per-player tier hit rate (gated on `without_n >= 3`): `hit_rate >= 0.70` → reconsider with "crosses 70% floor" reasoning; hit rate below 70% → does NOT reconsider but records reasoning for future analysis; (7) Path B fallback when no per-player data: check `_SA_POPULATION_DELTAS` for this prop/tier combo, only reconsider if `qualifier in {STRONG_PERSONAL_SIGNAL, POPULATION_ONLY, NEUTRAL_PERSONAL_DATA}` AND either the player's own `key_deltas[pop_key] > 0` (preferred when present) OR the qualifier is not NEUTRAL_PERSONAL_DATA (population fallback); (8) skip_reconsideration card assembled with all fields required by `save_opportunity_flags()` including empty `qualifying_tiers={}` and `upgrade_tiers={}` dicts for dedup compatibility, plus the new bespoke fields `prop_type`, `tier_considered`, `original_skip_reason`, `without_star_hit_rate_pct`, `without_star_n`, `star_absence_qualifier`, `reasoning` (semicolon-joined parts starting with "<star> confirmed OUT" and ending with "original skip: merit_below_floor"); diagnostic log line per card. Returns list of cards. **`main()` wiring** placed immediately after the existing `has_absences`/`build_opportunity_suggestions()` block, with separate debug and non-debug branches matching the same pattern used by `build_opportunity_suggestions()`. Reuses in-scope `game_log` via `game_log_recon = game_log if game_log is not None else load_game_log()` defensive ternary (`game_log` is always set inside the preceding `if has_absences:` block, but the ternary survives future refactors). Non-debug branch calls `save_opportunity_flags(recons)`; debug branch prints cards to stdout without saving. Both branches print a "no qualifying reconsiderations found" message when `recons` is empty. Gated on `has_absences` (same gate as the existing opportunity block). **Verification — 7-point spec grep matrix, all pass:** Python `ast.parse` clean; `def build_skip_reconsiderations` exactly 1; `_SA_POPULATION_DELTAS` refs = 2 (definition + usage); `skip_reconsideration` refs = 6 (card_type literal + 5 diagnostic log prefixes); `build_skip_reconsiderations` refs = 3 (definition + 2 call sites in debug/non-debug `main()` branches); `PERSONAL_DRAG_WARNING` refs = 2 (guard + diagnostic log); `prop_type.*PTS.*AST` = 2 (the new filter on line 676 + a pre-existing unrelated docstring on line 1306 — the 2 count is benign); `save_opportunity_flags(recons)` call = 1. **Logical invariant verified via AST-pattern grep:** `DRAG_WARNING` guard position (552) precedes `compute_without_player_rates` call (3995) and first `reconsider = True` assignment (4758); the DRAG_WARNING block exits with `continue` at position 3786, before any subsequent logic starts at 3874. **Runtime test via synthetic fixture (6 assertions, all pass):** built a synthetic `player_stats` dict with Tatum/Pritchard/White/Jalen Green entries, a fake `SKIPPED_PICKS_JSON` with 5 skip records (Tatum PTS T20 merit, Pritchard AST T4 merit, White REB merit, Tatum PTS T15 volatile_weak_combo, Tatum PTS T20 merit on wrong date, Jalen Green PTS T20 merit with DRAG qualifier), and `changes` with Brown OUT (BOS) + Booker OUT (PHX). Results: Tatum and Pritchard correctly reconsidered (STRONG_PERSONAL_SIGNAL with +27.3pp and +27.9pp key_deltas respectively — exactly the spec's motivating case); White REB filtered out by the PTS/AST-only filter; volatile_weak_combo filtered out by the merit_below_floor filter; wrong-date skip filtered out by the TODAY_STR filter; **Jalen Green PTS filtered out by the DRAG_WARNING guard** (logged with "DRAG_WARNING for Devin Booker, skipping reconsideration" as expected). All cards carry correct `card_type="skip_reconsideration"` and the required dedup fields. Motivating Tatum reasoning string: "Jaylen Brown confirmed OUT; player-specific delta +27.3pp at PTS_T20 [STRONG_PERSONAL_SIGNAL]; original skip: merit_below_floor". **Not touched** per spec: `build_opportunity_suggestions()` function unchanged, `compute_without_player_rates()` function unchanged, `load_morning_skips()` function unchanged, `save_opportunity_flags()` function unchanged (new cards flow through the same pipeline), no new JSON output files, `agents/quant.py`, `agents/analyst.py`, `agents/auditor.py`, `agents/parlay.py`, `agents/backtest.py`, `agents/build_site.py`, `agents/lineup_watch.py`, all `ingest/*.py`, all `.yml` workflow files, `context/nba_season_context.md`. **H26 downstream loop now closed** — Part 1 (annotation in quant/analyst) + Part 2 (reconsideration in lineup_update) both shipped same day. |
| **H26 Star-Absence Uplift Annotation — Part 1 of 2 (2026-04-11)** | Per-player star-absence lift data added to `agents/quant.py` + corresponding annotation block in `agents/analyst.py` `build_quant_context()`. When the team's leading scorer is confirmed OUT today, each teammate gets a `STAR_ABSENT_LIFT` two-line annotation showing (1) population-level impact from H26 backtest constants and (2) per-player with-vs-without deltas at H26 key tiers (PTS T15/T20/T25, AST T4/T6) with a directional qualifier. **Annotation-only** — informational context for confidence reasoning, does NOT override the existing WITHOUT-STAR BASELINE two-gate rule or any hard-skip rule. **Scope:** `agents/quant.py` + `agents/analyst.py` only. Part 2 (skip re-evaluation in `lineup_update.py`) is a separate prompt. **`agents/quant.py` — Part 1A constants:** new `SA_KEY_TIERS` (PTS 15/20/25, AST 4/6), `SA_POPULATION_DELTAS` (PTS_T15 +11.3, PTS_T20 +12.7, PTS_T25 +10.5, AST_T4 +9.6, AST_T6 +6.7 — from H26 backtest 2026-04-11), `SA_MIN_STAR_GAMES=20`, `SA_MIN_CONDITION_N=3`. **Part 1B `compute_star_absence_deltas()` function:** inserted between `compute_teammate_absence_splits()` and `build_player_stats()`. Companion function — `compute_teammate_absence_splits` uses top WHITELISTED teammate with absolute without-star rates for the WITHOUT-STAR BASELINE rule, while this new function uses the team's OVERALL leading scorer (full player_log, ≥20 games) and produces with-vs-without DELTAS at key tiers. Both fields coexist (`key_teammate_absent` and `star_absence_lift`). Returns `None` when player IS the star or has insufficient data. Directional qualifier: STRONG_PERSONAL_SIGNAL (mean delta > +3pp), NEUTRAL_PERSONAL_DATA (±3pp), PERSONAL_DRAG_WARNING (< −3pp), POPULATION_ONLY (< SA_MIN_CONDITION_N without-star games). Uses `.isin()` for game_id filtering so sort direction doesn't matter. DNP exclusion via `dnp != "1"` string comparison. **Part 1C team-stars pre-computation** in `build_player_stats()` placed right after `_rest_cache` block and before the whitelist-filtered `log` line — iterates `teams_today`, for each team scans the full player_log (not whitelist-filtered) to find the player with highest PTS avg and ≥`SA_MIN_STAR_GAMES` non-DNP games, then builds `(star_name, star_ppg, set_of_game_ids_where_star_was_absent)` tuple keyed by uppercase team abbrev. Stored in `_team_stars` dict. **Part 1D per-player loop call** — placed immediately after the `compute_teammate_absence_splits()` + trend-override block. Looks up `_team_stars.get(team)` and calls `compute_star_absence_deltas()` with the player name, team, star tuple. Guards against player being the star themselves. **Part 1E output field** — `"star_absence_lift": star_absence_lift` added to the `stats_out[player_name]` dict immediately after `"key_teammate_absent": key_teammate_absent`. Field structure: `{star_name, star_ppg, n_without, n_with, key_deltas (dict of f"{STAT}_T{tier}" → delta_pp), qualifier}`. **`agents/analyst.py` — Part 2A annotation rendering** in `build_quant_context()` placed after the `kta_line` computation. Two-line output when `sal["star_name"]` is in `_out_set`: (1) population reference `STAR_ABSENT_LIFT: [<star> OUT] population PTS T15-T25 +11 to +13pp, AST T4 +10pp`; (2) per-player line `Player-specific (Ng): <best_tier_key> delta +Xpp [<qualifier>]` showing the highest-delta key tier from `key_deltas`. PERSONAL_DRAG_WARNING case adds " — population lift does NOT apply to this player". POPULATION_ONLY case shows `Player-specific: <3 without-star games [POPULATION_ONLY]`. Hardcoded `3` for SA_MIN_CONDITION_N to avoid cross-module import coupling (documented in inline comment). **Part 2B wiring** — `star_lift_line` added to the `lines.append(...)` block between `kta_line` and `opp_absence_str`. **Part 2C qualifier reference** — added to TWO prompt rules: (a) `build_prompt()` `WITHOUT-STAR BASELINE — TWO REQUIRED GATES` block (line ~2095) — new paragraph after the intro explaining that STAR_ABSENT_LIFT is informational and does NOT override the two gates, with explicit per-qualifier rules (PERSONAL_DRAG_WARNING → do NOT apply population lift, STRONG_PERSONAL_SIGNAL → factor positively, POPULATION_ONLY → directional only, NEUTRAL → no lift). (b) `build_pick_prompt()` `KEY RULES — TEAMMATE ABSENCE USAGE ABSORPTION` block — same semantic paragraph with wording adapted to the Pick prompt's existing style (Pick prompt uses TEAMMATE ABSENCE USAGE ABSORPTION instead of the WITHOUT-STAR BASELINE TWO GATES framing). Both references explicitly say STAR_ABSENT_LIFT is informational context, not a directive rule, and does NOT override the existing rules. **Verification (9-point spec grep matrix + runtime invariant, all pass):** Python `ast.parse` clean on both files; `def compute_star_absence_deltas` present 1×; `SA_KEY_TIERS` definition 1×; `"star_absence_lift"` field present 1×; `_team_stars` refs 3×; `STAR_ABSENT_LIFT` refs in analyst 5× (annotation + 2 prompt rule references + 2 intra-annotation + 1 docstring); `star_lift_line` refs 6×; `population PTS T15-T25` marker 1×; `PERSONAL_DRAG_WARNING` refs 3×; `key_teammate_absent` count unchanged at 7 (existing consumers untouched, 1 new reference is in the new `compute_star_absence_deltas` docstring only). **Runtime invariant check** — executed `build_player_stats()` via `TODAY_STR` override against 2026-04-09 slate with real `player_log`: 49 players got `star_absence_lift` populated (BOS, DEN, HOU, etc. all had team stars with ≥20 games); `n_with + n_without == total player non-DNP games on team` passes for 49/49 players (0 failures); self-star-exclusion works (0 cases where a star shows lift for themselves); qualifier distribution: 37 STRONG_PERSONAL_SIGNAL, 6 NEUTRAL_PERSONAL_DATA, 5 POPULATION_ONLY, 1 PERSONAL_DRAG_WARNING. **Motivating BOS Tatum/Brown case reproduces exactly**: with Jaylen Brown in the OUT set, Tatum's star_absence_lift shows `n_without=3 n_with=11 qualifier=STRONG_PERSONAL_SIGNAL PTS_T25=+57.6pp PTS_T20=+27.3pp AST_T4=+36.4pp AST_T6=+21.2pp PTS_T15=+9.1pp`. Spec cited "+27pp PTS T20" — exact match. Pritchard shows `PTS_T20=+65.0pp` at n=10. Derrick White shows a weaker but still positive signal (AST_T6 +29.4pp). With empty OUT set, 0 STAR_ABSENT_LIFT annotations render (correctly gated); with `{"Jaylen Brown"}` in OUT set, 4 annotations render (the 4 non-Brown BOS teammates in today's whitelist) — the gate condition `sal.get("star_name", "") in _out_set` works correctly. **Generated `data/player_stats.json` from the test run was reverted to git HEAD** to avoid polluting the working directory — production workflow regenerates the file fresh each morning with real TODAY_STR, so no committed artifact needed. **Untouched** per spec: `compute_teammate_absence_splits()` function unchanged; `key_teammate_absent` output field unchanged; no `skip_reason` enum changes; no hard skip rules or confidence overrides; `build_scout_prompt()`, `build_review_prompt()`, `agents/auditor.py`, `agents/parlay.py`, `agents/backtest.py`, `agents/build_site.py`, `agents/lineup_watch.py`, `agents/lineup_update.py`, all `ingest/*.py`, all `.yml` workflow files, `context/nba_season_context.md`. **Part 2 deferred** per spec — skip re-evaluation in `lineup_update.py` that reads the `star_absence_lift` field from `player_stats.json` is a separate future prompt. |
| **Skip Rule Refinements — 4 edits (2026-04-11)** | Four surgical prompt-text edits to `agents/analyst.py` correcting high-false-skip-rate rules and aligning the monolithic `build_prompt()` with the H19-relaxed `build_pick_prompt()`. No Python logic changes, no schema changes, no new `skip_reason` enum values. All edits applied to BOTH `build_prompt()` and `build_pick_prompt()` (Scout prompt untouched — rules-free by design), except Edit D which was monolithic-only because Pick was already correct. **Motivation:** audit_summary.json showed `blowout_t25_skip` 100% FSR (2/2 — Maxey actual 32, Wembanyama actual 40), `blowout_secondary_scorer` 100% FSR (2/2), `3pm_blowout_trend_down` 66.7% FSR (2/3), and the H27 backtest (Apr 11, n=68 primary scorers at spread_abs ≥ 15) confirmed primary scorer PTS T25 hit rate at 58.8% — MARGINAL vs 50.6% baseline, NOT SUPPRESSED. **Edit A — `blowout_t25_skip` elite scorer exemption:** Two hunks in BOTH prompts, both applied via `replace_all=true` since the source text was byte-identical. (1) TIER CEILING RULES `PTS T25 BLOWOUT HARD SKIP` block rewritten to add an `EXCEPTION — elite scorers` clause: players with `raw_avgs PTS >= 27.0` are exempt from the hard skip and proceed through normal T25 evaluation, where the 80% hit rate bar (8+/10) still applies and the existing `spread_abs >= 15` confidence cap (74% max) still constrains the pick. Non-elite scorers (raw_avgs PTS < 27.0) remain hard-skipped. Rationale cites H27: population T25 hit rate at 58.8% is below floor, but elite scorers with 8+/10 individual rates significantly outperform population. (2) ELITE SCORER BLOWOUT EXEMPTION "Exception to the exemption" paragraph rewritten: the `spread_abs >= 15` cap still applies universally, but the note now clarifies that PTS T25 for elite scorers (raw_avgs PTS >= 27.0) is NOT hard-skipped — it proceeds through normal T25 eval at the 74% cap. Non-elite scorers remain hard-skipped. The two hunks are self-consistent and cross-reference each other. **Edit B — `blowout_secondary_scorer` threshold lowered + co-primary gate + T10 floor exception:** Applied separately in each prompt because the source texts differed (build_prompt had the stale pre-H19 `spread_abs > 8` version, build_pick_prompt had the H19-relaxed `spread_abs ≥ 15` version). Final replacement identical in both: (a) threshold lowered to `spread_abs >= 13.5` (between H19's 15 and the original > 8 — splits the difference where blowout is near-certain but not extreme); (b) new `T10 FLOOR EXCEPTION` clause — the skip does NOT apply when pick tier is T10 PTS, because T10 is the system's minimum PTS tier with near-certain population hit rates and the minute-compression risk is adequately handled by existing `min_floor` rules; (c) new `CO-PRIMARY SCORER GATE` — a player with `raw_avgs PTS >= 22.0` is always classified as PRIMARY regardless of teammate tier comparison, preventing the rule from firing against one of two co-primary scorers (example in text: Fox ~25 PPG and Wembanyama ~24 PPG are both above 22.0 → both primaries, rule fires for neither); (d) at `spread_abs 8–13`, apply the standard BLOWOUT_RISK confidence penalty (-10 to -15pp) INSTEAD of a hard skip — this is the H19 finding (n=140, lift=1.083 at 8–14 spread_abs) extended to the 13 cutoff. The pre-existing PRIMARY/SECONDARY tiebreaker text (based on best qualifying PTS tier) is retained as the fallback when the co-primary gate doesn't apply (i.e. when neither player is above 22.0 PPG). **Edit C — 3PM trend=down T1 iron_floor exception:** Applied to BOTH prompts via `replace_all=true` (identical source text). The unconditional T1 step-down skip now has an exception: if the player has `iron_floor` on 3PM OR a 9+/10 hit rate at T1, hold at T1 (do NOT skip) and apply a -5% confidence penalty to reflect the declining trend. Rationale: iron_floor or 9+/10 at T1 confirms a structural floor of 1+ make per game; the declining trend is real signal warranting a confidence deduction, but does not justify eliminating a near-certain prop. Fox 3PM T1 (actual 2) and Wembanyama 3PM T1 (actual 2) were both false-skipped by the unconditional T1 step-down on 2026-04-10. The exception applies ONLY to the trend=down step-down from T1 — the trend=down + tough DvP hard skip and the trend=down + limited minutes hard skip still override it when their conditions are met. **Edit D — Align monolithic build_prompt() 3PM blowout rule with H19:** The monolithic prompt had the stale pre-H19 `3PM hard skip — trend=down AND blowout_risk=True` block (hard-skipping all 3PM at `spread_abs > 8` on the favored side) while `build_pick_prompt()` was correctly updated by the H19 backtest (2026-03-22) to use step-down-only at `spread_abs 8–18`. Rewrote the monolithic block to match the Pick prompt's H19-aligned version: at `spread_abs 8–18` apply the existing trend=down step-down rule only (no hard skip); at `spread_abs >= 19` the unconditional extreme-blowout hard skip (pre-existing) still fires. The step-down now benefits from Edit C's T1 iron_floor exception for players who reach T1 via the step. The `build_pick_prompt()` version was explicitly NOT touched in this edit — it was already correct. Rationale cites H19 backtest (n=96–137) where blowout_win 3PM hits at 78.8–79.2% (lift=1.097–1.103), above the 71.8% baseline — the hard skip was blocking a high-hit-rate population. **Interaction with existing rules:** All four edits are annotation-only prompt text refinements. No new `skip_reason` enum values added or removed — the existing `blowout_t25_skip`, `blowout_secondary_scorer`, `3pm_blowout_trend_down` remain unchanged. `blowout_secondary_scorer` occurrence count in `analyst.py` = 8 (unchanged). No Python post-processors modified. No schema changes. The ROAD UNDERDOG NEAR-THRESHOLD PTS PENALTY rule immediately following the secondary scorer block is untouched in both prompts. **Verification (10-point spec grep matrix, all pass):** (1) Python `ast.parse` clean; (2) `EXCEPTION — elite scorers: Players with raw_avgs PTS >= 27.0` present exactly 2× (one per prompt function); (3) `spread_abs >= 13.5` present 4× (header + "At spread_abs >= 13.5 the blowout is near-certain" + T10 exception per prompt × 2 prompts = 4); (4) old `BLOWOUT_RISK SECONDARY SCORER SKIP.*spread_abs.*15` pattern = 0 (fully removed); (5) `CO-PRIMARY SCORER GATE` present 2×; (6) `T10 FLOOR EXCEPTION` present 2×; (7) `iron_floor on 3PM OR a 9+/10` present 2×; (8) old `3PM hard skip — trend=down AND blowout_risk=True` header = 0 (monolithic fully aligned); (9) new `3PM trend=down in blowout context (spread_abs 8–18)` header present 2× (both prompts now aligned); (10) `SECONDARY SCORER SKIP.*spread_abs > 8` = 0 (no stale `> 8` text remaining); (11) `blowout_secondary_scorer` enum count = 8 unchanged. Both `3PM hard skip — extreme blowout` headers (`spread_abs >= 19` in monolithic, `spread_abs ≥ 19` in Pick — Pick keeps its original unicode form since it wasn't touched) are preserved exactly as they were. **Not touched** per spec: `build_scout_prompt()` (rules-free by design), `build_review_prompt()`, `agents/auditor.py`, `agents/parlay.py`, `agents/backtest.py`, `agents/quant.py`, `agents/build_site.py`, `agents/lineup_watch.py`, `agents/lineup_update.py`, all `ingest/*.py`, all `.yml` workflow files, `context/nba_season_context.md`, skip_reason enum values, skip_reason documentation lines in OUTPUT FORMAT blocks, JSON output format, Python post-processors (`filter_self_skip_picks`, `reconcile_pick_values`, `enforce_market_gate`). `agents/analyst.py` only — 7 Edit calls total (2 via `replace_all=true`, 5 unique-context). |
| **Playoff Player Adjustments context block (2026-04-11)** | New `build_playoff_adjustments(today_str)` added to `agents/analyst.py` as a sibling function immediately after `build_playoff_context()`. Returns a `## PLAYOFF PLAYER ADJUSTMENTS` prompt block with per-player directional guidance synthesized from the H27–H32 backtest suite (career playoff data 2021–2025 from H28, minutes elasticity from H30, confidence calibration from H29, consistency index from H32, series progression from H31, blowout resilience from H27). **20 players covered** with net BOOST / CAUTION / NEUTRAL / PROP-SPECIFIC / AVOID directions and per-prop guidance: SGA (NEUTRAL, already optimal), Jokic (BOOST PTS/3PM, caution high-tier AST), Ant Edwards (BOOST G1-4, caution G5-7 PTS/3PM), Luka (BOOST PTS), Tatum (PROP-SPECIFIC — pick AST, avoid 3PM), Mitchell (CAUTION PTS, HARD AVOID 3PM), Brown (BOOST), Brunson (BOOST PTS), Harden (AST ONLY — largest -33pp late-series PTS fade in dataset), Curry (PTS G1-4, AST G5-7), Murray (BOOST early-series), Booker (pick REB), Embiid (STRONG CAUTION PTS/AST), KAT (STRONG CAUTION PTS/AST/3PM), Herro (AVOID ALL), Maxey (CAUTION PTS/3PM/AST), J. Williams (BOOST PTS/REB/AST, avoid 3PM in competitive games), Reaves (BOOST), Kawhi (BOOST PTS/REB when healthy), Banchero (BOOST, ⚠ limited playoff sample). **Date-gated to `PLAYOFFS_R1_DATE` (2026-04-18, inclusive)** — returns empty string during regular season and during play-in (4/14–4/17), since play-in teams have thin/no career playoff history. Sits alongside the existing `build_playoff_context()` which fires at the broader `PLAYOFF_START_DATE` (2026-04-14). **Annotation-only** — the block header explicitly says "annotation-only — use to inform confidence direction alongside quant data, not as hard overrides" and restates that rest/B2B vulnerabilities are neutralized in playoffs and AST suppression is widespread. No hard skip rules, no confidence modifiers, no behavioral overrides; the analyst reads the text and incorporates it into its reasoning. **Wired into all 3 prompt builders:** (1) `build_prompt()` (single-call v1 fallback path) — new `_playoff_adj` + `playoff_adjustments_section` variables constructed adjacent to the existing `playoff_context_section`, f-string injection between `{series_context_section}` and `## PLAYER RECENT PERFORMANCE`; (2) `build_scout_prompt()` (v2 scout path) — identical pattern (shared section-variable text with `build_prompt` — `replace_all` Edit applied both in one operation); (3) `build_pick_prompt()` (v2 pick path) — inline `{build_playoff_adjustments(TODAY_STR)}` call placed directly after the existing `{build_playoff_context(TODAY_STR)}` inline call, matching the Pick prompt's lighter-weight injection pattern (Pick doesn't receive the full section chain — it intentionally skips playoff_picture/leaderboard/team_defense per its docstring). `build_review_prompt()` explicitly untouched per spec — Review reviews existing picks, doesn't generate new ones. **Verification (20 assertions, all pass):** (1) Python `ast.parse` clean; (2) Date gate correct at 6 dates — 4/10/14/17 return empty, 4/18/20 and 5/15 return the full block; (3) 8 content markers present in the block (SGA/Jokic/Ant Edwards/Harden AST ONLY/Herro AVOID/Banchero BOOST/"Per-player directional guidance"/"PLAYER ADJUSTMENTS:"); (4) block is 4,376 chars / 53 lines; (5) At TODAY_STR=2026-04-20 each of 3 prompt builders emits the `PLAYOFF PLAYER ADJUSTMENTS` header exactly 1x and `SGA — NEUTRAL` marker exactly 1x; (6) At pre-R1 date TODAY_STR=2026-04-15 all 3 builders emit 0x adjustments header while still emitting 1x `PLAYOFF CONTEXT — POSTSEASON MODE` header (correctly scoped — general context fires at 4/14 while adjustments block waits for 4/18). Verified via stubbed `anthropic`/`httpx`/`json_repair` module load + runtime module-attribute `TODAY_STR` swap for each test date; no temporary `TODAY_STR` override was written into `analyst.py` itself, so nothing to revert before commit. **Not touched** per spec: `build_playoff_context()` unchanged (sibling function), `PLAYOFF PROFILE` annotations in `build_quant_context()` unchanged (separate per-player quant annotation system), `build_review_prompt()` unchanged, `PLAYOFFS_R1_DATE` and `PLAYOFF_START_DATE` constants unchanged, no hard skip rules added, `agents/quant.py`/`auditor.py`/`parlay.py`/`backtest.py`/`build_site.py` untouched, no `.yml` workflow changes. `agents/analyst.py` only — 1 new function + 2 section-variable blocks + 2 f-string injections (both via `replace_all` since `build_prompt` and `build_scout_prompt` share identical text) + 1 inline insertion in `build_pick_prompt`. |
| **Backtest: H31 playoff series progression (2026-04-11)** | New `--mode series-progression` added to `agents/backtest.py`. Tests whether tier hit rates shift across phases of a 7-game playoff series: early (games 1–2), mid (games 3–4), late (games 5–7). Uses `data/playoff_career_log.csv` (seasons 2021–2025, 1,883 playoff player-games, 73 series, 59 unique players, 44 qualified at ≥15 playoff games). Complements H28 (overall playoff delta) by adding a within-series temporal dimension. **Implementation** (`agents/backtest.py` only, does NOT use `load_player_log()` / `load_master()` / `load_whitelist()` — loads playoff_career_log.csv directly): new constants `H31_STATS`, `H31_STAT_COL`, `H31_TIERS`, `H31_KEY_TIER` (PTS T20, REB T6, AST T4, 3PM T2), `H31_PHASES=["early","mid","late"]`, `H31_MIN_PHASE_N=5`, `H31_MIN_TOTAL_PLAYOFF=15`, `H31_PROGRESSION_THRESHOLD=8.0`, `SERIES_PROGRESSION_JSON`. New function `run_series_progression_analysis(args)` in 7 steps: (1) load `playoff_career_log.csv`, filter `season_type == "playoff"` and `minutes > 0`; (2) infer series via sorted team-pair matchup_str, sort by (season, matchup_str, player_name, game_date), assign `game_in_series` per player via `cumcount() + 1`, derive `series_phase` (early G1-2, mid G3-4, late G5+), compute `series_length` per (season, matchup, player) group; (3) population-level phase analysis computing per-stat per-tier per-phase pooled hit rates + avg_stat + avg_minutes + early_to_late_delta_pp; (4) per-player iteration with ≥15 playoff games gate, compute per-phase hit rates at key tier with ≥5-game phase floor, progression_delta_pp (late − early) only when both phases qualify, per-stat flag (LATE_RISER ≥+8pp, LATE_FADER ≤-8pp, STABLE, None when insufficient), full `all_tiers` detail per stat for every tier/phase combo that meets the floor; (5) per-series game logs grouped by (season, matchup_str) with per-game pts/reb/ast/tpm/minutes + game_num + phase, sorted by date within each series and chronologically across series; (5b) overall player flag driven by PTS flag with cross-stat fallback; (6) stdout report with population table (4 stats × 3-4 tiers × 3 phases + early→late delta column), grouped per-player tables (LATE_RISERS sorted by PTS delta desc, STABLE by max_abs_delta, LATE_FADERS by |PTS delta| desc, INSUFFICIENT_DATA), sample size warning note; (7) JSON write with `metadata` (constants, flag_counts), `population` (pooled rates with early_to_late_delta_pp per tier), `players` (sorted entries with full series_log arrays), `summary` (name lists per flag). Wired `--mode series-progression` dispatch BEFORE the standard data-loading block in `main()` — this mode doesn't need `player_log`/`team_log`/`master_df`, so it exits early to avoid loading unused data. `--mode` help extended. **Production run result (preview numbers matched exactly):** 73 series, phase distribution 699/675/509 player-games, 44 players qualified. **Population deltas matched spec preview at every key tier:** PTS T20 +1.3pp (flat), REB T6 +4.4pp (slight late lift), AST T4 +3.7pp (slight late lift), 3PM T2 −1.9pp (slight late decline — only stat showing directional compression, defenses prioritize closing out on shooters). **Per-player distribution:** 11 LATE_RISER (PTS delta ≥ +8pp), 18 STABLE, 11 LATE_FADER (PTS delta ≤ −8pp), 4 INSUFFICIENT_DATA. **Top LATE_FADER: James Harden −33.1pp** (56% PTS T20 early → 23% late across 45 playoff games in 8 series) — quantifies his famous playoff collapses, largest fade in dataset. **Other notable LATE_FADERS:** Devin Booker −22.2pp, Paul George −22.2pp, **Stephen Curry −15.3pp** (surprising given his playoff-closer reputation; 87→93→71% suggests defensive adjustments catch up in games 5–7), **Anthony Edwards −15.0pp** (also a MINUTES_SCALER in H30 — the aggregate playoff minutes bump helps him, the within-series defensive adjustment catches up), Reaves −13.3pp, Derrick White −11.8pp, Jamal Murray −11.7pp, Tyrese Maxey −11.5pp. **Top LATE_RISERS:** Desmond Bane +61.4pp (thin sample), Julius Randle +25pp, Kawhi Leonard +20pp (already near ceiling at 80→100→100%), KAT +17.7pp, Andrew Wiggins +13pp, SGA +8.3pp, Kevin Durant +8.3pp. **STABLE cluster includes the elite playoff performers:** Jokic (+1.8pp), Tatum (+7.1pp, just under threshold with mid-series spike), Luka, LeBron, Embiid, Brunson, Jaylen Brown, Bam. **Verification (10 spec criteria, all pass):** file created, population table has 3 phases × all stat/tier combos, per-player gate enforced (all 44 players have ≥15 games), all per-player phase cells have n≥5, `game_in_series` correctly starts at 1 per (season, matchup, player) with sequential ordering, `series_length` matches actual games count in each series_log entry, series logs grouped by (season, matchup) with no duplicates and dates sorted within each series, population early→late deltas at key tiers match spec preview ranges exactly (+1.3pp PTS T20 / +4.4pp REB T6 / +3.7pp AST T4 / −1.9pp 3PM T2), per-stat flags consistent with ±8pp thresholds across all players, no regular-season leakage confirmed via cross-check against canonical 329 (season, matchup_str) playoff-only series entries. **Downstream consumer (deferred):** future playoff confidence-adjustment layer reading `progression_delta_pp` + current game-in-series at pick time — composes with H28 (overall playoff delta) and H30 (minutes elasticity) into a three-layer playoff confidence model. Curry/Edwards/Booker are the interesting cross-backtest cases: MINUTES_SCALER (H30) + LATE_FADER (H31) means the aggregate playoff minutes bump is canceled by within-series defensive adjustment, net effect likely neutral for early-round picks and negative for conference-finals picks. `agents/backtest.py` only — no analyst/auditor/parlay/quant/workflow changes. |
| **Backtest: H32 player consistency index (2026-04-11)** | New `--mode consistency-index` added to `agents/backtest.py`. Measures per-player tier hit rate stability across 3 context dimensions (Home/Away, Rest: B2B/Normal/Extended, Spread: Competitive/Blowout-risk) using `player_game_log.csv` + `nba_master.csv`. Formalizes the iron_floor intuition across multiple dimensions and identifies each player's single worst vulnerability (stat + dimension + range_pp). **Implementation** (`agents/backtest.py` only): new constants `H32_STATS`, `H32_STAT_COL`, `H32_MIN_SLICE_N=5`, `H32_MIN_TOTAL_GAMES=20`, `H32_ALLWEATHER_RANGE=10.0`, `H32_SENSITIVE_RANGE=20.0`, `H32_SPREAD_THRESHOLD=6.5` (mirrors quant.py), `CONSISTENCY_INDEX_JSON`. New function `run_consistency_index_analysis(player_log, master_df, args)` in 7 steps: (1) build spread lookup from `nba_master.csv` (same pattern as H19/H27); (2) add context columns — `rest_days` via `groupby.diff().dt.days - 1` (first game per player yields NaN and is excluded from rest bucket), `rest_bucket` = B2B/normal/extended, `spread_abs` + `spread_bucket` = competitive/blowout_risk at 6.5 threshold; (3) `add_best_tiers()` with rolling window; (4) per-player iteration with ≥20 total games gate — for each stat, compute modal `best_tier` across the player's qualifying games (characteristic tier), compute hit rate across up to 7 context slices (home/away/rest_B2B/rest_normal/rest_extended/spread_competitive/spread_blowout_risk) with n≥5 floor per slice, require ≥3 valid slices to rate the stat, compute range_pp = (max − min) × 100, std_pp, best_context, worst_context, per-dimension ranges (home_away abs diff of 2 slices, rest max−min of up to 3 slices, spread abs diff of 2 slices), worst_dimension, stat flag (ALL_WEATHER <10pp, MODERATE 10–20pp, CONTEXT_SENSITIVE ≥20pp); (5) overall player flag (ALL_WEATHER when all stats all-weather, HAS_VULNERABILITY when any stat context-sensitive, MODERATE otherwise), worst_vulnerability = (stat, dimension, dim_range) with highest dim_range across all stats; (6) stdout report with grouped tables (ALL_WEATHER / MODERATE with per-stat cells, CONTEXT_SENSITIVE with worst_vulnerability + slice detail strings); (7) JSON write with `metadata` (date, constants), `players` (sorted ALL_WEATHER by n_games desc, then MODERATE, then HAS_VULNERABILITY by worst dim_range desc), `summary` (name lists per flag + context_sensitive entries with vulnerability sub-objects). Wired `--mode consistency-index` dispatch in `main()` after `minutes-elasticity` (uses standard `player_log` + `master_df` loaders). `--mode` help extended. **Production run result (3,645 player-games, 58 unique players, 56 qualified at ≥20 games):** **Headline: zero ALL_WEATHER players** — context-invariance is the exception, not the rule. Only 4 MODERATE players: Julius Randle (all stats 9–18pp range), Jamal Murray, Jaylen Brown, Norman Powell. All other 52 qualified players (93%) have at least one stat with ≥20pp range across contexts. **Dominant vulnerability dimension: REST** (33 of 56 players, 59%). Spread was worst for 13 players (23%), Home/Away for 7 (12%). **Top worst vulnerabilities:** Jalen Williams 3PM(T1) spread 64.3pp (comp:0% blow:64%), Coby White PTS(T10) H/A 50.0pp (H:100% A:50%), **Joel Embiid PTS(T25) spread 48.3pp (comp:52% blow:100%) — opposite of blowout_t25_skip assumption**, Paul George 3PM(T2) spread 46.4pp, Bennedict Mathurin PTS(T15) rest 43.3pp, Tyler Herro PTS(T15) rest 40.4pp, Nikola Jokic AST(T8) rest 39.9pp (B2B:57% normal:97% extended:76% — matches existing B2B AST step-down rule), Kevin Durant PTS(T20) H/A 25.0pp (H:70% A:95%, road-preferring), Kawhi Leonard 3PM(T2) H/A 24.3pp (home-preferring mirror). **Verification (11 spec criteria, all pass):** file created, all 3 player group buckets present in summary, no players with n_games<20, all context slices have n≥5, all stats have ≥3 valid slices, range_pp formula `(max − min) × 100` verified and always ≥0, worst_vulnerability range matches max dim_range across all stats, per-dimension ranges correctly computed (home_away for 2 slices, rest for ≤3 slices, spread for 2 slices), modal best_tier values all integers in valid tier sets, DNP exclusion preserved (max n_games = 81 under 82-game regular season), rest bucket counts ≤ n_games confirming first-game NaN properly excluded. **Downstream consumer (deferred):** future playoff confidence-adjustment layer could read each player's `worst_vulnerability` + slice detail to flag specific contexts in which the player is reliable/unreliable. Today's slate position determines whether the vulnerability fires. Per-player rules should gate on slice n ≥ 10 and dim_range ≥ 25pp to be directive given slice-level sample noise. **Interesting finding for existing rules:** Embiid's T25 blowout vs competitive ratio (100% vs 52%) and the H27 primary-blowout finding together argue against the `blowout_t25_skip` hard-skip in its current form — primary scorers *benefit* from blowout-spread games at T25, not the opposite. `agents/backtest.py` only — no analyst/auditor/parlay/quant/workflow changes. |
| **Backtest: H30 minutes elasticity (2026-04-11)** | New `--mode minutes-elasticity` added to `agents/backtest.py`. Quantifies each player's production-vs-minutes curve across 4 absolute minutes buckets to identify who benefits most from the structural minutes increase in playoff basketball. Uses `player_game_log.csv` only (via existing `load_player_log()`). **Implementation** (`agents/backtest.py` only): new constants `H30_STATS`, `H30_STAT_COL`, `H30_TIERS` (PTS 15/20/25/30, REB 4/6/8/10, AST 2/4/6/8, 3PM 1/2/3), `H30_KEY_TIER` (PTS T20, REB T6, AST T4, 3PM T2), `H30_MIN_BUCKET_N=5`, `H30_MIN_TOTAL_GAMES=20`, `H30_MINUTES_BINS=[0,30,34,38,60]`, `H30_MINUTES_LABELS=["low","normal","high","extended"]`, `H30_SCALES_THRESHOLD=10.0`, `H30_INVERTS_THRESHOLD=-10.0`, `H30_POPULATION_MIN_MIN=28` (for starter-level population pool), `MINUTES_ELASTICITY_JSON`. New function `run_minutes_elasticity_analysis(player_log, args)` in 6 steps: (1) copy player_log, coerce `minutes_raw` to numeric, drop zero-minute rows, bucket via `pd.cut` with `right=False` so boundaries are [0,30)/[30,34)/[34,38)/[38,60); (2) per-player iteration with minimum-games gate (≥20), compute per-stat per-tier per-bucket hit rates (cells with n<5 excluded from output), avg_stat per bucket, `bucket_counts` covering all 4 buckets; (3) elasticity delta at the key tier = (extended − normal) × 100, per-stat flag (SCALES/PLATEAUS/INVERTS), monotonicity check (all rates non-decreasing from low→extended across buckets with ≥5 games), overall player flag: MINUTES_SCALER (PTS SCALES), SELECTIVE_SCALER (PTS PLATEAUS + other stat SCALES), MIXED (PTS PLATEAUS + 1+ INVERTS), MINUTES_INDEPENDENT (all PLATEAUS), MINUTES_INVERTER (PTS INVERTS), INSUFFICIENT_DATA (elasticity unavailable); (3b) playoff_projection sub-object includes `extended_key_rates` and `vs_overall_delta` per stat; (4) population aggregation pooled across starter-level players (avg min ≥ 28), per-stat per-tier bucket rates + population elasticity; (5) stdout report with population table (4 stats × 3–4 tiers × 4 buckets + elasticity column), grouped per-player tables (SCALERS / SELECTIVE_SCALERS / MIXED / INDEPENDENT / INVERTERS / INSUFFICIENT_DATA) showing avg_min / n_ext / PTS(T20) low→norm→high→ext progression string (with `—` for excluded buckets) / elasticity / monotonic marker (↑ / · / ?) / flag; (6) JSON write with `metadata` (date, constants, flag_counts), `population` (pooled bucket rates + elasticity per tier), `players` (sorted by PTS elasticity descending with full tier/bucket detail + monotonic flags + playoff_projection), `summary` (name lists per flag category). Wired `--mode minutes-elasticity` dispatch in `main()` after `primary-blowout` (uses standard `player_log` loader). `--mode` help extended. **Production run result (3,533 non-DNP games, 58 unique players, 56 qualified at ≥20 games, 49 starter-level for population pool):** **Population elasticity is massive and monotonically increasing** at every PTS/REB/AST tier. Key results: PTS T15 +16.1pp, PTS T20 +26.0pp, PTS T25 +28.9pp, PTS T30 +26.6pp, REB T6 +17.0pp, AST T4 +21.1pp, AST T6 +26.7pp, 3PM T2 +9.6pp. 3PM is shot-allocation-bound not minutes-bound. **Flag distribution:** 23 MINUTES_SCALERS (biggest deltas Sengun +54.7pp, Miles Bridges +54.2pp, Jalen Johnson +50.6pp, Paolo Banchero +48.3pp, Bam +40.9pp, Scottie Barnes +40.4pp, Brandon Ingram +38.9pp, Anthony Edwards +37.5pp, Jokic +33.3pp, Luka +12.5pp 57→88→100→100%, Jamal Murray, Maxey, etc.), 6 SELECTIVE_SCALERS (Gobert REB, Kawhi, Pritchard, Cameron Johnson, Amen Thompson, Durant), 3 MIXED (SGA with 100/100/100/100 PTS T20 ceiling, Jaylen Brown, Cade Cunningham), 0 MINUTES_INDEPENDENT, 2 MINUTES_INVERTERS (Donovan Mitchell −19.1pp n_ext=7, LeBron James −28.0pp n_ext=5 — both small-sample, directionally consistent with age/efficiency narratives but not definitive), 22 INSUFFICIENT_DATA (players whose avg minutes sits below ~32 so they don't log ≥5 games at 38+). **Canonical extreme case:** Anthony Edwards PTS T25 progression 25% → 50% → 69.6% → **86.4%** = +61.4pp span (matches spec's "~30% → ~90% ~60pp" sanity target precisely). **Verification (10 spec criteria, all pass):** file created, all 6 player groups present in flag counts, no players with n_games<20, all bucket cells have n≥5, `elasticity_delta_pp = (extended − normal) × 100` formula verified, minutes boundaries correct via `right=False` (Edwards has 22 games in [38,60) extended bucket), `bucket_counts` sum to `n_games` exactly (4 buckets cover [0,60) inclusive), `monotonic_up` correctly computed across all player-stats with ≥3 valid buckets, Ant Edwards T25 span +61.4pp confirms dramatic scaling, SGA PTS T20 = 100% at all 4 buckets confirms PLATEAUS, `playoff_projection.vs_overall_delta = (extended_rate − overall_rate) × 100` formula verified across every player. **Downstream consumer (deferred):** future playoff confidence adjustment layer could use each scaler's `extended_key_rates` or `playoff_projection.vs_overall_delta` as per-player playoff confidence bumps. Prime candidates for +5 to +10pp bumps: Banchero, Ant Edwards, Jalen Johnson, Sengun, Bam, Jokic, Scottie Barnes, Luka, Jamal Murray, Maxey. No bump for MIXED/INSUFFICIENT. Per-player rules should gate on n_extended ≥ 10 to be directive given sample noise at individual-player level. `agents/backtest.py` only — no analyst/auditor/parlay/quant/workflow changes. |
| **Backtest: H29 player-level confidence calibration (2026-04-11)** | New `--mode confidence-calibration` added to `agents/backtest.py`. Audits the analyst's assigned confidence against actual hit rates, per player, using graded picks from `picks.json` exclusively (no `player_game_log.csv` or `playoff_career_log.csv` dependency). **Implementation** (`agents/backtest.py` only): new constants `H29_MIN_PICKS=10`, `H29_MIN_PROP_PICKS=5`, `H29_MIN_BAND_PICKS=3`, `H29_OVER_CONF_THRESHOLD=8.0`, `H29_UNDER_CONF_THRESHOLD=8.0`, `H29_CONF_BANDS` (70-75 / 76-80 / 81-85 / 86+), `CONFIDENCE_CAL_JSON`. New function `run_confidence_calibration_analysis(picks, args)` in 7 steps: (1) load picks from existing `load_picks_json()`, apply optional `--start`/`--end` date filter, enrich each pick with `{player, prop_type, confidence_pct, hit, pick_value, actual_value, date}`, defensively skip picks without confidence; (2) population confidence band calibration computing per-band `n`, `hits`, `hit_rate`, `avg_confidence`, `delta_pp` (actual - assigned); (3) per-player grouping with minimum-picks gate (≥10), computing `actual_hit_rate`, `avg_confidence`, `calibration_delta_pp`, per-prop breakdown (props with ≥5 picks), per-band breakdown (bands with ≥3 picks), miss severity (`avg_miss_margin` = mean of `pick_value - actual_value` across misses with valid data, positive = below threshold); (4) per-player flag: OVER_CONFIDENT when delta ≤ -8pp, UNDER_CONFIDENT when delta ≥ +8pp, WELL_CALIBRATED otherwise; (5) summary lists grouping players by flag; (6) stdout report with population bands table, per-player calibration table sorted by `calibration_delta_pp` ascending (most over-confident first), and detailed over/under-confident highlight sections with per-prop breakdowns and `←` tags for prop-specific over/under-confidence; (7) JSON write with `metadata` (date, counts, constants, overall_hit_rate, avg_confidence), `population_bands`, `players` (full per-player entries with by_prop/by_band/miss data), `summary` (counts + name lists of over/under-confident players). Wired `--mode confidence-calibration` dispatch BEFORE the standard data-loading block in `main()` — this mode doesn't need `player_log`/`team_log`/`master_df`, so it exits early after calling `load_picks_json()`. `--mode` help extended. **Production run result (815 graded picks, overall 87.1% hit rate, avg assigned 77.1%):** 31 players qualified (≥10 picks). **Population bands**: 70-75 → 85.5% actual (+12.5pp UNDER_CONFIDENT), 76-80 → 88.3% (+9.2pp UNDER_CONFIDENT), 81-85 → 85.7% (+2.8pp within window), 86+ → 96.0% (+6.6pp within window). 88% of picks sit in the first two bands and both are well into UNDER_CONFIDENT territory — consistent with existing `audit_summary.json` `confidence_calibration_totals` and validates the pick mechanism. **OVER_CONFIDENT (2 players):** Brandon Ingram (n=13, -15.2pp; PTS 40% and AST 66.7% both flagged prop-specific), Donovan Mitchell (n=23, -10.4pp; PTS 60% n=5 and 3PM 42.9% n=7 are the culprits, AST 77.8% n=9 is fine). **UNDER_CONFIDENT (18 players):** Paolo Banchero (+19.5pp, 95.7% actual), Cade Cunningham (+18.6pp), Jaylen Brown (+18.3pp), Wembanyama (+17.5pp), LeBron (+17.3pp), Luka (+15.7pp), Kon Knueppel (+14.6pp), SGA (+13.5pp), LaMelo Ball (+13.6pp), Austin Reaves (+13.5pp), Giannis (+12.3pp), Sengun (+12.2pp), Desmond Bane (+11.5pp), Kawhi Leonard (+11.2pp), Jokic (+10.6pp), James Harden (+10.2pp), Scottie Barnes (+9.8pp), Julius Randle (+8.5pp). **WELL_CALIBRATED (11 players):** Derrick White, Payton Pritchard, RJ Barrett, Jalen Duren, Jalen Johnson, Cooper Flagg, Brandon Miller, Anthony Edwards, Kevin Durant, Devin Booker, Jamal Murray — all within ±8pp. **Verification (10 spec criteria, all pass):** file created, population bands table has 4 bands with n/hit_rate/avg_confidence/delta_pp, per-player sorted ascending by calibration_delta_pp (-15.2 → +19.5), no players with n<10, per-prop breakdowns all have n≥5, `n_hits <= n_picks` and `actual_hit_rate == round(n_hits/n_picks, 3)` invariants hold across all 31 players, `calibration_delta_pp == actual_hit_rate*100 - avg_confidence` formula verified, flags consistent with ±8pp thresholds, `avg_miss_margin >= 0` for all players with miss data (positive = below tier, as expected since MISS implies actual < tier), overall hit rate 87.1% matches the ~87% season sanity window. **Downstream consumer (deferred):** future analyst prompt enhancement injecting per-player calibration floors/caps — OR per-player overrides in the `bet_recommendation.calibrated_prob` computation in `ingest/odds_today.py` `_get_calibrated_prob()`. Care needed to avoid double-counting with the existing band-based calibration already feeding the odds layer. `agents/backtest.py` only — no analyst/auditor/parlay/quant/workflow changes. |
| **Backtest: H28 playoff-career tier performance (2026-04-11)** | New `--mode playoff-career` added to `agents/backtest.py`. Compares per-player regular vs playoff career tier hit rates across PTS/REB/AST/3PM using `data/playoff_career_log.csv` (2021–2025, 18,168 regular + 1,883 playoff games, 58 players qualified). **Implementation** (`agents/backtest.py` only, does NOT use `load_player_log()` / `load_whitelist()` / `player_game_log.csv` — reads `playoff_career_log.csv` directly for apples-to-apples same-window comparison): new constants `H28_STATS`, `H28_STAT_COL`, `H28_TIERS` (PTS 15/20/25/30, REB 4/6/8/10, AST 2/4/6/8, 3PM 1/2/3), `H28_KEY_TIER` (PTS T20, REB T6, AST T4, 3PM T2), `H28_MIN_PLAYOFF_GAMES=10`, `H28_LIMITED_SAMPLE_MIN=5`, `H28_ELEVATOR_THRESHOLD=5.0`, `H28_SUPPRESSOR_THRESHOLD=-5.0`, `H28_ALLSTAR_TEAMS` (CAN/CHK/DUR/EAST/GIA/KEN/LEB/SHQ/WEST — filtered from regular-season baseline), `PLAYOFF_CAREER_CSV`, `PLAYOFF_CAREER_JSON`. New function `run_playoff_career_analysis(args)` in 7 steps: (1) load CSV, filter All-Star team abbreviations + zero-minute rows (86 all-star + 2 zero-min = 88 regular rows filtered from 18,256), split into regular/playoff DataFrames; (2) per-player iteration, compute per-stat per-tier hit rates (`reg_hits`, `reg_rate`, `po_hits`, `po_rate`, `delta_pp`) directly from raw stat columns (NOT `add_best_tiers` — tier-specific rates required); (3) per-stat flag at key tier (ELEVATOR/STABLE/SUPPRESSOR) based on `H28_ELEVATOR_THRESHOLD`/`H28_SUPPRESSOR_THRESHOLD`; (4) overall flag from cross-stat counts (STRONG_ELEVATOR when 3+ ELEVATOR, ELEVATOR when 2+ELEVATOR and no SUPPRESSOR, STRONG_SUPPRESSOR when 3+ SUPPRESSOR, SUPPRESSOR when 2+SUPPRESSOR and no ELEVATOR, STABLE when all 4 stable, MIXED otherwise); (4b) per-player entry includes `n_playoff`, `n_regular`, `playoff_seasons`, `sample_flag` (reliable / limited_sample), minutes comparison, full `stats` dict, `overall_flag`, and sorted `playoff_game_log` (date/season/opponent/home_away/minutes/pts/reb/ast/tpm); (5) population aggregation pooled across qualifying players; (6) stdout report with population table (4 stats × 3–4 tiers) and per-player tables (reliable + limited separated) showing `{reg%→po% ±Xpp}` at each key tier, sorted by overall_flag priority (STRONG_ELEVATOR first) then by total absolute delta magnitude descending; (7) JSON write with `metadata` (date, constants, counts), `population`, `players` (full per-player entries with game logs), `summary` dict mapping `player_name → overall_flag` for downstream quick-lookup. Wired `--mode playoff-career` dispatch BEFORE the standard data-loading block in `main()` — this mode doesn't need `player_log` / `team_log` / `master_df` and exits early to avoid loading 20K unused rows. `--mode` help extended. **Production run result (2025 full career dataset):** 58 players qualified (47 reliable ≥10 games / 11 limited 5–9 games); 6 STRONG_ELEVATOR (Banchero, Paul George, Jalen Williams, CJ McCollum, Anthony Edwards, Austin Reaves), 6 ELEVATOR (Kawhi, Jokic, Jamal Murray, Hartenstein, Aaron Gordon, Curry), 15 MIXED (Tatum, Jaylen Brown, Durant, Luka, Brunson, Jalen Green, LeBron, Mitchell, …), 1 STABLE (SGA at all +3–5pp), 4 SUPPRESSOR (Embiid, Jarrett Allen, Gobert, Harden), 15 STRONG_SUPPRESSOR including Tyler Herro (−37pp PTS T20), Julius Randle, KAT, Tyrese Maxey, Mikal Bridges, Payton Pritchard (uniformly crushed). Jokic PTS is an ELEVATOR at every tier (T15 +5.8pp → T30 +12.6pp) — opposite of the H27-motivating anecdote. **Verification (11 spec criteria, all pass):** file created, population table has 4 stats × 3–4 tiers each with reg/po/delta, per-player sorted by flag priority with reliable-then-limited grouping (47+11), limited_sample flag gated to 5–9 games range, no players with <5 games, `minutes_delta` present, `po_hits <= n_playoff` and `po_rate == round(po_hits/n_playoff, 3)` invariants hold across every tier of every player, per-stat flags consistent with thresholds (+5/−5), playoff game logs sorted by date with length matching n_playoff, 86 All-Star rows correctly filtered from regular-season baseline (18256 raw → 18168 used with 2 additional zero-minute rows also dropped), sanity checks pass (Jokic PTS T20 +9.4pp ELEVATOR, Jokic AST T6 −12.2pp matches spec's ~-13pp expectation when reading the `tiers` sub-dict rather than the key-tier flag, Edwards STRONG_ELEVATOR, Embiid PTS T20 −12.3pp SUPPRESSOR). **Downstream:** output feeds a future playoff context block injection into analyst prompt (separate task). No analyst.py changes in this backtest — data ingest + analysis only. `agents/backtest.py` only. |
| **Backtest: H27 primary-scorer blowout PTS (2026-04-11)** | New `--mode primary-blowout` added to `agents/backtest.py`. Tests whether `blowout_t25_skip` (hard-skip of primary scorer PTS T25/T30 at spread_abs ≥ 15) is correctly suppressing a structural risk. Motivated by 100% false skip rate on graded skips (Maxey 32, Wembanyama 40) and the hypothesis that Jokic's blowout distribution style is player-specific, not structural. **Implementation** (`agents/backtest.py` only): new constants `H27_SPREAD_THRESHOLDS = [10, 12, 15]`, `H27_PTS_TIERS = [15, 20, 25, 30]`, `H27_MIN_N = 10`, `H27_MIN_PLAYER_N = 3`, `H27_ACTUAL_BLOWOUT_MARGIN = 15`, `PRIMARY_BLOWOUT_JSON`. New function `run_primary_blowout_analysis(player_log, master_df, args)` in 9 steps: (1) build `spread_map` from `nba_master.csv` (home_spread/away_spread, signed, same pattern as H19); (2) `classify_player_role()` to get `scorer_role`; (3) join signed spread to player-game rows, compute `spread_abs` and `is_favorite`; filter to `signed_spread.notna() & scorer_role != "unknown"`; (4) build `margin_lookup` and join `actual_margin` + `actual_blowout` (margin ≥ 15) as secondary overlay; (5) per-cell tier hit rates at T15/T20/T25/T30 for 8 buckets (primary/secondary × all/actual_blowout/competitive + two baselines), computed directly from `pts` column (NOT `add_best_tiers` — tier-specific rates required); (6) per-player breakdown at spread_abs ≥ 15 filtered to primary scorers, with per-player baseline lookup (`baseline_by_player`), per-tier hits/rates, `T25_delta_pp`, `minutes_delta`, actual_blowout vs competitive sub-split T25 rates, and individual game log (date, opponent, spread_abs, actual_margin, actual_blowout, minutes, pts, T20_hit, T25_hit) sorted by date; sorted by `T25_delta_pp` ascending so Jokic-type outliers surface at top; (7) verdict logic: population MAINTAINS/MARGINAL/SUPPRESSED/INSUFFICIENT_SAMPLE per threshold (≥0.65 / 0.55–0.65 / <0.55), OUTCOME_INDEPENDENT vs GAME_SCRIPT_DEPENDENT at spread_abs ≥ 15 (>10pp delta threshold), Jokic isolation check (with vs without Jokic at ≥15, >5pp delta threshold), per-player flags (PLAYER_SUPPRESSED at ≤-15pp + n≥3, PLAYER_RESILIENT at ≥0pp + n≥3, MINUTES_COMPRESSED at ≤-4 min); (8) stdout report with per-threshold table (8 buckets × 4 tiers × avg_min), per-player table sorted by T25_delta_pp, verdict block; (9) JSON write to `data/backtest_primary_blowout.json` with all results, per-player table including game_log arrays, verdicts, constants, and date range. Wired `--mode primary-blowout` dispatch in `main()` between star-absence and calibration-only fast path; `--mode` help string extended. **Production run result (2025-26 season, 228/154/68 primary instances at ≥10/≥12/≥15):** primary T25 at ≥15 = 58.8% (MARGINAL, ABOVE 50.6% baseline). Non-monotone across thresholds (≥12 dips to 54.5% SUPPRESSED, ≥10 and ≥15 both MARGINAL). **Most important finding — GAME_SCRIPT_DEPENDENT at spread_abs ≥ 15:** when blowout materialized (margin ≥15, n=35), primary T25 dropped to 45.7%; when game stayed competitive despite high spread (n=19), primary T25 elevated to 73.7%. Delta = +28.0pp — the rule is firing on a population that's roughly half false-positive. **Jokic isolation NOT CONFIRMED** — only 1 qualifying Jokic game in sample (n_with=68 vs n_ex=67, delta +0.9pp); insufficient data to test the player-specific hypothesis. **Per-player findings (n≥3):** 3 PLAYER_SUPPRESSED (Durant HOU -31.1pp, Cunningham DET -29.7pp + MINUTES_COMPRESSED, Luka MINUTES_COMPRESSED -10.3 min), 3 PLAYER_RESILIENT (Mitchell CLE +17.2pp, Ball CHA +1.8pp, Murray DEN +51.6pp), 3 middle-of-road (SGA -6.6pp n=21, Brown -3.6pp, Maxey -1.7pp). Suppression concentrated in HOU/DET roster contexts and minutes compression, not structural. **Verification (12 spec criteria, all pass):** file created, 3 threshold sections, 8 buckets per section, per-player sorted ascending by T25_delta_pp (-31.1 → +51.6), blow/comp columns present, `hits <= n` and `hit_rate == round(hits/n, 3)` invariants hold across every cell, elite baseline T25 rates in 56–83% range (realistic), Jokic isolation check runs, 6 rows have per-player flags (PLAYER_SUPPRESSED/PLAYER_RESILIENT/MINUTES_COMPRESSED), game_log arrays sorted by date with all 9 expected fields, no `scorer_role=="unknown"` rows leak in, `n_actual_blowout + n_competitive == n_high_spread − n_null_margin` (null-margin games are ingest gaps — SGA has 9, Durant 2, Brown/Mitchell 1 each — cleanly excluded from sub-split while preserved in n_high_spread). **Action:** DEFERRED — do not modify `analyst.py` in-season based on a single-season sample. GAME_SCRIPT_DEPENDENT finding is compelling but requires knowing game script at pick time (we don't). Revisit after playoffs for multi-season verification and candidate rule: replace hard skip with -5pp cap, or retire entirely. Per-player flags inform offseason player-specific rules. `agents/backtest.py` only — no rule changes shipped. |
| **Pretip sweep: closing-line re-fetch architecture (2026-04-11)** | Replaces the "fetch once, dedup forever" model in `pretip_sweep()` step 4 with a "re-fetch when closer to tip-off" model. Under the old model, each game was fetched at most once per day — whichever hourly sweep first hit the game's window captured its odds. This produced noisy CLV data because a line captured 3–5 hours before tip doesn't reflect the market's final assessment; sharp-money movement, injury news, and lineup confirmations all happen in the last 1–2 hours. New model: each sweep re-fetches a game when `(prior_minutes_to_tip − current_minutes_to_tip) >= CLOSING_LINE_MIN_IMPROVEMENT` (30 min). Commenced games (`minutes_to_tip < 0`) are locked — no re-fetch, no credit burn. Combined with the tip-off guard shipped earlier today, the last successful pre-tip fetch becomes the definitive closing line. **Implementation** (`ingest/odds_today.py` only): (1) New constant `CLOSING_LINE_MIN_IMPROVEMENT = 30` near `PRETIP_GRACE_MINUTES`; the 30-min floor prevents marginal re-fetches from wasting credits on cron jitter (e.g., two GitHub Actions runs 5 min apart). With hourly cron the typical improvement is ~60 min so every run qualifies. (2) Step 4 dedup block replaced entirely. Old code built `already_fetched` set from `pretip_data.snapshots` keys and filtered to only unfetched events. New code iterates `in_window_events` and classifies each as: first-fetch (no prior snapshot → include), commenced (`current_mtt < 0` → skip with `n_skip_commenced += 1`), missing prior mtt (legacy snapshot → re-fetch defensively with `n_refetch += 1`), significant improvement (`prior_mtt − current_mtt >= 30` → re-fetch with `n_refetch += 1`), or marginal (skip with `n_skip_marginal += 1`). Status print line shows the per-bucket counts (e.g. `Closing-line filter: 7 re-fetch (closer to tip), 1 commenced (locked), 1 first capture`). (3) Step 5 fetch log updated to distinguish first-fetch from re-fetch — when `prior_snapshots.get(event_id)` has a `minutes_to_tip`, logs `Re-fetched: AWAY @ HOME (was +Nmin, now +Mmin to tip)` showing the improvement; otherwise logs the original `Fetched: AWAY @ HOME (tips in N min)` line. **Untouched:** snapshot save path (`pretip_data.setdefault("snapshots", {})[event_id] = {...}` already overwrites by event_id, so re-fetched games naturally replace their prior snapshot — only the closest-to-tip capture is preserved); step 6 annotation loop (picks get `market_implied_prob` overwritten with latest odds, morning guard preserved, tip-off guard preserved); `compute_edge()` (runs after annotation, recalculates calibrated edge against the latest market price); `_write_pretip_file()` + picks.json write; `PRETIP_GRACE_MINUTES`; `main()` morning annotation; `save_morning_baseline()`. **Credit budget** (paid tier, 20k credits/month): ~20–28 credits/game/day (5–7 hourly fetches × 4 credits for 4 alternate markets) vs 4 credits/game/day under old model. For a 10-game slate: ~200–280 credits/day, well within budget. **Unit tests** (9 total): (1) synthetic 8-sweep walkthrough matching the spec's table — Game A tips 4:30 PM, Game B tips 7:00 PM: Game A gets 5 fetches (12/1/2/3/4 PM) → 20 credits, locked at 5 PM when commenced; Game B gets 8 fetches (12/1/2/3/4/5/6/7 PM) → 32 credits, closing line captured at +0 min (right at tip); total = 52 credits for 2 games, matching spec arithmetic. (2–7) edge cases: 5-min improvement → skip (marginal); exactly 30-min improvement → fetch (threshold is `>=`); first fetch → `n_refetch` not incremented; legacy snapshot missing `minutes_to_tip` → defensive re-fetch; `mtt=0` (right at tip) → fetch (not yet commenced); `mtt=-5` (commenced) → locked. All 6 edge cases pass. **Verification:** `grep -c "already_fetched" ingest/odds_today.py` = 0 (old dedup set fully removed); `grep -n "CLOSING_LINE_MIN_IMPROVEMENT\|re-fetch\|Re-fetched\|Closing-line\|n_refetch\|improvement\|marginal" ingest/odds_today.py` shows 19 references covering the constant, classification logic, counters, status print, and Re-fetched log line. Python `ast.parse` clean. `ingest/odds_today.py` only — no workflow, agent, or schema changes. |
| **Pretip sweep: tip-off guard on market_implied_prob (2026-04-11)** | Prevents `pretip_sweep()` in `ingest/odds_today.py` from overwriting `market_implied_prob` on picks whose game has already started. On 2026-04-10, MIN@HOU was fetched at `minutes_to_tip=-29` (29 min AFTER tip-off) because the 30-minute `PRETIP_GRACE_MINUTES` grace window allows post-tip fetches for dedup/snapshot purposes. The spec contract is that `market_implied_prob` must be a pre-game closing line for CLV integrity — Amen Thompson's AST T4 price was overwritten from 81.13 (pre-game) to 81.82 (potentially live), contaminating CLV. Root cause: step 6 (the pick annotation loop) had no tip-off awareness — it used the same `fetched` dict regardless of whether each event was pre- or post-tip. **Fix:** two-part change in `pretip_sweep()` only. (1) After `in_window_events` construction and the window summary print, a new `commenced_teams: set[str]` is built by iterating `in_window_events` for any entry with `minutes_to_tip < 0` and adding both `home_abbr` and `away_abbr` to the set. Log line `[odds-pretip] Commenced teams (live line guard active): ...` fires when non-empty. Placed BEFORE the early-empty-exit and BEFORE the dedup step so it reflects the full in-window population, not just the to-fetch subset. (2) In the step 6 annotation loop, immediately after the match lookup (`match = fetched.get(...)`) and before the morning-implied capture, a tip-off guard block checks `pick.get("team")` and `pick.get("opponent")` against `commenced_teams`. If either team has commenced AND the pick already has a `market_implied_prob` value, `continue` — preserving the pre-game price. If the game has commenced but the pick has no prior price (edge case: morning annotation missed this pick), a WARNING is logged and the post-tip line is allowed through — having *some* price, even live, is better than null for `compute_edge()`, but the warning flags it as CLV-unreliable. **Key behaviors preserved:** Phase 1 (fetch/dedup/snapshot) is completely unchanged. `PRETIP_GRACE_MINUTES` still allows post-tip fetches — snapshots are still written to `odds_pretip.json` so future runs correctly dedup on event_id. Only the pick annotation in step 6 is guarded. **CLV semantics:** for picks whose game commences before a clean pretip capture, CLV now degrades to 0 (morning = market, no movement) instead of being contaminated — which is correct: missing movement is not the same as negative movement. **Unit test** (synthetic 4-pick walkthrough matching the 4/10 HOU incident): `commenced_teams = {HOU, MIN}` from one event at `minutes_to_tip=-29`; Amen Thompson HOU (`market_implied_prob=81.13`) and Sengun HOU (`97.22`) both hit the skip branch and preserve their pre-game prices; Cade Cunningham DET @ CHA (`minutes_to_tip=+204`) is NOT in `commenced_teams` and proceeds to normal overwrite; a synthetic Ghost player with `market_implied_prob=None` on the commenced HOU game triggers the WARNING log and allows the first-capture overwrite. All 4 assertions pass. `grep -n "commenced_teams\|tip-off guard\|post-tip" ingest/odds_today.py` shows 5 `commenced_teams` refs + the guard comment + the WARNING log line. Python `ast.parse` clean. **Not touched** per spec: `PRETIP_GRACE_MINUTES` constant, the fetch/dedup logic in steps 1–5, `morning_implied_prob` handling (set-once guard unchanged), `main()` (morning annotation — always pre-tip by design), `compute_edge()`, `save_morning_baseline()`, `_write_pretip_file()`. `ingest/odds_today.py` only. |
| **Opportunity Flags: Odds Enrichment for Tier Cards (2026-04-11)** | Adds FanDuel market implied probability to opportunity-flag tier entries so users can see betting edge at a glance on each opportunity card. Motivated by the 2026-04-10 odds landscape where 67% of picks were FADE (negative calibrated edge) — knowing the market price next to the quant hit rate is critical context when evaluating a late-scratch opportunity. Two-file change, purely additive (no behavioral changes for consumers that don't read the new fields). **File 1 — `agents/lineup_update.py`:** (1) New module path constant `ODDS_AVAILABLE_JSON = DATA / "odds_available.json"` placed next to the existing `INJURIES_JSON` / `MASTER_CSV` constants. (2) New helper `_norm_odds_name(name)` immediately after `_norm()`; strips all punctuation with `re.sub(r"[^a-z0-9 ]", "", name.lower()).strip()` to match the normalization used by `ingest/odds_today.py` (lowercase, collapse hyphens/apostrophes/periods). `re` imported inline inside the helper so the module-level imports are untouched. Deliberately DIFFERENT from `_norm()` which handles team abbreviations — both helpers coexist without collision. (3) New odds-loading block inside `build_opportunity_suggestions()` placed after the `excluded_players` construction and before the `# Morning spread from player_stats` comment. Reads `odds_available.json`, validates `date == TODAY_STR`, extracts the `players` dict into `odds_players: dict`. Graceful no-op on any failure path (missing file, stale date, JSON parse error, any exception) — logs a diagnostic line and proceeds with `odds_players = {}` so the rest of the function is unaffected. (4) Tier enrichment inside the per-prop scanning loop, placed immediately after the existing `tier_entry` dict construction and the `without_player_*` annotation. For each candidate `(teammate_name, prop, tier)` triple: normalize the teammate name via `_norm_odds_name()`, look up `odds_players.get(odds_key, {})`, walk the `prop_odds` list for an entry with matching `tier`, and copy `implied_prob` and `odds` onto the tier_entry as `market_implied_pct` and `market_odds`. Both fields are optional — the tier_entry dict remains valid with or without them. Covers both the qualifying_tiers path (new picks) and the upgrade_tiers path (both share the same `tier_entry` reference). **File 2 — `agents/build_site.py`:** (1) `qtLines` template (opportunity card qualifying-tier rows) extended with a market-price span that renders when `info.market_implied_pct` is present. Format: `(mkt N%)` at font-size 10px, color coded green (`#4CAF50`) when `market_implied_pct <= hit_rate_pct` (system has edge) or orange (`#FF9800`) otherwise (market is more confident than the quant data — no edge). Rounded to integer percent for display. Appended to the existing hr/whr span so the row reads `STAT Ttier  HR% · whr (mkt N%)`. (2) `utLines` template (upgrade-tier rows) given the same treatment with the `(mkt N%)` span appended after the existing `(upgrade)` label. Both templates use f-string double-brace escaping (`{{` / `}}`) because the JS is inside a Python f-string. **Graceful degradation paths:** (a) If `odds_available.json` is missing or stale, `odds_players = {}` and no tier_entry gains market fields → frontend renders exactly as before. (b) If a specific player is in the odds file but the requested tier isn't offered by FanDuel, the tier_entry gets no market fields → that row renders without the `(mkt N%)` span. (c) If `market_implied_pct` is `null` or `undefined`, the JS guard `(info.market_implied_pct !== undefined && info.market_implied_pct !== null)` returns empty string → no span. (d) `market_odds` is copied but currently unused in the display; available for future UI expansion. **Verification:** `grep` confirms constant, helper, load block, enrichment, and template updates are all present. Python `ast.parse` clean on both files. Standalone unit test against real `data/odds_available.json` (137 players, 2026-04-10): (a) `_norm_odds_name` produces correct output for all 5 previously-broken hyphen/apostrophe/period cases (`karlanthony towns`, `deaaron fox`, `cj mccollum`, `jabari smith jr`, matching the fix pattern established in the 2026-04-09 player name normalization bug); (b) 5 of 6 test names match real file keys (SGA missing because OKC isn't playing today — expected, not a bug); (c) synthetic enrichment path successfully looks up Jaylen Brown PTS T20 → `mkt=88.89% odds=-800` and PTS T15 → `mkt=98.36% odds=-6000`; (d) no-market graceful path (Ghost Player) leaves tier_entry untouched — `market_implied_pct` correctly absent; (e) color logic unit test confirms all 5 edge/no-edge scenarios render the correct color (green when mkt ≤ hit, orange when mkt > hit). **Not touched** per spec: `opportunity_flags.json` schema (additive fields only, legacy consumers unaffected), `save_opportunity_flags()`, `compute_lineup_diff()`, dedup key, card_type logic, the opportunity card's left-column player/trigger rendering, all pick-card rendering outside the two template functions. Two files only — `agents/lineup_update.py` + `agents/build_site.py`. |
| **Opportunity flags: filter OUT/DOUBTFUL players (2026-04-11)** | Fixes a bug in `build_opportunity_suggestions()` in `agents/lineup_update.py` where players themselves listed OUT, DOUBTFUL, or OFS were being surfaced as late-scratch opportunities. On 2026-04-10, when Jokic's absence triggered the opportunity scan on the DEN/OKC game, the Opportunities section displayed **SGA, Chet Holmgren, Isaiah Hartenstein, and Jalen Williams** (all confirmed OUT for rest) as opportunities — the same OKC players the analyst had already excluded at the injury pre-filter. Quickley (OUT for TOR) also appeared when his own absence on another game triggered a scan. **Root cause:** the scanning loop only skipped the absent player themselves (`if name_lower == absent_name.strip().lower(): continue`), never checking whether the candidate player being scanned was also unavailable. **Implementation:** four-part change in `agents/lineup_update.py` only. (1) New optional `injuries: dict \| None = None` kwarg on `build_opportunity_suggestions()`. Default `None` preserves backward compatibility — if a caller doesn't pass injuries, the filter is a no-op (same behavior as before). (2) New `excluded_players: set[str]` built near the top of the function, right after the `absence_changes` early return. Iterates the `injuries` dict using the same `team → list of {player_name, status}` convention as the rest of the file (skipping the `fetched_at` metadata key). Any row with `status` matching OUT, DOUBTFUL, or OFS (case-normalized) is added to the set keyed by `player_name.strip().lower()`. (3) New exclusion check in the scanning loop, placed immediately after the existing "skip absent player themselves" guard: `if name_lower in excluded_players: continue`. (4) The `main()` call site at line 1386 now passes `injuries=injuries` — the `injuries` dict is already loaded at line 1276 and in scope at the call site, so no new load is needed. Docstring updated to explain the new parameter. **Unit test** reproduces the 4/10 scenario exactly: synthetic DEN + OKC rosters with Jokic triggering the absence scan, 4 OKC players marked OUT in the injuries dict. Pre-fix behavior: 5 suggestions surfaced (Jamal Murray + the 4 OKC OUT players). Post-fix behavior: 1 suggestion surfaced (Jamal Murray only). The legitimate Murray opportunity (DEN teammate who benefits from Jokic's absence) is preserved; only the broken cases are filtered. Invariant holds: `0 OUT players in post-fix suggestions, down from 4 in pre-fix`. **Not touched** per spec: `compute_lineup_diff()`, `save_opportunity_flags()`, the `opportunity_flags.json` schema, `agents/lineup_watch.py`, `_norm()`, `classify_absent_player()`. `agents/lineup_update.py` only — 4 string insertions (signature + docstring + excluded_players build + exclusion check + call site kwarg). |
| **Analyst: JSON integrity instructions for Scout/Pick/Review prompts (2026-04-11)** | Adds a closing JSON-integrity instruction to the OUTPUT FORMAT section of all four prompt builders in `agents/analyst.py`. Root cause: on 2026-04-10, all three agent calls (Scout, Pick, Review) required `json_repair` fallback on the same run because Claude produced malformed JSON — trailing commas, unterminated strings, and at least one unbalanced brace. The `json_repair` fallback caught these, but the failure rate is high enough to justify a prompt-side mitigation. **Implementation:** identical instruction inserted into four locations: (1) `build_prompt()` (Pick v1, single-call fallback mode) — before the closing `"""`, after the final `skips rules` bullet; (2) `build_scout_prompt()` — after the shortlist JSON schema closing brace, before the closing `"""`; (3) `build_pick_prompt()` (Pick v2, scout+pick mode) — after the final `skips rules` bullet, before `## AUDITOR FEEDBACK FROM PREVIOUS DAYS`; (4) `build_review_prompt()` — after the "After the closing ] you may add a brief note" line, before the closing `"""`. Instruction text (identical in all 4): `"CRITICAL: Verify your JSON is well-formed before stopping. Every opening {{ must have a matching }}. Every opening [ must have a matching ]. Every string must be closed with a matching quote. Do not emit a trailing comma after the last element in any array or object."` The `{{` / `}}` in the source render as literal `{` / `}` in the final prompt (single-level f-string escaping — all four prompts are `return f"""..."""` without nested interpolation). **Deviation from spec:** the task spec suggested using `{{{{` / `}}}}` (4-char) in the Pick v2 and Review inserts because the surrounding schemas use that convention. Empirical test showed 4-char `{{{{` renders as `{{` (two literal braces) in all four prompts, which would produce an incoherent instruction ("Every opening `{{` must have a matching `}}`"). Used `{{` / `}}` (2-char, renders as `{` / `}`) in all four inserts so the instruction describes actual JSON syntax. Rendered all four prompts with dummy args and confirmed each contains the literal text `"Every opening { must have a matching }. Every opening [ must have a matching ]."`. Pre-existing `{{{{` quirk in the Scout, Pick v2, and Review schema examples (which renders JSON schema with `{{` instead of `{`) is unrelated to this task and was not touched — the spec explicitly forbids modifying schema definitions, and Claude has been correctly interpreting the double-brace schema as single-brace JSON in production. The `json_repair` fallback in `call_scout()`, `call_analyst()`, and `call_review()` is retained as a safety net. Verification: `grep -c "Verify your JSON is well-formed" agents/analyst.py` = 4; Python parse clean; all four prompts render the instruction with literal `{` and `}` characters at offsets 70267, 5153, 68419, 6239 respectively. `agents/analyst.py` only — four string insertions, no logic changes. |
| **Analyst: review file staleness check on re-run (2026-04-11)** | Fixes a bug in `apply_review_flags()` in `agents/analyst.py` where same-day analyst re-runs with different pick sets were blocked from writing fresh `picks_review_YYYY-MM-DD.json` files. Root cause: the existence guard at line 4085 treated ANY existing review file as manual (Oliver-written), so same-day re-runs (e.g. after the Scout 4096→12288 token fix on 4/10 that expanded the shortlist from 8 to 30+ players) would find the old auto-generated file and return without writing. Downstream effect on 4/10: 18 new picks from the expanded Scout shortlist had no review coverage, and `build_site.py` rendered them without any review badges. **Implementation:** replaced the simple `if review_path.exists(): return` block with a staleness-aware guard that inspects the existing file's contents. (1) Load the existing file (or treat as empty on JSON parse error); (2) compute `has_manual = any(e.get("source") != "auto" for e in existing_review)` — if ANY entry lacks the `source: "auto"` marker (including mixed auto+manual files), treat the file as manual and return without overwriting; (3) otherwise compute set-difference between the current pick keys `(name.lower(), prop_type, pick_value)` and the existing review keys; (4) if `current - existing` is empty, the existing auto file covers all current picks → skip regeneration (no unnecessary API-burn); (5) otherwise the file is stale → log `"picks_review file is STALE — covers N picks but current run has M (K uncovered). Regenerating."` and fall through to the existing verdict-mapping and write logic. Docstring updated to document the four guard paths (manual never-overwrite, covers-all skip, stale regenerate, missing regenerate). **Key behaviors preserved:** manual review files (any entry without `source: "auto"`) are NEVER overwritten — identical to current behavior. Auto files with complete coverage are never regenerated unnecessarily. **Key behavior changed:** auto files that are stale relative to the current pick set are now regenerated instead of silently skipped. **Unit tests (6 scenarios):** (a) No existing file → `no-file` → regenerate ✓; (b) Existing manual file → `manual` → skip (never overwrite) ✓; (c) Mixed auto+manual file → `manual` → skip (any non-auto entry wins) ✓; (d) Auto file covers all current picks → `covers-all` → skip (no API burn) ✓; (e) **Auto file STALE (4/10 re-run bug case)** — 15-pick auto file vs 33-pick current run → `stale` (18 new picks uncovered) → regenerate ✓; (f) Empty auto file `[]` + picks present → `stale` → regenerate ✓; (g) Corrupt JSON file → treated as empty → `stale` → regenerate ✓. All 6 pass. `agents/analyst.py` only (only the `apply_review_flags()` guard block + its docstring). Untouched: `build_review_context()`, `build_review_prompt()`, `call_review()`, the verdict mapping, the write logic, `main()` call site, `build_site.py` review-badge rendering, the `picks_review_YYYY-MM-DD.json` schema. |
| **picks.json cross-workflow merge guard for injuries.yml (2026-04-10)** | Prevents the race condition where an in-flight `injuries.yml` run clobbers `picks.json` with a stale version after `analyst.yml` pushes new picks between the injuries checkout and commit. 4/10 incident: re-run #3 Analyst committed 33 picks for 2026-04-10 at 15:56:44 PT, and 6 seconds later (15:56:50 PT) an injuries.yml run pushed `Lineup update: picks amended` which reverted picks.json to the pre-rerun 15-pick state — two further lineup commits chained on top of the clobbered state before the incident was noticed. Manually recovered via Option-C merge in commit `a131c1f` (see prior entry). This fix ships an automatic version of the same recovery logic as a guard in the commit path. **Implementation:** (1) New standalone script `ingest/merge_picks.py` compares a local (post-lineup_watch or post-lineup_update) picks.json against the origin (post-`git pull --rebase`) version. If origin has MORE today-picks than local, the analyst must have pushed a new pick set during our run — use origin as the base and overlay local's injury-mutation fields onto matching picks (by key `(player_name.lower(), prop_type, pick_value, date)`). Mutation fields carried from local → origin: `voided`, `void_reason`, `lineup_risk`, `injury_status_at_check`, `injury_check_time`, `lineup_update`. Every other field on each pick is trusted from origin. On any uncaught exception the script falls back to copying local → output and exits 1 — preserving current (pre-guard) behavior so the guard can never make things worse than the status quo. Matches the exact recovery logic that was proven to work on the 4/10 incident. (2) `.github/workflows/injuries.yml` commit path modified in two places: (a) the "Commit picks status" step (after `lineup_watch.py`) and (b) the "Commit lineup amendments" step (after `lineup_update.py`). Each step now, after `git pull --rebase origin main`: (i) saves origin's picks.json to `data/picks.json.origin`, (ii) runs `python ingest/merge_picks.py --local --origin --output` to produce the merged picks.json, (iii) cleans up the `.local` and `.origin` temp files, (iv) falls back to direct `mv "$f.local" "$f"` if either file is missing. The "Commit lineup amendments" step additionally skips picks.json in the original `mv "$f.local" "$f"` restore loop so `opportunity_flags.json` is still restored directly (no race concern for that file — it's an accumulating-log file that only lineup_update writes). The first commit step ("Commit injuries") is unchanged because it doesn't touch picks.json. **Unit tests (5 scenarios via synthetic picks):** (a) Normal path — local and origin have the same today-pick count → no-op, local preserved with its mutations ✓; (b) **Race condition** — local has 15 today-picks, origin has 33 (the 4/10 incident pattern) → MERGE TRIGGERED, origin used as base, 3 overlap picks receive local's `voided`/`lineup_update`/`injury_status_at_check` mutations, 30 fresh picks preserved, 12 local-only phantom picks correctly dropped ✓; (c) Error path — local file is corrupt JSON → falls back to copying local verbatim, exits 1, preserves pre-guard behavior ✓; (d) Analyst reduction — local has 20 picks, origin has 15 → guard doesn't fire (20 ≤ 15 is false), local wins, mutations preserved ✓; (e) Empty today-slate — both 0 today-picks → no-op ✓. **Known limitations** (not blocking; flagged for future hardening if needed): (i) Analyst re-runs that *reduce* pick count (e.g. 33 → 20) are NOT caught by the guard because the heuristic is "origin has more" — the smaller set would get clobbered back to the larger local set. This is rare and recoverable via the same manual process used for the 4/10 incident. (ii) Same-count-but-different-picks scenarios are also not caught. Both edge cases are strict improvements over status quo. **Not touched** per spec: `agents/analyst.py`, `agents/lineup_watch.py`, `agents/lineup_update.py`, `agents/auditor.py`, `agents/parlay.py`, `agents/build_site.py`, `agents/quant.py`, `.github/workflows/analyst.yml`, `.github/workflows/auditor.yml`, `.github/workflows/ingest.yml`. `ingest/merge_picks.py` (NEW), `.github/workflows/injuries.yml` (commit steps 2 and 3 only — step 1 "Commit injuries" unchanged). |
| **Parlay: FADE pre-filter + candidate diversification (2026-04-10)** | Moves the "max 1 FADE leg per parlay" constraint from the Claude prompt into Python pre-filtering inside `build_candidates()`, and adds candidate-pool diversification to prevent a single dominant player from monopolizing the top-N candidates sent to Claude. Root-cause incident: on 4/10, `build_candidates()` produced 15 top candidates where **every single one contained Kawhi Leonard** (his high raw confidence inflated composite scores despite FADE edge). Claude tried to reason through the unresolvable constraint tension ("max 1 FADE leg" when all 15 combos had multiple FADE legs) and output prose analysis instead of JSON, which would have crashed the parlay step (now gracefully handled by the empty-result fallback shipped in the prior fix, but the root cause — unresolvable constraint tension — still needed a Python-side solution). Design principle: **constraint satisfaction (filtering) belongs in Python; curation (selecting the best) belongs in the LLM.** **Three-part fix in `agents/parlay.py` only:** (1) **FADE pre-filter** — new block in `build_candidates()` immediately after the existing trim/human-review check counts `recommendation_tier == "FADE"` legs in each combo; combos with 2+ FADE legs are `continue`d. Graceful when `bet_recommendation` is absent (pre-odds-enrichment days) — the `.get()` chain returns `None`, equality check fails, count stays at 0, combo survives the filter. Every candidate Claude now receives has at most 1 FADE leg — the "all combos have 2+ FADE legs" paralysis scenario is impossible by construction. (2) **Candidate diversification** — after the sort-by-composite-score, if any single player appears in more than 80% of the top `TOP_N_TO_CLAUDE` (15) candidates, the bottom third of the top-N is replaced with the best-scored candidates from the remaining pool that exclude the dominant player. Uses `Counter` from `collections` to count player appearances across the top 15; log line prints the dominant player name, their count, and the number of alternatives substituted in (e.g. `[parlay] Diversity fix: Kawhi Leonard appeared in 15/15 top candidates — replaced bottom 5 with alternatives`). When no alternatives exist (genuine monopoly), the top-N is returned unchanged — graceful degradation. Early return if `len(candidates) <= TOP_N_TO_CLAUDE` skips the diversity check entirely for small candidate pools. (3) **Prompt FADE rules softened to advisory** — the hard constraint "Never include 2+ FADE legs in the same parlay" (rule 6) is replaced with "Candidates with 2+ FADE legs have already been filtered out — every candidate below has at most 1 FADE leg. When a candidate has exactly 1 FADE leg, prefer combos where the other legs are POSITIVE or STRONG to compensate." The AVOID item "Parlays where more than one leg has edge_tier='FADE'" is replaced with "Parlays where the single allowed FADE leg is the weakest leg by confidence AND the other legs are all NEUTRAL — prefer combos where at least one non-FADE leg is POSITIVE or STRONG." Both changes move the language from prescriptive constraint to advisory selection guidance. **Unit tests (synthetic data):** (a) 3 FADE + 2 POSITIVE picks → 3 candidates returned, all with ≤1 FADE leg; (b) 5 all-FADE picks → 0 candidates returned (empty list, no crash, graceful degradation — `main()` short-circuits to `sys.exit(0)` via the prior fix); (c) Dominant "Star" player forced into every top candidate → diversity log line fires, bottom 5 replaced with alternatives, final distribution 10 with Star + 5 without (67% Star, down from 100%); (d) Dominant player with incompatible odds → odds-window filter independently prevents monopoly (0/15 contain the dominant player, proving the other filters still work correctly alongside diversification). **Untouched:** `score_combination()` (composite score formula), odds window filter, negative correlation filter, trim filter, `load_todays_picks()`, `load_player_stats()`, `enforce_concentration_cap()`, `call_parlay_agent()` (graceful-failure fix from earlier in this session), `save_parlays()`, `main()` flow. `agents/parlay.py` only. |
| **Parlay: graceful failure + empty-result handling (2026-04-10)** | parlay graceful failure handling — `call_parlay_agent()` now returns `[]` instead of `sys.exit(1)` on parse failure, with `json_repair` fallback, explicit empty-array check, API error catch, and main() short-circuit on zero parlays. Root-cause incident: on 4/10, 30 picks were generated and odds-enriched successfully, but `parlay.py` crashed with `sys.exit(1)` when Claude returned prose analysis (explaining why it couldn't build valid parlays due to too many FADE legs) instead of a JSON array. Parlay runs BEFORE the commit step in `analyst.yml`, so the crash killed the workflow and prevented all 30 picks from being committed to the repo — parlays are supplementary, a parlay failure should never block pick deployment. **Three-part fix in `agents/parlay.py` only:** (1) `call_parlay_agent()` rewritten — the line-583 `sys.exit(1)` on parse failure is replaced with `return []`, and all downstream failure paths also `return []` instead of raising. New `try/except` around the `client.messages.create()` call catches API errors (timeout, rate limit, auth) and returns `[]` with a warning log. New explicit-empty-array check (`if raw.strip() in ("[]", "[ ]")`) catches the clean case where Claude correctly determined no valid parlays exist. New Fallback 1 (bracket-match extraction) tightened to use `rfind("]")` for proper slice bounds. New Fallback 2 (`json_repair` via the already-installed `json_repair` library used by `analyst.py`) attempts one more recovery before giving up. Final failure path logs the raw response preview and returns `[]`. The ONLY remaining `sys.exit(1)` is the missing-API-key config guard at line 551 — every other failure path now degrades gracefully. (2) `build_parlay_prompt()` OUTPUT FORMAT section extended with an explicit empty-array escape hatch: "If no valid parlays can be constructed from the candidates (e.g. too many FADE legs, insufficient POSITIVE/STRONG legs to anchor a parlay, all combos violate the edge quality rules), return an empty JSON array: `[]`" plus a reinforcing line "Returning `[]` is explicitly acceptable and preferred over reasoning about why parlays can't be built. NEVER output prose analysis — always output a JSON array, even if it's empty." This fixes the root cause of the 4/10 incident: Claude's prose response was reasonable behavior because the prompt never told it what to do when no valid parlays existed. (3) `main()` post-Claude block adds a graceful short-circuit — if `call_parlay_agent()` returns `[]`, print "No parlays to save — skipping save step" and `sys.exit(0)`. This bypasses `enforce_concentration_cap()` and `save_parlays()` entirely, avoiding writing an empty bundle to `parlays.json` on days when no valid parlays can be constructed. **Spec invariants verified:** (a) exactly one `sys.exit(1)` remains in the file (the API key config check); (b) the new failure path (Claude returns prose → parse fails → returns `[]` → main() prints skip message → `sys.exit(0)`) allows `analyst.yml` to continue to the commit step and deploy picks; (c) all existing logic untouched — `build_candidates()`, `score_combination()`, `enforce_concentration_cap()`, `save_parlays()`, `load_todays_picks()`, `load_player_stats()` are all unchanged; (d) FADE leg limits, edge quality rules, and parlay selection criteria in the prompt are unchanged (only the output escape hatch was added). `agents/parlay.py` only — no other files touched. Downstream effect: parlay parse failures are now non-fatal, parlay runs on "unfavorable" days (too many FADE legs) cleanly produce zero parlays, and the 4/10 incident class of workflow-killing crashes is eliminated. |
| **Frontend: Display calibrated confidence on pick cards (2026-04-10)** | Pick card headline confidence now shows `bet_recommendation.calibrated_prob` (the system's actual expected hit rate after historical calibration) instead of the raw stated `confidence_pct`. Raw stated confidence moved to the Odds + Sizing drawer as the first line (`System conf: X%`). **Motivation:** the previous display created a visual mismatch — the card showed "84% conf" next to "NEUTRAL (+2.8pp)" but the user couldn't reconcile the numbers because the edge calculation uses the calibrated value (88.3%), not the raw 84%. Now the headline and the edge are visually consistent: e.g., Tyrese Maxey PTS T20 on 4/10 shows `85% conf` with `FADE (-8.5pp)`, which reads as 85 − 93.3 ≈ −8.3 (rounds to the displayed -8.5). **Three-part implementation in `agents/build_site.py` only:** (1) New JS helper `displayConf(p)` returns `Math.round(bet_recommendation.calibrated_prob)` when available, falls back to `confidence_pct` when `bet_recommendation` is null/absent/NO_MARKET. The fallback preserves pre-odds-integration behavior for any pick without market data. (2) Three headline render sites updated: regular pick card (`<div class="conf-line">`), top-picks card (`<div class="tp-conf">`), best-bets card (`<div class="tp-conf">`). All three now use `${displayConf(p)}% conf` instead of `${p.confidence_pct}% conf`. No CSS changes — the existing `conf-line` and `tp-conf` classes use flat colors (`var(--muted)` and `var(--accent)`) so there was no conditional color logic to preserve. (3) `buildOddsSizing()` drawer gained a new first line `System conf: X%` sourced from `p.confidence_pct`, guarded by `!= null` so picks with missing confidence still render cleanly. Placed above Market line as the first drawer item to group system-side info at the top. **Untouched per spec:** `bet_recommendation` computation in `ingest/odds_today.py`, `confidence_pct` on picks (still the raw stated value in the data), `buildEdgeLine()` (still reads `br.recommendation_tier` + `br.calibrated_edge_pct`), Results tab pick history (shows raw `confidence_pct` in the historical table), Parlays tab leg display, lineup-update morning/revised reasoning text, and Python-side scoring logic in `get_top_picks()` + best-bets sort tiebreaker (still uses raw `confidence_pct` for ranking). **Browser-verified** on the 2026-04-10 15-pick slate: 15 regular pick cards and 3 top-picks cards all rendered with calibrated values (85, 88, 85, …); sample drawer showed `System conf: 74%` as the first line; 18 edge badges rendered unchanged with correct FADE/NEUTRAL/STRONG coloring. `agents/build_site.py` only. |
| **Analyst: Scout token budget + shortlist validation fix (2026-04-10)** | Root cause: `SCOUT_MAX_TOKENS = 4096` physically limited the Scout call to ~8-9 structured shortlist entries before JSON truncation, because each entry in the compact Scout output schema requires ~400–500 tokens (player name + team + props + tier ranges + priority + notes). The Scout prompt said "Target 20–25 players" but the output budget made that impossible. On 2026-04-10, a 57-player slate produced a Scout shortlist of only 8 players — the JSON repair path triggered and the Pick stage never saw strong candidates like Brunson (PTS≥20 at 75%), KAT (PTS≥15 at 85%), OG Anunoby (PTS≥10 at 90%), or 40+ others. The model was pre-filtering aggressively to avoid mid-JSON truncation. **Three-part fix in `agents/analyst.py` only:** (1) `SCOUT_MAX_TOKENS` raised 4096 → 12288 (25 entries × 500 tokens + preamble overhead = ~12,500 tokens; 12288 gives headroom without waste). The Scout output is pure JSON with no tier_walk reasoning, so token density is predictable. (2) New "shortlist undersize check" in `main()` immediately after `shortlist_names` is printed: if `active_count >= 40` AND `len(shortlist_names) < 12`, print a warning and fall back to single-call `build_prompt()` mode which sees the entire quant context. Uses the identical fallback pattern as the existing `<5 matched` check (same `build_prompt()` args, same `save_picks`/`save_skips` + `return` sequence). Gated on large slates only — normal-slate Scout behavior (<40 active players) is unchanged. (3) `build_scout_prompt()` INCLUSION GUIDANCE rewritten with stronger imperative language: "CRITICAL: You are the only gate between 57 eligible players and the Pick agent. Any player you exclude here CANNOT receive a pick today — there is no second chance." Explicitly names B2B, tough defense, and blowout risk as NON-reasons to exclude (those are Pick agent penalty rules, not Scout gates). Explicitly forbids the Scout from applying tier-walk or confidence penalty reasoning — that's the Pick agent's job. Cost impact: ~$0.03–0.05 per run at Opus pricing (the large-slate Scout model), negligible vs the ~$0.36/day total. `agents/analyst.py` only — no changes to `call_scout()` JSON parsing, `call_analyst()`, `call_review()`, `build_pick_prompt()`, `build_review_prompt()`, `MAX_TOKENS`, `MODEL`, `MODEL_LARGE`, or `LARGE_SLATE_THRESHOLD`. |
| **Backtest: H26 star absence teammate impact (2026-04-10)** | New `--mode star-absence` added to `agents/backtest.py`. Motivated by 4/9 Tatum scenario — his PTS pick was penalty-skipped but he scored 24 on a T20 when Jaylen Brown was confirmed OUT. Measures teammate tier hit rate deltas (with vs without the team's leading scorer) across PTS/REB/AST/3PM at every valid tier. **Implementation:** new module constants `SA_MIN_STAR_GAMES=20` (games to qualify as team's leading scorer), `SA_MIN_ABSENT_GAMES=5` (minimum star-absent games to analyze a team), `SA_MIN_CONDITION_N=3` (minimum games per with/without bucket per teammate). `STAR_ABSENCE_JSON = DATA / "backtest_star_absence.json"`. New `run_star_absence_analysis(player_log, args)` function: (1) loads the raw `player_game_log.csv` directly so DNP rows are available for absence detection (the shared `load_player_log()` already filters DNPs out); (2) applies `--start` / `--end` / `--season` filters to the full DataFrame; (3) identifies each team's leading scorer from the DNP-filtered `player_log` by highest PTS avg with ≥20 games, broken by `drop_duplicates(subset=team, keep=first)` after sorting PTS avg descending; (4) for each qualifying team, computes star-absent dates as `team_dates − star_played_dates` (team played but star didn't — DNP or missing row entirely); (5) per teammate, splits rows by `_star_absent` flag and computes tier hit rate deltas at every tier; (6) population-level aggregation across all teams uses weighted average by `min(n_with, n_without)` with unweighted comparison and positive-delta percentage; (7) verdict section prints SIGNAL / NOISE / negative classification at key tiers (PTS_T15/T20/T25, AST_T4/T6). Mode wired into `main()` dispatch between `elite-opp-rebounder` and the calibration-only fast path. `--mode` help string extended with `'star-absence'`. **Production run result (14 teams, 31 teammate observations, 2025-10-21 → 2026-04-09):** PTS_T15 +11.3pp (77% positive), PTS_T20 +12.7pp (71% positive), PTS_T25 +10.5pp (71% positive), AST_T4 +9.6pp (71% positive), AST_T6 +6.7pp (55% positive) — all five key tiers cross the +3pp SIGNAL threshold with ≥10 observations. Motivating BOS scenario confirmed: Tatum PTS T20 with Brown=72.7% / without Brown=100% (4 games), Δ=+27.3pp; Pritchard PTS T20 with=28.8% / without=100% (6 games), Δ=+71.2pp. Invariant check: 31/31 teammate `n_with + n_without` matches raw ground truth (no double-counting). **One bug found and fixed during verification:** initial sort key `x.split("T")[1]` crashed on PTS tier keys because `"PTS_T20".split("T") == ['P', 'S_', '20']`; fixed to `x.split("_T")[1]`. Downstream consumers (not yet wired): skip re-evaluation feature in `lineup_update.py` and absence-driven analyst confidence rule; both deferred pending multi-season verification and confirmed-OUT gate alignment with existing Without-Star Baseline rule philosophy. `agents/backtest.py` only — no other files touched. |
| **Pre-Game Reporter: team abbreviation fix + ESPN error split (2026-04-10)** | Two bugs in `agents/pre_game_reporter.py` — 10 players silently dropped and 58 ESPN errors lumped into an undiagnosable combined list. **Fix 1 (team abbreviation)**: `load_target_players()` now checks BOTH `team_abbr` (Rotowire-style: NYK, SAS, GSW) AND `team_abbr_alt` (ESPN-style: NY, SA, GS) from `playerprops/player_whitelist.csv` when matching against `teams_today` (derived from `nba_master.csv`, which uses ESPN short codes). Previously only `team_abbr` was checked, so every NYK/SAS/GSW player was filtered out before any ESPN fetch. Recovered 10 players: Brunson, KAT, OG Anunoby, Mikal Bridges (NYK); Wembanyama, De'Aaron Fox, Stephon Castle, Devin Vassell, Dylan Harper (SAS); Stephen Curry (GSW). Verified offline by passing `teams_today={'NY', 'SA', 'GS'}` — returned exactly 10 players matching the expected list. Mirror fix in parallel to the 2026-03-25 whitelist-tuple fix that had already addressed the same root cause in `agents/quant.py` and `agents/analyst.py`. **Fix 2 (error split)**: `main()`'s single `fetch_errors: list[str]` replaced with three typed lists — `no_id_errors` (no athlete_id in `player_dim.csv`), `espn_errors` (ESPN HTTP/network failure), `no_news_players` (ESPN succeeded but returned no items). `fetch_errors` is retained as `no_id_errors + espn_errors` for backward compatibility. Log line rewritten to show all four counts (`no_id` / `HTTP` / `no_news` / `with items`) — the fourth is computed from the total minus the other three so every target player is accounted for. `write_output()` signature extended with three new optional kwargs (defaulting to `[]`), all three lists written as top-level keys in `pre_game_news.json`. `write_empty()` keeps working unchanged — its call to `write_output()` inherits the new defaults. The spec's accountability invariant holds: every target player ends up in exactly one of `player_notes` (with items that survived Claude summarization) / `no_id_errors` / `espn_errors` / `no_news_players` / implicit "items fetched but not noteworthy" (raw items present but Claude produced no note). No workflow files touched. Downstream compatibility: analyst reads `pre_game_news.json` only for `player_notes` / `game_notes` / `suggested_context_updates` / `staleness_flags` — the new diagnostic fields are additive and don't affect any consumer. `agents/pre_game_reporter.py` only. |
| **Odds: morning baseline capture + pre-tip sweep timing fix (2026-04-10)** | Two related bugs that together prevented `morning_implied_prob` from ever being written to `picks.json`, blocking CLV computation in `auditor.save_audit()`. **Primary fix** — `ingest/odds_today.py main()` now writes `morning_implied_prob` directly in its match block (after `odds_fetched_at`), guarded by `if "morning_implied_prob" not in pick`. Previously only `pretip_sweep()` wrote this field, and pretip was failing to execute due to timing issues (see secondary). Re-run safe: the guard preserves the original baseline across same-day re-runs of `main()`. **Secondary fix** — `pretip_sweep()` timing relaxed to survive GitHub Actions cron delays: new module constant `PRETIP_GRACE_MINUTES = 30` allows capture of games that started up to 30 minutes ago (FanDuel pre-game lines linger briefly after tip); default `window_minutes` widened from 75 → 360 (6h) in both the function signature and the argparse default — combined with existing event_id deduplication, this costs nothing extra (each game is still fetched at most once). Filter updated from `minutes_to_tip < 0` to `minutes_to_tip < -PRETIP_GRACE_MINUTES`. Log lines + docstring updated to reflect new grace window. **Workflow fix** — `.github/workflows/odds_pretip.yml` cron schedule rewritten from 10 entries every 30 min 3:00–7:30 PM PT → 8 entries hourly noon–7 PM PT. GitHub's typical 1–6h cron delays meant the old 3 PM start almost always executed after games had tipped; starting at noon gives 4+ hours of buffer before earliest tip-offs. Design invariant after the fix: every matched pick has both `market_implied_prob` AND `morning_implied_prob` after the morning `analyst.yml` run; if pretip later executes it overwrites `market_implied_prob` with pre-tip odds but leaves `morning_implied_prob` untouched; CLV = `market_implied_prob (pretip) − morning_implied_prob (morning)`. If pretip never runs, the two fields stay equal and CLV degrades gracefully to 0 (no movement detected). Verification: grep confirms `morning_implied_prob` in both `main()` and `pretip_sweep()` under the same guard; `PRETIP_GRACE_MINUTES` referenced in constant, filter, docstring, and log lines; `360` in both function signature and argparse default; 8 cron entries in workflow file; `python ingest/odds_today.py --help` shows all three modes with `--window-minutes` default 360. `ingest/odds_today.py`, `.github/workflows/odds_pretip.yml`. |
| **Frontend — Playoff Career Profiles panel + Playoff Game Explorer (2026-04-10)** | Research tab in `agents/build_site.py` now shows three stacked collapsible sections instead of one. (1) **Playoff Career Profiles** — sortable overview cards for every whitelisted player with ≥5 career playoff games (58 players). Each card shows colour-coded PTS/REB/AST/3PM/FG% deltas (playoff − regular, same-season comparison), season chips (`'22: 24g`), playoff per-game averages, small-sample badge for <10 games, and a `<details>` Per-season breakdown table. Sort buttons: Games / PTS Δ / REB Δ / AST Δ / 3PM Δ / FG% Δ / A–Z. Clicking a sort button toggles direction on repeat click (ascending for A–Z by default, descending elsewhere). Panel is `<details open>` by default. (2) **Playoff Game Explorer** — a separate filterable explorer (closed by default) with player / stat / season / round / opponent / home-away dropdowns. Produces the same visual pattern as the existing current-season Player Explorer: stat strip (avg/median/min/max), tier hit rate table with bar charts coloured by prop (PTS/REB/AST/3PM), and game log grouped by series with inferred round labels (R1 / R2 / CF / Finals). Round number is inferred from sequential opponent changes within each season during the Python build step. (3) **Player Explorer** — the existing current-season explorer, extracted into a new `renderCurrentSeasonExplorer(container)` wrapper and rendered into its own `<details open>` section with a "Player Explorer — 2025-26 season game logs" header. The extraction is byte-for-byte: `runExplorer()` stays global and unchanged, all `ex-*` DOM ids preserved, filter logic untouched. New Python function `build_playoff_data()` reads `data/playoff_career_log.csv` once at build time, filters zero-minute rows and All-Star abbrevs (EAST/WEST), loads active whitelist team lookup, groups by player, computes playoff vs regular deltas (FG% from aggregate totals), builds per-season breakdown and per-game list with inferred series numbers, returns `{profiles: [...], games: {...}}`. Graceful no-op when `playoff_career_log.csv` is missing. New JS helpers: `ROUND_LABELS` dict + `roundLabel()` + `round1()`, plus `renderPlayoffProfiles()`, `renderPlayoffExplorer()`, `runPlayoffExplorer()`, and `renderCurrentSeasonExplorer()`. `renderResearch()` is rewritten to create three `<div>` containers and delegate to the three render functions. `DATA.playoff` is embedded in the JS DATA object alongside `DATA.explorer`. Pure static, no LLM calls, no workflow changes. **Offline build result**: 58 profiles, 1,879 total playoff game rows, site size 1,138,619 → 1,529,470 bytes (+390 KB). **Browser verification** (via preview server): all 4 JS functions defined, 58 profile cards rendered on Research tab, 3 sections present (profiles open by default, playoff explorer closed, current-season open), 7 sort buttons, 59 players in playoff player dropdown, 69 in current-season dropdown. Sort interactions: clicking PTS Δ swaps Banchero (+4.3) to first card, active button highlighted in accent orange. Filter test: Tatum × 2023-24 × R1 shows 5 games vs MIA, avg 21.8 PTS, small-sample warning. Current-season Player Explorer runs unchanged (LeBron PTS query still renders tier table and game log exactly as before). `agents/build_site.py` only — no other files touched. |
| **Daily ingest `season_type` integration + playoff dual-write (2026-04-10)** | Added `season_type` column to the daily ingest pipeline so regular-season and postseason games can be distinguished without relying on manual backfill re-runs. `ingest/espn_daily_ingest.py`: new `season_type` key in `MASTER_COLUMNS` (position 4, after `game_time_utc`) and extracted in `parse_scoreboard()` via `safe_int((ev.get("season") or {}).get("type"))` — returns int 1/2/3 or `None` when missing. Docstring updated. `ingest/espn_player_ingest.py`: new `load_master_season_type_lookup(master_path)` helper reads `nba_master.csv` once and returns `{game_id: int|None}` (graceful: returns `{}` on missing file or missing column). `ingest_events()` signature extended with `season_type_lookup` param; player row dicts carry `season_type` after `season_end_year`. Output `col_order` updated. New `append_playoff_rows(player_rows)` dual-write function called after `player_game_log.csv` is written — filters to `season_type == 3` rows, transforms to `PLAYOFF_LOG_COLUMNS` schema, upserts by `(player_id, game_id)` against `data/playoff_career_log.csv`, sorts by `(player_name, season, game_date)` matching backfill convention, writes atomically. Entirely inert during regular season (returns immediately when zero playoff rows — no file I/O, no output message). Failure non-fatal (wrapped in try/except with warning log). `.github/workflows/ingest.yml`: `data/playoff_career_log.csv` added to the commit `FILES` list — during regular season the file will not change and `git status` will show it clean, so no noise in commit history until playoffs start. **Verification**: ran both ingests against 2026-04-09 (6 FINAL games, regular season); nba_master.csv has `season_type` at column 4 with 21 new rows at value 2 and 1,203 historical rows at NaN; player_game_log.csv has `season_type` at column 2 with 36 new rows at value 2 and 9,429 historical rows at NaN; `playoff_career_log.csv` unchanged at 20,139 rows (no "Playoff dual-write" log line printed — no-op confirmed). Synthetic playoff-row unit test against the dual-write function confirmed: (a) mixed regular+playoff call writes only the playoff row (1 new), (b) idempotent re-run produces 0 new / 1 updated, (c) NaN `season_type` is not treated as playoff. Quant re-run verified no regressions — 68 players in output, 58 with `playoff_profile`, SGA still at 33 playoff games. `ingest/espn_daily_ingest.py`, `ingest/espn_player_ingest.py`, `.github/workflows/ingest.yml`. |
| **Playoff career splits quant integration (2026-04-09)** | New `compute_playoff_splits(player_name, playoff_df)` in `agents/quant.py`. Reads `data/playoff_career_log.csv` (2021–2025, populated by `ingest/espn_playoff_backfill.py`) and computes career playoff vs regular-season averages and deltas for PTS, REB, AST, 3PM, and FG%. Regular-season comparison is restricted to the same seasons where the player has playoff data — apples-to-apples. FG% is computed from aggregate FGM/FGA totals (not per-game mean of percentages). Returns `None` when `playoff_df` is None, player not found, or player has fewer than `PLAYOFF_MIN_GAMES = 5` career playoff games. New `load_playoff_career_data()` helper loads the CSV once at the top of `build_player_stats()` (filters zero-minute rows) and is graceful — returns `None` without crashing when file is missing. New `PLAYOFF_CAREER_LOG` path constant added near existing CSV constants. `playoff_profile` field wired into the per-player dict in `build_player_stats()`, positioned after `shooting_regression`. `agents/analyst.py`: new `PLAYOFFS_R1_DATE = "2026-04-18"` constant (separate from `PLAYOFF_START_DATE` — play-in slate 4/14–17 is excluded because play-in teams have thin/no playoff history). `build_quant_context()` renders a compact annotation line after the H2H block when `TODAY_STR >= PLAYOFFS_R1_DATE` (inclusive) and `playoff_profile` is non-null: `  PLAYOFF PROFILE (61g, 5 seasons): PTS +3.2 | REB +1.1 | AST -0.3 | 3PM +0.1 | FG% +1.3%`. `(small sample)` suffix appended when `playoff_games < 10`. **Annotation-only — no directive rules, no confidence modifiers, no tier adjustments.** Offline verification against the 20,137-row CSV: 58/65 players have profiles (7 below threshold), 11 small-sample (5–9 games), zero invariant violations across all `playoff_avgs - regular_avgs = deltas` checks and FG% consistency. Data sanity check confirmed real NBA narratives (Brunson +4.0 PTS playoff boost, Kawhi +3.9, Tatum −2.8 FG%, Herro −6.9 PTS, Embiid −4.9 PTS). `agents/quant.py`, `agents/analyst.py`. |
| **Player name normalization mismatch in athlete ID lookups (2026-04-09)** | `data/player_dim.csv` stores `player_name_norm` with hyphens replaced by spaces and apostrophes/periods removed, but three lookup callers (`ingest/espn_playoff_backfill.py`, `agents/pre_game_reporter.py`, `agents/post_game_reporter.py`) queried the map with raw `.lower()` which preserves those characters. Result: 5 active whitelisted players were silently skipped in every athlete ID lookup — Karl-Anthony Towns, Shai Gilgeous-Alexander, De'Aaron Fox, Nickeil Alexander-Walker, Jabari Smith Jr. Impact: pre-game reporter fetched no ESPN news for them (counted as fetch errors), post-game reporter fetched no exit news, playoff backfill script collected zero playoff career data. Fix: added identical `_norm_name()` helper to all three files (hyphens → space, apostrophes/periods stripped, whitespace collapsed, lowercase). Applied at all lookup call sites: 1 in `espn_playoff_backfill.main()`, 1 in `pre_game_reporter.main()` (left `target_names_lower` headline-matching set as raw `.lower()` per spec — it's used for substring match, not dim lookup), 2 in `post_game_reporter` (news-fetching loop and DNP fallback loop — both matched the bug pattern). Backfill re-run for the 5 players added **+1,617 rows** (20,139 total, 65 players). All 68 active whitelisted players now resolve athlete IDs correctly (3 remaining gaps are 2025-draft rookies with no pre-2026 NBA history — expected, not a bug). Unit tests verified: `_norm_name('Karl-Anthony Towns')='karl anthony towns'`, `_norm_name("De'Aaron Fox")='deaaron fox'`, `_norm_name('Jabari Smith Jr.')='jabari smith jr'`, idempotent on already-normalized input, no-op on names without special characters. Not refactored into a shared module — per spec, these files are standalone and no shared utils module exists. `ingest/espn_playoff_backfill.py`, `agents/pre_game_reporter.py`, `agents/post_game_reporter.py`. |
| **ESPN Playoff Career Backfill script (2026-04-09)** | New standalone local-run script `ingest/espn_playoff_backfill.py` fetches 2021–2025 regular season AND playoff gamelogs for active whitelisted players from the ESPN public v3 gamelog endpoint (`site.web.api.espn.com/apis/common/v3/sports/basketball/nba/athletes/{id}/gamelog`). Probe-first format discovery via `--probe` flag writes `data/probe_gamelog_response.json` for manual inspection. Actual ESPN response format differs from all three patterns in the original spec: top-level `labels` list, top-level `events` dict keyed by event_id (game metadata), and `seasonTypes[].categories[].events[]` with inline `{eventId, stats:[array]}` parallel to the top-level labels. Compound shooting fields (`FG="11-21"`, `3PT="3-7"`, `FT="5-7"`) split inline by `_split_compound()`. Season type filtering via `_season_type_matches()` accepts `Postseason` for type 3 and `Regular Season` (including Play-In Regular Season) for type 2; `Preseason` skipped. API returns all season types regardless of `seasontype` query param — parser filters by `displayName` substring. Legacy Pattern B/C parsers kept as fallbacks. Writes `data/playoff_career_log.csv` (21 columns, upsert idempotent on `(player_id, game_id, season_type)`). Full backfill run complete: **18,522 rows (16,768 regular / 1,754 playoff) for 60 players** across seasons 2021–2025. Zero null PTS/minutes, zero FGM>FGA / 3PM>3PA / FTM>FTA invariant violations. 5 active whitelisted players missing from `player_dim.csv` due to name normalization (Karl-Anthony Towns, Shai Gilgeous-Alexander, De'Aaron Fox, Nickeil Alexander-Walker, Jabari Smith Jr.) — logged as warnings, not fatal. Downstream consumer: future `compute_playoff_splits()` in `quant.py` (separate prompt). `ingest/espn_playoff_backfill.py` (NEW), `data/playoff_career_log.csv` (NEW). |
| **H2H Splits — per-opponent tier hit rates annotation (2026-04-09)** | Added `compute_h2h_splits()` to `agents/quant.py` — computes per-stat tier hit rates from the player's full season game log against today's specific opponent. `H2H_MIN_GAMES = 2` threshold (returns `None` below). Wired into `build_player_stats()` as new `h2h_splits` field in `player_stats.json` (positioned between `matchup_tier_hit_rates` and `spread_split_hit_rates`). DNP exclusion applied once to the filtered H2H subset — `games` count is shared across all tiers and stats (sample invariant). Rendered in `agents/analyst.py` `build_quant_context()` as a compact annotation line appended to `stat_parts` after the per-stat loop: `H2H vs BOS (4g): PTS T20 3/4(75%) | AST T6 3/4(75%)`. Shows the highest qualifying tier (≥70%) per stat; no line rendered when no stat qualifies or when H2H is null. **Annotation-only — no directive rules, no confidence adjustments, no tier step-downs.** Analyst reads H2H as context and applies its own judgment on small samples. Replaces the formal H9 backtest — annotation-only deployment does not require backtest validation at small samples (n=2–4 reflects deliberate defensive attention in divisional matchups and playoff series; qualitatively different from noise). `agents/quant.py`, `agents/analyst.py`. |
| **Playoff Context Block — date-gated analyst prompt (2026-04-09)** | Added `PLAYOFF_START_DATE = "2026-04-14"` constant and `build_playoff_context()` function to `agents/analyst.py`. Returns `## PLAYOFF CONTEXT — POSTSEASON MODE` block on/after 4/14, empty string before. Injected into all 3 prompt builders: `build_prompt()` and `build_scout_prompt()` via `{playoff_context_section}` variable (after playoff picture, before leaderboard), `build_pick_prompt()` via inline call (after scout shortlist, before series context). Annotation-only — provides behavioral framing for postseason play (rotation tightening, pace compression, play-in intensity, series dynamics, data window caution). No directive rules, no confidence penalties. Complementary to `series_context` from playoff_matchup.py (matchup-specific data). `agents/analyst.py` only. |
| **CLV Tracking — Layer 2 (2026-04-09)** | Closing Line Value tracking added. `ingest/odds_today.py`: `pretip_sweep()` now persists `morning_implied_prob` on each pick before overwriting with pretip odds (set-once guard prevents later overwrites). `agents/auditor.py`: per-pick `clv_pp` computed in `save_audit()` as `market_implied_prob − morning_implied_prob` (positive = beat the close). `save_audit_summary()` aggregates `clv_summary` block: total picks with CLV data, beat/lost/no-movement counts, avg CLV pp, and hit rate split by CLV direction. Silently skipped for picks without pretip data — no crash, no incorrect data. Answers the key question: does beating the closing line predict higher hit rates? `ingest/odds_today.py`, `agents/auditor.py`. |
| **Pre-Tip Odds Sweep — Layer 1 (2026-04-09)** | Game-time-aware pre-tip odds fetching added to `ingest/odds_today.py`. New `--pretip` CLI mode runs `pretip_sweep()` — fetches FanDuel odds for games tipping within a configurable window (default 75 min), updates `picks.json` with latest odds, calls `compute_edge()` to recompute calibrated edge, and logs line movement vs morning baseline. New helper `_parse_fanduel_outcomes()` extracted from `main()` inline parsing — shared by both `main()` and `pretip_sweep()`. `main()` now saves morning baseline to `data/odds_pretip.json` via `_save_morning_baseline()`. Pre-tip sweeps append per-event snapshots to the same file (deduped by event_id — never re-fetches). New workflow `.github/workflows/odds_pretip.yml`: 10 cron entries every 30 min from 3:00–7:30 PM PT, commits `odds_pretip.json` + `picks.json`, uses copy-aside pattern. Most runs are no-ops (0 credits) — only games in the pre-tip window incur API cost (~32 credits/day avg). Enables future CLV tracking (Layer 2) and frontend movement indicators (Layer 3). `ingest/odds_today.py`, `.github/workflows/odds_pretip.yml` (NEW). |
| **Python market gate enforcement in save_picks() (2026-04-09)** | Added `enforce_market_gate()` to `agents/analyst.py` — third post-processor in the `save_picks()` chain after `filter_self_skip_picks()` and `reconcile_pick_values()`. Cross-checks every emitted pick against `odds_available.json`; rejects picks where the player or player+prop+tier has no FanDuel market. Uses `load_available_markets()` (existing function). Name normalization matches `odds_today.py` `_norm_name()` pattern. Gate disabled entirely when `odds_available.json` is missing or stale — never blocks picks due to absent odds data. Catches Claude prompt compliance failures (e.g. Pritchard PTS T10 emitted on 4/9 despite zero FanDuel markets for that player). `agents/analyst.py` only. |
| **Pre-game reporter staleness token fix + ESPN error logging (2026-04-09)** | Fixed `call_claude_staleness_check()` in `agents/pre_game_reporter.py`: `max_tokens` raised from hardcoded 1024 to module constant `MAX_TOKENS` (2048) — truncation was causing JSON parse failures on large context files. Added `json_repair` fallback for truncated responses (same pattern as analyst.py). Split ESPN fetch error logging in `main()` into separate `no_id_count` (missing athlete_id in player_dim.csv) and `http_fail_count` (ESPN API failures) for diagnostics. `fetch_errors` list still populated identically — downstream code unaffected. `agents/pre_game_reporter.py` only. |
| **Removed trim/caution badges from frontend (2026-04-09)** | Removed `trim` verdict from `reviewBadge` JS rendering in `agents/build_site.py`. Trim picks hit at 92.9% vs keep at 89.0% — the "⚠ Caution" / "🤖 Auto-Review" badge was actively misleading, flagging the system's safest picks. Review data preserved in `picks_review_*.json` for offline analysis. `manual_skip` / "⚠ Flagged" badge retained (92.3% precision on 13 picks). CSS class `.review-badge-trim` left as harmless dead code. `agents/build_site.py` only — JS review badge logic, no Python changes. |
| **Alt-tier edge display in Odds + Sizing drawer (2026-04-09)** | `agents/build_site.py` extended with alt-tier display inside the existing Odds + Sizing drawer. New `enrich_alt_tiers()` function joins `odds_available.json` (FanDuel lines at all tiers) with `player_stats.json` (quant tier hit rates) to produce a list of alternate tiers per pick. Drawer shows non-picked tiers with market-implied prob and quant hit rate; rows where hit rate exceeds market prob by ≥5pp highlighted in teal (`has-edge` class). New path constants: `ODDS_AVAILABLE_JSON`, `PLAYER_STATS_JSON`. New CSS: `.alt-tiers-header`, `.alt-tier-row`, `.alt-tier-row.has-edge`. `buildOddsSizing()` JS updated to render alt-tier rows after existing market data; all lines wrapped in `<div>` (was `<br>`-separated text). Graceful no-op when either data file is missing or stale. Informational only — no pick system changes. `agents/build_site.py` only. |
| **`walked_tier` field + reconciler rewrite (2026-04-09)** | Added `walked_tier` integer field to pick JSON schema (both `build_pick_prompt()` and `build_prompt()` copies). Reconciler now reads `walked_tier` directly instead of parsing tier_walk prose via regex. Primary path trusts the analyst's explicit declaration — no one-tier-step guard needed. Regex logic preserved as fallback for transition (walked_tier missing). Updated pick_value output contract to reference both fields. Fixes OG Anunoby PTS T15→T10 mismatch that went uncorrected on 4/9 (11/12 picks logged RECONCILE_SKIP due to verbose tier_walk prose defeating regex patterns). `agents/analyst.py` only. |
| **Best Bets section + Top Pick edge tiebreaker (2026-04-08)** | Added `💰 BEST BETS` section to frontend showing POSITIVE and STRONG edge picks ranked by calibrated edge descending. Cards reuse Top Picks layout with teal/green border-left, edge label, Odds + Sizing drawer. Python data prep: `best_bets` list filtered from `today_picks` on `recommendation_tier in (POSITIVE, STRONG)`, sorted by `calibrated_edge_pct` descending. JS: `renderBestBets()` function, called after `renderTopPicks()`. CSS: `.best-bets-header`, `.best-bet-card`, `.bb-edge` classes. HTML: `#best-bets-container` div between top-picks and picks containers. Section hidden when no qualifying picks. Also: added EDGE TIEBREAKER language to analyst `## TOP PICKS` prompt block (both `build_pick_prompt()` and `build_prompt()` copies) — prefer POSITIVE/STRONG edge picks when prediction quality is comparable. Renamed header from "⚡ TOP PICKS TODAY" to "⚡ TOP PICKS". `agents/analyst.py` (prompt text only), `agents/build_site.py`. |
| **Frontend odds display fixes (2026-04-08)** | Two bugs in `agents/build_site.py`: (1) `buildOddsSizing()` multiplied `calibrated_prob` by 100, but the field is already a percentage from `audit_summary.json` `hit_rate_pct` — displayed 8740.0% instead of 87.4%. Removed `* 100`. `kelly_quarter` line unchanged (correctly uses `* 100` — that field IS a decimal). (2) `renderTopPicks()` rendered `buildEdgeLine(p)` (tier label) but not `buildOddsSizing(p)` (expandable drawer). Added `buildOddsSizing(p)` between `lineupUpdateBadge` and `tierWalk` in the Top Picks card template, matching regular pick card order. `agents/build_site.py` only. |
| **Post-processor greedy regex fix — `reconcile_pick_values()` (2026-04-08)** | Strategy 1 regex in `reconcile_pick_values()` used `[^\d]*` (unbounded non-digit gap) between step keywords and destination tier digits. Matched `2` from `B2B` prose ~40 chars after a `→` keyword, producing false AST T4→T2 corrections. Two picks affected on 4/8 (SGA AST, Fox AST) — both overridden to T2, then tagged NO_MARKET (no FanDuel line at AST T2). Fix: `[^\d]*` → `[^\d]{0,20}` caps the gap to 20 characters. Legitimate step-down patterns span ≤12 chars. One-tier-step guard and Strategy 2 regex unchanged. `agents/analyst.py` only — single regex edit in `reconcile_pick_values()`. |
| **Odds calibration key mismatch fix (2026-04-08)** | `_load_calibration_bands()` in `ingest/odds_today.py` read `audit_summary.json["confidence_calibration"]` — correct key is `"confidence_calibration_totals"`. Also: band keys used `"70-75%"` format but actual schema uses `"70-75"` (no `%` suffix); field read `"rate"` but actual schema uses `"hit_rate_pct"` (percentage, not decimal). All three mismatches caused silent fallback to raw stated confidence as calibrated probability, making edge systematically negative and every pick tagged FADE. Fixed: key → `confidence_calibration_totals`, band keys → `"70-75"/"76-80"/"81-85"/"86+"`, field → `hit_rate_pct` with `/100.0` conversion. Verified: stated 80% now maps to cal 87.4% (actual band hit rate). `ingest/odds_today.py` only. |
| **Playoff Matchup Agent — per-series context for analyst (2026-04-07)** | New pure-Python agent `agents/playoff_matchup.py`. Reads `data/playoff_bracket.json` (manually created by Oliver when bracket is confirmed) + `nba_master.csv` + `player_game_log.csv` + `player_whitelist.csv`. Computes per-series: game results, series record, series state tags (dominant/favorable/even/trailing/desperate), game-script implications. Per-player: series averages + tier hit counts (from completed playoff games) + full regular season H2H averages + tier hits + home/away H2H split. Season H2H always shown alongside series data — persistent baseline, not a fallback. Writes `data/playoff_matchup.json` including pre-formatted `context_block` text. `agents/analyst.py`: `PLAYOFF_MATCHUP_JSON` constant, `load_series_context()` function, `series_context` parameter added to all three prompt builders (`build_prompt`, `build_scout_prompt`, `build_pick_prompt`), injected as `## SERIES CONTEXT — PLAYOFFS` section. Wired into `main()` at all 4 call sites (scout, 2 fallbacks, pick). `.github/workflows/analyst.yml`: step added between quant and pre_game_reporter, `data/playoff_matchup.json` added to commit FILES list. Graceful no-op during regular season: `playoff_matchup.py` exits immediately if `playoff_bracket.json` absent; `load_series_context()` returns `""` if `playoff_matchup.json` absent or stale. Zero regular-season impact. `agents/playoff_matchup.py` (NEW), `agents/analyst.py`, `.github/workflows/analyst.yml`. |
| **Post-Game Reporter rewrite — ESPN recaps + Rotowire news + Claude classification (2026-04-07)** | Complete rewrite of intelligence-gathering and classification pipeline in `agents/post_game_reporter.py`. Replaced Brave Search web narratives with two structured sources: ESPN game recap pages (fetched per `game_id` from `nba_master.csv`, parsed via BeautifulSoup `div.Story__Body` with `<article> <p>` fallback, truncated to 3000 chars) and Rotowire authenticated news feed (`news.php`, same login pattern as `ingest/rotowire_injuries_only.py`, matched by last name). Replaced all deterministic phrase-matching classification (`_INJURY_EXIT_TERMS`, `_DNP_TERMS`, `_RESTRICTION_TERMS`, `news_contains_injury_language()`, `classify_from_news()`) with a single Claude API call (`call_claude_classify_events()`) that receives three source blocks per missed-pick player (ESPN athlete news, ESPN recap excerpt, Rotowire blurbs) and classifies `event_type` (injury_exit/dnp/minutes_restriction/no_data) with `confidence` (confirmed/inferred) and `web_narrative`. Removed functions: `fetch_web_narratives()`, `call_claude_summarise_narratives()`, `news_contains_injury_language()`, `classify_from_news()`. Removed constants: `BRAVE_SEARCH_URL`, `INJURY_SCAN_TERMS`, `WEB_SEARCH_MODEL`, `WEB_SEARCH_TOKENS`. Added functions: `load_yesterdays_game_ids()`, `fetch_espn_recaps()`, `login_rotowire()`, `fetch_rotowire_news()`, `call_claude_classify_events()`. Added constants: `ESPN_RECAP_URL`, `ROTOWIRE_NEWS_URL`, `ROTOWIRE_LOGIN_URL`, `CLAUDE_MODEL`, `CLAUDE_MAX_TOKENS`, `RECAP_MAX_CHARS`, `MASTER_CSV`. Kept unchanged: `should_fetch()`, `parse_minutes()`, `is_dnp_row()`, `fetch_espn_news()`, all pick/game-log loaders. Output schema fully preserved (`post_game_news.json` keys: `date`, `generated_at`, `players`, `fetch_errors`; per-player keys: `event_type`, `detail`, `minutes_played`, `source_url`, `confidence`, `injury_status_at_check`, `injury_language_detected` (legacy, always False), `injury_scan_term` (legacy, always None), `espn_fetch_ok`, `web_narrative`). Graceful degradation: if ESPN recaps or Rotowire fail, Claude classifies with available sources; if Claude fails entirely, fallback deterministic DNP detection from box score data. Root cause fixed: Wembanyama 4/6 rib injury classified as `no_data` because Rotowire's "won't return" phrasing was not in the hardcoded `_INJURY_EXIT_TERMS` list. `agents/post_game_reporter.py` only — no workflow or other agent changes. `BRAVE_API_KEY` secret no longer required by this agent (still used by other consumers if any). |
| **Frontend Odds Integration Display — `agents/build_site.py` (2026-04-07)** | Pick cards now show recommendation tier label (STRONG/POSITIVE/NEUTRAL/FADE) with calibrated edge below the confidence line, and an expandable "Odds + Sizing" drawer showing market line, market prob, calibration band, calibrated prob, edge (pp), and quarter-Kelly fraction. Top Picks cards also show the tier label. All rendering gracefully handles missing `bet_recommendation` (shows nothing when absent or NO_MARKET). New CSS classes: `.edge-line` (with `.strong`/`.positive`/`.neutral`/`.fade` color variants), `.odds-sizing-toggle`, `.odds-sizing-body`. New JS functions: `toggleOddsSizing()` (expand/collapse drawer), `buildEdgeLine()` (colored tier+edge label), `buildOddsSizing()` (drawer with market data and Kelly sizing). Follows existing toggle pattern (`btn.nextElementSibling`). `agents/build_site.py` only — no workflow or agent changes. |
| **Parlay Agent Edge Awareness — `agents/parlay.py` (2026-04-07)** | `build_parlay_prompt()` extended to surface `bet_recommendation` data on each parlay leg. Two new fields added to slim leg construction: `edge_tier` (from `bet_recommendation.recommendation_tier`) and `cal_edge` (from `bet_recommendation.calibrated_edge_pct`). Defensive `.get()` chain ensures graceful `null` when `bet_recommendation` is absent. Criterion #6 (Edge quality) added to SELECTION CRITERIA: prefer STRONG/POSITIVE edge legs, avoid FADE legs, max 1 FADE per parlay with all other legs POSITIVE/STRONG. AVOID section extended with compound FADE prohibition (2+ FADE legs = structurally unprofitable). Advisory only — no Python hard gate on `build_candidates()` scoring; Claude uses edge data as a selection signal. `agents/parlay.py` only — no workflow or other agent changes. |
| **Calibration-Corrected Edge Enrichment — `ingest/odds_today.py` (2026-04-07)** | Added `compute_edge()` function to `ingest/odds_today.py`. Reads confidence calibration bands from `audit_summary.json`, maps each pick's stated confidence to the band's actual historical hit rate (calibrated probability), computes edge vs market implied probability, derives quarter-Kelly fraction, and assigns a recommendation tier (STRONG/POSITIVE/NEUTRAL/FADE/NO_MARKET). Writes `bet_recommendation` sub-object to each today's pick in `picks.json`. Three new functions: `_load_calibration_bands()` (reads `audit_summary.json` confidence_calibration), `_get_calibrated_prob()` (maps stated confidence to calibrated probability via band lookup), `compute_edge()` (orchestrates enrichment for all today's picks). New constant: `AUDIT_SUMMARY_JSON`. `main()` refactored: compute_edge runs after odds annotation, single atomic write for both odds and edge data. Graceful degradation: missing audit_summary or calibration data falls back to stated confidence as probability. Recommendation tier thresholds: STRONG >+8pp, POSITIVE +3–8pp, NEUTRAL ±3pp, FADE <−3pp. Quarter-Kelly formula: `(edge / (odds_decimal - 1)) / 4`, floored at 0. `ingest/odds_today.py` only — no workflow or agent changes. |
| **Market Availability Gate — analyst.py + analyst.yml + odds.yml (2026-04-07)** | Integrated `odds_available.json` (written by `ingest/odds_today.py --prefetch`) into the analyst pipeline as an unconditional market availability gate. If no FanDuel alternate market exists for a player+prop at the analyst's selected tier, the pick MUST NOT be generated (`no_market` skip_reason). Three code files changed: (1) `agents/analyst.py` — `ODDS_AVAILABLE_JSON` constant, `load_available_markets()` + `format_available_markets()` functions, `available_markets` parameter added to all 3 prompt builders (`build_prompt`, `build_scout_prompt`, `build_pick_prompt`), `MARKET AVAILABILITY GATE` as priority 0 in KEY FRAMEWORK (above hard skips), `no_market` added to skip_reason enum and HARD SKIPS list, wired in `main()` at all 4 call sites. (2) `.github/workflows/analyst.yml` — `ODDS_API_KEY` env var, prefetch step (`python ingest/odds_today.py --prefetch`) before analyst, enrichment step (`python ingest/odds_today.py`) after analyst, `odds_available.json` + `odds_today.json` added to commit FILES list. (3) `.github/workflows/odds.yml` — converted to manual-only trigger (`workflow_dispatch` only); primary odds flow now runs within `analyst.yml`. Graceful degradation: if `odds_available.json` is missing or stale, gate is disabled entirely (all picks proceed normally). |
| **Odds prefetch mode (`--prefetch`) — `ingest/odds_today.py` (2026-04-07)** | Added `--prefetch` CLI mode that fetches ALL available FanDuel alternate player prop markets for today's games before the analyst runs. Writes `data/odds_available.json` — filtered to system-valid tiers only (PTS 10/15/20/25/30, REB 4/6/8/10/12, AST 2/4/6/8/10/12, 3PM 1/2/3/4). Games filtered to those with at least one active whitelisted team (saves API credits). New constants: `VALID_TIERS`, `ODDS_AVAILABLE_JSON`, `WHITELIST_CSV`. New functions: `_load_whitelist_teams()`, `prefetch_all_markets()`. CLI dispatch via `argparse` — `--prefetch` runs prefetch, bare invocation runs existing `main()` unchanged. Prerequisite for market availability gate in `analyst.py` (separate task). `ingest/odds_today.py` only. |
| **Standings rank assignment sorted by win percentage — `ingest/espn_daily_ingest.py` (2026-04-07)** | ESPN API returns conference standings entries in internal order (division-grouped), not by record. `fetch_standings()` was assigning rank via `enumerate` on the raw API array, producing incorrect seedings in `standings_today.json` (e.g., LAL shown as West 1-seed instead of OKC). Fix: sort teams by win percentage descending (with total wins tiebreaker) before assigning sequential rank. W-L records and GB values were always correct — only the rank field was wrong. Has been incorrect all season. `ingest/espn_daily_ingest.py` only. |
| **Empty injury report drawer on frontend after Analyst workflow — `analyst.yml` (2026-04-07)** | Added `data/injuries_today.json` to the `FILES` preservation list in `analyst.yml` commit step. The morning Rotowire refresh (run as the first step of `analyst.yml`) wrote a fresh `injuries_today.json`, but the copy-aside/clean-checkout/rebase/restore pattern in the commit step did not include it — `git checkout -- .` wiped the file before `build_site.py` could read it, producing an empty injury dropdown on the deployed frontend. `.github/workflows/analyst.yml` only — 1 line changed. |
| **Git rebase autostash conflict fix — all workflow files (2026-04-05)** | Replaced `git rebase --autostash origin/main` with a copy-aside/clean-checkout/rebase/restore pattern in all 5 workflow commit steps across `injuries.yml` (3 steps), `analyst.yml`, `auditor.yml`, `ingest.yml`, `odds.yml`. Root cause: `--autostash` stashes local changes before rebase, then `stash pop` conflicts when remote also modified the same data file (e.g. `injuries_today.json`). With injuries running every 30 min, two overlapping runs or a concurrent workflow push would trigger this reliably. New pattern: (1) `cp` freshly-generated files to `.local` copies, (2) `git checkout -- .` to clean working tree, (3) `git pull --rebase origin main` (no stash needed), (4) `mv .local` copies back. Local data is always freshest — correct file always wins. Triggered by 2026-04-05 injuries.yml failure: `U data/injuries_today.json` unmerged conflict after autostash pop. `.github/workflows/injuries.yml`, `.github/workflows/analyst.yml`, `.github/workflows/auditor.yml`, `.github/workflows/ingest.yml`, `.github/workflows/odds.yml`. |
| **Defensive field access in `save_picks()` — `agents/analyst.py` (2026-04-05)** | Fixed `KeyError: 'reasoning'` crash in `save_picks()` print/logging line. Bare dict indexing (`p['reasoning']`, `p['player_name']`, etc.) replaced with defensive `.get()` calls on all pick fields. Pipeline crashed on 2026-04-05 large-slate day (31 players) when Claude omitted the `reasoning` field from a pick object. The crash occurred in the summary print loop after picks were already written to `picks.json` — non-destructive but caused process exit code 1, blocking downstream parlay + deploy steps. `agents/analyst.py` only — single line in `save_picks()`. |
| **pick_value output contract — `agents/analyst.py` (2026-04-01)** | Added explicit rule to both `build_pick_prompt()` and `build_prompt()` that emitted `pick_value` must ALWAYS equal the final stepped-down tier threshold, not the pre-step original tier. Contract placed adjacent to `pick_value` field definition in output format section; reinforcing reminder added to TIER_WALK FORMAT section. Audit evidence: Harden AST T6→T4 step-down 2026-03-31 — tier_walk correctly stated "b2b <5g → step down → T4" but `pick_value=6` was emitted instead of 4. `agents/analyst.py` only — prompt text in both functions. |
| **Minimum-tier REB confidence cap — `agents/analyst.py` (2026-04-01)** | New prompt rule in both `build_pick_prompt()` and `build_prompt()`: when a REB pick is at T4 (minimum valid REB tier) AND `b2b_hit_rates` is null for REB (`b2b <5g`), cap final confidence at 72% regardless of overall hit rate, iron_floor status, or trend direction. Tier_walk must flag explicitly: "minimum-tier REB, no B2B validation — confidence capped at 72%." Rationale: T4 is the floor — the system cannot step lower; when B2B sample is also absent, the combined uncertainty cannot be fully absorbed by -5% adjustment alone. Stacks with existing B2B <5g one-tier-down penalty. Audit evidence: Harden REB T4 2026-03-31 — 0 actual in 28 min, 78% confidence, competitive road game. `agents/analyst.py` only — prompt text in both functions. |
| **VOLATILE + iron_floor 3PM incompatibility — `agents/analyst.py` (2026-04-01)** | New prompt rule in both `build_pick_prompt()` and `build_prompt()`: when a 3PM prop has BOTH `iron_floor=true` AND the VOLATILE tag, iron_floor is treated as OVERRIDDEN for that prop. Standard non-iron-floor 3PM rules apply; confidence capped at 80% regardless of B2B hit rate, historical floor rate, or trend. iron_floor must not be cited as supporting factor in tier_walk. Rationale: iron_floor is valid for creation-based props (AST, low-threshold REB) where output floor is role-structural; for 3PM, the floor depends on shot attempts and a VOLATILE shooter can produce 0 makes on 0 attempts. Audit evidence: Donovan Mitchell 3PM T2 2026-03-31 — iron_floor=true + VOLATILE, 0 actual in 24 min, 91% B2B iron_floor pick. `agents/analyst.py` only — prompt text in both functions. |
| **Amendment gates: sub-70% auto-skip + B2B <5g upside block — `agents/lineup_update.py` (2026-04-01)** | Two deterministic gates added to `apply_amendments()`. **Gate 1 (Sub-70% auto-skip):** when `direction=="down"` and `revised_confidence_pct < 70`, the pick is immediately voided. The `lineup_update` sub-object remains on the voided pick for audit trail. Fixes SGA AST T4 miss 2026-03-30 (amended to 68%, ran anyway). **Gate 2 (B2B <5g upside block):** when `direction=="up"` and `player_stats[player].b2b_hit_rates[prop]` is null (fewer than 5 B2B games) AND player is `on_back_to_back`, the amendment is overridden to `direction="unchanged"` with original confidence preserved. Opponent lineup changes do not resolve B2B sample uncertainty. Fixes Harden AST T4 + REB T6 misses 2026-03-31 (both had null B2B hit rates, both amended up, both missed). Function signature extended with `player_stats: dict \| None = None`; call site passes `player_stats=player_stats`. `agents/lineup_update.py` only — 3 edits. |
| **Network error retry in analyst streaming — `agents/analyst.py` (2026-03-31)** | `call_analyst()` inline retry loop and `_call_with_overload_retry()` helper now catch `httpx.RemoteProtocolError`, `httpx.ReadTimeout`, and `httpx.ConnectError` alongside existing `overloaded_error` handling. Same exponential backoff (10s/20s/40s/80s, 4 retries). `import httpx` added. Fixes 2026-03-31 complete analyst workflow failure: Scout call succeeded (7 players shortlisted), Pick call crashed mid-stream with `httpx.RemoteProtocolError: peer closed connection without sending complete message body (incomplete chunked read)` — unhandled network-level error during chunked HTTP streaming. Scout (`call_scout()`) and Review (`call_review()`) were already resilient via bare `except Exception` fallbacks; only `call_analyst()` lacked network error coverage. `agents/analyst.py` only — 1 import added, 2 except blocks added. |
| **Odds Phase 1 alternate markets fix + simplification — `ingest/odds_today.py` (2026-03-31)** | First production run matched zero picks — FanDuel main markets only post the consensus/natural line (e.g. Luka PTS 33.5 OVER), which is structurally higher than NBAgent's conservative tier floors (e.g. Luka T25 = 24.5 target). Fix: dropped main markets entirely; `PROP_MARKET_MAP` now maps 4 alternate market keys → prop types (was main market keys); `ALTERNATE_MARKET_MAP` and `ALL_MARKET_KEYS` removed as redundant. `markets_str` requests 4 alternate markets only. `VALID_PROP_TYPES` helper set added for pick filtering. Alternate markets contain the full X+ line menu at multiple thresholds, matching the conservative floor targets used by NBAgent's tier system. Credit cost: 4 credits/game (unchanged from original Phase 1 design). Typical daily cost: 12–28 credits/day. Very conservative picks (e.g. T10 PTS for a 25 PPG scorer) may still not match if FanDuel's alternate menu starts above that threshold — this is expected behavior, not a bug. `ingest/odds_today.py` only. |
| **Odds Integration Phase 1 — `ingest/odds_today.py` + `odds.yml` (2026-03-31)** | New standalone odds agent fetches FanDuel NBA player prop lines from The Odds API after each analyst run (also manually triggerable via `workflow_dispatch`). Annotates today's picks in `picks.json` with `market_line`, `market_implied_prob`, `market_book`, `edge_pct`, `odds_fetched_at`. Writes diagnostic cache to `data/odds_today.json` (overwritten each run). Fully decoupled from `analyst.yml` — `analyst.yml` untouched. Graceful degradation: exits 0 on any failure. FanDuel only, exact line matching (`pick_value − 0.5`), ~4 credits/game/day (4 alternate markets). Required secret: `ODDS_API_KEY`. Future: chain off `injuries.yml` for intraday refresh when significant lineup changes detected. `ingest/odds_today.py`, `.github/workflows/odds.yml` — two new files. |
| **W7 — Rotowire Parser Post-Fix Monitoring CLOSED (2026-03-31)** | Watch item closed — Rotowire parser rewrite (deployed 2026-03-25) confirmed working in production. `parse_rotowire_lineups` (On/Off button anchor), `parse_projected_minutes` (logo img anchor), `parse_onoff_usage` (graceful JS-loaded skip) all functioning as designed. `docs/ROADMAP_active.md` watch item removed. |
| **`volatile_weak_combo` PTS trigger raised T20→T25 — `agents/analyst.py` (2026-03-31)** | Tier threshold for `volatile_weak_combo` PTS hard skip raised from T20+ to T25+. Full-season skip archive showed 0/3 correct skips at T20 post-narrowing: Jalen Johnson (actual 20, HIT), Donovan Mitchell (actual 28, HIT), Wembanyama (actual 41, HIT). T20 VOLATILE 7/10 is a false-skip population. T15–T24 VOLATILE picks with 7/10 or 8/10 now proceed under standard -5% treatment. Both `build_prompt()` and `build_pick_prompt()` updated identically. `agents/analyst.py` only. |
| **Wembanyama `volatile_weak_combo` PTS exemption — `agents/analyst.py` (2026-03-31)** | Added exception (c) to `volatile_weak_combo` exceptions in both prompt copies (`build_prompt` and `build_pick_prompt`). Wembanyama PTS props now receive standard VOLATILE treatment (-5% confidence) rather than a hard skip when at 7/10 + T20+. Consistent with existing blowout exemption philosophy. Audit evidence: PTS T20 skipped 2026-03-30, actual 41 points. `agents/analyst.py` only. |
| **BLOWOUT_SECONDARY_SCORER PPG-tier tiebreaker — `agents/analyst.py` (2026-03-31)** | Added "PRIMARY vs. SECONDARY SCORER TIEBREAKER" paragraph to both prompt copies (`build_prompt` and `build_pick_prompt`). Tiebreaker: if a whitelisted teammate has a higher best qualifying PTS tier in today's quant data, that player is primary and this player is secondary, regardless of ball-handler or playmaking role. Falls back to season PPG when no teammate quant data is available. Fixes 2026-03-30 Harden PTS T15 miss (ball-handler rationalization bypassed the skip at spread=17.5). `agents/analyst.py` only. |
| **Amendment sub-70% auto-skip — `agents/lineup_update.py` (2026-03-31)** | Added deterministic gate in `apply_amendments()`. When a lineup amendment revises confidence below 70% with `direction="down"`, the pick is immediately voided with `void_reason` referencing the revised confidence. The `lineup_update` sub-object and its reasoning remain visible on the voided pick. Fixes 2026-03-30 SGA AST T4 miss (amended to 68%, ran anyway). `agents/lineup_update.py` only. |
| **Overload retry logic — `agents/analyst.py` (2026-03-31)** | Added `_call_with_overload_retry` helper and retry loops in `call_scout()` and `call_analyst()`. Exponential backoff: 10s/20s/40s/80s, 4 retries. Scout retries before falling back to single-call mode; analyst retries before raising. Fixes 2026-03-31 complete run failure due to unhandled `overloaded_error`. `agents/analyst.py` only. |
| **Diagnostic files committed for git history (2026-03-26)** | Added `pre_game_news.json` and `team_defense_narratives.json` to `analyst.yml` commit loop; added `post_game_news.json` to `auditor.yml` commit loop. All three files were already written and consumed correctly within their workflows — this change preserves them in git for retroactive debugging of miss classifications and pre-game context. `scout_omitted_*.json` glob was already removed from analyst.yml in the earlier Scout omitted cleanup. `.github/workflows/analyst.yml`, `.github/workflows/auditor.yml` — 1 line changed each. |
| **Without [X] context line gated on teammate OUT status (2026-03-26)** | `build_quant_context()` now only renders the `Without [teammate]` line when the named teammate is confirmed OUT/OFS in `injuries_today.json`. Previously rendered whenever `key_teammate_absent` had ≥3 historical absence games, even when the teammate was healthy and playing today — showing misleading baseline data as if it were active context. Added `players_out_today: set[str] | None = None` parameter to `build_quant_context()` and `load_players_out_today()` helper (returns full player names with OUT/OFS status). Gate condition: `kta.get("teammate_name", "") in _out_set`. Both call sites in `main()` pass `_players_out_names`. Matches the trend override gate added to `build_player_stats()` on 2026-03-25. `agents/analyst.py` only. |
| **standings_today.json committed by ingest.yml (2026-03-26)** | `data/standings_today.json` was written by `espn_daily_ingest.py` in `ingest.yml` but never committed there. `auditor.yml` chains directly off `ingest.yml` and reads the file for the `## PLAYOFF PICTURE` block — but got a fresh checkout with stale/missing standings. Added to ingest.yml FILES array. `.github/workflows/ingest.yml` only — 1 line added. |
| **parlays.json committed by auditor.yml (2026-03-26)** | `data/parlays.json` grading results (`result`, `legs_hit`, `leg_results`) were written by `auditor.py` but never committed by `auditor.yml` — parlay grading results were silently discarded after every auditor run since launch. The frontend Parlays tab was showing ungraded parlays indefinitely. Added to auditor.yml commit step `for` loop. `.github/workflows/auditor.yml` only — 1 line changed. |
| **lineups_today.json committed by injuries.yml (2026-03-26)** | `data/lineups_today.json` was written to the runner disk by `rotowire_injuries_only.py` during hourly injury refreshes but never committed — the file disappeared when the runner exited. Added to the `injuries.yml` commit step `for` loop alongside `injuries_today.json` and `injury_log.csv`. This ensures lineup data (starters + projected minutes) persists across hourly runs and is available to `lineup_update.py` and `analyst.py` in subsequent workflow checkouts. `.github/workflows/injuries.yml` only — 1 line changed. |
| **Projected minutes integer extraction fix (2026-03-26)** | Fixed `parse_projected_minutes()` returning 0 minutes for every player despite finding them correctly. `el.parent` was the `<a>` tag's immediate wrapper (containing only the player name); the minutes integer is a sibling text node inside the enclosing `<li>`. Changed to `el.find_parent("li") or el.parent` so `get_text()` captures the full list item including the integer. `ingest/rotowire_injuries_only.py` only — 1 line changed. |
| **Road underdog near-threshold PTS penalty (2026-03-26)** | Added `ROAD UNDERDOG NEAR-THRESHOLD PTS PENALTY` to KEY RULES — SPREAD / BLOWOUT RISK in both `build_prompt()` and `build_pick_prompt()`. -5% confidence when: away team, spread_abs 4–7 (positive spread = underdog), raw_avgs PTS − pick_value ≤ 5 (near-threshold cushion). Iron_floor exempts. Addresses underdog-side blowout risk not covered by existing BLOWOUT_RISK rules (which only fire at spread_abs ≥ 8 for the favored side). Audit evidence: Barnes PTS T10 (raw_avg ~14, cushion=4) missed at 9 when TOR lost by 25 as 4.5-point road underdog at LAC. `agents/analyst.py` only — prompt text in both functions. |
| **volatile_weak_combo PTS skip narrowed (2026-03-26)** | Trigger conditions changed from `(7/10 OR 8/10) + T15+` to `(7/10 only) + T20+`. Audit evidence: 3/3 false skip rate — KD T20 (actual 30), Sengun T15 (actual 30), plus one Ingram instance. 8/10 at any tier is not a weak combination after the mandatory -5% VOLATILE deduction (80% - 5% = 75%, a legitimate pick). T15–T19 VOLATILE picks with 8/10 hit rates now proceed under standard VOLATILE treatment (-5% confidence deduction, no hard skip). Exception (a) baseline text updated from "8/10" to "7/10". Both `build_prompt()` and `build_pick_prompt()` updated identically. `agents/analyst.py` only — prompt text in both functions. |
| **Scout omitted block removed (2026-03-25)** | Removed the Scout omitted block entirely — prompt section, `call_scout()` omitted extraction + sentinel injection, `save_scout_omitted()` function, `SCOUT_OMITTED_JSON` constant, and `analyst.yml` commit glob. The omitted block never populated correctly in 6 consecutive days of production runs (always `[]` or sentinel-injected). Nothing downstream consumed the `scout_omitted_YYYY-MM-DD.json` file. The Review agent (Stage 3) now handles pick coverage concerns. `call_scout()` return type simplified from `tuple[list[dict] | None, list[dict]]` to `list[dict] | None`. All `return None, []` failure paths changed to `return None`. Saves prompt output tokens on every Scout call. `agents/analyst.py`, `.github/workflows/analyst.yml`. |
| **Absence-aware trend activation guard (2026-03-25)** | Fixed bug where absence-aware trend override fired unconditionally — even when the key teammate was healthy and playing today. The override in `build_player_stats()` now requires the key teammate to be confirmed OUT or OFS in `injuries_today.json` before overriding the standard L5-vs-L20 trend. Added `load_players_out_today()` helper (reads injuries file, returns set of OUT/OFS player names) and `players_out_today: set[str] | None = None` parameter to `build_player_stats()`. Gate condition: `_teammate_name in _out_set`. Safe fallback: empty `players_out_today` means override never fires (standard trend always used). The full `key_teammate_absent` dict (including `absence_trend`) is still written to `player_stats.json` regardless — only the trend label override is gated. Root cause: Luka's 3PM trend reflected "without Reaves" stats even though Reaves was healthy, because Reaves had missed some games earlier in the season and `absence_trend` was populated. `agents/quant.py` only — 4 locations changed. |
| **Rotowire parser rewrite (2026-03-25)** | Rewrote three broken parse functions in `ingest/rotowire_injuries_only.py`. (1) `parse_rotowire_lineups` — was returning 0 teams every run; old parser walked ancestor DOM trees looking for `data-team` on container divs, which doesn't exist there. Rewritten to anchor on `data-team` attribute in the On/Off Court Stats `<button>` (same anchor used by the working `parse_rotowire_injuries`); slices HTML between consecutive buttons to isolate each team's Expected Lineup section; reads player names from `<a title="">` attribute (full name) rather than link text (may be abbreviated); detects inline injury status keywords. (2) `parse_projected_minutes` — was returning 0 teams; the `div.lineups-viz` class it searched for does not exist on `projected-minutes.php`. Rewritten to anchor on team logo `<img src="/100{ABBREV}.png">` pattern; walks up ancestors to find player link containers; handles Subscriber Exclusive gating with `break`; extracts STARTERS/BENCH/MAY NOT PLAY/OUT section headers from NavigableString text nodes. (3) `parse_onoff_usage` — On/Off Court Stats panel is JavaScript-loaded via AJAX button click, never present in server-rendered HTML. Replaced 120-line parser with clean `return {}` + diagnostic log. Root cause across all three: page structure had changed; old class-name selectors and ancestor DOM walk patterns were stale. `parse_rotowire_injuries` unchanged (already working). `ingest/rotowire_injuries_only.py` only. |
| **Absence-aware trend fix (2026-03-25)** | `trend` label now overrides standard L5-vs-L20 computation with absence-window-scoped trend when `key_teammate_absent` is active (≥5 absence games). Fixes regime-crossing artifact: players with elevated role post-star-injury showed `trend=up` because L20 baseline included pre-absence lower-usage games. Confirmed: Randle `trend=up→stable` on 2026-03-25 (L5=22.6 vs absence-window=24.6, delta=−8.1%). `absence_trend` dict also stored in `key_teammate_absent` for audit purposes. `agents/quant.py` only — `compute_teammate_absence_splits()` and `build_player_stats()`. |
| **Streak pill removed from pick cards (2026-03-25)** | Pick card streak pills were displaying system-wide prop-type streak (e.g. "system's last 8 PTS picks all hit") rather than player-specific streak, making them actively misleading on individual cards. Removed `streakSpan` variable and `buildMicroStats()` param from pick card rendering loop. Results tab streak cards unchanged. `agents/build_site.py` only — 3 lines changed. |
| **is_skip boolean field — robust skip gate (2026-03-25)** | Added `is_skip: true/false` to pick JSON schema in both `build_pick_prompt()` and `build_prompt()`. `filter_self_skip_picks()` now checks `is_skip=true` as primary gate before regex fallback. Review Step 0 backstop updated to check `is_skip` field first. `save_picks()` defaults `is_skip=false` for picks missing the field. Eliminates regex whack-a-mole for skip detection — boolean field is authoritative, regex patterns remain as fallback. Root cause of recurring skip leaks: every new skip pathway produced new vocabulary no regex covered. `agents/analyst.py` only — 7 locations. |
| **Scout omitted block fix (2026-03-25)** | `scout_omitted_YYYY-MM-DD.json` was returning `[]` for all 6 days since Scout→Pick→Review launch. Two root causes: (1) prompt never told Scout to account for every quant stats player — only that the array must not be empty, which the model ignored; (2) `call_scout()` accepted empty array silently. Fix: rewrote `OMITTED BLOCK` instruction to explicitly require one entry per non-shortlisted player; updated omitted schema comment to reinforce the per-player requirement; added sentinel injection in `call_scout()` as enforcement backstop with `WARNING` log on compliance failure. `agents/analyst.py` only — prompt text + ~10 lines in `call_scout()`. |
| **Team abbreviation mismatch fix (2026-03-25)** | 10 whitelisted players were completely invisible to quant and analyst due to ESPN abbreviation mismatch: `nba_master.csv` and `player_game_log.csv` use ESPN short abbrevs (SA, GS, NY, UTAH) while `player_whitelist.csv` `team_abbr` uses standard NBA abbrevs (SAS, GSW, NYK, UTA). `team_abbr_alt` column already existed in whitelist with correct ESPN values but was never read. Fix: `load_whitelist()` in both `quant.py` and `analyst.py` now emits tuples for both `team_abbr` AND `team_abbr_alt`. Secondary fix: `load_injuries()` in `analyst.py` now normalizes `teams_today` through `_ABBR_NORM` before matching against `injuries_today.json` standard-abbrev keys. Affected players: Wembanyama, Castle, Fox (SAS), Brunson, KAT, Anunoby, M.Bridges (NYK), Curry (GSW), Markkanen, George (UTA). `agents/quant.py` and `agents/analyst.py` only — 2 functions each. |
| **Signed spread in quant context header (2026-03-25)** | `build_quant_context()` player header now shows signed spread (`spread=-10.5(abs=10.5)` or `spread=+1.5(abs=1.5)`) in addition to abs value, allowing the Analyst to determine favored side directly from header data. Root cause: Randle tier_walk on 2026-03-25 misidentified MIN as home favorite (was +1.5 underdog) because header only showed `spread_abs=1.5`. `agents/analyst.py` only — 2 locations, ~5 lines changed. |
| **Lineup snapshot staleness fix (2026-03-25)** | Removed over-broad early-return guard in `write_analyst_snapshot()` in `agents/analyst.py`. Guard `if raw.get('snapshot_at_analyst_run'): return` blocked all updates once any snapshot existed, preserving a stale `teams: {}` snapshot from 2026-03-12 through 13 daily runs. Replaced with a date + non-empty-teams check so the snapshot refreshes each new calendar day. `agents/analyst.py` only — 2 lines changed. |
| **merit_below_floor skip_reason added (2026-03-25)** | Added `merit_below_floor` to the skip_reason enum in both `build_pick_prompt()` and `build_prompt()`, and to `SKIP_CONCLUSIONS` in `filter_self_skip_picks()`. Distinguishes confidence-floor failures (penalties push confidence below prop-type floor) from `3pm_blowout_trend_down` (spread_abs ≥ 19 unconditional hard skip only). Fixes skip_validation table pollution: all prior `3pm_blowout_trend_down` false skips were actually merit-floor failures. `agents/analyst.py` only. |
| **filter_self_skip_picks merit-floor regex gap (2026-03-25)** | Added two patterns to `SKIP_CONCLUSIONS` to catch merit-floor skips where confidence falls below the prop-type minimum floor (3PM: 75%, REB: 78%) after penalties. These `tier_walk` phrases were not matching any existing pattern, causing LaMelo Ball 3PM and Brandon Miller 3PM to emit as picks on 2026-03-24 despite explicit skip language in the analyst's own reasoning. `agents/analyst.py` only — `filter_self_skip_picks()` `SKIP_CONCLUSIONS` list, net +3 lines. |
| **Self-skip picks leaking into picks_review (2026-03-25)** | Fixed bug where picks removed by `filter_self_skip_picks()` still appeared on the frontend via `picks_review_YYYY-MM-DD.json`. Root cause: `save_picks()` did not return the filtered list, so `main()` passed the original unfiltered picks to the Review stage (Stage 3). The Review agent wrote verdicts for ghost picks that had no corresponding entry in `picks.json`, and `build_site.py` surfaced them with Auto-Review badges. Fix: `save_picks()` now returns the filtered picks list; all three call sites in `main()` capture the return value with `picks = save_picks(picks)`. `agents/analyst.py` only — net +4 lines. |
| **Scout omitted block reframe (2026-03-23)** | Redefined the Scout prompt's `omitted` block to capture deprioritized candidates (medium-priority shortlisted players with meaningful situational risk) in addition to hard exclusions. Added `risk_type` field (`hard_exclusion` | `deprioritized` | `none`) to the omitted schema. Added explicit `## OMITTED BLOCK — REQUIRED, NOT OPTIONAL` section instructing that omitted must never be empty — minimum a sentinel entry if all candidates are clean. Fixes `scout_omitted` always returning `[]` on small slates. `agents/analyst.py` only — `build_scout_prompt()` only. |
| **Skip archive append-log (2026-03-23)** | Added persistent skip archive at `data/skipped_picks_archive.json`. Auditor appends fully graded skip records after each run via `save_skip_archive()`, using the same append-log pattern as `audit_log.json`. De-duplicates on re-run by date. Committed by `auditor.yml` alongside existing audit outputs. Enables longitudinal false-skip-rate analysis and retrospective skip debugging across the full season. `agents/auditor.py`, `.github/workflows/auditor.yml` only. |
| **volatile_weak_combo AST elite playmaker exemption (2026-03-23)** | Added elite playmaker exemption (raw_avgs AST ≥ 6.0 + iron_floor=true) to the `volatile_weak_combo` skip rule in both `build_prompt()` and `build_pick_prompt()`. Also tightened scope language to explicitly prevent the rule from being applied to AST picks below T6 (those are governed by the VOLATILE AST skip rule at its own T6 threshold). Fix motivated by 100% false skip rate on Jokic T8 AST and Barnes T4 AST. Both occurrences of the rule in `agents/analyst.py` updated identically. No other files modified. |
| **H18 — Wembanyama Rim Deterrent CLOSED after research phase (2026-03-22)** | Research phase Q1–Q3 completed. Pattern does NOT hold — no backtest warranted. Q2: miss players split 4 interior/balanced vs 2 perimeter; Miller (motivating case) classifies as perimeter (49.77% 3PA rate); Ball (cited as "unaffected") also missed AST T4 vs SA same game. Q3: opponents take +0.52 more 2PA vs SA (directionally opposite to hypothesis). The interior/balanced vs perimeter split is not visible in 6 misses. Existing SAS floor compression note from H15 already covers SA suppression. No code written. `docs/ROADMAP_active.md` updated. |
| **H14 — Elite Opposing Rebounder backtest implemented + run, CLOSED (2026-03-22)** | `--mode elite-opp-rebounder` added to `agents/backtest.py`. `build_opp_rebounder_lookup()` builds (game_id, team) → max_opp_rolling_reb_avg lookup from full player log (not just whitelist); `run_elite_opp_rebounder_analysis()` tests H14a (individual thresholds 8/10/12) and H14b (team REB median split). Run result: **NO_SIGNAL** — thresh=10.0 delta=−0.5pp; H14b flat (70.5% vs 70.1%). No rule change. `agents/backtest.py` only. `docs/BACKTESTS.md`, `docs/ROADMAP_active.md` updated. |
| **H21 — Miss Anatomy backtest implemented (2026-03-22)** | `--mode miss-anatomy` added to `agents/backtest.py`. Tests whether near-miss (gap ≤ 2) vs. blowup (gap ≥ 3) miss severity predicts next-game hit rate at the same tier. Constants: `H21_JSON`, `H21_NEAR_MISS_THRESH=2`, `H21_BLOWUP_THRESH=3`, `H21_MIN_MISSES=3`, `H21_MIN_N=15`. `run_miss_anatomy_analysis()` added; dispatch block wired in `main()` after H20. Writes `data/backtest_miss_anatomy.json`. Analyst wiring (annotation + prompt rule) explicitly deferred until verdict reviewed. `agents/backtest.py` only. |
| **H17 — Spread Context CLOSED (2026-03-22)** | `--mode spread-context` second run (n=538) confirmed NOISE. Overall spread-to-hit-rate flat; current ≤6 vs >6 split gap = −0.3pp. Best threshold 9.5 yields only 3.6pp gap — insufficient for a rule. 3PM at spread_abs ≥ 10 ran 69–71% (n≈30) but contradicted by H19 blowout_win 3PM finding. No rules warranted. CLOSED permanently. `docs/BACKTESTS.md` and `docs/ROADMAP_active.md` updated. |
| **H19 Finding 2: 3PM blowout trend-down hard skip retired for spread_abs 8–18 (2026-03-22)** | H19 backtest (n=96–137) showed `blowout_win` 3PM hits at 78.8–79.2% (lift=1.097–1.103) — above the 71.8% baseline. Hard skip was blocking above-baseline picks. Players with 24+ minutes in blowout wins get catch-and-shoot opportunities; garbage-time collapse affects sub-24-minute players already excluded from picks. Skip validation also showed 100% false skip rate for `3pm_blowout_trend_down` on Mar 20. Change: `3PM hard skip — trend=down AND blowout_risk=True` block replaced with a note deferring to the existing trend=down step-down rule. The `spread_abs ≥ 19` unconditional hard skip is unchanged. `agents/analyst.py` only — prompt text in `build_pick_prompt()` only; `build_prompt()` (fallback) unchanged. |
| **H19 Finding 1: Secondary Scorer PTS Skip narrowed to spread_abs ≥ 15 (2026-03-22)** | H19 backtest (n=140) showed `blowout_win_secondary` PTS hits at 80.7% (lift=1.083) — above baseline 74.5%. Rule was blocking above-baseline picks at spread_abs 8–14. At spread_abs ≥ 15 blowout is near-certain; skip remains appropriate. Change: `BLOWOUT_RISK SECONDARY SCORER SKIP` threshold raised from `spread_abs > 8` to `spread_abs ≥ 15` in `build_pick_prompt()`. spread_abs 8–14 now uses existing BLOWOUT_RISK confidence penalty only, not a hard skip. `agents/analyst.py` only — prompt text in `build_pick_prompt()` only; `build_prompt()` (fallback) unchanged. |
| **H19 backtest implemented — blowout-regime mode added to backtest.py (2026-03-22)** | `--mode blowout-regime` added to `agents/backtest.py`. Tests favored-side vs. underdog-side secondary scorer tier hit rates in actual blowout games (final margin ≥ 15). Minutes gate ≥ 24 min excludes garbage time. `classify_player_role()` added (primary = highest rolling-window PPG whitelisted player per team per game). Reuses `build_game_result_lookup()` and `add_best_tiers()`. Writes `data/backtest_blowout_regime.json`. `agents/backtest.py` only. |
| **H20 — Losing-Side Blowout AST Suppression backtest (2026-03-22)** | `--mode losing-side-ast` added to `agents/backtest.py`. Tests whether pre-game underdog spread_abs ≥ 10 suppresses AST tier hit rate. Constants: `H20_JSON`, `H20_UNDERDOG_THRESHOLDS=[6.5,10.0,15.0]`, `H20_PRIMARY_AST_AVG=6.0`, `H20_MIN_N=20`. `run_losing_side_ast_analysis()` function added; dispatch block wired in `main()` after spread-context. Run result: **NO_SIGNAL** — underdog_10plus AST hit rate 75.9% vs baseline 74.1% (lift=1.024, n=54), directionally opposite to suppression hypothesis. Rule NOT shipped. `agents/backtest.py` only. |
| **Without-Star Baseline: Confirmed-OUT Gate + Minimum Sample Guard (2026-03-22)** | Added `WITHOUT-STAR BASELINE — TWO REQUIRED GATES` block to `build_pick_prompt()` in `agents/analyst.py`. Gate 1: Without-Star baseline may only be used as primary tier qualifier when the star is confirmed OUT in today's injury report (QUES/GTD/PROBABLE → fall back to standard baseline). Gate 2: Without-Star sample must be ≥10 games (n < 10 → supplementary context only). Both gates must pass independently. Motivated by LeBron PTS T15 miss on 2026-03-21 (actual 12, Luka listed QUES, Without-Luka n=6 used as sole qualifying path); auditor classified as model_gap_rule. `agents/analyst.py` only — prompt text in `build_pick_prompt()` only; `build_prompt()` (fallback) unchanged. |
| **3PM extreme blowout hard skip — spread_abs ≥ 19 (2026-03-20)** | New companion rule added to KEY RULES — SEQUENTIAL GAME CONTEXT in both `build_prompt()` and `build_pick_prompt()`: when BLOWOUT_RISK=True AND spread_abs ≥ 19, skip ALL 3PM picks for ALL players on the favored team regardless of trend direction (even trend=up or stable). Additive to the existing trend=down rule. Reuses `3pm_blowout_trend_down` skip_reason enum. Motivated by SGA 0-for-3 in a 29-point OKC blowout win on 2026-03-18 despite trend=up and 9/10 T1 hit rate. `agents/analyst.py` only — prompt text change in both functions. |
| **Blowout secondary scorer skip direction fix (2026-03-20)** | Rule body said "player's team is the large underdog (spread of +8 or worse)" — inverted from the correct trigger. BLOWOUT_RISK=True in quant means the player's team IS the heavily favored side, not the underdog. Confirmed cause: Jalen Green PTS skip on 2026-03-19 (PHX +9.5 underdog) — rule fired on the wrong side. Fix: rewrote rule body to correctly reference BLOWOUT_RISK=True (favored side only); added explicit CRITICAL DIRECTION CHECK paragraph prohibiting application to underdog players. Fixed identically in both `build_prompt()` and `build_pick_prompt()`. `agents/analyst.py` only — prompt text change in both functions. |
| **Auto-review badge rendering bug fix (2026-03-20)** | `build_site.py` was never attaching `human_verdict`, `trim_reasons`, or `auto_reviewed` to today's pick objects — the old code only built an `auto_review_keys` set but never wrote the verdict fields back onto picks, so `p.human_verdict` was always `''` and no badges rendered. Fix: replaced `auto_review_keys` set with `review_lookup` dict keyed by `(name_lower, prop_type, pick_value)` — covers both auto and manual review entries (distinguished via `source == "auto"`). Game-time annotation loop extended to attach `human_verdict`, `trim_reasons`, and `auto_reviewed` directly onto each today's pick, or set empty defaults when no entry found. `agents/build_site.py` only — no schema or logic changes. |
| **Teammate absence splits (2026-03-20)** | `compute_teammate_absence_splits()` added to `quant.py` — computes player's raw avgs + tier hit rates in games where their top-PPG whitelisted teammate was absent (min 3 games); wired into `build_player_stats()` as `key_teammate_absent` field. `build_quant_context()` in `analyst.py` surfaces this as `Without [X]` line when n ≥ 3. `KEY RULES — TEAMMATE ABSENCE USAGE ABSORPTION` directive added to `build_pick_prompt()` only — uses absence baseline as primary evaluation when teammate is OUT/DOUBTFUL. `agents/quant.py`, `agents/analyst.py`. |
| **Scout omitted block persistence (2026-03-20)** | `call_scout()` now returns `(shortlist, omitted)` tuple instead of bare list; all failure paths return `(None, [])`. `save_scout_omitted()` writes `data/scout_omitted_YYYY-MM-DD.json` daily immediately before Stage 2 begins. `analyst.yml` commits `scout_omitted_*.json` alongside picks_review. `agents/analyst.py`, `.github/workflows/analyst.yml`. |
| **filter_self_skip_picks + Review backstop (2026-03-20)** | Added `HARD SKIP fires` and `mandatory hard skip` patterns to `SKIP_CONCLUSIONS` in `filter_self_skip_picks()`; added Step 0 skip-escape pre-check to `build_review_prompt()` so Review catches any filter escapes and forces `stay_away` verdict. `agents/analyst.py` only. |
| **build_site.py TODAY_STR ordering bug (2026-03-20)** | `PICKS_REVIEW_JSON` path constant (line 33) referenced `TODAY_STR` before it was defined (line 43). Fixed by moving `PT` and `TODAY_STR` above the path constants block, immediately after `SITE = ROOT / "site"`. `agents/build_site.py` only. |
| **Review Agent — Adversarial Pick Stress-Test (2026-03-20)** | Added Review as Stage 3 of the analyst pipeline in `agents/analyst.py`. Review receives per-pick vulnerability cards (volatility, B2B hit rates, blowout risk, minutes floor, bounce_back, FT safety margin) + audit miss pattern summary. Outputs clean/concern/stay_away verdicts per pick. Writes `data/picks_review_YYYY-MM-DD.json` in existing schema (auditor already reads it). Existing manual review files take priority — `apply_review_flags()` skips write if file exists. Failure is non-fatal — picks delivered regardless. Four new functions: `build_review_context()`, `build_review_prompt()`, `call_review()`, `apply_review_flags()`. `build_site.py` updated to show auto-review badges (`🤖 Auto-Review` / `🤖 Stay Away`) distinct from manual review badges (`⚠ Caution` / `⚠ Flagged`). `analyst.yml` updated to commit `picks_review_*.json` files. Review model: always Sonnet; max_tokens: 4096. `agents/analyst.py`, `agents/build_site.py`, `.github/workflows/analyst.yml`. |
| **Scout → Pick two-stage analyst pipeline (2026-03-20)** | Replaced single-call analyst with a two-stage LLM pipeline to fix attention dilution on large slates. **Stage 1 (Scout):** receives all contextual data (quant stats, game logs, season context, news, lineups, profiles, leaderboard, team defense, playoff picture) but NO rules; produces a structured shortlist of 20–25 players with per-player prop-type assessments. Scout model = Opus on large slates (>30 players), Sonnet otherwise. `SCOUT_MAX_TOKENS = 4096`. **Stage 2 (Pick):** receives Scout shortlist + filtered quant stats (shortlisted players only) + full rulebook + audit feedback; no redundant context blocks. Pick model = always Sonnet (shortlist ~20 players, never triggers Opus threshold). **Fallback:** if Scout parse fails or <5 players match quant stats, falls back to original single-call `build_prompt()` + `call_analyst()` path — all legacy functions preserved byte-for-byte. New functions: `build_scout_prompt()`, `call_scout()` (returns None on any failure — never sys.exit), `build_pick_prompt()` (full rulebook from `build_prompt()`, references SCOUT SHORTLIST). `main()` refactored: Scout → shortlist filtering → Pick → `save_picks()`/`save_skips()`. Output schemas (`picks.json`, `skipped_picks.json`) unchanged. `agents/analyst.py` only. |
| **filter_self_skip_picks — hard gate and named skip pattern coverage (2026-03-19)** | Added 7 regex patterns to `SKIP_CONCLUSIONS` in `filter_self_skip_picks()` in `agents/analyst.py`: `HARD GATE FIRES`, `BLOWOUT_SECONDARY_SCORER SKIP fires`, case-insensitive `hard gate fires`, named rule skip phrases (volatile_weak_combo, ast_hard_gate, blowout_secondary_scorer, 3pm_blowout_trend_down, reb_floor_skip, blowout_t25_skip, fg_margin_thin_tier_step), `SKIP T{N} AST/PTS/REB/3PM` + hard gate, `mandatory skip`, `Record as skip`. Previously these tier_walk conclusions were not matched, allowing hard-gate-skipped picks to pass through to `picks.json`. Caught via Banchero AST (ast_hard_gate) and Jalen Green PTS (blowout_secondary_scorer) on 2026-03-19. `agents/analyst.py` only — `filter_self_skip_picks()` SKIP_CONCLUSIONS list. |
| **T25 PTS blowout hard skip (spread_abs ≥ 15) + 3PM confidence floor 75% (2026-03-19)** | Two rule additions to `build_prompt()` in `agents/analyst.py`. **(1) T25 blowout hard skip:** Upgraded spread_abs ≥ 15 T25/T30 PTS from confidence cap (74%) to hard skip (`blowout_t25_skip`). Applies to favored-side only (negative spread). Confirmed by three instances: Jokic Mar 17 (spread 15.5, 8 PTS actual), SGA Mar 18 (spread 19.5, 20 PTS, human manual_skip pre-game), plus ongoing pattern. `PTS T25 BLOWOUT HARD SKIP` rule added to TIER CEILING RULES block; cross-reference note added to ELITE SCORER BLOWOUT EXEMPTION exception clause. `blowout_t25_skip` added to skip_reason enum. **(2) 3PM confidence floor 75%:** Added prop-specific confidence floor of 75% for 3PM picks (system floor is 70%). `3PM CONFIDENCE FLOOR` rule added to SELECTION RULES block. Motivated by binary outcome risk and thin R/R at marginal confidence levels — preserves iron_floor picks at 78%+ while eliminating borderline 71–74% 3PM picks. No skip record needed when floor gates (pick simply does not emit). `agents/analyst.py` only. |
| **fg_margin_thin redesign — step-down cascade not hard skip (2026-03-19)** | Replaced hard skip behavior with full tier step-down cascade (T20→T15→T10→skip). Only skips PTS prop if T10 also fails after full cascade. Renamed skip label from `fg_margin_thin_no_valid_tier` → `fg_margin_thin_tier_step`. Motivated by 100% false skip rate on graded instances (Brown T20 skip → actual 32, Adebayo flagged today). `agents/analyst.py` only — two locations: FG% Safety Margin rule block and skip_reason enum. |
| **Analyst Prompt Rules — Blowout Exemption Cap + VOLATILE AST Gate + Iron-Floor B2B Gate + 3PM Top-Pick Gate (2026-03-18)** | Four prompt-only rules added to `build_prompt()` in `agents/analyst.py`, all motivated by confirmed misses from March 17, 2026 audit. **(1) Elite scorer blowout exemption upper bound:** Jokic PTS T15 miss in a 28-point DEN blowout (spread_abs=15.5, 25 minutes played) showed the existing raw_avgs PTS ≥ 27.0 exemption is too broad at extreme spreads. Fix: added "Exception to the exemption" clause immediately after the existing exemption statement — when spread_abs >= 15, the full 74% blowout confidence cap applies to ALL players regardless of elite scorer status. Exemption still applies in the 8–14 spread range. 27.0 PPG threshold unchanged. **(2) VOLATILE + AST T6 hard block in AST rules section:** Doncic AST T6 miss (actual 4, trim-flagged correctly pre-game) showed the elite playmaker exemption does not protect against floor instability. Added VOLATILE + HIGH AST TIER BLOCK after the AST T4+ HARD GATE section: VOLATILE + AST tier >= T6 = hard SKIP, no exceptions, regardless of elite playmaker exemption status. **(3) Iron-floor B2B road gate for non-primary BHs:** Bane AST T2 miss — B2B road game at OKC (tough defense), 9/10 hit rate, iron_floor tag, actual 1. Fix: IRON-FLOOR B2B ROAD GATE added to REST & FATIGUE block — when on_back_to_back=True AND home_away="A" AND opp_defense=tough AND raw_avgs AST < 6.0, the iron_floor tag does NOT waive the gate; requires opp_defense="soft" to pick AST, otherwise SKIP. **(4) 3PM top-pick requires iron_floor:** Reaves 3PM T1 miss (top_pick=true, 9/10 hit rate, actual 0) and 81–85% 3PM band miscalibration. Fix: 3PM TOP-PICK RESTRICTION added to TOP PICKS block — top_pick=true on 3PM requires iron_floor=true; without iron_floor, cap 3PM confidence at 80% and exclude from top_pick. `agents/analyst.py` only. |
| **Post-Game Reporter + Auditor — in-game injury exit detection fix (2026-03-18)** | Root cause: two confirmed misclassification failures — Cade Cunningham (back spasms, Q1 exit, 5 min) and LaMelo Ball (foul trouble, low minutes) — both returned `event_type="minutes_restriction"` from `post_game_reporter.py` because their exit phrasing ("back spasms", "ruled out for the remainder") was not in `_INJURY_EXIT_TERMS`. Auditor received no injury_exit signal, classified misses as `model_gap_rule`, and generated rule-change recommendations built on a false blowout-management premise. Two-part fix: **(1) `agents/post_game_reporter.py`** — `_INJURY_EXIT_TERMS` extended with 8 new terms ("spasms", "spasm", "ruled out for the remainder", "ruled out for remainder", "did not finish", "did not complete", "hard fall", "took a fall", "fell hard", "carted off", "stretchered", "could not return", "unable to return"); new promotion block added after the existing `no_data` promotion: when `event_type=="minutes_restriction"` AND `from_news=False` (box score inference path) AND `inj_detected=True` AND `minutes < MINUTES_LOW_THRESHOLD`, promote to `injury_exit` with an "inferred" confidence label and log `[post_game_reporter] ↑ name: promoted minutes_restriction → injury_exit`. **(2) `agents/auditor.py`** — STEP 2 in `build_audit_prompt()` now opens with a FAST PATH block: if `event_type=injury_exit` appears in `## POST-GAME NEWS CONTEXT` for a player, classify as `injury_event` immediately without game-script analysis; fast path also checks for `post_game_event="injury_exit"` on the pick object itself. `injury_event` criteria widened: very low minutes (< 10) alone is sufficient regardless of individual stat values (a player can score 6 PTS in 5 minutes and still be an injury exit). New Python annotation pass in `build_audit_prompt()` after `hits_and_misses` is built: iterates `post_game_news`, stamps `post_game_event="injury_exit"` and `post_game_detail` directly onto pick dicts for players with injury_exit events — belt-and-suspenders with the news block. `agents/post_game_reporter.py`, `agents/auditor.py`. |
| **Analyst prompt hardening — three rule additions (2026-03-17)** | Three new directive rules added to `build_prompt()` in `agents/analyst.py`, all motivated by March 15–16 audit evidence. (1) **VOLATILE + AST T6 hard skip (`volatile_ast_t6`):** If a stat is tagged [VOLATILE] AND the selected AST tier is T6 or higher, skip the AST pick entirely — no lower-tier fallback. Elite playmaker exemption (raw_avgs AST ≥ 8.0) does NOT override this rule; that exemption only protects against the low-volume position gate in the AST T4+ HARD GATE. Iron_floor exception: iron_floor AND trend=up at exactly T6 exempts the pick; T8+ skips regardless. Inserted in KEY RULES — VOLATILITY block after the VOLATILE PTS skip exception paragraph. `volatile_ast_t6` added to named skip list in HARD SKIPS priority framework. Audit evidence: Luka Doncic AST T6, actual 4, L10 range 4–13. (2) **3PM confidence ceiling at 80% for non-iron-floor picks:** Do not assign confidence_pct > 80 on any 3PM pick unless iron_floor=true. Cap applied after all other penalties. top_pick=true ineligible on non-iron-floor 3PM picks. Inserted at end of KEY RULES — SEQUENTIAL GAME CONTEXT block, before KEY RULES — INJURY STATUS ON SHOOTING PROPS. Audit evidence: Austin Reaves 3PM T1 at 84% (top_pick=true), 9/10 hit rate, actual 0. (3) **VOLATILE tag neutralizes absence-driven upside arguments:** When [VOLATILE] appears on a stat, do not raise confidence citing teammate/opponent absence — treat as direction unchanged. Genuine role-change reassignments evaluated separately with explicit floor-change reasoning. Iron_floor overrides. Inserted in KEY RULES — VOLATILITY block after the VOLATILE AST skip. Audit evidence: Kevin Durant PTS (VOLATILE) amended 74%→79% citing Sengun OUT — scored 18 on T20 threshold. All three are prompt-only. `agents/analyst.py` only. |
| **JSON repair layer in analyst.py parse_claude_response() (2026-03-17)** | Root cause: analyst run hard-failed on 2026-03-17 when Claude produced structurally valid JSON containing a bad character inside a `tier_walk` string field (`Expecting ',' delimiter at char 81930`). Fix: `_repair_json(raw)` helper added immediately before `call_analyst()` in `agents/analyst.py`. Two-step repair: (1) `json_repair` library attempt (handles unescaped chars, trailing commas, truncated strings); (2) targeted character sanitization — char-by-char scan tracking string state, replaces literal `\n`/`\r`/`\t` inside string values with escaped equivalents. Both except blocks in `call_analyst()` (object-format path and flat-array fallback path) now call `_repair_json()` before `sys.exit(1)`. Repair logs a WARNING (not ERROR) on success so the pick run continues with visible log line. Happy path (valid JSON) is completely unchanged — `_repair_json()` is never called when `json.loads(raw)` succeeds. `json-repair` added to pip install step in `.github/workflows/analyst.yml`. `agents/analyst.py`, `.github/workflows/analyst.yml`. |
| **Picks Review integration — human_verdict field + audit precision + parlay stub + frontend badge (2026-03-16)** | Wires manually-produced daily `data/picks_review_YYYY-MM-DD.json` files into three agents. Review file schema: `[{date, player_name, team, prop_type, pick_value, verdict, trim_reasons}]`; verdicts: "keep"/"trim"/"manual_skip"; join key: `(player_name_lower, prop_type, pick_value)`. (1) `auditor.py`: `PICKS_REVIEW_DIR = DATA` constant; `load_picks_review(date_str)` loads the file and returns `{(name_lower, prop_type, pick_value): entry}` dict; `apply_human_verdicts(graded_picks, review)` tags each graded pick with `human_verdict` and `trim_reasons` (null/[] when not reviewed); both called in `main()` after `grade_picks()`; `save_audit_summary()` extended with `human_flag_precision` block — reads all picks from `picks.json`, groups by `human_verdict`, computes `{hits, misses, total, hit_rate_pct}` per verdict type over full season history. (2) `parlay.py` stub: `PICKS_REVIEW_DIR = DATA` constant; `load_todays_picks_review()` returns `{(name_lower, prop_type, pick_value): verdict}`; `load_todays_picks()` loads review, excludes `manual_skip` picks with log, and carries `_human_verdict` tag on remaining picks; `build_candidates()` enforces max-1-trim-per-card and requires ≥2 clean anchors (non-trim legs) before a trim pick can appear. (3) `build_site.py`: `.review-badge-trim` (amber) and `.review-badge-skip` (red) CSS classes; `reviewVerdict`/`reviewBadge`/`reviewReasons`/`reviewHtml` vars in pick card renderer; `{{reviewHtml}}` injected below `{{statusBadge}}` in card template — renders "⚠ Caution" (trim) or "⚠ Flagged" (manual_skip) with inline reasons; no badge on keep/null. All consumers degrade gracefully when review file absent. `agents/auditor.py`, `agents/parlay.py`, `agents/build_site.py`. |
| **Opportunity Flags overhaul — per-player cards, absent self-exclusion, upgrade tiers, frontend fix (2026-03-16)** | Four coordinated changes across `agents/lineup_update.py` and `agents/build_site.py`. Root cause: four production failures — (1) Garland 3× duplicate cards per absence (one per prop, should be one per player); (2) Kawhi Leonard included in his own absence card (absent player was not excluded from teammate scan); (3) "No qualifying tiers" on all cards (field name mismatch: old code read `info.hit_rate`, new schema writes `hit_rate_pct`); (4) "Unknown absence" shown on all cards (old code read `triggered_by_player`, new schema writes `triggered_by`). Fixes: (A) `lineup_update.py` inner prop loop replaced — per-player card collapse: iterates all props per player, builds `qualifying_tiers` dict (props with ≥70% not yet picked) and `upgrade_tiers` dict (props where quant best tier > morning pick tier, with `morning_tier`/`morning_confidence_pct` sub-fields), determines `card_type` ("new_pick"/"upgrade"/"mixed"), skips player when both dicts empty, deduplicates by `name_lower` (not `(name_lower, prop)`). Absent player self-exclusion added (`name_lower == absent_name.strip().lower()` guard). (B) `seen` dict type annotation updated to `dict[str, dict]` (keyed by player name only). (C) `save_opportunity_flags()` dedup key changed from `(date, player_name, prop_type)` to `(date, player_name, triggered_by)` — one card per player per triggering absence. (D) Debug print in `main()` updated to render `card_type`, `qualifying_tiers`, and `upgrade_tiers`. (E) `build_site.py` opportunity card renderer replaced — reads `qualifying_tiers` with `hit_rate_pct` (amber rows, label "OPPORTUNITY"), `upgrade_tiers` (blue rows, T{morning}→T{new}, label "UPGRADE"), `triggered_by` string, `card_type`; `caution_flags` removed; `side` shown as "(opp)" suffix when `side=="opponent"`. `agents/lineup_update.py`, `agents/build_site.py`. |
| **Analyst — floor gate skip enforcement + return-from-injury rule (2026-03-16)** | Two additions to `agents/analyst.py`. (1) `filter_self_skip_picks()` extended with 4 new floor gate failure patterns in `SKIP_CONCLUSIONS` (total: 9 patterns): catches `"floor gate fails"`, `"no variance buffer"`, `"SKIP: 3rd-lowest"`, and `"3rd-lowest equals pick value"` language — prevents lineup amendment agent from overriding a floor gate SKIP by ensuring the pick never enters `picks.json`. Docstring updated to reflect pre-confidence-arithmetic firing. Prototype failure: Ausar Thompson REB T4 correctly SKIP'd by floor gate but filed at 78% after Cunningham OUT amendment. (2) `games_available < 8` surfaced as `[SHORT_SAMPLE:Ng]` in player header via `ga_flag` (read from `s.get("games_available", 10)` in `build_quant_context()`); `RETURN FROM INJURY — SHORT SAMPLE INSTABILITY` rule added to `KEY RULES — REST & FATIGUE`: mandatory one-tier step-down on REB/AST, confidence -5% on PTS, no T20+ unless ≥7 games at that tier. `agents/analyst.py` only. |
| **Analyst prompt — meta-framework for rule conflict resolution (2026-03-16)** | Added `KEY FRAMEWORK — HOW TO REASON WHEN RULES CONFLICT` block to `build_prompt()` in `agents/analyst.py`, inserted immediately before `KEY RULES — MATCHUP QUALITY`. Seven components: (1) 5-level priority order for conflicting rules (hard skips → mandatory tier steps → confidence penalties → caps → positive signals); (2) PENALTY STACK LIMIT — >3 independent penalties triggers re-examination or skip; (3) TIER_WALK FORMAT — clean chain documentation requirements; (4) SANITY CHECK — 4-point self-validation gate before emitting each pick, including smell test for incoherent low-tier picks on elite scorers; (5) ON COMPLEX SLATES — frames multi-signal days as normal and manageable; (6) PLAYER TIER CONTEXT — anchors elite scorer recognition to leaderboard block; (7) CONFIDENCE THRESHOLD IS A FLOOR, NOT A TARGET — prevents rounding up from honest sub-70% assessment. Also updated `## SELECTION RULES` confidence threshold line to reinforce floor vs. target distinction. Prompt-only, no Python changes. Motivated by 2026-03-15 SGA T10 cascade failure and recurring workflow_gap misses. `agents/analyst.py` only. |
| **Analyst — whitelisted player leaderboard injection (2026-03-16)** | Added `build_player_leaderboard(game_log, whitelist)` to `agents/analyst.py`. Computes season avg and L20 avg rankings for all whitelisted players per stat (PTS/REB/AST/3PM), top 15 per stat. L20 rank shown with arrow (↑/↓/→, threshold ±3 spots) to flag surging/declining players. Injected as `## WHITELISTED PLAYER RANKINGS` block between PLAYOFF PICTURE and TEAM DEFENSIVE PROFILES in the analyst prompt. Zero cost — pure pandas from already-loaded game_log. Motivations: (1) ground elite scorer recognition (27.0 PPG blowout exemption rule needs leaderboard context to apply correctly); (2) absence impact calibration; (3) season vs L20 divergence as rise/decline signal. `agents/analyst.py` only. |
| **reconcile_pick_values hardening + elite scorer blowout exemption (2026-03-16)** | Two `reconcile_pick_values()` improvements in `analyst.py`: (1) **One-tier-step guard** — after the "never raise" check, computes `next_lower_tier = valid[current_idx - 1]` and RECONCILE_SKIPs when `final_tier != next_lower_tier`; prevents multi-tier cascades from ambiguous tier_walk text matching two tier mentions in the same sentence. (2) **Strategy 2 `check_hits` gate** — `if final_tier is None and check_hits:` (was bare `if final_tier is None:`); Strategy 2 (→T{N} arrow fallback) only fires when Strategy 1 found at least one ✓-qualified tier, blocking false positives from unrelated tier mentions in tier_walk text without ✓ marks. Also: **elite scorer blowout exemption** prompt rule added to KEY RULES — SPREAD / BLOWOUT RISK: players with `raw_avgs PTS ≥ 27.0` are exempt from all blowout-driven PTS caps (BLOWOUT -10%, spread_abs > 8 → cap 80%, LARGE SPREAD PTS CAP ≥ 12 → cap 74%). Rationale: 27+ PPG scorers have protected minutes regardless of game script; applying role-player blowout penalties produces absurd outputs (T10 for a 30-PPG star). Exemption covers PTS only; REB/AST/3PM props for same player still subject to normal blowout rules. `agents/analyst.py` only. |
| **Lineup Update — opportunity surfacing redesign (2026-03-15)** | Rewrote `build_opportunity_suggestions()` in `agents/lineup_update.py`. New design: (1) scans ALL whitelisted teammates AND opponents for every confirmed absence — no role-tag gating; (2) includes players skipped this morning (annotated with skip context, not blocked); (3) deduplicates by (player, prop) not by (player, prop, trigger); (4) adds live spread delta via `fetch_live_spreads()` (ESPN scoreboard API) — annotates cards with ≥3pt line moves as market signal of absence impact; (5) adds `load_morning_skips()` for morning context annotation. `save_opportunity_flags()` dedup key simplified to 3 fields. Suggestion schema adds `side`, `spread_delta`, `morning_context` fields. `classify_absent_player()` retained for amendment path. `agents/lineup_update.py` only. |
| **Quant — B2B flag team vs. player distinction (2026-03-15)** | `on_back_to_back` in `player_stats.json` now reflects whether the individual player appeared in yesterday's game, not just whether their team played. Fix: `_player_played_yesterday` check added at line 1745 in `build_player_stats()` — compares `grp["game_date"].iloc[0]` (player's most recent game, newest→oldest sort) against yesterday's date string. `on_b2b` is now `(team in b2b_teams) AND _player_played_yesterday`. Prototype case: Giannis ruled OUT for MIL March 14 game; flagged B2B=True for March 15 despite being rested; B2B hit rate used instead of overall rate. `agents/quant.py` only — single line change. |
| **Post-Game Reporter — dual Brave query for missed picks (2026-03-15)** | `fetch_web_narratives()` in `agents/post_game_reporter.py` now runs two Brave queries per missed-pick player: (1) `"{name} {team} NBA recap {date}"` (existing), and (2) `"{name} injury"` (new). Snippets are combined and deduplicated by title prefix before being passed to `call_claude_summarise_narratives()`. Motivated by Kawhi Leonard mid-game ankle injury (March 14, 2026) — the injury article was published as a standalone ESPN News item and did not surface in the recap search; the auditor had no context to explain the 26-minute, 0-assist outcome. `_brave_query()` helper extracted as an inner function. `agents/post_game_reporter.py` only. |
| **Workflow enforcement — self-identified skip filtering (2026-03-15)** | Added `filter_self_skip_picks()` to `agents/analyst.py`. Runs as first step of `save_picks()` before `reconcile_pick_values()`. Scans each pick's `tier_walk` field for unambiguous skip-conclusion language (e.g. "below 70% SKIP", "→ 65% SKIP"). Picks whose own reasoning concludes skip are removed from the final pick list before writing to `picks.json`. Conservative: override signals ("proceed", "exception applies", etc.) after the skip language prevent false removals. Filtered picks are logged to stdout as `SELF_SKIP_FILTERED` for observability — not written to `skipped_picks.json`. Motivated by 6 workflow_gap misses this season, prototype case: Payton Pritchard PTS T10 (March 14, 2026) — tier_walk read "VOLATILE -5% → 65% → below 70% SKIP" but pick was submitted and missed (4 actual). `agents/analyst.py` only. |
| **Skip rule fixes — reb_floor_skip T4 exemption + Jokic ast_hard_gate exemption (2026-03-15)** | Two prompt-only fixes to `agents/analyst.py` `build_prompt()`. (1) reb_floor_skip: exact-match gate now has an explicit T4 exemption — the gate fires normally for pick_value ≥ 6 but does NOT apply at pick_value = 4 (minimum valid REB tier, no lower step available). Motivated by 100% false skip rate at T4 in skip validation data (Reaves, White, Kawhi all would have hit). (2) ast_hard_gate: added "elite playmakers" exemption for players with raw_avgs AST ≥ 8.0 per game — gate was never designed for primary playmaking engines regardless of position. Motivated by Jokic AST T6 false skip (14 actual). `agents/analyst.py` only — no Python logic, no schema changes. |
| T2 REB Deprecation (2026-03-14) | Removed T2 (pick_value=2) from valid REB tiers in `quant.py` (`REB_TIERS`) and `analyst.py` (`VALID_TIERS` + prompt text). T4 is now the REB minimum qualifying tier. Motivation: T2 REB has no corresponding betting market at mainstream books (FanDuel/DraftKings). Season data showed 15/16 (93.8%) hit rate — near-certainty with zero betting value. Removing inflated 0.4pp hit rate boost; true system hit rate is 85.0%. Volume impact: ~1 pick per 3–4 days. (Extended 2026-03-14) Propagated to `agents/backtest.py` (`REB_TIERS` global) and `agents/lineup_update.py` (local `TIERS` dict + docstring). All tier lists in the system now consistently use T4 as REB minimum. |
| `lineup_watch.py` sticky void bug (March 14, 2026) | CLEAR block now guards against clearing `voided=True` + `void_reason` picks. Rotowire post-game roster resets move all active players back to PROB/unlisted — previously this un-voided confirmed OUT picks. Fix: `else` branch checks `p.get("voided") and p.get("void_reason")` before clearing; sticky voids are left untouched. Speculative `lineup_risk` flags (DOUBTFUL/QUESTIONABLE) still clear normally. `agents/lineup_watch.py` only. |
| `lineups_today.json` snapshot clobbered by hourly Rotowire refreshes (March 12, 2026) | `write_lineups_json()` overwrote `snapshot_at_analyst_run` on every `injuries.yml` run, causing `lineup_update.py` to always skip with "no snapshot found." Fix: read existing file and carry forward the key before constructing payload. `ingest/rotowire_injuries_only.py` only. |
| `lineup_update.py` reasoning overhaul (March 12, 2026) | Added `load_player_stats()`, `build_pick_quant_summary()`, `classify_absent_player()`, `build_absent_player_profiles()`. System prompt rewritten with 4-step reasoning framework, role-tag-based absent player classification, and explicit opponent-side defensive anchor logic. Per-pick quant context injected. `player_stats` passed through `main()` → `call_lineup_update()`. |
| API key "balance too low" error | Create new API key after adding credits — old key had sync issue |
| JSON truncation on large slates | analyst.py MAX_TOKENS increased 4096 → 8192 → 16384 |
| All 30 teams' injuries sent to prompt | `load_injuries()` filters to today's teams only |
| All whitelisted players sent regardless of who's playing | `build_player_context()` + Quant filter to whitelisted players on today's teams |
| Traded players appearing under old team | Whitelist filter changed from name-only to `(name, team)` tuple in both `analyst.py` and `quant.py` |
| Audit context too large | Reduced from 20 → 5 most recent entries |
| `Brandon MIller` typo in whitelist | Fixed to `Brandon Miller` — capital I caused name match failure |
| `injuries_today.json` empty on first run | Expected — hourly injuries workflow populates it; all agents handle empty gracefully |
| Parlays tab missing from live site | `build_site.py` merged with full Parlays tab (session March 5, 2026) |
| `SyntaxWarning: invalid escape sequence '\d'` in build_site.py | Pre-existing cosmetic warning in JS canvas regex block — does not affect runtime |
| **Improvement Proposal #2 — Opponent-Specific Tier Hit Rates** | Implemented in `quant.py` (`compute_matchup_tier_hit_rates()`, `MIN_MATCHUP_GAMES=3`) and `analyst.py` (`load_player_stats()`, `build_quant_context()`, new QUANT STATS prompt section). `player_stats.json` now includes `matchup_tier_hit_rates` field. |
| **P1 — Game Script Filter (Spread-Adjusted Blowout Risk)** | Implemented across `espn_daily_ingest.py`, `quant.py`, `analyst.py`. Spread + blowout_risk flag + spread_split_hit_rates in player output; prompt rules for BLOWOUT_RISK and spread_abs > 13. |
| **P1 (formerly) — B2B Quantified Tier Adjustment + P3 (formerly) — Days of Rest / Schedule Density** | `build_b2b_game_ids()`, `compute_b2b_hit_rates()`, `compute_rest_context()` in `quant.py`. `b2b_hit_rates`, `rest_days`, `games_last_7`, `dense_schedule` in `player_stats.json`. KEY RULES — REST & FATIGUE block in analyst prompt. |
| **Backtest-driven prompt + quant calibration (March 2026)** | `agents/backtest.py` added. Findings: tier ceiling rules, 3PM opp_defense inversion, trend/home-away removed as directive signals, `PLAYER_WINDOW` raised 10→20. |
| **Bounce-back analysis + player-level integration (March 2026)** | `build_bounce_back_profiles()` in `quant.py`; `bounce_back` key in `player_stats.json`; `bb_lift` / `[iron_floor]` annotations in analyst; SELECTION RULES updated. |
| **Grading correction + full backtest re-run (March 2026)** | All code corrected from `>` to `>=` grading. All 5 backtests re-run. 3PM T2 now above threshold; 3PM opp_defense changed to NOISE; `BACKTESTS.md` rewritten. |
| **Prompt calibration from corrected backtests (March 2026)** | 3PM opp_defense inversion removed; 3PM cold-streak decline rule added; PTS T25 requires ≥80% individual hit rate. |
| **REB opp_defense decoupled (March 2026)** | Stat-specific OPPONENT DEFENSE block in analyst prompt; REB explicitly excluded; REB T8 ceiling rule updated. |
| **Offensive-first player REB floor rule (March 2026)** | Added to analyst SELECTION RULES: players with PTS avg > 20 or AST avg > 6 targeted at or below 25th-percentile recent REB output. |
| **Auditor root cause discipline (March 2026)** | ROOT CAUSE DISCIPLINE block added to `auditor.py`: three pre-flight checks before any miss root cause assignment. |
| **Lineup watch script (March 2026)** | `agents/lineup_watch.py` added. OUT → `voided=True`; DOUBTFUL → `lineup_risk="high"`; QUESTIONABLE → `lineup_risk="moderate"`. `injuries.yml` updated; `build_site.py` updated with voided/risk display. |
| **Auditor season context injection (March 2026)** | `load_season_context()` in `auditor.py`; OFS framing injected into audit prompt. PERMANENT ABSENCES rule block added to `nba_season_context.md`. |
| **Auditor pre-computed statistics (March 2026)** | `prop_type_breakdown` and `confidence_calibration` pre-computed in Python before Claude call. `## PRE-COMPUTED STATISTICS` section added to audit prompt. Both fields added to `audit_log.json`. |
| **Auditor 4-step miss analysis + miss_classification (March 2026)** | PICK ANALYSIS TASK replaced with 4-step protocol. `miss_classification` field added to `miss_details` schema. |
| **Auditor player stats context injection (March 2026)** | `load_player_stats_for_audit()` added. `## PLAYER STATS CONTEXT` injected into audit prompt. *(Superseded March 8, 2026 — function removed; auditor now reads quant context from pick object fields to avoid date-gate bug.)* |
| **Parlay agent reads audit feedback (March 2026)** | `load_parlay_audit_feedback()` added to `parlay.py`. `## PARLAY AUDIT FEEDBACK FROM PREVIOUS DAYS` injected into parlay prompt. |
| **Longitudinal audit summary (March 2026)** | `save_audit_summary()` in `auditor.py` writes `data/audit_summary.json`. `load_audit_summary()` in `analyst.py` injects `## ROLLING PERFORMANCE SUMMARY`. |
| **Injury report display overhaul (March 2026)** | `build_site.py` `load_injuries_display()` rewritten. Game grouping from `nba_master.csv`; whitelist filtering by `(canonical_team, last_name)` tuple; abbrev normalization via `_ABBR_NORM`. |
| **PWA start_url fix (March 2026)** | `site/manifest.json`: `"start_url": "."` and `"scope": "."`. Fixes GitHub Pages subpath resolution for PWA launcher. |
| **Site accent color → orange (March 2026)** | All purple accent instances replaced with `#E8703A` in `build_site.py`. |
| **ML win probability odds on game headers (March 2026)** | `load_game_ml_odds()` added to `build_site.py`. `DAL (24%) @ ORL (80%)` inline in game group headers. |
| **Team abbrev normalization — systematic fix (March 2026)** | `_ABBR_NORM` dict in `build_site.py`; `normAbbr()` JS helper; both Python and JS lookups handle legacy 2-char forms (`GS`, `SA`, `NO`, `UTAH`, `WSH`). |
| **Top Picks section (March 2026)** | `get_top_picks()` in `build_site.py`. `⚡ TOP PICKS TODAY` header above game groups; conf ≥ 85%, ranked by iron_floor + confidence + hit rate + stat priority; min 3 to display. *(Superseded March 12, 2026 — see analyst-driven selection entry above.)* |
| **Picks tab cosmetic refinements (March 2026)** | Streak pills threshold raised to ≥5 consecutive; moved inline. Results tab "OVER " prefix removed from Pick column. |
| **P2 — Rolling Volatility Score (March 2026)** | `compute_volatility()` in `quant.py`. `[VOLATILE]` / `[consistent]` in analyst. KEY RULES — VOLATILITY block. |
| **P1 — Positional DvP (March 2026)** | `position` column in whitelist. `compute_positional_dvp()` in `quant.py`. `DvP [POS]` line per player in analyst prompt. |
| **Auditor crash bug fixes — three sequential bugs in `save_audit_summary()` (March 6, 2026)** | Bug 1: `.items()` on list — added `isinstance` guard. Bug 2: `conf_schema` built as list — rebuilt as dict. Bug 3: `TODAY_STR` NameError — replaced with `TODAY.strftime(...)`. |
| **Lineup Watch — injury snapshot fields on all picks (March 6, 2026)** | `lineup_watch.py` writes `injury_status_at_check` and `injury_check_time` to ALL of today's picks on every run. |
| **Auditor — expanded miss classifications + injury lesson exclusion (March 6, 2026)** | Two new classifications: `injury_event` and `workflow_gap`. Injury/workflow misses excluded from lesson generation. `miss_classification` updated to 5 valid values. |
| **Post-Game Reporter — new agent + auditor wiring (March 6, 2026)** | `agents/post_game_reporter.py` added. Fetches ESPN news for flagged players (DNP, <15 min, zero stats). Classifies as `injury_exit` / `dnp` / `minutes_restriction` / `no_data`. Writes `data/post_game_news.json`. `auditor.py` and `auditor.yml` wired. |
| **Frontend enhancements — Results tab + timezone labels (March 6, 2026)** | ET → PT labels; pick history date format M/D/YY; parlay history key added; Top Picks stats banner on Results tab; collapsible drawers (Top Picks / Pick History / Parlay History). |
| **Pre-Game Reporter — new agent + analyst wiring (March 6, 2026)** | `agents/pre_game_reporter.py` added. Fetches ESPN player + league news; filters to prop-relevant items; single Claude call to summarize. `## PRE-GAME NEWS` injected into analyst prompt. `analyst.yml` wired. |
| **P4 — Tier-Walk Audit Trail (March 6, 2026)** | `tier_walk` field in analyst output. STEP 5 in auditor grading. `tier_walk_flag` in `miss_details`. Tier-walk displayed on pick cards. |
| **JSON-first analyst output enforcement (March 7, 2026)** | `## OUTPUT FORMAT — EMIT THIS FIRST` header in analyst prompt. `bracket_idx` fallback extraction in `call_analyst()`. |
| **Post-Game Reporter — broadened injury detection (March 7, 2026)** | `INJURY_SCAN_TERMS` constant; universal fetch for all missed picks; `injury_language_detected` field in `post_game_news.json`. |
| **Auditor — absence context block + ⚠ injury news flag (March 7, 2026)** | `build_absence_context()` added. `## YESTERDAY'S NOTABLE ABSENCES` injected before pick analysis. `⚠ INJURY LANGUAGE IN NEWS` flag on relevant entries. |
| **Auditor — injury_event hit rate exclusion from audit_summary (March 7, 2026)** | `injury_event` misses excluded from per-prop and overall hit rate denominators in `audit_summary.json`. `injury_exclusions` key added. |
| **Auditor — parlay summary field + PARLAY ANALYSIS TASK rewrite (March 7, 2026)** | PARLAY ANALYSIS TASK rewritten with items 4–8. `parlay_summary` field added to `parlay_results` schema. Markdown audit report extended. |
| **Parlay audit feedback card on frontend (March 7, 2026)** | Parlay card added to `renderAudit()` in `build_site.py`. `parlay_reinforcements` / `parlay_lessons` displayed. Parlay history filter includes `PARTIAL`. |
| **3PM trend=down mandatory step-down rule (March 7, 2026)** | New bullet in KEY RULES — SEQUENTIAL GAME CONTEXT: 3PM trend=down → step down one full tier before finalizing. Scoped to 3PM only. |
| **Parlay concentration cap (March 7, 2026)** | `parlay.py`: no single player-prop combination in more than 2 of today's parlays. |
| **iron_floor field propagated to picks.json (March 7, 2026)** | `"iron_floor"` added to analyst OUTPUT FORMAT schema. `save_picks()` extended with defensive default. |
| **P3 — Shooting Efficiency Regression (March 7–8, 2026)** | `espn_player_ingest.py` collects FG/3P shooting stats. `compute_shooting_regression()` in `quant.py`. `[FG_HOT]`/`[FG_COLD]` annotations in analyst. KEY RULES — SHOOTING EFFICIENCY REGRESSION block. |
| **H11 — FG% Safety Margin (March 2026)** | `ft_safety_margin` in `quant.py` and `analyst.py`. Structural feature, no backtest. |
| **Miss Anatomy quant fields (March 2026)** | `near_miss_rate`, `blowup_rate`, `typical_miss` added to `build_bounce_back_profiles()` in `quant.py`. Analyst wiring deferred pending backtest. |
| **Minutes Floor (March 2026)** | `minutes_floor` in `quant.py` and `analyst.py`. Structural feature, no backtest. |
| **Player Profiles (March 2026)** | `build_player_profiles()` in `quant.py`. `## PLAYER PROFILES` injected into analyst prompt. Live statistical portraits computed fresh daily. |
| **Season Context Improvement 0 — Standings Snapshot (March 8, 2026)** | `fetch_standings()` in `espn_daily_ingest.py` writes `data/standings_today.json`. `render_playoff_picture()` formatter in `analyst.py` and `auditor.py`. Bucketed `## PLAYOFF PICTURE` injected into both prompts. |
| **Season Context Improvement 1 — Auto-Generated Team Defense Narratives (March 8, 2026)** | `build_team_defense_narratives()` in `quant.py` writes `data/team_defense_narratives.json` (last 15g PPG + rank per team). `format_team_defense_section()` in `analyst.py` replaces static `## TEAM DEFENSIVE PROFILES` section; validates `as_of == TODAY` and returns fallback if stale. |
| **Season Context Improvement 2 — Staleness Detection (March 8, 2026)** | `detect_staleness_flags()` (Pass 1 — Python only, no LLM) added to `pre_game_reporter.py`. Parses SEASON FACTS dates and flags stale facts (7d/5d/60d rules). Flags appended to `data/context_flags.md` and `staleness_flags` key in `pre_game_news.json`. `analyst.py` picks up via existing `⚠ CONTEXT FLAG` mechanism. |
| **Auditor — NO_DATA handling + player stats date gate (March 8, 2026)** | Removed `load_player_stats_for_audit()` and `PLAYER_STATS_JSON` entirely — eliminates today's-data-for-yesterday's-audit confabulation bug. HIT/MISS picks now go to `## FULL GRADED PICKS` for standard miss analysis; NO_DATA picks split into separate `## NO_DATA PICKS` block with dedicated `## NO_DATA ANALYSIS TASK`. Auditor directed to read quant context from pick object fields (`reasoning`, `hit_rate_display`, `tier_walk`, `opponent`). `no_data_details` array added to `audit_log.json` output schema. |
| **Post-Game Reporter — QUESTIONABLE pre-game status + NO_DATA promotion (March 8, 2026)** | `load_yesterdays_picks_with_status()` added — reads `injury_status_at_check` from `picks.json`, returns highest-severity status per player. `classify_from_news()` now accepts `injury_status` param; pre-game status inference block fires when status ∈ {QUESTIONABLE, DOUBTFUL, OUT} and minutes are 0/low — returns `dnp` or `minutes_restriction` without needing ESPN confirmation. Separate promotion block upgrades `no_data` → `dnp`/`injury_exit` when injury language detected. `injury_status_at_check` added to `post_game_news.json` output. |
| **Analyst — AST T4+ hard gate + 3PM hard skip (March 8, 2026)** | Two unconditional gates added to `build_prompt()`. (1) AST T4+ hard gate: PF/C or raw_avgs AST < 4.0 → opponent AST DvP must be "soft"; mid/tough = skip, no override. (2) 3PM hard skip: trend=down AND avg_minutes_last5 ≤ 30 → skip all 3PM picks including T1 (step-down rule does not apply). Both gates are additive; existing rules unchanged. |
| **Lineup Watch — wiring fix (March 9, 2026)** | `lineup_watch.py` was not in the `injuries.yml` chain — injuries refreshed hourly but `picks.json` `voided` flags never updated. Fixed: `lineup_watch.py` added between `rotowire_injuries_only.py` and `build_site.py` in `injuries.yml`. Name matching hardened to last-name + team-abbrev key (handles Rotowire abbreviated format). Stale flag clearing added: status improvements between hourly runs now remove prior `voided`/`lineup_risk` flags. `AGENTS.md` workflow diagram updated. |
| **Analyst — min_floor confidence cap + BLOWOUT_RISK secondary scorer skip + parlay player-level concentration cap (March 9, 2026)** | Three hard gates added. (1) min_floor cap: PTS pick with `floor_minutes < 24` → confidence capped at 84% regardless of streak/iron_floor tag. (2) BLOWOUT_RISK secondary scorer skip: spread ≥ +8 underdog AND non-primary scorer → PTS pick skipped entirely (not just -5% deduction). (3) Parlay concentration cap widened from player-prop level to player level: no single `player_name` in more than 2 of today's parlays regardless of prop type. Post-selection validation added to `parlay.py` as reliable enforcement point. |
| **Analyst — OUT/DOUBTFUL hard pre-filter (March 9, 2026)** | Root cause: `analyst.py` relied on LLM self-exclusion of OUT players — no Python filter existed. Two-layer fix: (1) `rotowire_injuries_only.py` added as first step of `analyst.yml` so injuries are always fresh before picks run. (2) `load_out_players()` added to `analyst.py` — reads `injuries_today.json`, builds `(last_name, team_abbrev)` exclusion set, strips OUT/DOUBTFUL players from `player_stats` dict before any prompt-building call. Claude never receives stats for excluded players. INJURY EXCLUSION hard rule added to KEY RULES as backstop. `parlay.py` candidate pool filters excluded players as defense-in-depth. Exclusions logged to stdout. `AGENTS.md` updated. |
| **Projected Lineup Scraping + Analyst Context Injection (March 9–10, 2026)** | `rotowire_injuries_only.py` extended with `parse_rotowire_lineups()` and `write_lineups_json()` → writes `data/lineups_today.json`. Guard condition prevents 0-team parse from overwriting good existing data. `analyst.py` adds `format_lineups_section()` (reads + staleness-checks `lineups_today.json`), `write_analyst_snapshot()` (writes `snapshot_at_analyst_run` into `lineups_today.json` at pick time), LINEUP CONTEXT key rule in SELECTION RULES. `## PROJECTED LINEUPS` injected between injury report and pre-game news. `AGENTS.md` updated. |
| **P5 — Afternoon Lineup Update Agent (March 10, 2026)** | New `agents/lineup_update.py`: snapshot-based diff against `snapshot_at_analyst_run`; detects `new_absence` and `starter_replaced` change types; calls Claude (single prompt) for affected picks (team or opponent changed, tip-off > 20 min); writes `lineup_update` sub-object `{triggered_by, updated_at, direction, revised_confidence_pct, revised_reasoning}` in-place; fully idempotent — overwrites on each hourly run; `direction=unchanged` still written as audit evidence. `injuries.yml` wired (`anthropic` dep + run step + commit step). `build_site.py` badge: `↑/↓ Updated HH:MM` with expandable detail panel on pick cards and Top Picks. `AGENTS.md` updated. |
| **Rotowire session login + projected_minutes/onoff_usage scraping (March 10, 2026)** | `login_rotowire(session)`, `parse_projected_minutes(soup)`, `parse_onoff_usage(soup)` added to `rotowire_injuries_only.py`. `fetch_rotowire_html()` refactored to accept optional session (backward-compatible). `write_lineups_json()` extended with optional `projected_minutes`/`onoff_usage` params. `main()` creates `requests.Session()`, authenticates, and conditionally calls new parsers only when authenticated. `lineups_today.json` now optionally carries per-team projected minutes and on/off usage data. `ROTOWIRE_EMAIL`/`ROTOWIRE_PASSWORD` env vars injected into both `injuries.yml` and `analyst.yml`. All new parsing is graceful-degradation — returns `{}` on any exception, never crashes. |
| **Analyst lineup context wiring (March 10, 2026)** | `load_lineup_context()` added to `analyst.py` — reads `lineups_today.json`, staleness-checks `asof_date`, builds per-team normalized lookup dict. `build_quant_context()` signature updated to accept optional `lineup_context` param. Three new per-player annotations injected when Rotowire creds present: (1) `proj_min=N` in header (Rotowire projected minutes), (2) `[USG_SPIKE:+N.Npp vs X.Name]` in header when usage_change ≥ 5.0 AND minutes_sample ≥ 100, (3) `⚠ OPP: Name OUT (proj=0min)` after DvP line for opponent key absences (capped at 3). `main()` wired to call `load_lineup_context()` and pass result to `build_quant_context()`. |
| **Knowledge staleness awareness block in analyst build_prompt() (March 10, 2026)** | `## IMPORTANT: YOUR TRAINING KNOWLEDGE IS POTENTIALLY YEARS OUT OF DATE` block inserted in `build_prompt()` between `Today is {TODAY_STR}.` and `## YOUR TASK`. Distinguishes perishable knowledge (player roles and usage, rosters and depth charts, team systems and pace, H2H matchup history, season narratives — do NOT rely on training data for any of these) from durable knowledge (general basketball principles, tier logic and statistical reasoning, role archetype reasoning — apply freely). Instructs Claude: if a fact is specific to a named player or team, trust the injected data; if it is a general basketball principle, apply it freely. |
| **Analyst — three prompt rule hardenings (March 11, 2026)** | (1) **min_floor mandatory tier step-down:** `min_floor < 24 AND tier ≥ T15 → mandatory step to T10` (was: confidence cap only). Exception: `avg_minutes > 36`. (2) **Iron-floor scope clarification:** `[iron_floor]` protects tier only, not confidence. VOLATILE -5% still applies even with iron_floor. AST iron_floor does not suppress VOLATILE deduction for wing scorers (SG/SF) with down trend. (3) **VOLATILE PTS skip:** VOLATILE + 7/10 + T15+ PTS → skip. Exception: iron_floor AND trend=up. All three rules are prompt-only — no code changes. |
| **Post-Game Reporter — Brave Search web narrative layer (March 11, 2026)** | `fetch_web_narratives()` added to `post_game_reporter.py` — queries Brave Search API for each missed-pick player (`"{name} {team} NBA recap {date}"`, 3 results). `call_claude_summarise_narratives()` makes a single batch Claude call (2048 tokens) and returns `{player_name_lower: narrative_string}`. `web_narrative` field added to `players_out` entries in `post_game_news.json` (default null). Auditor `build_audit_prompt()` appends `📰 WEB RECAP: {narrative}` to POST-GAME NEWS CONTEXT per missed player. `auditor.yml` wired with `BRAVE_API_KEY`. ESPN flow unchanged; web layer is additive and fully gracefully-degrading. Root cause addressed: ejections and foul-trouble narratives were invisible to reporter and auditor. |
| **Skip Validation — analyst skip records + auditor grading (March 11, 2026)** | Analyst now emits `{"picks": [...], "skips": [...]}` JSON object (was flat array). Backward-compatible flat-array fallback retained. Skip records written to `data/skipped_picks.json` (overwrite daily) for hard-rule-forced skips where blocked tier had ≥70% hit rate. Eight `skip_reason` values: `min_floor_tier_step`, `volatile_weak_combo`, `blowout_secondary_scorer`, `3pm_trend_down_tough_dvp`, `3pm_trend_down_low_minutes`, `ast_hard_gate`, `fg_margin_thin_no_valid_tier`, `reb_floor_skip`. `call_analyst()` returns `(picks, skips)` tuple. `save_skips()` writes null grading fields. Auditor grades via pure-Python `grade_skips()` (no Claude call) — fills `actual_value`, `would_have_hit`, `skip_verdict`, `skip_verdict_notes`. `save_audit_summary()` rolls up `skip_validation` block (per-rule `false_skip_rate`). Daily audit report includes `## Skip Validation` table. `skipped_picks.json` committed in both `analyst.yml` and `auditor.yml`. |
| **Injuries workflow — extended evening cron schedule (March 11, 2026)** | Hourly injury refresh runs extended from 6 PM PT cutoff to 10 PM PT. Seven new cron entries added (4 PM–10 PM PT). Covers all NBA tipoff windows (5 PM, 7 PM, 8 PM, 8:30 PM PT) and late-scratch window (~90 min post-tip). All schedule comments corrected from ET to PT. Root cause: late scratches announced after last scheduled refresh were leaving picks un-voided through tip-off. |
| **Team Momentum Indicator (March 11, 2026)** | `build_team_momentum()` in `quant.py` computes L10 W-L record, avg point margin, and hot/cold/neutral tag for all teams playing today from `nba_master.csv`; `team_momentum` field in `player_stats.json` with `{team: {...}, opponent: {...}}` structure; `Momentum —` annotation line in `build_quant_context()` between DvP line and stat lines; annotation only — no directive rules. |
| **M2 — Defensive Recency Split (March 12, 2026)** | `compute_opp_defense_recency()` added to `quant.py`; `def_recency` field in `player_stats.json`; `DEF↑`/`DEF↓` inline header annotation in analyst `build_quant_context()`; annotation only — no directive rules; validation gate at 30+ flagged instances. |
| **Analyst — three prompt rule tightenings (March 12, 2026)** | (1) REB pick value gate: "at or below" → "strictly less than" 3rd-lowest L10 value; exact match now forces step-down (motivated by Sengun REB miss: 3rd-lowest=6, pick=6, actual=2). (2) VOLATILE PTS skip extended to 8/10: condition now fires on 7/10 OR 8/10 at T15+; exception clause updated to "8/10 baseline" (motivated by Ingram PTS O15 ×3 in 8 days, twice at 8/10). (3) FG_COLD ≥15% mandatory tier step-down on T15+ PTS: severe FG_COLD no longer informational-only at high tiers; step to lower tier and re-qualify; `fg_cold_tier_step` skip_reason added for cases where step-down finds no qualifying tier (motivated by Flagg PTS O15 miss with FG_COLD:-18%). All prompt-only — no quant or schema changes. |
| **Auditor — Game Results Context Injection (March 12, 2026)** | Root cause addressed: auditor had no direct access to final game scores, forcing it to infer blowout context from ESPN news and Brave Search hits — both unreliable (Durant vs. Jokic HOU/DEN same-game miss asymmetry, March 11, 2026). Fix: `MASTER_CSV = DATA / "nba_master.csv"` path constant added; `load_game_results()` reads yesterday's completed rows, keys result dict by BOTH home and away team abbrev (O(1) lookup from either side), skips unparseable score rows, returns `{}` on any error; `build_game_results_block()` deduplicates via `seen` set, labels each game BLOWOUT (≥20 margin) / COMPETITIVE (10–19) / CLOSE (<10), sorts alphabetically, returns formatted block; `build_audit_prompt()` signature extended with `game_results_block: str = ""`; block injected between season context and playoff picture; STEP 0 — ESTABLISH GAME CONTEXT added as first step of PICK ANALYSIS TASK — auditor looks up each player's team in the game results block before analyzing any miss, ensuring shared game-script evidence is applied consistently across all players from the same game; existing STEP 1–6 numbering unchanged; `main()` wired with `game_results = load_game_results()` + `build_game_results_block(game_results)` + kwarg pass. `agents/auditor.py` only — no other files changed. |
| **Post-Game Reporter — three bug fixes (March 12, 2026)** | Root cause: reporter ran before auditor graded picks → `result == null` on all picks → all 14 players qualified as "missed picks" (should have been 0, it was a 92.1% hit rate day). Bug 1 (timing): `load_yesterdays_missed_pick_names()` filter changed from `result in ("MISS", None)` to `result == "MISS"` only — null result picks no longer treated as misses. Bug 2 (silent ESPN fetch errors): `fetch_ok = None` tracking added; `"espn_fetch_ok"` field written to `players_out` entries for observability. Bug 3 (web narrative not saved): `web_narrative` merge loop verified to run before `json.dump()`. |
| **Top Picks — analyst-driven selection + Results tab redesign (March 12, 2026)** | Top Picks: replaced mechanical 85% confidence gate in `build_site.py` with analyst-declared `top_pick: true/false` field on pick objects. `get_top_picks()` updated to read `top_pick` flag with backwards-compatibility fallback to ≥85% gate for pre-flag historical picks. `past_top_picks` filter updated similarly. Analyst prompt: `top_pick` field added to OUTPUT FORMAT JSON schema; `## TOP PICKS — FINAL SELECTION STEP` reasoning block added instructing qualitative convergence selection (1–5 picks, signal convergence criteria, not a confidence threshold). Results tab: redesigned from stacked banners to five named cards — Overall (hero + confidence band table), Yesterday (last night's date/rate/by-prop breakdown sourced from `audit_summary.json`), Props (four stat sub-cards), Top Picks (season hit rate, hidden until ≥3 graded), Daily Hit Rate (chart unchanged). `AUDIT_SUMMARY_JSON` path constant added; `load_yesterday_summary()` function added to `build_site.py`; new CSS classes added; `renderResults()` JS rewritten. |
| **Auditor — voided pick grading bug fix + historical data repair (March 14, 2026)** | Root cause: `grade_picks()` in `auditor.py` had no guard checking `voided` before assigning results. Bug A: picks with `voided=True` (set by `lineup_watch.py`) were falling through the grading loop and receiving `result="MISS"` when the player's box score showed 0 stats (DNP). Bug B: picks with `void_reason` set post-hoc (late-DNP detected) but `voided` not flipped were graded `result="MISS", actual_value=0.0` instead of being promoted to voided. Affected: 4 Derrick White picks (2026-03-12, Bug A) + 2 Alperen Sengun picks (2026-03-13, Bug B). Fix 1A: voided guard added immediately after `p = pick.copy()` in the main grading loop — sets `result=None, actual_value=None` and continues. Fix 1B: second-pass loop added after the main loop (before `return graded`) — detects `result=="MISS" AND actual_value==0.0 AND void_reason set AND not voided`, promotes to `voided=True, result=None`. `build_audit_prompt()` and `save_audit_summary()` were already correct (already split voided/active; denominator already `hits/(hits+misses)`). `scripts/repair_void_grading.py` created — patches Bug A and Bug B in `picks.json`, recomputes affected `audit_log.json` entries (2026-03-12: 84.6%→91.7%, 2026-03-13: 84.0%→91.3%), regenerates `audit_summary.json`. Corrected season: 299H/51M = 85.4% (was 84.0% with 6 corrupted misses inflating denominator). `agents/auditor.py` only for the production fix; `scripts/repair_void_grading.py` is a standalone one-time repair. |
| **Whitelisted teammates grounding (March 14, 2026)** | Fixes stale training data hallucination where the Analyst free-associated historical teammate relationships (e.g. Harden + VanVleet) with no current-season grounding. Three-part fix: (1) `quant.py` `build_player_stats()` — added `whitelisted_teammates: sorted(teammate_corr.keys())` as second field in `stats_out[player_name]` dict, immediately after `"team"`; value is the list of other active whitelisted players on the same team playing today, derived from the already-computed `teammate_corr` dict (zero new computation). (2) `analyst.py` `build_quant_context()` — added `teammates_line` ("  Teammates (active/whitelisted): Name1, Name2") rendered after `momentum_line` in the `lines.append()` block; empty string (not rendered) when no whitelisted teammates playing today. (3) `analyst.py` `build_prompt()` — added `TEAMMATE REFERENCES` guardrail in picks rules block: Analyst may only name teammates appearing on the `Teammates (active/whitelisted)` line or in today's injury report; prohibits naming historical or absent teammates not present in today's context. |
| **lineup_update.py Comprehensive Overhaul (March 14, 2026)** | Fixed six failure modes causing the agent to silently skip on every real-world absence event. **F1 (root cause):** `compute_lineup_diff()` now uses TWO sources — snapshot starters AND picks.json. Any player with open picks today who is OUT/DOUBTFUL is now a `new_absence` regardless of snapshot state. Function signature extended with `today_picks`; picks loading moved before diff call in `main()`. **F2:** `build_opportunity_suggestions()` now runs an opponent scan for `defensive_anchor`/`rim_anchor` absences — finds whitelisted players on the opposing team; new `side="opponent"` field. **F4:** `classify_absent_player()` now falls back to picks.json when player not in player_stats — infers role_tags from pick_value (PTS≥20→high_usage, REB≥6→rim_anchor+defensive_anchor, AST≥4→primary_creator). **F5:** `--debug` flag added to main() — full diagnostic output without LLM call or file writes. **F3/F6:** Resolved as consequence of F1. Real-world trigger: Giannis OUT on 2026-03-14 — not in Rotowire snapshot starters, diff returned empty, Jalen Johnson opponent opportunity never surfaced. |
| **3PM Blowout Hard Skip + Parlay One-Player-Per-Card (March 14, 2026)** | **3PM blowout hard skip (`analyst.py`):** New rule added to KEY RULES — SEQUENTIAL GAME CONTEXT: when a player's 3PM trend is "down" AND BLOWOUT_RISK=True (favored team, spread_abs > 8), skip all 3PM picks including T1. Rule explicitly overrides [iron_floor]. New skip_reason key: `3pm_blowout_trend_down`. Motivated by: Donovan Mitchell 3PM T1 miss (2026-03-13) in a 33-point CLE blowout win where iron_floor provided no protection against game-script volume compression. **Parlay one-player-per-card (`parlay.py`):** Tightened concentration cap from max-2-parlays-per-player to max-1-parlay-per-player. Both `> 2` thresholds changed to `> 1` in `enforce_concentration_cap()`; `## AVOID` section in `build_parlay_prompt()` updated to match; docstring and log messages updated. Motivated by: Alperen Sengun appearing in 2 parlays on 2026-03-13 — single DNP caused both to miss simultaneously. |
| **Rotowire projected minutes — correct data source (March 12, 2026)** | Root cause: `parse_projected_minutes()` and `parse_onoff_usage()` were parsing `nba-lineups.php` HTML, which does not include premium panels in its server-rendered response. Diagnostic confirmed `/basketball/projected-minutes.php` is fully statically rendered and contains all target CSS classes (`lineups-viz`: 505, `minutes-meter__proj`: 439, `lineups-viz__player-name`: 145). `/basketball/projections.php?type=today` confirmed JS-rendered (0 tables, 0 player links despite 367K HTML). Fix: `ROTOWIRE_MINUTES_URL` constant added; `fetch_rotowire_minutes_html()` added; `main()` makes second authenticated fetch for the dedicated page and passes resulting soup to premium parsers. Health-check log added to `parse_projected_minutes()`: reports teams count + players-with-minutes-gt-0 count per run. First run with real projected minutes data expected next analyst cycle. |
